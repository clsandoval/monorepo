# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-03-15 | 1.1 TaxKlaro design tokens | ~2min | 26 CSS vars, DM Sans/Serif Display fonts, 6-step type scale, 3 shadow levels, Tailwind Slate neutrals, blue-700 primary (diverges from Angkin navy #1E3A5F), 4 tax-specific semantic colors, shadcn/ui component layer with 17 components + 20 Lucide icons |
| 2 | 2026-03-15 | 1.2 Inheritance design tokens | ~3min | Primary already #1E3A5F (exact Angkin navy), gold accent #C5A44E, Inter+Lora fonts (differs from TaxKlaro DM Sans), 5-step shadow scale, animation tokens, navy sidebar pattern, deeper semantic colors, 18 shadcn components (new-york style), ~30 Lucide icons, print stylesheet |
| 3 | 2026-03-15 | 1.3 Design system reverse context | ~5min | 37 analysis files from 27-aspect loop. Option 2 (Radical Clarity) closest to selected 2C logo. 7 audience segments, 8 tool archetypes (43% single-form calculators), result moment is #1 design priority. Option 2 scored 43/60 — strong on accessibility/mobile/simplicity, weak on friendliness/cultural fit/memorability. Treatment deck must keep strengths while adding dark mode, iconography, sub-tool branding, and navigation patterns that Option 2 excluded. Both existing apps use shadcn/ui + Lucide + Tailwind 4. |
| 4 | 2026-03-15 | 1.4 Logo anatomy | ~3min | Documented 2C Radical Clarity mark geometry: circle + diagonal slash = negative-space 'A'. Clear space = 1/4 diameter. 6 size specs (16px favicon → 128px hero). 4 color variants (navy-on-white, white-on-dark, monochrome black, monochrome white). Canonical SVG definitions for inline use. Horizontal + stacked lockup specs. 10 "don't" rules. Loading animation spec (stroke-dasharray). Image generation deferred — SVGs serve as canonical mark for HTML deck. |
| 5 | 2026-03-15 | 2.1 Color system | ~4min | 3 scales (navy 50-950, gold 50-800, navy-undertoned neutral 0-950), 4 semantic colors (success/warning/error/info-teal), 6 chart colors, 10 compliance-specific tokens (peso amounts, regime comparisons), ~20 shadcn/ui mappings, ~10 sidebar tokens. ~80 total CSS custom properties. Key decisions: navy at 600 position, teal for info (not blue — avoids brand confusion), custom navy-undertoned neutrals (not Tailwind Slate), gold-600+ for text (gold-400 fails WCAG on white). Full contrast audit passes WCAG AA. |
| 6 | 2026-03-15 | 2.2 Dark mode palette | ~3min | ~50 dark mode CSS custom properties. Navy-tinted blacks (navy-950 #060D18 base, never pure black). 4-tier surface elevation model (surface-0 through surface-3). Primary shifts navy-600→navy-400 for visibility. Gold-400 unchanged — natural dark mode star (8.4:1 on base). Text uses warm off-white #E8EDF2 to prevent halation. Semantic colors shift to 400-level brighter variants with very-dark-tinted backgrounds. Logo inverts to white fill with navy-950 slash. Borders use subtle #1E2D42. All combos WCAG AA, most AAA. Sidebar goes darker-than-content (navy-900). Includes toggle mechanism spec (prefers-color-scheme + data-theme + localStorage). |
| 7 | 2026-03-15 | 2.3 Typography system | ~4min | 3 fonts: Plus Jakarta Sans (headings, geometric authority), DM Sans (body, warm readability — kept from TaxKlaro), IBM Plex Mono (peso amounts, institutional precision). 10-step type scale (48px hero → 11px overline). 4-step peso amount scale (40px → 13px). ~30 CSS custom properties for families, sizes, line heights, letter spacing, weights. ~135KB total font budget (variable fonts). Peso display rules: always mono, always tabular-nums, always ₱ prefix, always 2 decimals. Migration path: TaxKlaro keeps DM Sans body, replaces DM Serif Display headings; Inheritance replaces both Inter and Lora. |
| 8 | 2026-03-15 | 2.4 Spacing & layout system | ~4min | 4px base unit, 8px rhythm, 16-step spacing scale (0→96px). 4 content widths: 576px narrow (wizards) → 1120px full (platform home). 960px default max-width — optimal for compliance forms. 3 grid gap sizes (tight/default/loose). Component spacing: cards 16→24px padding responsive, forms with 6px label gap / 16px field gap / 24px group gap, inputs 40px height, buttons 32/40/48px. Nav 56→64px height, sidebar 256px. Navy-tinted shadows (rgba 30,58,95) across 5 elevations. 7 radius steps (6→pill, 8px default). 6 z-index layers. Animation tokens from Inheritance. 5 breakpoints (Tailwind defaults). ~55 CSS custom properties. |
| 9 | 2026-03-15 | 2.5 Sub-tool branding | ~5min | Core decision: Angkin is the brand, tools are features — NO per-tool accent colors, NO per-category palettes. Only TaxKlaro + Inheritance grandfathered as named flagships (minimal special treatment). "by Angkin" badge: 16px mark + 12px text on every tool page. Tool card: category icon + name + description + archetype badge. Category system: 14 domains differentiated by icons only (defined in 2.6). Featured/seasonal tools get gold border accent (max 2-4). Tool header: nav + breadcrumb (Angkin › Category › Tool) + title + subtitle. Shared result attribution: 12px logo + "angkin.ph". ~25 new CSS custom properties for cards, badges, headers. TaxKlaro drops blue, adopts navy; Inheritance unchanged. |
| 10 | 2026-03-15 | 2.6 Iconography | ~4min | Phosphor Icons selected (6,000+ icons, 6 weights, MIT). Regular weight (1.5px stroke) for all UI; duotone for category icons in 40px tool card containers. 14 category icons mapped: Receipt (Tax), Users Three (Labor), Shield Check (Social Insurance), Airplane Takeoff (OFW), Buildings (Corporate), House Line (Property), Storefront (Business Reg), Certificate (Professional), Package (Customs), Lightbulb (IP), Fire Simple (Safety), Bank (Financial), Lightning (Energy), Scales (Legal). 8 archetype icons. ~25 UI icons for nav/forms/data. Anti-patterns: no filled icons, no emoji, no custom SVGs, no mixed libraries, no animated icons. ~10 new CSS custom properties. |
| 11 | 2026-03-15 | 3.1 Buttons | ~5min | 5 variants (primary/secondary/ghost/danger/outline-danger), 3 sizes (32/40/48px), 6 states each = 90 combinations. Primary: navy-600 fill, white text, shadow-xs. No opacity on disabled (navy-200 fill instead). 1px translateY on press. Ghost uses weight-500 (quieter). Danger uses error-700 (not 500 — darker = serious, not alarming). Large buttons keep 15px font (same as medium — padding increases, not text). Button groups: column-reverse on mobile (primary on top for thumb zone). Full CSS implementation with shadcn/ui variant mapping. 8 anti-patterns documented. Icon-only buttons spec'd at all 3 sizes. |
| 12 | 2026-03-15 | 3.2 Form inputs | ~6min | 8 input types (text, number, peso, select, radio, checkbox, toggle, textarea) × 6+ states = ~50 state combinations. Navy-600 focus border + 3px soft navy outline (15% opacity glow). Shadow-xs at rest for subtle lift. No opacity on disabled (gray bg instead). Peso input: ₱ prefix in IBM Plex Mono, format on blur. Radio card variant for regime selection. Checkbox 18px (up from shadcn 16px). Toggle 44×24px (iOS proportions). Error replaces help text (doesn't stack). No floating labels, no underline-only inputs, no colored backgrounds. Validate on blur not keystroke. Full CSS implementation + shadcn/ui mapping. ~25 new CSS custom properties. 8 anti-patterns documented. |
| 13 | 2026-03-15 | 3.3 Cards | ~5min | 4 card types (content, tool, result, stat) sharing one visual language. Cards get radius-lg (10px) — larger than inputs/buttons (8px) for container hierarchy. Content card: quiet, non-interactive by default, sectioned variant with header/body/footer. Tool card: always interactive, navy-300 hover border, shadow-sm→shadow-md on hover, gold featured variant with "Timely" badge. Result card is the crown jewel: radius-xl (14px), shadow-md, 3px navy top accent bar, large peso amount in IBM Plex Mono 32px, dotted leaders in breakdown table, semantic variants (optimal=success, warning, suboptimal). Stat card: compact 16px padding, number-first layout, optional trend indicator, semantic left-border variants. No shadow in dark mode (surface elevation + borders instead). 8 anti-patterns (no card-in-card-in-card, no carousel, no gradient borders). ~20 new CSS custom properties. Full dark mode for all 4 types. |
| 14 | 2026-03-15 | 3.4 Navigation | ~6min | 2 navigation modes: top bar (public/tool pages, 64px desktop/56px mobile) + sidebar (authenticated views, 256px navy-600). Top bar: sticky with blur, logo lockup + nav links + search/theme icons. Tools mega-menu: 720px dropdown with 3-column category grid + inline search. Breadcrumbs: net-new pattern — Angkin › Category › Tool, 3 levels max. Sidebar: navy-600 bg, gold-400 3px left-border active indicator, white text at 80% opacity. Mobile: left-sliding sheet (280px), 44px touch targets, backdrop overlay. Tab bar: horizontal scroll, 2px bottom active indicator, used for category filters + in-page sections. Full dark mode for all 5 nav patterns. 8 anti-patterns (no bottom tab bar, no icon-only collapsed sidebar, no transparent hero-overlapping bar). ~25 new CSS custom properties. Migration notes for both apps. |
| 15 | 2026-03-15 | 3.5 Data display | ~5min | 6 components: peso amount display (4 sizes, 4 semantic colors, overline+caption context), comparison table (sticky first column, recommended column highlight, scroll on mobile, difference row), progress bars (default/segmented/threshold/indeterminate, 3 sizes, 5 colors), badges (8 color variants × 3 fill styles × 3 sizes, pill shape, dot/count/icon sub-elements), tooltips (dark default, 280px max, arrow, rich variant with formula block, light variant for dark surfaces), stat blocks (vertical + inline orientations, total row emphasis). ~20 new CSS custom properties. Full dark mode for all 6 components. TaxKlaro migration map: RegimeComparisonTable→comparison-table, Badge variants preserved, peso amounts switch to IBM Plex Mono. 8 anti-patterns (no red for neutral tax due, no animated counters, no pie charts for comparisons, no truncated peso amounts). |
| 16 | 2026-03-15 | 3.6 Modals & overlays | ~5min | 3 overlay types: confirmation dialog (440px max, scale+fade in), info modal (560px max, sticky header/footer, scrollable body, custom scrollbar), bottom sheet (mobile, full-width, slide up 300ms, drag handle 40×4px). Navy-tinted backdrop rgba(11,22,40,0.6) — not black. Shared: radius-lg (10px), shadow-xl, z-modal (40), focus trap, Escape close. Destructive confirmations: warning icon in tinted circle + ghost cancel + danger action. No nested modals, no fullscreen desktop modals, no auto-dismissing modals. Bottom sheet: env(safe-area-inset-bottom) for notch devices, column-reverse button layout. Dark mode: surface-1 bg, higher opacity backdrop (75%), dark shadows. 7 anti-patterns. Full CSS implementation for all 3 types + backdrop + dark mode. |
| 17 | 2026-03-15 | 3.7 Wizard pattern | ~5min | Unified wizard pattern for 3-step to 14-step flows. Desktop: 32px numbered circles + 2px connector lines, horizontally scrollable at 10+ steps (scroll-snap, center current). Mobile: compact "Step X of Y · Name" text + 4px progress bar. Step content: 576px narrow max-width, centered. Step transitions: 24px directional slide + fade, 250ms. Nav buttons: flex row desktop (Back ghost ← → Continue primary), column-reverse mobile (Continue on top for thumb zone). Validation: scroll-to-first-error + button shake (300ms) + inline field errors. 5+ errors show summary banner (error-50 bg, 3px left border, clickable items). Auto-save indicator: 12px text in nav area (saving/saved/error states). Conditional steps: dynamic show/hide with toast notification. Dark mode: navy-400 circles/fill, surface-0 backgrounds. 7 anti-patterns (no vertical desktop list, no clickable jump-to-step, no wizard-in-modal). ~45 CSS custom properties. Migration paths for both TaxKlaro (replace percentage bar) and Inheritance (add mobile progress bar). |
| 18 | 2026-03-15 | 4.1 Platform home | ~5min | 3 mockups generated: above-the-fold (hero + nav + tool grid), scrolled view (category sections + footer), tools mega-menu dropdown. Hero: text-only ("Philippine Compliance, Simplified"), 48px extrabold, 96px vertical padding, neutral-50 bg. Category filter: horizontal pill bar, sticky below nav on scroll. Tool grid: 3-column at 1120px max-width, 24px gap, category section headings. Featured cards: gold border + "Timely" badge. Footer: logo + 3-col link groups + copyright. Mega-menu: 720px dropdown, search + 3-col category grid + "View all 148 tools". Note: logo reference image unavailable — mockups use AI-approximated mark. |
| 19 | 2026-03-15 | 4.2 TaxKlaro landing | ~5min | 3 mockups generated: hero + how-it-works, features + regime explainer, social proof + pricing + CTA + footer. Two-column hero: pain-point headline ("Stop Guessing Your Tax Regime") + regime comparison table preview. "BY ANGKIN" overline establishes parent brand. 3-step walkthrough with connected circles. 2×3 feature grid with gold PRO badges. 3-path regime explainer cards with formulas. Navy social proof strip (4 stats). 3-tier pricing (Free/Pro ₱299yr/Enterprise) with gold-accented Pro card. Navy CTA band + shared Angkin footer. No TaxKlaro-specific accent color — uses system navy throughout per 2.5 decision. Logo reference unavailable — AI-approximated mark. |
| 20 | 2026-03-15 | 4.3 TaxKlaro results | ~5min | 3 mockups generated: regime comparison cards, detailed breakdown, bottom sections. Progressive disclosure IA: verdict (banner) → comparison (3 cards) → breakdown (dotted leaders) → credits/balance → BIR form → installments → full comparison table. Key decisions: recommended path always leftmost, balance payable in navy (not red — tax is normal), credits in accounting parentheses, no pie charts. Uses result-card--optimal/suboptimal variants from 3.3, comparison-table from 3.5, segmented progress from 3.5. Realistic data from actual TaxKlaro engine types (3 paths, CWT, quarterly payments, Form 1701A). Logo reference unavailable — AI-approximated mark. |
| 21 | 2026-03-15 | 4.4 Inheritance wizard | ~5min | 3 mockups generated: Family Tree step (person cards grid), person edit slide-out panel, Estate Details opening step. Sidebar wizard stepper (240px, sticky) + content area layout. Person cards in 2-column grid with green left border for compulsory heirs. 480px right slide-out panel for editing person details (17+ fields with conditional visibility). Estate step: large peso money input + radio card succession type. Progressive complexity: 2 fields (Step 1) to N×17 (Step 3). Step 4 conditional on testate. Based on actual app data: 11 relationship types, 6 filiation proof types, adoption/unworthiness/renunciation accordions. Logo reference unavailable — AI-approximated mark. |
| 22 | 2026-03-15 | 4.5 New tool template | ~5min | 3 mockups generated: empty/input state, results state, validation error state. Example tool: 13th Month Pay Calculator (Labor, single-form archetype). Two-mode layout: centered narrow (576px) for input → two-column (960px) for results with sticky result card. Result card uses 3px navy accent, 32px IBM Plex Mono peso amount, dotted-leader breakdown, legal reference note. Error handling: summary banner (error-50 bg, 3px left border, clickable items) + per-field red borders replacing help text. Template file structure documented for developer copy-paste. Archetype adaptation table: same components across all 8 archetypes, only layout geometry changes. Logo reference unavailable — AI-approximated mark. |
| 23 | 2026-03-15 | 4.6 Mobile treatments | ~5min | 4 mockups generated at 375×812px: platform home (hamburger nav, centered logo, search bar, 2-col tool grid, sticky category pills), wizard step (text progress + 4px bar, single-col person cards, column-reverse nav buttons), results page (stacked regime cards, accordion breakdown, sticky CTA), nav menu (280px slide-in sheet, category list with icons, dark mode toggle). Key decisions: no bottom tab bar, stacked cards over horizontal scroll, column-reverse buttons for thumb zone, 44px minimum touch targets, safe-area-inset-bottom on all sticky elements. Typography scale reduces hero 48→24px, peso amounts 32→24px. Logo reference unavailable — AI-approximated mark. |
| 24 | 2026-03-15 | 4.7 Dark mode screens | ~4min | 3 mockups generated at 1280px: platform home (navy-950 base, surface-1 tool cards, gold "Timely" badge pops at 8.4:1 AAA), TaxKlaro results (regime comparison with green-tinted recommended card #071F16, navy-200 monospace peso amounts, teal/gold chart), wizard step (navy-900 sidebar darker-than-content, surface-2 input fields, navy-400 focus glow). Gold accent is the dark mode star — natural high-contrast on dark surfaces. Layered elevation system (#060D18→#0B1628→#101D30→#152438) creates depth without heavy borders. Logo reference unavailable — AI-approximated mark. |
| 25 | 2026-03-15 | 5.1 Scaffold HTML | ~5min | Built `output/ui-treatment-deck.html` — single-file scaffold with cover (128px inverted SVG logo, navy hero), sticky TOC (4-col grid, gold numbers, compact-on-scroll), 12 sections (alternating bg, section-number/title/desc/content pattern), and footer. ~250 CSS custom properties inlined: full color system (navy/gold/neutral/semantic/chart/compliance), typography (3 fonts via Google Fonts, 10-step scale, peso sizes), spacing (16-step scale, 7 radius steps, 5 navy-tinted shadows, transitions, z-index). Reusable CSS classes for deck content: content-grid, content-block, mockup (with browser chrome), swatch-row, code-block, type-specimen, token-table, spacing-demo. All 12 sections have placeholder-content divs ready for 5.2 population. |
