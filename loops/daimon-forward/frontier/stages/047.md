# Stage 047 — Table + Pagination Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/table.tsx` with sortable columns, row click handler, and responsive horizontal scroll on mobile
2. Create `src/components/ui/pagination.tsx` with page numbers, prev/next buttons, and items per page selector

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 047 - Table + Pagination components`
