# Stage 051 — AuthLayout + Public Layout

## Spec Files
- `frontend/component-library.md`
- `source/brand-guidelines.md`

## Instructions
1. Create `src/components/layout/auth-layout.tsx` with centered card, logo, and background
2. Create `src/components/layout/auth-card.tsx` as a white card container
3. Create `src/components/layout/public-navbar.tsx` with logo, nav links, and Login/Sign Up buttons
4. Create `src/components/layout/public-layout.tsx` with navbar + content + footer
5. Create `src/components/layout/public-footer.tsx` with links and copyright

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 051 - AuthLayout + Public Layout`
