# Stage 031 — API Key Integration API: Store + Delete

## Spec Files
- `api/routes.md`
- `integrations/api-key-services.md`

## Instructions
1. Create `src/app/api/integrations/api-key/route.ts` — POST handler:
   - Accept service name and API key
   - Validate the API key by making a test request to the service endpoint
   - Store validated key in Supabase Vault
   - Create `tenant_service_connections` row with auth_type = api_key
2. Create `src/app/api/integrations/[service]/route.ts` — DELETE handler:
   - Remove the service connection
   - Delete the Vault secret
3. Both routes require authenticated session

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 031 - api key integration store and delete`
