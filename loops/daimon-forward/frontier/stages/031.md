# Stage 031 — API Key Integration API: Store + Delete

## Spec Files
- `api/routes.md`
- `integrations/api-key-services.md`

## Instructions
1. Create `src/app/api/integrations/api-key/route.ts` — POST handler that validates the API key against the service's test endpoint, stores it in Vault, and creates a tenant_service_connections row
2. Create `src/app/api/integrations/[service]/route.ts` — DELETE handler that removes the service connection and cleans up the Vault secret
3. Both routes require authentication and tenant membership

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 031 - api key integration api: store + delete`
