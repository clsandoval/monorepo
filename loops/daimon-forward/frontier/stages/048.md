# Stage 048 — StatCard + ActivityFeed + CopyToClipboard

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/stat-card.tsx` with metric value, label, trend arrow (up/down), and color
2. Create `src/components/ui/activity-feed.tsx` with timeline showing icon, description, and timestamp per entry
3. Create `src/components/ui/copy-to-clipboard.tsx` with click to copy and "Copied!" feedback

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 048 - StatCard + ActivityFeed + CopyToClipboard`
