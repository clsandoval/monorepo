# Stage 094 — Meta Tags + OpenGraph Per Page

## Spec Files
- `seo-and-growth/seo-strategy.md`

## Instructions
1. Add metadata exports to every page using Next.js Metadata API
2. Set title, description, OpenGraph image, and Twitter card per page
3. Create `src/app/layout.tsx` default metadata as fallback
4. Create `src/lib/seo/metadata.ts` helper for consistent metadata generation
5. Ensure each page overrides defaults with page-specific meta

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 094 - meta tags and opengraph per page`
