# Stage 024 — Stripe Webhook Handlers: Checkout + Subscription

## Spec Files
- `integrations/stripe.md`
- `api/webhooks.md`

## Instructions
1. Implement `handleCheckoutCompleted` — link Stripe customer ID to the tenant
2. Implement `handleSubscriptionCreated` — insert or update tenant_subscriptions row
3. Implement `handleSubscriptionUpdated` — sync plan and status from Stripe to database
4. Implement `handleSubscriptionDeleted` — downgrade tenant to free plan
5. Wire all handlers into the webhook route switch statement

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 024 - stripe webhook handlers checkout and subscription`
