# Stage 035 — Admin API: Revoke Key + Delete Connection

## Spec Files
- `api/routes.md`
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/api/admin/tenants/[id]/revoke-api-key/route.ts` — POST handler that removes the API key from Vault, deletes the tenant_api_keys row, and writes an audit log entry
2. Create `src/app/api/admin/tenants/[id]/service-connections/[connectionId]/route.ts` — DELETE handler that removes the service connection, cleans up Vault, and writes an audit log entry
3. Both routes require admin-only authentication check

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 035 - admin api: revoke key + delete connection`
