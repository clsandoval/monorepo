# Stage 017 — Auth Context + useAuth Hook

## Spec Files
- `source/existing-auth.md`
- `frontend/auth-pages.md`

## Instructions
1. Create `src/lib/auth/auth-context.tsx` with AuthProvider that wraps the app, manages Supabase auth state, and provides context
2. Create `src/lib/auth/use-auth.ts` hook exposing: user, session, loading, signIn, signUp, signOut, resetPassword
3. Wire AuthProvider into root layout (`src/app/layout.tsx`)

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 017 - auth context and useauth hook`
