# Stage 084 — Phantom Pages: Changelog + About

## Spec Files
- `frontend/phantom-pages.md`

## Instructions
1. Create `src/app/changelog/page.tsx` with reverse-chronological release log
2. Populate with placeholder entries from spec
3. Create `src/app/about/page.tsx` with company description and team section from spec
4. Use PublicLayout wrapper for both pages
5. Style changelog entries with date headers and version badges

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 084 - phantom pages changelog and about`
