# Stage 025 — Stripe Webhook Handlers: Invoice + Customer

## Spec Files
- `integrations/stripe.md`
- `api/webhooks.md`

## Instructions
1. Implement `handleInvoicePaymentSucceeded` — refresh current_period_start and current_period_end dates
2. Implement `handleInvoicePaymentFailed` — mark subscription status as past_due
3. Implement `handleCustomerDeleted` — clear stripe_customer_id from tenant
4. Complete the webhook handler switch for all 8 event types

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 025 - stripe webhook handlers invoice and customer`
