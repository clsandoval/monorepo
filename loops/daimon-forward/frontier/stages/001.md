# Stage 001 — Next.js + TypeScript Strict Scaffold

## Spec Files
- `deployment/infrastructure.md`

## Instructions
1. Run `npx create-next-app@latest apps/daimon-saas --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
2. Open `apps/daimon-saas/tsconfig.json` and set `"strict": true` in compilerOptions
3. Verify the default page renders by running `npm run dev` and loading localhost:3000

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 001 - next.js + typescript strict scaffold`
