# Stage 021 — Auth Middleware + Protected Routes

## Spec Files
- `api/auth.md`
- `source/existing-auth.md`

## Instructions
1. Create `src/middleware.ts` with Supabase Auth session check
2. Protect all `/dashboard/*` and `/admin/*` routes — require authenticated session
3. Redirect unauthenticated users to `/login`
4. Redirect authenticated users away from `/login` and `/signup` to `/dashboard`
5. Handle auth callback at `/auth/callback` (allow through without session)

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 021 - auth middleware and protected routes`
