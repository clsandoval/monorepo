# Stage 034 — Admin API: Plan Override + Impersonate

## Spec Files
- `api/routes.md`
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/api/admin/tenants/[id]/plan/route.ts` — PATCH handler:
   - Override tenant plan directly (bypass Stripe)
   - Write entry to admin_audit_log with old and new plan
2. Create `src/app/api/admin/tenants/[id]/impersonate/route.ts` — POST handler:
   - Set impersonation session cookie with target tenant context
   - Write entry to admin_audit_log
3. Both routes require admin-only auth check

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 034 - admin api plan override and impersonate`
