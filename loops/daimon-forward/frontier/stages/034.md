# Stage 034 — Admin API: Plan Override + Impersonate

## Spec Files
- `api/routes.md`
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/api/admin/tenants/[id]/plan/route.ts` — PATCH handler that overrides the tenant's plan directly and writes an audit log entry
2. Create `src/app/api/admin/tenants/[id]/impersonate/route.ts` — POST handler that sets an impersonation session cookie for the target tenant and writes an audit log entry
3. Both routes require admin-only authentication check

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 034 - admin api: plan override + impersonate`
