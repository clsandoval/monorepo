# Stage 026 — Billing API: Checkout Session

## Spec Files
- `api/routes.md`
- `integrations/stripe.md`

## Instructions
1. Create `src/app/api/billing/checkout/route.ts`
2. Implement POST handler that creates a Stripe Checkout Session
3. Accept plan and billing period (monthly/annual) in the request body
4. Set success_url and cancel_url pointing back to the app
5. Pre-fill customer email from the authenticated user session
6. Require authentication — return 401 if no session

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 026 - billing api: checkout session`
