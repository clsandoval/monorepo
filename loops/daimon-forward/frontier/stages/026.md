# Stage 026 — Billing API: Checkout Session

## Spec Files
- `api/routes.md`
- `integrations/stripe.md`

## Instructions
1. Create `src/app/api/billing/checkout/route.ts`
2. POST handler creates a Stripe Checkout Session with:
   - Plan selection (starter/pro)
   - Billing period (monthly/annual)
   - Success and cancel URLs
   - Customer email from authenticated user
3. Require authenticated session (return 401 if not authenticated)
4. Return the checkout session URL

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 026 - billing api checkout session`
