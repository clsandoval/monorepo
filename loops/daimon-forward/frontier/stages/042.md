# Stage 042 — AlertBanner Component

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/alert-banner.tsx` with 5 semantic variants: info, success, warning, error, tip
2. Each variant includes icon, title, description, and optional dismiss button
3. Apply colors from brand spec for each variant

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 042 - AlertBanner component`
