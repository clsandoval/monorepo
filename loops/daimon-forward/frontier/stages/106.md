# Stage 106 — Playwright Config + Local Dev Setup Stage

## Spec Files
- `qa/playwright-verification.md`

## Instructions
1. Update `playwright.config.ts`: webServer starts `npm run dev` on port 3000
2. Add `npx supabase start` as globalSetup
3. Create `e2e/fixtures/auth.setup.ts` that signs in test user from seed data
4. Create `e2e/fixtures/test-data.ts` with TEST_USER constant

## Verify
- `cd apps/daimon-saas && npx playwright test --list` — shows test files

## Commit
`daimon(forward): stage 106 - playwright config and local dev setup`
