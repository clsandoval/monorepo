# Stage 003 — Playwright + Vitest Infrastructure

## Spec Files
- `qa/playwright-verification.md`

## Instructions
1. Install playwright, vitest, and @testing-library/react as dev dependencies
2. Create `playwright.config.ts` with: desktop viewport 1280x800, mobile viewport 375x812, baseURL localhost:3000, sequential execution, single worker
3. Create `vitest.config.ts` with appropriate settings for the Next.js project
4. Create `e2e/` directory
5. Create a smoke test that loads the dev page and asserts it renders

## Verify
- `cd apps/daimon-saas && npx playwright install chromium && npx vitest run --passWithNoTests` — exits 0

## Commit
`daimon(forward): stage 003 - playwright + vitest infrastructure`
