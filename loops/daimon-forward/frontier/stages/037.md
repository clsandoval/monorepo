# Stage 037 — Design Tokens + Tailwind Brand Theme

## Spec Files
- `source/brand-guidelines.md`
- `ui/design-system.md`

## Instructions
1. Update `tailwind.config.ts` (or CSS variables in `globals.css` for Tailwind 4) with PyMC brand:
   - 6 brand colors with exact hex values from spec
   - Type scale using `clamp()` for fluid typography
   - Spacing system
2. Create `src/lib/design-tokens.ts` exporting color, font, and spacing constants for programmatic use outside of Tailwind classes

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 037 - design tokens + tailwind brand theme`
