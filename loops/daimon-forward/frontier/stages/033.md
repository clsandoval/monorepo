# Stage 033 — Admin API: Suspend + Unsuspend

## Spec Files
- `api/routes.md`
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/api/admin/tenants/[id]/suspend/route.ts` — POST handler:
   - Set tenant status to suspended
   - Write entry to admin_audit_log
2. Create `src/app/api/admin/tenants/[id]/unsuspend/route.ts` — POST handler:
   - Restore tenant to previous status (active)
   - Write entry to admin_audit_log
3. Both routes require admin-only auth check (verify user role is admin)

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 033 - admin api suspend and unsuspend`
