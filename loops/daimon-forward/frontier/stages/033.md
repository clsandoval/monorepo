# Stage 033 — Admin API: Suspend + Unsuspend

## Spec Files
- `api/routes.md`
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/api/admin/tenants/[id]/suspend/route.ts` — POST handler that sets tenant status to suspended and writes an entry to admin_audit_log
2. Create `src/app/api/admin/tenants/[id]/unsuspend/route.ts` — POST handler that restores the tenant's previous status and writes an audit log entry
3. Both routes require admin-only authentication check

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 033 - admin api: suspend + unsuspend`
