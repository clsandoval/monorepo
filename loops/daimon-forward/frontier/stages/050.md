# Stage 050 — DashboardLayout + Sidebar + Topbar

## Spec Files
- `frontend/component-library.md`
- `source/design-spec.md`

## Instructions
1. Create `src/components/layout/dashboard-layout.tsx` with sidebar + main content area
2. Create `src/components/layout/sidebar.tsx` with logo, nav links (Dashboard, Integrations, Billing, Settings), active state, and collapse on mobile
3. Create `src/components/layout/dashboard-topbar.tsx` with workspace name, user avatar, and dropdown with Sign Out

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 050 - DashboardLayout + Sidebar + Topbar`
