# Stage 049 — DropdownMenu + Tabs Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/dropdown-menu.tsx` with trigger button, menu items, dividers, and keyboard navigation
2. Create `src/components/ui/tabs.tsx` with tab list, tab panels, active indicator, and keyboard left/right navigation

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 049 - DropdownMenu + Tabs components`
