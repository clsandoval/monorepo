# Stage 029 — Discord Connection API: CRUD

## Spec Files
- `api/routes.md`
- `integrations/discord.md`

## Instructions
1. Create `src/app/api/discord-connections/route.ts` — POST handler:
   - Validate Discord bot token format with regex
   - Store token in Supabase Vault
   - Create `discord_connections` row with vault_secret_id and initial status
2. Create `src/app/api/discord-connections/[id]/route.ts`:
   - PATCH handler — update guild_id or rotate token (store new token in Vault)
   - DELETE handler — remove Vault secret and delete the connection row
3. All routes require authenticated session

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 029 - discord connection api crud`
