# Stage 029 — Discord Connection API: CRUD

## Spec Files
- `api/routes.md`
- `integrations/discord.md`

## Instructions
1. Create `src/app/api/discord-connections/route.ts` — POST handler that validates the bot token against a regex pattern, stores it in Vault, and creates a discord_connections row
2. Create `src/app/api/discord-connections/[id]/route.ts`:
   - PATCH handler to update guild_id or rotate the bot token
   - DELETE handler to remove the connection and clean up the Vault secret
3. All routes require authentication and tenant membership

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 029 - discord connection api: crud`
