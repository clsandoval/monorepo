# Stage 078 — Admin Panel: Actions (Suspend, Plan Override, Impersonate)

## Spec Files
- `frontend/admin-panel.md`

## Instructions
1. Wire Suspend button to POST /api/admin/tenants/[id]/suspend with confirmation dialog
2. Wire Unsuspend button with same pattern
3. Wire Override Plan with dropdown selector + confirmation
4. Implement Impersonate as special session with banner "Viewing as [tenant]"

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 078 - admin panel actions for suspend, plan override, and impersonate`
