# Stage 038 — Button + IconButton + Link Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/button.tsx` with:
   - 4 variants: primary, secondary, outline, ghost
   - 3 sizes: sm, md, lg
   - Loading state with spinner
   - Disabled state
2. Create `src/components/ui/icon-button.tsx` — icon-only button with same variants and sizes
3. Create `src/components/ui/link.tsx` — styled wrapper around Next.js Link component

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 038 - button + iconbutton + link components`
