# Stage 095 — Schema.org Markup

## Spec Files
- `seo-and-growth/seo-strategy.md`

## Instructions
1. Create `src/components/seo/json-ld.tsx` component for rendering JSON-LD
2. Add Organization schema to landing page
3. Add SoftwareApplication schema to landing page
4. Add FAQPage schema to docs/faq page
5. Add BreadcrumbList schema to all pages
6. Validate JSON-LD output structure matches schema.org specs

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 095 - schema.org markup`
