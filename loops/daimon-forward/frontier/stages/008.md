# Stage 008 — Tenant API Keys Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`

## Instructions
1. Create migration `supabase/migrations/20260400000004_create_tenant_api_keys.sql`
2. Define tenant_api_keys table with columns: id, tenant_id (FK), type, vault_secret_id, name, key_hint, created_at, updated_at
3. Add 2 indexes from spec
4. Add update_updated_at trigger
5. Add RLS policies
6. Enable Realtime on the tenant_api_keys table

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 008 - tenant api keys table`
