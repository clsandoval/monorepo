# Stage 030 — OAuth Integration API: Start + Callback

## Spec Files
- `api/routes.md`
- `integrations/oauth-services.md`

## Instructions
1. Create `src/app/api/integrations/oauth/start/route.ts` — GET handler that redirects to the OAuth provider's authorization URL with state parameter and correct scopes per service (GitHub, Google, Linear)
2. Create `src/app/api/integrations/oauth/callback/route.ts` — GET handler that exchanges the authorization code for tokens, stores tokens in Vault, and creates a tenant_service_connections row
3. Both routes require authentication

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 030 - oauth integration api: start + callback`
