# Stage 030 — OAuth Integration API: Start + Callback

## Spec Files
- `api/routes.md`
- `integrations/oauth-services.md`

## Instructions
1. Create `src/app/api/integrations/oauth/start/route.ts` — GET handler:
   - Accept service parameter (GitHub, Google, Linear)
   - Generate state token and store in session
   - Redirect to OAuth provider with correct scopes per service
2. Create `src/app/api/integrations/oauth/callback/route.ts` — GET handler:
   - Verify state parameter matches session
   - Exchange authorization code for access + refresh tokens
   - Store tokens in Supabase Vault
   - Create `tenant_service_connections` row with vault_secret_id
   - Redirect to settings page with success indicator

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 030 - oauth integration api start and callback`
