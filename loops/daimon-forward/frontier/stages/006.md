# Stage 006 — Tenant Members Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`

## Instructions
1. Create migration `supabase/migrations/20260400000002_create_tenant_members.sql`
2. Define tenant_members table with columns: id, tenant_id (FK to tenants), user_id (FK to auth.users), role, created_at
3. Add 2 indexes from spec
4. Add RLS policies for tenant member access

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 006 - tenant members table`
