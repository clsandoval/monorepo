# Stage 002 — Supabase Local Dev Setup

## Spec Files
- `deployment/infrastructure.md`
- `database/schema.md`

## Instructions
1. Run `cd apps/daimon-saas && npx supabase init`
2. Configure `supabase/config.toml` with project_id: daimon-saas, port 54321, auth enabled, Realtime enabled
3. Create an empty `supabase/seed.sql` file
4. Verify `npx supabase start` works and all services come up healthy

## Verify
- `cd apps/daimon-saas && npx supabase start` — completes successfully
- `cd apps/daimon-saas && npx supabase status` — shows healthy services

## Commit
`daimon(forward): stage 002 - supabase local dev setup`
