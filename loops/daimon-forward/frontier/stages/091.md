# Stage 091 — Stripe Webhook Tests

## Spec Files
- `integrations/stripe.md`
- `api/webhooks.md`

## Instructions
1. Create `src/__tests__/api/stripe-webhook.test.ts`
2. Test all 8 event types with mock payloads from Stripe
3. Test idempotency: duplicate event_id should not cause duplicate processing
4. Test signature verification failure returns 400
5. Test unknown event type is handled gracefully (logged and ignored)

## Verify
- `cd apps/daimon-saas && npx vitest run src/__tests__/api/stripe-webhook.test.ts` — passes

## Commit
`daimon(forward): stage 091 - stripe webhook tests`
