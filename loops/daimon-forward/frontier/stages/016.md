# Stage 016 — Supabase Auth Configuration

## Spec Files
- `source/existing-auth.md`
- `api/auth.md`

## Instructions
1. Update `supabase/config.toml`:
   - Enable email/password auth
   - Set JWT expiry
   - Configure redirect URLs for localhost:3000
2. Create auth email templates directory if needed

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 016 - supabase auth configuration`
