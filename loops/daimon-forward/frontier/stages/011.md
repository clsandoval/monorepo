# Stage 011 — Stripe Webhook Events Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`

## Instructions
1. Create migration `supabase/migrations/20260400000007_create_stripe_webhook_events.sql`
2. Define stripe_webhook_events table with columns: id, event_id (UNIQUE), event_type, processed_at, created_at
3. Add 2 indexes from spec
4. Add RLS policies restricting access to service-role only

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 011 - stripe webhook events table`
