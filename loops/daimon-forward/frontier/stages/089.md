# Stage 089 — Plan Gating Logic

## Spec Files
- `premium/features-by-tier.md`
- `premium/tiers.md`

## Instructions
1. Create `src/lib/plans/gate.ts` with plan check utilities
2. Implement canAccessTool(plan, toolName) based on tier gating rules from spec
3. Implement getMaxConnections(plan) returning connection limits per tier
4. Implement isPlanFeature(plan, feature) for generic feature checks
5. Wire into integrations page to disable connections beyond limit
6. Wire into tool reference to show plan badge per tool

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 089 - plan gating logic`
