# Stage 039 — FormInput + PasswordInput Components

## Spec Files
- `frontend/component-library.md`
- `frontend/validation-rules.md`

## Instructions
1. Create `src/components/ui/form-input.tsx` with:
   - Label
   - Error message display
   - Helper text
   - Required indicator
   - Accepts react-hook-form `register` props
2. Create `src/components/ui/password-input.tsx` with:
   - Show/hide toggle button
   - Same label, error, helper, required features as FormInput
   - Accepts react-hook-form `register` props

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 039 - forminput + passwordinput components`
