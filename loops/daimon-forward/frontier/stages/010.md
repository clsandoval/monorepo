# Stage 010 — Tenant Subscriptions Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`

## Instructions
1. Create migration `supabase/migrations/20260400000006_create_tenant_subscriptions.sql`
2. Define tenant_subscriptions table with columns: id, tenant_id (FK), plan, status, stripe_subscription_id, stripe_price_id, current_period_start, current_period_end, trial_end, canceled_at, cancel_at_period_end, created_at, updated_at
3. Add 3 indexes from spec
4. Add update_updated_at trigger
5. Add RLS policies
6. Add plan cascade trigger to sync subscription plan changes to the tenants table

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 010 - tenant subscriptions table`
