# Stage 105 — Micro-Interactions + Transitions

## Spec Files
- `ui/micro-interactions.md`

## Instructions
1. Add page transitions (fade) on route changes
2. Add sidebar expand/collapse animation (slide)
3. Add modal open/close animation (scale + fade)
4. Add toast enter/exit animation (slide)
5. Add dropdown open animation (scale)
6. Add button hover effect (subtle scale)
7. Use CSS transitions only, no heavy animation library

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 105 - micro-interactions and transitions`
