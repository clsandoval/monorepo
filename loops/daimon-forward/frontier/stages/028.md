# Stage 028 — Billing API: API Key Management

## Spec Files
- `api/routes.md`
- `database/vault-encryption.md`

## Instructions
1. Create `src/app/api/billing/api-keys/route.ts` — POST handler that saves an Anthropic or OpenAI API key to Supabase Vault and stores the reference in the tenant_api_keys table
2. Create `src/app/api/billing/api-keys/[id]/route.ts` — DELETE handler that removes the key from Vault and deletes the row from tenant_api_keys
3. Both routes require authentication and tenant membership

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 028 - billing api: api key management`
