# Stage 028 — Billing API: API Key Management

## Spec Files
- `api/routes.md`
- `database/vault-encryption.md`

## Instructions
1. Create `src/app/api/billing/api-keys/route.ts` — POST handler:
   - Accept Anthropic or OpenAI API key
   - Store the key securely in Supabase Vault
   - Create a `tenant_api_keys` row with vault_secret_id reference and key_hint (last 4 chars)
2. Create `src/app/api/billing/api-keys/[id]/route.ts` — DELETE handler:
   - Remove the key from Vault
   - Delete the tenant_api_keys row
3. Both routes require authenticated session

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 028 - billing api key management`
