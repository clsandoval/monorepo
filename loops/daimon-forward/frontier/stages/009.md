# Stage 009 — Tenant Service Connections Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`

## Instructions
1. Create migration `supabase/migrations/20260400000005_create_tenant_service_connections.sql`
2. Define tenant_service_connections table with columns: id, tenant_id (FK), service, auth_type, status, vault_secret_id, metadata, created_at, updated_at
3. Add 2 indexes from spec
4. Add update_updated_at trigger
5. Add RLS policies
6. Enable Realtime on the tenant_service_connections table
7. Add pg_cron job for periodic token refresh

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 009 - tenant service connections table`
