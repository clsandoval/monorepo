# Stage 086 — Stripe Environment + Price Config

## Spec Files
- `integrations/stripe.md`
- `deployment/environment.md`

## Instructions
1. Create `src/lib/stripe/config.ts` with price ID mapping (STRIPE_STARTER_MONTHLY_PRICE_ID, etc.)
2. Add plan-to-price lookup and billing period helpers
3. Create `src/lib/stripe/client.ts` initializing Stripe SDK with STRIPE_SECRET_KEY
4. Add all Stripe env vars to `.env.local.example`
5. Export typed config for use across billing features

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 086 - stripe environment and price config`
