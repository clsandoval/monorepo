# Stage 044 — ConfirmDialog + Modal Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/modal.tsx` as a generic modal with overlay
2. Implement close on escape key and click-outside
3. Create `src/components/ui/confirm-dialog.tsx` extending modal with title, message, confirm/cancel buttons
4. Add input confirmation variant for destructive actions (e.g., "type workspace name to delete")

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 044 - ConfirmDialog + Modal components`
