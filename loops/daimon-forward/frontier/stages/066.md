# Stage 066 — Integrations: Status + Error Handling

## Spec Files
- `frontend/integrations-page.md`
- `frontend/error-states.md`

## Instructions
1. Wire service connection status (active, expired, revoked, error) to visual states
2. Add error banner for failed connections
3. Add "Reconnect" action for expired OAuth tokens
4. Add loading skeletons for service grid

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 066 - integrations status + error handling`
