# Stage 108 — Desktop: Auth Pages Screenshots

## Spec Files
- `qa/screenshot-manifest.md`

## Instructions
1. Create `e2e/auth.spec.ts`
2. Screenshot `/login`: empty state, filled state, validation errors
3. Screenshot `/signup`: empty state, filled state, validation errors
4. Screenshot `/reset-password`: empty state, submitted state
5. Viewport 1280x800

## Verify
- `cd apps/daimon-saas && npx playwright test e2e/auth.spec.ts` — passes

## Commit
`daimon(forward): stage 108 - desktop auth pages screenshots`
