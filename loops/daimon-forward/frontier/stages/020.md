# Stage 020 — Password Reset Pages

## Spec Files
- `frontend/auth-pages.md`
- `frontend/copy.md`

## Instructions
1. Create `src/app/reset-password/page.tsx` — request form with email input, sends password reset email
2. Create `src/app/reset-password/confirm/page.tsx` — new password form shown after clicking the email link, accepts and confirms new password
3. Use copy strings from spec exactly

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 020 - password reset pages`
