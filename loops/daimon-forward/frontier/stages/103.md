# Stage 103 — Responsive: Mobile Layout for All Pages

## Spec Files
- `frontend/responsive-behavior.md`

## Instructions
1. Audit all pages at 375px viewport width
2. Fix sidebar to collapse to hamburger menu on mobile
3. Fix tables to get horizontal scroll on narrow viewports
4. Fix forms to go full-width on mobile
5. Fix cards to stack vertically on mobile
6. Ensure touch targets are at least 44px
7. Fix landing page responsive sections

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 103 - responsive mobile layout for all pages`
