# Stage 103 — Responsive: Mobile Layout for All Pages

## Spec Files
- `frontend/responsive-behavior.md`

## Instructions
1. Audit all pages at 375px viewport width
2. Ensure sidebar collapses to hamburger menu on mobile
3. Add horizontal scroll to tables on narrow viewports
4. Make forms go full-width on mobile
5. Stack cards vertically on mobile
6. Ensure all touch targets are at least 44px
7. Verify landing page responsive sections render correctly

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 103 - responsive mobile layout for all pages`
