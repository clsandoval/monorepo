# Stage 018 — Login Page

## Spec Files
- `frontend/auth-pages.md`
- `frontend/copy.md`
- `frontend/validation-rules.md`

## Instructions
1. Create `src/app/login/page.tsx` with email + password form
2. Add validation from spec (email format, password minimum length)
3. Add "Forgot password?" link pointing to /reset-password
4. Add "Sign up" link pointing to /signup
5. Use copy strings from spec exactly

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 018 - login page`
