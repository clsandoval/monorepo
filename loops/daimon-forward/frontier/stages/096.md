# Stage 096 — Sitemap + Robots.txt

## Spec Files
- `seo-and-growth/seo-strategy.md`

## Instructions
1. Create `src/app/sitemap.ts` using Next.js sitemap generation
2. Include all public pages in sitemap
3. Exclude admin and dashboard pages from sitemap
4. Create `src/app/robots.ts` allowing crawlers on public pages
5. Disallow /dashboard/*, /admin/*, /api/* in robots.txt

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 096 - sitemap and robots.txt`
