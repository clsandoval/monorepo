# Stage 032 — Settings API: Workspace + Account

## Spec Files
- `api/routes.md`
- `frontend/settings-page.md`

## Instructions
1. Create `src/app/api/settings/workspace/route.ts`:
   - POST — update tenant name
   - DELETE — delete workspace with cascade (remove all related rows)
2. Create `src/app/api/settings/account/display-name/route.ts`:
   - POST — update user display name in auth.users metadata
3. Create `src/app/api/settings/account/password/route.ts`:
   - POST — change password via Supabase Auth updateUser
4. All routes require authenticated session

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 032 - settings api workspace and account`
