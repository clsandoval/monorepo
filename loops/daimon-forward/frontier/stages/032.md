# Stage 032 — Settings API: Workspace + Account

## Spec Files
- `api/routes.md`
- `frontend/settings-page.md`

## Instructions
1. Create `src/app/api/settings/workspace/route.ts`:
   - POST handler to update tenant name
   - DELETE handler to delete workspace with full cascade (all related rows)
2. Create `src/app/api/settings/account/display-name/route.ts` — POST handler to update user display name
3. Create `src/app/api/settings/account/password/route.ts` — POST handler to change password via Supabase Auth
4. All routes require authentication

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 032 - settings api: workspace + account`
