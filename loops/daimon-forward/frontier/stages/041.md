# Stage 041 — ApiKeyInput + SearchInput Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/api-key-input.tsx` with masked display showing only last 4 characters
2. Add copy-to-clipboard functionality to ApiKeyInput
3. Create `src/components/ui/search-input.tsx` with search icon and clear button
4. Implement debounced onChange handler in SearchInput

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 041 - ApiKeyInput + SearchInput components`
