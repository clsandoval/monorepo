# Stage 100 — Error States: All Pages

## Spec Files
- `frontend/error-states.md`

## Instructions
1. Create Next.js `error.tsx` files for: dashboard, integrations, billing, settings, admin
2. Each shows ErrorState component with appropriate message
3. Create global `src/app/not-found.tsx` for 404 errors
4. Create global `src/app/error.tsx` for 500 errors
5. Use copy from spec for each error message

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 100 - error states for all pages`
