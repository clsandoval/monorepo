# Stage 005 — Tenants Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`

## Instructions
1. Create migration `supabase/migrations/20260400000001_create_tenants.sql`
2. Define tenants table with columns: id, name, slug, plan, status, stripe_customer_id, created_at, updated_at
3. Add 4 indexes from spec
4. Add update_updated_at trigger function and attach to tenants table
5. Add RLS policies for tenant access
6. Enable Realtime on the tenants table

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 005 - tenants table`
