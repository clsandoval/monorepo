# Stage 014 — Seed Data

## Spec Files
- `database/schema.md`
- `qa/playwright-verification.md`

## Instructions
1. Write `supabase/seed.sql` with realistic test data:
   - 1 admin user
   - 3 test tenants (free, starter, pro plans)
   - tenant_members rows linking users to tenants
   - 1 discord connection per tenant in various statuses (connected, disconnected, error)
   - Sample API keys (Anthropic, OpenAI) per tenant
   - Sample service connections in different states
   - Sample subscriptions in different states (active, trialing, past_due)
2. This seed data enables Playwright testing against realistic scenarios

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0, seed data visible in Supabase Studio

## Commit
`daimon(forward): stage 014 - seed data`
