# Stage 004 — Enum Types (SQL + TypeScript)

## Spec Files
- `database/schema.md`
- `database/migrations.md`

## Instructions
1. Create migration `supabase/migrations/20260400000000_create_enums.sql` with all 8 enum types from spec: tenant_plan, tenant_status, tenant_member_role, discord_connection_status, service_connection_status, subscription_status, api_key_type, service_auth_type
2. Create `src/lib/types/enums.ts` with matching TypeScript enums for each SQL enum type

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 004 - enum types (sql + typescript)`
