# Stage 040 — Select + Toggle + Checkbox Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/select.tsx` — dropdown component with options array, placeholder text, and error state
2. Create `src/components/ui/toggle.tsx` — switch/toggle component with label
3. Create `src/components/ui/checkbox.tsx` — checkbox component with label and optional description text

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 040 - select + toggle + checkbox components`
