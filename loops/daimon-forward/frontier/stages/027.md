# Stage 027 — Billing API: Portal + Downgrade

## Spec Files
- `api/routes.md`
- `integrations/stripe.md`

## Instructions
1. Create `src/app/api/billing/portal/route.ts` — POST handler that creates a Stripe Customer Portal session and returns the URL
2. Create `src/app/api/billing/downgrade/route.ts` — POST handler that schedules a plan downgrade at the end of the current billing period (cancel_at_period_end)
3. Both routes require authentication

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 027 - billing api: portal + downgrade`
