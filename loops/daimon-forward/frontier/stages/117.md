# Stage 117 — Discovery: Schema Mismatch Audit

## Spec Files
- `database/data-model-reconciliation.md`

## Instructions
1. Compare all frontend Supabase queries against the actual schema
2. Verify column names in queries match the database schema
3. Verify RLS policies allow the queries used by the frontend
4. Fix any mismatches found (column renames, missing fields)
5. Update types if schema changes are made

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 117 - schema mismatch audit`
