# Stage 077 — Admin Panel: Tenant Detail

## Spec Files
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/admin/tenants/[id]/page.tsx`
2. Show tenant details: name, plan, status, Stripe customer ID
3. Show member list, Discord connections, API keys (masked), and service connections
4. Add action buttons: Suspend, Unsuspend, Override Plan

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 077 - admin panel tenant detail page`
