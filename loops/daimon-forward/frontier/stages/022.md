# Stage 022 — Auth Callback Route

## Spec Files
- `frontend/auth-pages.md`

## Instructions
1. Create `src/app/auth/callback/route.ts` as a Next.js route handler
2. Handle the PKCE exchange for email confirmations and OAuth callbacks
3. Exchange the authorization code for a Supabase session
4. Redirect the user to /dashboard on success

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 022 - auth callback route`
