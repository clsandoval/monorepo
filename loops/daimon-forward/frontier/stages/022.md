# Stage 022 — Auth Callback Route

## Spec Files
- `frontend/auth-pages.md`

## Instructions
1. Create `src/app/auth/callback/route.ts` handling the PKCE exchange for email confirmations and OAuth callbacks
2. Exchange the authorization code for a session using Supabase server client
3. Redirect to `/dashboard` on success
4. Redirect to `/login` with error on failure

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 022 - auth callback route`
