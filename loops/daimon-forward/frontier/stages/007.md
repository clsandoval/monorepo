# Stage 007 — Discord Connections Table

## Spec Files
- `database/migrations.md`
- `database/schema.md`
- `database/vault-encryption.md`

## Instructions
1. Create migration `supabase/migrations/20260400000003_create_discord_connections.sql`
2. Define discord_connections table with columns: id, tenant_id (FK), guild_id, status, vault_secret_id, error_message, last_heartbeat, created_at, updated_at
3. Add 3 indexes from spec
4. Add update_updated_at trigger
5. Add RLS policies
6. Enable Realtime on the discord_connections table
7. Create `get_decrypted_secret()` Vault helper function for secure token retrieval

## Verify
- `cd apps/daimon-saas && npx supabase db reset` — exits 0

## Commit
`daimon(forward): stage 007 - discord connections table`
