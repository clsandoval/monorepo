# Stage 076 — Admin Panel: Layout + Tenant List

## Spec Files
- `frontend/admin-panel.md`

## Instructions
1. Create `src/app/admin/page.tsx` with admin-only access check (verify user role)
2. Build tenant list table with columns: Name, Plan (badge), Status (badge), Members count, Created
3. Add search and filter by plan/status
4. Add pagination

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 076 - admin panel layout + tenant list`
