# Stage 036 — Admin Audit Log Query

## Spec Files
- `frontend/admin-panel.md`
- `database/schema.md`

## Instructions
1. Create `src/app/api/admin/audit-log/route.ts` — GET handler with paginated query of admin_audit_log table
2. Support filtering by: action, tenant_id, actor_id, date range (start_date, end_date)
3. Return paginated results with total count for UI pagination
4. Admin-only authentication check required

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 036 - admin audit log query`
