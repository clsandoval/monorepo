# Stage 043 — Toast System

## Spec Files
- `frontend/component-library.md`
- `frontend/copy.md`

## Instructions
1. Create `src/components/ui/toast.tsx` with 4 types: success, error, warning, info
2. Create `src/lib/toast.ts` with useToast hook
3. Implement auto-dismiss after 5 seconds
4. Add stacking support with max 3 visible toasts
5. Position toasts top-right

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 043 - toast system`
