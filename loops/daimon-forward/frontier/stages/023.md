# Stage 023 — Stripe Webhook Route + Signature Verification

## Spec Files
- `api/webhooks.md`
- `integrations/stripe.md`

## Instructions
1. Install `stripe` package
2. Create `src/app/api/stripe/webhook/route.ts`
3. Implement signature verification using `STRIPE_WEBHOOK_SECRET` env var
4. Parse event type from the verified payload
5. Create idempotency check against `stripe_webhook_events` table — skip already-processed events
6. Stub handler functions for each event type (they will be implemented in stages 024-025)

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0 with no errors

## Commit
`daimon(forward): stage 023 - stripe webhook route and signature verification`
