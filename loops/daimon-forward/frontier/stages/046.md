# Stage 046 — Badge + StatusIndicator Components

## Spec Files
- `frontend/component-library.md`

## Instructions
1. Create `src/components/ui/badge.tsx` with variants: plan (free=gray, starter=blue, pro=purple), status (active=green, suspended=red, pending=yellow), role (owner=purple, admin=blue, member=gray)
2. Create `src/components/ui/status-indicator.tsx` with states: online (green dot pulse), offline (gray), error (red), loading (yellow pulse)

## Verify
- `cd apps/daimon-saas && npm run build` — exits 0

## Commit
`daimon(forward): stage 046 - Badge + StatusIndicator components`
