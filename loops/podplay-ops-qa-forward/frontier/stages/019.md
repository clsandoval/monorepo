# Stage 019 — Wire AdjustmentModal to /inventory Page (F07)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F07

## Instructions
1. Edit `apps/podplay/src/components/inventory/StockLevelsTable.tsx` (or the component that renders the inventory table on `/inventory`)
2. Add an "Adjust" button to each row (small button, right side of row)
3. Add state for the modal: `const [adjustingItem, setAdjustingItem] = useState<{id, name, qty} | null>(null)`
4. Render `<AdjustmentModal>` conditionally when `adjustingItem` is set
5. On modal close: `setAdjustingItem(null)`
6. On success: re-fetch inventory data (use whatever data refresh pattern the component uses — likely re-calling the Supabase query)

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 019 - wire AdjustmentModal to /inventory page`
