# Stage 003 — Migration: Seed Inventory (F06)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F06

## Instructions
1. Create `apps/podplay/supabase/migrations/00015_seed_inventory.sql`
2. Contents:
```sql
INSERT INTO inventory (item_id, quantity_on_hand, quantity_allocated, reorder_point)
SELECT id, 0, 0, 5
FROM hardware_catalog
WHERE is_active = true
ON CONFLICT (item_id) DO NOTHING;
```
3. Run `cd apps/podplay && npx supabase db reset`

## Verify
- `npx supabase db reset` exits 0
- Query `SELECT count(*) FROM inventory;` returns 47

## Commit
`podplay(qa-forward): stage 003 - migration: seed inventory`
