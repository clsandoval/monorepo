# Stage 029 — Add Recurring Fees Tab to Financials Wizard (F12)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F12

## Instructions
1. Edit `apps/podplay/src/routes/_auth/projects/$projectId/financials.tsx`
2. Add 'Recurring Fees' to the `FINANCIALS_TABS` array (as 5th tab)
3. Import `RecurringFeesTab` from `components/wizard/financials/RecurringFeesTab`
4. Add the conditional render: `{activeTab === 'Recurring Fees' && <RecurringFeesTab projectId={projectId} />}`
5. Update the `FinancialsTab` type to include `'Recurring Fees'`

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 029 - add Recurring Fees tab to financials wizard`
