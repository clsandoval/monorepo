# Stage 022 — Replace SKU Swap Select with SearchableSelect (F08)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F08

## Instructions
1. Edit `apps/podplay/src/components/wizard/procurement/BomReviewTable.tsx`
2. Find the SKU swap `<select>` element (used when swapping a BOM item for a different catalog item)
3. Replace it with `<SearchableSelect>` from `components/ui/SearchableSelect`
4. Build options from `hardware_catalog` data: `{ value: item.id, label: \`\${item.sku} — \${item.name}\` }`
5. Keep the same onChange behavior (update the BOM item's catalog reference)

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 022 - replace SKU swap with SearchableSelect`
