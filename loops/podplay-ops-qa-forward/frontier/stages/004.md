# Stage 004 — Migration: Replace Team Contacts (F09)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F09

## Instructions
1. Create `apps/podplay/supabase/migrations/00016_seed_team_contacts.sql`
2. Contents:
```sql
DELETE FROM team_contacts;

INSERT INTO team_contacts (name, role, department, email, phone, is_active) VALUES
  ('Niko', 'Operations Lead', 'Operations', 'niko@podplay.co', NULL, true),
  ('Chad', 'Technical Lead', 'Engineering', 'chad@podplay.co', NULL, true),
  ('Andy', 'Project Manager', 'Operations', 'andy@podplay.co', NULL, true),
  ('Ernesto', 'Field Installer', 'Operations', 'ernesto@podplay.co', NULL, true),
  ('Carlos', 'Field Installer', 'Operations', 'carlos@podplay.co', NULL, true),
  ('Marco', 'Field Installer', 'Operations', 'marco@podplay.co', NULL, true);
```
3. Run `cd apps/podplay && npx supabase db reset`

## Verify
- `npx supabase db reset` exits 0
- Query `SELECT count(*) FROM team_contacts;` returns 6
- Query `SELECT name FROM team_contacts ORDER BY name;` returns Andy, Carlos, Chad, Ernesto, Marco, Niko

## Commit
`podplay(qa-forward): stage 004 - migration: replace team contacts`
