# Stage 023 — Replace Installer Select with SearchableSelect (F08)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F08

## Instructions
1. Edit `apps/podplay/src/components/wizard/intake/InstallerSelectionStep.tsx`
2. Find the installer `<select>` element
3. Replace with `<SearchableSelect>` from `components/ui/SearchableSelect`
4. Build options from installers data: `{ value: installer.id, label: \`\${installer.name}\${installer.company ? \` (\${installer.company})\` : ''}\` }`
5. Keep the same onChange behavior

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 023 - replace installer select with SearchableSelect`
