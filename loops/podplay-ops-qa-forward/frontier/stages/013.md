# Stage 013 — ISP Dropdown Tests (F03)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F03

## Instructions
1. Find existing IspInfoStep tests (or create `apps/podplay/src/__tests__/components/wizard/intake/IspInfoStep.test.tsx`)
2. Add/update test cases:
   - Renders SearchableSelect with 7 ISP options
   - Selecting "PLDT Fiber" sets isp_provider value
   - Selecting "Other" reveals custom text input
   - Entering custom ISP name stores correctly
   - Starlink validation still blocks submission (existing test)
3. Ensure all existing IspInfoStep tests still pass

## Verify
- `cd apps/podplay && npx vitest run` — all tests pass (full suite, not just ISP tests)

## Commit
`podplay(qa-forward): stage 013 - ISP dropdown tests`
