# Stage 024 — SearchableSelect Integration Tests (F08)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F08

## Instructions
1. Update existing tests for BomReviewTable, InstallerSelectionStep, and IspInfoStep
2. Verify the SearchableSelect renders in each location
3. Test that filtering works in context (type a few characters, verify filtered results)
4. Ensure all existing tests for these components still pass

## Verify
- `cd apps/podplay && npx vitest run` — all tests pass (full suite)

## Commit
`podplay(qa-forward): stage 024 - SearchableSelect integration tests`
