# Stage 017 — Project State Persistence Tests (F04)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F04

## Instructions
1. Create or update `apps/podplay/src/__tests__/routes/intake.test.tsx`
2. Test cases:
   - New project (wizard_step=0): wizard starts at step 0
   - Existing project (wizard_step=3): wizard starts at step 3
   - Step transition calls supabase update with new wizard_step
   - Project with status 'procurement': redirects away from intake
   - Project with status 'deployment': redirects to deployment page
3. Also create or update `apps/podplay/src/__tests__/routes/dashboard.test.tsx`
4. Test cases:
   - Project with status 'intake': link goes to /projects/$id/intake
   - Project with status 'procurement': link goes to /projects/$id/procurement

## Verify
- `cd apps/podplay && npx vitest run` — all tests pass

## Commit
`podplay(qa-forward): stage 017 - project state persistence tests`
