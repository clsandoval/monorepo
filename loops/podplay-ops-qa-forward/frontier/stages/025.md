# Stage 025 — VendorSettings Component (F11)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F11

## Pattern Reference
- Follow `apps/podplay/src/components/settings/TeamSettings.tsx` exactly

## Instructions
1. Create `apps/podplay/src/components/settings/VendorSettings.tsx`
2. Follow TeamSettings pattern:
   - Props: `{ vendors: Vendor[] }` (loaded via route loader)
   - Table columns: Name, Contact Name, Email, Phone, Website, Lead Time (days), Active
   - "Add Vendor" button opens modal form
   - Edit button per row, Delete button with confirmation
   - Supabase CRUD: `supabase.from('vendors').insert/update/delete`
   - Toast on success/error
3. Define Vendor type (or add to `lib/types.ts`):
```typescript
interface Vendor {
  id: string;
  name: string;
  contact_name: string | null;
  email: string | null;
  phone: string | null;
  website: string | null;
  lead_time_days: number | null;
  notes: string | null;
  is_active: boolean;
}
```

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 025 - VendorSettings component`
