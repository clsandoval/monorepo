# Stage 021 — Inventory Adjustment Tests (F07)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F07

## Instructions
1. Create `apps/podplay/src/__tests__/components/inventory/AdjustmentModal.test.tsx`
2. Test cases:
   - Renders with item name and current quantity
   - Direction toggle between Increase and Decrease
   - Quantity input validation (min 1, required)
   - Reason input required
   - Decrease cannot exceed current quantity
   - Submit calls Supabase insert (inventory_movements) and update (inventory)
   - Success callback is called after submission
   - Cancel closes modal without changes

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/components/inventory/AdjustmentModal.test.tsx` — all tests pass

## Commit
`podplay(qa-forward): stage 021 - inventory adjustment tests`
