# Stage 001 — Migration: Seed Installers (F01)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F01

## Instructions
1. Create `apps/podplay/supabase/migrations/00013_seed_installers.sql`
2. Contents:
```sql
INSERT INTO installers (name, company, email, phone, installer_type, regions, hourly_rate, is_active) VALUES
  ('PodPlay Install Team', 'PodPlay', 'installs@podplay.co', NULL, 'podplay_vetted', ARRAY['NCR', 'Calabarzon', 'Central Luzon'], 500.00, true),
  ('External Installer (TBD)', NULL, NULL, NULL, 'client_own', ARRAY[]::TEXT[], NULL, true);
```
3. Run `cd apps/podplay && npx supabase db reset` to apply all migrations

## Verify
- `npx supabase db reset` exits 0
- Query `SELECT count(*) FROM installers;` returns 2

## Commit
`podplay(qa-forward): stage 001 - migration: seed installers`
