# Stage 015 — Status-Based Routing from Dashboard (F04)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F04

## Instructions
1. Edit `apps/podplay/src/routes/_auth/projects/index.tsx` (dashboard)
2. Find where project rows are clickable/linked — update the navigation target based on `project_status`:
   - `'intake'` → `/projects/${id}/intake`
   - `'procurement'` → `/projects/${id}/procurement`
   - `'deployment'` → `/projects/${id}/deployment`
   - `'financial_close'` or `'completed'` → `/projects/${id}/financials`
   - `'cancelled'` → `/projects/${id}/financials`
3. If using `<Link>` components, update the `to` prop dynamically
4. If using `useNavigate`, update the navigate call

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 015 - status-based routing from dashboard`
