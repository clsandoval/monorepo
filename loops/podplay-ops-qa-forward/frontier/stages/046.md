# Stage 046 — CONVERGE: Convergence Gate

## Instructions
1. Run `cd apps/podplay && npm run build` — must exit 0
2. Run `cd apps/podplay && npx vitest run` — must exit 0
3. Verify `loops/podplay-ops-qa-forward/screenshots/` contains screenshots for all Playwright stages
4. Review `frontier/current-stage.md` — confirm all 45 prior stages are marked done
5. If ANY check fails: do NOT write converged.txt. Fix the issue or note it.
6. If ALL checks pass: write `loops/podplay-ops-qa-forward/status/converged.txt` with:
   - Total stages completed
   - Test count and pass rate
   - Build status
   - QA findings resolved (list each F-number)
   - Date of convergence
7. This is the FINAL stage. Only write `converged.txt` if everything truly passes.

## Verify
- `cd apps/podplay && npm run build` — exits 0
- `cd apps/podplay && npx vitest run` — exits 0
- `cat loops/podplay-ops-qa-forward/status/converged.txt` — file exists with summary

## Commit
`podplay(qa-forward): converged — all 9 QA findings resolved, all tests pass`
