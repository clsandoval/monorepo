# Stage 009 — InstallerSettings Component (F01)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F01

## Pattern Reference
- Follow `apps/podplay/src/components/settings/TeamSettings.tsx` exactly

## Instructions
1. Create `apps/podplay/src/components/settings/InstallerSettings.tsx`
2. Follow the TeamSettings pattern:
   - Props: `{ installers: Installer[] }` (loaded via route loader)
   - Table with columns: Name, Company, Type (podplay_vetted/client_own), Regions, Hourly Rate, Active
   - "Add Installer" button opens a modal/dialog form
   - Edit button per row opens same form pre-filled
   - Delete button per row with confirmation dialog
   - Supabase CRUD: `supabase.from('installers').insert/update/delete`
   - Toast notifications on success/error (use existing toast pattern from `lib/toast-operations.ts`)
3. Use enum labels from `lib/enum-labels.ts` for installer_type display
4. Regions displayed as comma-separated badges/tags

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 009 - InstallerSettings component`
