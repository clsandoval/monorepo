# Stage 011 — InstallerSettings Tests (F01)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F01

## Instructions
1. Create `apps/podplay/src/__tests__/components/settings/InstallerSettings.test.tsx`
2. Test cases:
   - Renders table with seeded installer data (2 rows)
   - Displays columns: Name, Company, Type, Regions, Rate, Active
   - Add button opens form modal
   - Edit button opens form pre-filled with installer data
   - Delete button shows confirmation dialog
   - Form validates required fields (name, installer_type)
3. Use existing test patterns from the codebase

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/components/settings/InstallerSettings.test.tsx` — all tests pass

## Commit
`podplay(qa-forward): stage 011 - InstallerSettings tests`
