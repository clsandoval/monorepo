# Stage 032 — Full Build + Test Suite Check

## Instructions
1. Run `cd apps/podplay && npx tsc --noEmit` — must exit 0 with 0 errors
2. Run `cd apps/podplay && npm run build` — must exit 0
3. Run `cd apps/podplay && npx vitest run` — must exit 0, all tests pass
4. If any failures: fix them before proceeding. Do NOT skip this stage.
5. Note the test count in `frontier/current-stage.md` (should be > 971 from original)

## Verify
- `npx tsc --noEmit` — 0 errors
- `npm run build` — exits 0
- `npx vitest run` — all pass, 0 failures

## Commit
`podplay(qa-forward): stage 032 - full build + test suite green`
