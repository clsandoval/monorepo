# Stage 033 — Playwright: /settings/installers (F01)

## Instructions
1. Start Supabase: `cd apps/podplay && npx supabase start` (if not running)
2. Start dev server: `cd apps/podplay && npm run dev` (port 5175)
3. Open Playwright browser at viewport 1280x800
4. Navigate to `http://localhost:5175/settings/installers`
5. Take screenshot → `loops/podplay-ops-qa-forward/screenshots/desktop-settings-installers.png`
6. Verify:
   - Table renders with 2 seeded installer rows
   - "Installers" tab is active in settings nav
   - Columns visible: Name, Company, Type, Regions, Rate, Active
   - No console errors, no placeholder text
7. If any check fails, fix and re-verify

## Verify
- `screenshots/desktop-settings-installers.png` exists and shows populated installer table

## Commit
`podplay(qa-forward): stage 033 - playwright verify /settings/installers`
