# Stage 008 — SearchableSelect Tests (F08)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F08

## Instructions
1. Create `apps/podplay/src/__tests__/components/ui/SearchableSelect.test.tsx`
2. Test cases:
   - Renders with placeholder text
   - Shows all options when input is focused
   - Filters options as user types
   - Calls onChange when option is clicked
   - Keyboard: arrow down highlights next option, Enter selects it
   - Escape closes the dropdown
   - Disabled prop prevents interaction
   - Shows selected option label in input
3. Use the existing test patterns (vitest + @testing-library/react)

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/components/ui/SearchableSelect.test.tsx` — all tests pass

## Commit
`podplay(qa-forward): stage 008 - SearchableSelect tests`
