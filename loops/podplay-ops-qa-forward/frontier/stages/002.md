# Stage 002 — Migration: wizard_step Column (F04)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F04

## Instructions
1. Create `apps/podplay/supabase/migrations/00014_wizard_step.sql`
2. Contents:
```sql
ALTER TABLE projects ADD COLUMN wizard_step INTEGER NOT NULL DEFAULT 0;
```
3. Run `cd apps/podplay && npx supabase db reset`

## Verify
- `npx supabase db reset` exits 0
- Query `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='projects' AND column_name='wizard_step';` returns one row

## Commit
`podplay(qa-forward): stage 002 - migration: wizard_step column`
