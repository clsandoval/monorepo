# Stage 027 — VendorSettings Tests (F11)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F11

## Instructions
1. Create `apps/podplay/src/__tests__/components/settings/VendorSettings.test.tsx`
2. Test cases:
   - Renders table with seeded vendor data (7 rows)
   - Displays columns: Name, Contact, Email, Phone, Website, Lead Time, Active
   - Add button opens form modal
   - Edit button opens form pre-filled
   - Delete button shows confirmation
   - Form validates required field (name)

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/components/settings/VendorSettings.test.tsx` — all tests pass

## Commit
`podplay(qa-forward): stage 027 - VendorSettings tests`
