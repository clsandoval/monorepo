# Stage 043 — Discovery: Hunt for Gaps

## Instructions
1. Run `cd apps/podplay && npx tsc --noEmit` — fix any errors
2. Run `cd apps/podplay && npm run build` — fix any errors
3. Run `cd apps/podplay && npx vitest run` — fix any failures
4. Open every route in Playwright and check for console errors:
   - `/login`
   - `/projects`
   - `/projects/new`
   - `/inventory`
   - `/financials`
   - `/settings/pricing`
   - `/settings/catalog`
   - `/settings/team`
   - `/settings/travel`
   - `/settings/installers`
   - `/settings/vendors`
5. Fix any console errors found
6. Check that the settings nav has all 6 tabs in order: Pricing, Catalog, Team, Installers, Vendors, Travel

## Verify
- All routes load without console errors
- Build + tests still pass

## Commit
`podplay(qa-forward): stage 043 - discovery gap hunting`
