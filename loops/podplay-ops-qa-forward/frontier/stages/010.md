# Stage 010 — Installers Route + Settings Nav (F01)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F01

## Pattern Reference
- Follow `apps/podplay/src/routes/_auth/settings/team.tsx` for route file
- Follow `apps/podplay/src/routes/_auth/settings.tsx` for nav tabs

## Instructions
1. Create `apps/podplay/src/routes/_auth/settings/installers.tsx`
   - Route: `createFileRoute('/_auth/settings/installers')`
   - Loader: query `supabase.from('installers').select('*').order('name')`
   - Component: render `<InstallerSettings installers={data} />`
2. Edit `apps/podplay/src/routes/_auth/settings.tsx`:
   - Add `{ label: 'Installers', to: '/settings/installers' }` to `SETTINGS_TABS` array (after 'Team')

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 010 - installers route + settings nav link`
