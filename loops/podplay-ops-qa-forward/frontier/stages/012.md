# Stage 012 — ISP Provider Dropdown (F03)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F03

## Instructions
1. Edit `apps/podplay/src/components/wizard/intake/IspInfoStep.tsx`
2. Import `SearchableSelect` from `components/ui/SearchableSelect`
3. Define ISP options array:
```typescript
const PH_ISP_OPTIONS = [
  { value: 'PLDT Fiber', label: 'PLDT Fiber' },
  { value: 'Globe Fiber', label: 'Globe Fiber' },
  { value: 'Converge Fiber', label: 'Converge Fiber' },
  { value: 'Sky Broadband', label: 'Sky Broadband' },
  { value: 'Globe LTE', label: 'Globe LTE' },
  { value: 'Smart LTE', label: 'Smart LTE' },
  { value: 'Other', label: 'Other' },
];
```
4. Replace the freeform `<Input>` for `isp_provider` with `<SearchableSelect>` using `PH_ISP_OPTIONS`
5. Add conditional: when value is "Other", show a text `<Input>` below for custom ISP name. Store the custom name in `isp_provider` field.
6. Keep all existing Starlink validation logic (lines 73-102 or wherever it is)

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 012 - ISP provider dropdown with SearchableSelect`
