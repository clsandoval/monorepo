# Stage 026 — Vendors Route + Settings Nav (F11)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F11

## Pattern Reference
- Follow `apps/podplay/src/routes/_auth/settings/team.tsx` for route
- Follow `apps/podplay/src/routes/_auth/settings.tsx` for nav

## Instructions
1. Create `apps/podplay/src/routes/_auth/settings/vendors.tsx`
   - Route: `createFileRoute('/_auth/settings/vendors')`
   - Loader: query `supabase.from('vendors').select('*').order('name')`
   - Component: render `<VendorSettings vendors={data} />`
2. Edit `apps/podplay/src/routes/_auth/settings.tsx`:
   - Add `{ label: 'Vendors', to: '/settings/vendors' }` to `SETTINGS_TABS` array (after 'Installers')

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 026 - vendors route + settings nav link`
