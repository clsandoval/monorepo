# Stage 020 — Wire AdjustmentModal to Procurement Inventory Check (F07)

## Spec Reference
- `loops/podplay-ops-qa-forward/spec/qa-fixes-spec.md` § F07

## Instructions
1. Edit `apps/podplay/src/components/wizard/procurement/InventoryCheckPanel.tsx`
2. Add the same "Adjust" button pattern from stage 019
3. Import and render `AdjustmentModal` with the same state pattern
4. On success: re-fetch the inventory check data

## Verify
- `cd apps/podplay && npx tsc --noEmit` — exits 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(qa-forward): stage 020 - wire AdjustmentModal to procurement inventory check`
