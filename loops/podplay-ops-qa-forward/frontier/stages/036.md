# Stage 036 — Playwright: /inventory (F06 + F07)

## Instructions
1. Navigate to `http://localhost:5175/inventory`
2. Take screenshot → `loops/podplay-ops-qa-forward/screenshots/desktop-inventory.png`
3. Verify:
   - Table shows 47 inventory rows (not empty state)
   - All qty_on_hand show 0
   - Each row has an "Adjust" button
   - No console errors
4. Click "Adjust" on any row
5. Take screenshot of the modal → `loops/podplay-ops-qa-forward/screenshots/desktop-inventory-adjust.png`
6. Verify modal shows: direction toggle, quantity input, reason input, cancel + submit buttons
7. If any check fails, fix and re-verify

## Verify
- `screenshots/desktop-inventory.png` shows 47 rows with Adjust buttons
- `screenshots/desktop-inventory-adjust.png` shows adjustment modal

## Commit
`podplay(qa-forward): stage 036 - playwright verify /inventory`
