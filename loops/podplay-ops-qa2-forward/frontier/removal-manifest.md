# Removal Manifest

Generated by stage 002. Lists all files and references to remove for 4 deprecated features.

---

## Feature 1: CC Terminal (`cc_terminal` / `CcTerminal`)

### Component files to delete
- `apps/podplay/src/components/wizard/procurement/CcTerminalOrder.tsx`

### Route files to delete / update
- `apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`
  — Remove import of `CcTerminalOrder`
  — Remove `<CcTerminalOrder projectId={projectId} />` JSX block

### Type/enum references to clean up
- `apps/podplay/src/lib/types.ts` line 76
  — Delete: `export type CcTerminalStatus = 'not_ordered' | 'ordered' | 'delivered' | 'installed';`
- `apps/podplay/src/lib/enum-labels.ts`
  — Remove import of `CcTerminalStatus`
  — Remove `ccTerminalStatusLabels` record (lines ~374-387)
  — Remove `ccTerminalStatusBadgeClass` record (lines ~381-387)
  — Remove `ccTerminalStatusSortOrder` record (lines ~388-395)
  — Remove `cc_terminal_status` entries from aggregate label/badge maps
- `apps/podplay/src/lib/toast-messages.ts`
  — Remove `TOAST_CC_TERMINAL_*` exports (6 constants, lines ~182-212)
  — Remove `CC_TERMINAL_*` keys from TOASTS aggregate object
- `apps/podplay/src/lib/loading.ts`
  — Remove `LOADER_PROCUREMENT_ORDER_CC_TERMINAL` (line 49)
  — Remove `LOADER_PROCUREMENT_UPDATE_CC_TERMINAL` (line 50)
  — Remove `procurementOrderCcTerminal` and `procurementUpdateCcTerminal` from LOADERS aggregate
- `apps/podplay/src/lib/validation-messages.ts`
  — Remove `cc_terminal` block from procurement section (line ~220-234)
  — Remove `cc_terminal_pin` field from settings section (line ~488-498)
  — Remove `cc_terminal: 'CC terminal must be ordered (if applicable)'` from checklist
- `apps/podplay/src/services/settingsService.ts`
  — Remove `cc_terminal_pin: string` field from settings type (line 34)

### Test files to delete
- `apps/podplay/src/__tests__/wizard/procurement/cc-terminal.test.ts`

### Test files to update (remove cc_terminal references)
- `apps/podplay/src/__tests__/ui/validation-messages.test.ts`
- `apps/podplay/src/__tests__/settings/pricing.test.ts`
- `apps/podplay/src/__tests__/settings/team.test.ts`
- `apps/podplay/src/__tests__/settings/travel.test.ts`
- `apps/podplay/src/__tests__/db/financial-tables.test.ts`
- `apps/podplay/src/__tests__/enum-labels.test.ts`
- `apps/podplay/src/__tests__/enums.test.ts`

### DB migrations (DO NOT delete — schema record)
- `apps/podplay/supabase/migrations/00005_financial_tables.sql` — contains cc_terminal_status enum + column
- `apps/podplay/supabase/migrations/00001_enums.sql` — contains cc_terminal_status enum definition

---

## Feature 2: Replay Signs (`replay_sign` / `ReplaySign`)

### Component files to delete
- `apps/podplay/src/components/wizard/procurement/ReplaySignFulfillment.tsx`

### Route files to update
- `apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`
  — Remove import of `ReplaySignFulfillment`
  — Remove `<ReplaySignFulfillment projectId={projectId} />` JSX block

### Settings component to update
- `apps/podplay/src/components/settings/PricingSettings.tsx`
  — Remove `replay_sign_multiplier` field/section

### Type/enum references to clean up
- `apps/podplay/src/lib/toast-messages.ts`
  — Remove `TOAST_REPLAY_SIGNS_*` exports (7 constants, lines ~214-244)
  — Remove `REPLAY_SIGNS_*` keys from TOASTS aggregate object
- `apps/podplay/src/lib/validation-messages.ts`
  — Remove `replay_signs` block from procurement section (line ~235-253)
  — Remove `replay_sign_multiplier` field from settings section (line ~499)
  — Remove `replay_signs: 'Replay signs outreach must be initiated'` from checklist
- `apps/podplay/src/services/settingsService.ts`
  — Remove `replay_sign_multiplier: number` field (line 39)

### Test files to delete
- `apps/podplay/src/__tests__/wizard/procurement/replay-signs.test.ts`

### Test files to update (remove replay_sign references)
- `apps/podplay/src/__tests__/ui/validation-messages.test.ts`
- `apps/podplay/src/__tests__/settings/pricing.test.ts`
- `apps/podplay/src/__tests__/settings/team.test.ts`
- `apps/podplay/src/__tests__/settings/travel.test.ts`
- `apps/podplay/src/__tests__/db/financial-tables.test.ts`

### DB migrations (DO NOT delete — schema record)
- `apps/podplay/supabase/migrations/00005_financial_tables.sql` — contains replay_signs columns
- `apps/podplay/supabase/migrations/00002_core_tables.sql` — contains replay_sign references

---

## Feature 3: Travel Settings

### Component files to delete
- `apps/podplay/src/components/settings/TravelSettings.tsx`

### Route files to delete
- `apps/podplay/src/routes/_auth/settings/travel.tsx`

### Route files to update
- `apps/podplay/src/routes/_auth/settings.tsx`
  — Remove `{ label: 'Travel', to: '/settings/travel' }` from settings nav tabs (line 9)

### Navigation to update
- `apps/podplay/src/components/layout/AppLayout.tsx`
  — Remove `{ to: '/settings/travel', label: 'Travel', icon: Plane }` from sidebar nav (line 62)

### Type/enum references to clean up
- `apps/podplay/src/lib/toast-messages.ts`
  — Remove `TOAST_TRAVEL_DEFAULTS_SUCCESS` and `TOAST_TRAVEL_DEFAULTS_ERROR` (lines ~569-576)
  — Remove `TRAVEL_DEFAULTS_SUCCESS` and `TRAVEL_DEFAULTS_ERROR` from TOASTS aggregate
- `apps/podplay/src/lib/loading.ts`
  — Remove `LOADER_SETTINGS_SAVE_TRAVEL` (line 98)
  — Remove `settingsSaveTravel` from LOADERS aggregate (line 154)
- `apps/podplay/src/lib/validation-messages.ts`
  — Remove `travel` section (line ~538+)
- `apps/podplay/src/routeTree.gen.ts`
  — Auto-regenerated; will update when route file is deleted and routes are rebuilt

### Test files to delete
- `apps/podplay/src/__tests__/settings/travel.test.ts`

### Test files to update
- `apps/podplay/src/__tests__/settings/nav.test.ts`
  — Remove assertions about Travel nav link/tab

---

## Feature 4: PingPod WiFi (`has_pingpod_wifi`)

### Component files to update (remove field)
- `apps/podplay/src/components/wizard/intake/VenueConfigStep.tsx`
  — Remove `has_pingpod_wifi` from zod schema (line 29)
  — Remove defaultValues assignment (line 52)
  — Remove checkbox input and label (lines ~136-142)
- `apps/podplay/src/components/wizard/intake/ReviewStep.tsx`
  — Remove `<Row label="Has PingPod WiFi" ...>` (line 99)

### Route files to update
- `apps/podplay/src/routes/_auth/projects/$projectId/intake.tsx`
  — Remove `has_pingpod_wifi` from form defaults (line 118)

### Lib files to update
- `apps/podplay/src/services/bom.ts`
  — Remove PingPod WiFi AP conditional block (lines ~143-144+)
- `apps/podplay/src/lib/bom-generation.ts`
  — Remove PingPod Specific conditional section (line ~96)
  — Remove Step 4B comment and logic (lines ~284, 367+)

### Test files to update
- `apps/podplay/src/__tests__/wizard/intake/review.test.ts`
- `apps/podplay/src/__tests__/wizard/intake/submit.test.ts`
- `apps/podplay/src/__tests__/wizard/intake/venue-config.test.tsx`
- `apps/podplay/src/__tests__/bom/generation.test.ts`

### DB migrations (DO NOT delete — schema record)
- `apps/podplay/supabase/migrations/00002_core_tables.sql` — contains has_pingpod_wifi column
- `apps/podplay/supabase/migrations/00006_seed_hardware.sql` — contains pingpod_wifi seed data

---

## Summary

| Feature | Files to Delete | Files to Update |
|---------|----------------|-----------------|
| CC Terminal | 1 component, 1 test | 7 source files, 7 test files |
| Replay Signs | 1 component, 1 test | 6 source files, 5 test files |
| Travel Settings | 1 component, 1 route, 1 test | 4 source files, 1 test file |
| PingPod WiFi | — | 5 source files, 4 test files |
