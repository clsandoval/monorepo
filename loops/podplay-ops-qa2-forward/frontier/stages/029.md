# Stage 029 — Inventory On-Order: Schema Migration

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q20

## Task

Add on-order tracking columns to the inventory schema.

1. Create migration `apps/podplay/supabase/migrations/00020_inventory_on_order.sql`:
   - Add `qty_on_order` integer column to `inventory` table (default 0)
   - Create `order_status` enum type: `not_ordered`, `ordered`, `partial`, `received`
   - Add `order_status` column to `inventory` table (default `not_ordered`)
2. Run `npx supabase db reset` to verify

## Verify

```bash
cd apps/podplay && npx supabase db reset 2>&1 | tail -10
```
