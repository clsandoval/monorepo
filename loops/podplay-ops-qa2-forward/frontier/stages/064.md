# Stage 064 — Mobile Verify: /projects

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/projects` at 375x812 viewport with Playwright. Assert:
1. Project cards render correctly on mobile (stacked layout)
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. All interactive elements have touch targets >= 44px
4. Delete button is accessible on mobile

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/projects
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px on buttons
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/064-projects-mobile.png
```
