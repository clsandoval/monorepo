# Stage 037 — Wizard Navigation: Wire Into Deployment + Financials

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q21

## Task

Wire the WizardNavigation component into deployment and financials wizards.

1. Open `apps/podplay/src/routes/_auth/projects/$projectId/deployment.tsx`
   - Wire WizardNavigation with deployment phase sections
   - Integrate with step locking
2. Open `apps/podplay/src/routes/_auth/projects/$projectId/financials.tsx`
   - Wire WizardNavigation with financials tabs
   - Integrate with step locking

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
