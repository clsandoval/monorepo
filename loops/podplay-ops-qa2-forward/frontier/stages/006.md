# Stage 006 — Remove Replay Sign Component

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q02

## Task

Delete the replay sign component and remove it from the procurement wizard tabs.

1. Delete `apps/podplay/src/components/wizard/procurement/ReplaySignFulfillment.tsx`
2. Remove the replay sign tab from the procurement page (`apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`)
3. Remove any imports of the deleted component

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
