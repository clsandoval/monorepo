# Stage 067 — Mobile Verify: /projects/:id/procurement

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/procurement` at 375x812 viewport with Playwright. Assert:
1. Procurement tabs render correctly on mobile (horizontal scroll or stacked)
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. All interactive elements have touch targets >= 44px
4. Tab content is readable on mobile width

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/projects/{project_id}/procurement
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px on tabs and buttons
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/067-procurement-mobile.png
```
