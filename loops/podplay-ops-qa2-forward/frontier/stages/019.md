# Stage 019 — Installer Multi-Select: Types, Schema, Review

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q10

## Task

Update types, DB schema, and review step for multi-select installers.

1. Check current schema — if `projects` has a single `installer_id` FK:
   - Option A: Add a `project_installers` junction table (migration 00019+)
   - Option B: Change to `installer_ids` array column
   - Choose whichever is simpler and consistent with existing patterns
2. Update `apps/podplay/src/lib/types.ts` to reflect the new installer data shape
3. Update the Zod schema in the installer step to validate an array
4. Update `apps/podplay/src/components/wizard/intake/ReviewStep.tsx` to display all selected installers (comma-separated or as a list)

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
