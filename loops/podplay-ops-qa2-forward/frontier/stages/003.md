# Stage 003 — Remove CC Terminal Component

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q01

## Task

Delete the CC terminal component and remove it from the procurement wizard tabs.

1. Delete `apps/podplay/src/components/wizard/procurement/CcTerminalSetup.tsx` (or similar name)
2. Remove the CC terminal tab from the procurement page (`apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`)
3. Remove any imports of the deleted component

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
