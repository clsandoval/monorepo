# Stage 068 — Mobile Verify: /projects/:id/deployment

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/deployment` at 375x812 viewport with Playwright. Assert:
1. Deployment checklist renders correctly on mobile
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. Checkbox touch targets >= 44px
4. Advance button is accessible on mobile

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/projects/{project_id}/deployment
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px on checkboxes and buttons
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/068-deployment-mobile.png
```
