# Stage 070 — Mobile Verify: /inventory

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/inventory` at 375x812 viewport with Playwright. Assert:
1. Inventory table renders correctly on mobile (responsive table or card layout)
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. All interactive elements have touch targets >= 44px
4. Key data (item name, status, on-order) is visible without horizontal scroll

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/inventory
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/070-inventory-mobile.png
```
