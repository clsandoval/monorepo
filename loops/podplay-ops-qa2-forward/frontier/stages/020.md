# Stage 020 — Installer Multi-Select: Tests

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q10

## Task

Update installer selection tests for multi-select behavior.

1. Read `apps/podplay/src/__tests__/wizard/intake/installer*.test.ts*`
2. Update tests to verify:
   - Multiple installers can be selected
   - Selected installers show as chips
   - Removing a chip deselects the installer
   - Form submits an array of installer IDs
   - Review step shows all selected installers

## Verify

```bash
cd apps/podplay && npm run test -- --reporter=verbose 2>&1 | tail -50
```
