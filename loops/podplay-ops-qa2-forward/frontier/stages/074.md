# Stage 074 — Mobile Verify: /settings/catalog

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/settings/catalog` at 375x812 viewport with Playwright. Assert:
1. Hardware catalog renders correctly on mobile (responsive table or card layout)
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. All interactive elements have touch targets >= 44px
4. iPad 128GB entry is visible and readable on mobile

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/settings/catalog
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px
# 5. Assert iPad 128GB entry is visible
# 6. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/074-settings-catalog-mobile.png
```
