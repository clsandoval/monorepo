# Stage 025 — iPad 128GB Price Fix

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q13

## Task

Update the iPad entry in the hardware catalog to 128GB.

1. Read `apps/podplay/supabase/migrations/00006_seed_hardware.sql`
2. Find the iPad entry — update from 64GB to 128GB
3. Update the price to reflect current 128GB iPad pricing
4. Update the description/name to say "128GB" instead of "64GB"
5. Check if any test fixtures reference the old iPad spec and update them

## Verify

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npx supabase db reset 2>&1 | tail -20
```
