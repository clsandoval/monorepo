# Stage 048 — Desktop Verify: /projects (Dashboard)

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/projects` at 1280x800 viewport with Playwright. Assert that project cards render with real content (project names, status badges). Verify the delete button is visible on each project card. Confirm the page is not showing placeholder/stub content.

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/projects
# 3. Assert at least one project card is visible
# 4. Assert delete button exists on project cards
# 5. Assert no placeholder text like "Coming soon" or "TODO"
# 6. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/048-projects-desktop.png
```
