# Stage 059 — Desktop Verify: /settings/vendors

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/settings/vendors` at 1280x800 viewport with Playwright. Assert:
1. Vendors list renders with existing vendor entries (working after bug fix)
2. CRUD controls are visible (add, edit, delete buttons)
3. Vendor details (name, contact info) are displayed
4. No error state or crash on page load

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/settings/vendors
# 3. Assert vendors list is visible (no error boundary)
# 4. Assert add/edit/delete controls exist
# 5. Assert no console errors on load
# 6. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/059-settings-vendors-desktop.png
```
