# Stage 047 — Desktop Verify: /login

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/login` at 1280x800 viewport with Playwright. Assert that the login page renders with a visible authentication button (e.g. "Sign in with Google" or similar OAuth button). Verify the page has no console errors and the auth button is clickable.

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/login
# 3. Assert auth button is visible and has text content
# 4. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/047-login-desktop.png
```
