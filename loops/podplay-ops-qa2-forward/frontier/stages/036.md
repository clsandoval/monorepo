# Stage 036 — Wizard Navigation: Wire Into Procurement

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q21

## Task

Wire the WizardNavigation component into the procurement wizard.

1. Open `apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`
2. Replace tab navigation with `WizardNavigation` (or integrate alongside tabs if procurement uses a tab pattern)
3. Pass procurement tabs/steps as steps array
4. Wire navigation handlers
5. Integrate with step locking

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
