# Stage 060 — Desktop Verify: /settings/pricing

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/settings/pricing` at 1280x800 viewport with Playwright. Assert:
1. Pricing settings page renders with pricing configuration
2. Minimum deposit field is visible and editable
3. Pricing values are displayed (not placeholder/zero)

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/settings/pricing
# 3. Assert pricing configuration is visible
# 4. Assert minimum deposit field exists
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/060-settings-pricing-desktop.png
```
