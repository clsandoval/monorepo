# Stage 004 — Remove CC Terminal Types & Labels

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q01

## Task

Remove all CC terminal references from shared lib files.

1. `apps/podplay/src/lib/enum-labels.ts` — remove CC terminal enum labels
2. `apps/podplay/src/lib/toast-messages.ts` — remove CC terminal toast messages
3. `apps/podplay/src/lib/confirmation-dialogs.ts` — remove CC terminal confirmation dialogs
4. `apps/podplay/src/lib/validation-messages.ts` — remove CC terminal validation messages (if any)
5. `apps/podplay/src/lib/types.ts` — remove CC terminal types/interfaces

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
