# Stage 027 — Deposit Minimum: Settings Schema + UI

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q16

## Task

Add a configurable minimum deposit amount to Settings > Pricing.

1. Create migration `apps/podplay/supabase/migrations/00019_minimum_deposit.sql`:
   - Add `minimum_deposit` column to `settings` table (numeric, default 500.00)
2. Open `apps/podplay/src/components/settings/PricingSettings.tsx`:
   - Add a "Minimum Deposit" field (currency input)
   - Help text: "The minimum deposit amount required when creating a project invoice"
   - Add to the Zod schema with `z.number().min(0)`
3. Run `npx supabase db reset` to verify migration

## Verify

```bash
cd apps/podplay && npx supabase db reset 2>&1 | tail -10 && npx tsc --noEmit
```
