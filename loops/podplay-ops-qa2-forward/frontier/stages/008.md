# Stage 008 — Remove Replay Sign Tests

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q02

## Task

Delete or update test files that reference replay signs.

1. Delete `apps/podplay/src/__tests__/wizard/procurement/replay-sign*.test.ts*` (if exists)
2. Search all test files for `replay_sign` / `ReplaySign` references and remove them
3. Update any procurement test that lists replay sign as a tab

## Verify

```bash
cd apps/podplay && npm run test -- --reporter=verbose 2>&1 | tail -50
```
