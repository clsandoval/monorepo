# Stage 014 — Fix Customer Name Pre-fill

**Category**: Bug Fix
**Priority**: 2 — FIX
**Finding**: Q08

## Task

Fix the customer info step so that the customer name is pre-filled when returning to an existing project.

1. Read `apps/podplay/src/components/wizard/intake/CustomerInfoStep.tsx`
2. Check how default values are loaded — the form should read from existing project data
3. Read the intake route (`apps/podplay/src/routes/_auth/projects/$projectId/intake.tsx`) to see how project data is fetched and passed to the step
4. Ensure the `customer_name` (or equivalent field) from the DB is passed as a default value to the form
5. Fix the data flow so saved values populate the form on load

## Verify

```bash
cd apps/podplay && npx tsc --noEmit && npm run test -- --reporter=verbose 2>&1 | tail -50
```
