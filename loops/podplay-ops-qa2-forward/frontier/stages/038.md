# Stage 038 — Wizard Navigation: Tests

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q21

## Task

Add tests for the wizard navigation component.

1. Create `apps/podplay/src/__tests__/wizard/navigation.test.tsx`
2. Test cases:
   - Renders all steps with correct labels
   - Completed steps are clickable
   - Locked steps are not clickable and greyed out
   - Current step is highlighted
   - Previous button hidden on first step
   - Next button shows "Complete" on last step
   - onPrevious/onNext callbacks fire correctly
   - onStepClick fires for completed steps only

## Verify

```bash
cd apps/podplay && npm run test -- --reporter=verbose 2>&1 | tail -50
```
