# Stage 075 — Mobile Verify: /guide

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/guide` at 375x812 viewport with Playwright. Assert:
1. Operations guide renders correctly on mobile
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. All interactive elements (print button, navigation) have touch targets >= 44px
4. Guide sections are readable and scrollable on mobile
5. Print button is accessible

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/guide
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px on print button and navigation
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/075-guide-mobile.png
```
