# Stage 009 — Remove Travel Tab

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q03

## Task

Delete the Settings > Travel route, component, and navigation link.

1. Delete `apps/podplay/src/routes/_auth/settings/travel.tsx`
2. Delete the travel settings component (likely in `apps/podplay/src/components/settings/TravelSettings.tsx` or similar)
3. Remove the "Travel" link from the settings navigation/sidebar
4. Remove any travel-related tests

## Verify

```bash
cd apps/podplay && npx tsc --noEmit && npm run test -- --reporter=verbose 2>&1 | tail -50
```
