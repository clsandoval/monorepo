# Stage 001 — Schema Verification

**Category**: Scaffold
**Priority**: N/A (audit only)

## Task

Read all 18 Supabase migrations in `apps/podplay/supabase/migrations/` and cross-reference every `.from('` query call in the codebase.

1. List every table and column from the migration files
2. Grep all `supabase.from('` calls in `apps/podplay/src/`
3. For each query, verify the table name and selected columns exist in the schema
4. Document any mismatches in `loops/podplay-ops-qa2-forward/frontier/schema-audit.md`

## Verify

```bash
echo "Schema audit complete — check frontier/schema-audit.md"
```

## Notes

This is an audit stage — no code changes. The output informs later stages.
