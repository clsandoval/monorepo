# Stage 058 — Desktop Verify: /settings/installers

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/settings/installers` at 1280x800 viewport with Playwright. Assert:
1. Installers list renders with existing installer entries
2. CRUD controls are visible (add, edit, delete buttons)
3. Installer details (name, contact info) are displayed

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/settings/installers
# 3. Assert installers list is visible
# 4. Assert add/edit/delete controls exist
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/058-settings-installers-desktop.png
```
