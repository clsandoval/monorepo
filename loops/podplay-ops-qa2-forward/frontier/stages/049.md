# Stage 049 — Desktop Verify: /projects/new

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/projects/new` at 1280x800 viewport with Playwright. Assert that the new project form renders with input fields (project name, location, etc.) and a submit button. Verify the form is functional and not a stub.

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/projects/new
# 3. Assert form inputs are visible (name, location fields)
# 4. Assert submit/create button is visible
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/049-projects-new-desktop.png
```
