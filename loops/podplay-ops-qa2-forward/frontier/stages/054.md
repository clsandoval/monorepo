# Stage 054 — Desktop Verify: /inventory

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/inventory` at 1280x800 viewport with Playwright. Assert:
1. Inventory table renders with hardware items
2. On-order columns are visible in the table
3. Status column shows item statuses (in-stock, on-order, etc.)
4. Table is not empty or showing placeholder content

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/inventory
# 3. Assert table is visible with rows
# 4. Assert "on-order" or equivalent column header exists
# 5. Assert status column is present
# 6. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/054-inventory-desktop.png
```
