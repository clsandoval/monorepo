# Stage 081 — Flow QA: Guide + PDF Export

**Category**: Flow QA
**Priority**: 8

## Task

End-to-end flow test with Playwright at 1280x800: Test guide page and PDF export functionality.

1. Navigate to `/guide`
2. Assert all guide sections render with real content:
   - Verify section headings are present
   - Verify each section has body content (not empty or placeholder)
3. Scroll through the entire guide to verify no broken layouts
4. Test PDF export for BOM:
   - Navigate to a project's procurement page
   - Click the BOM PDF export button
   - Assert the download initiates (check for download event or new tab)
5. Test PDF export for packing list:
   - Click the packing list PDF export button
   - Assert the download initiates

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to execute the full flow described above
# Assert guide content is complete
# Assert PDF exports trigger downloads
# Take screenshots at key steps

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/081-flow-guide-sections.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/081-flow-bom-export.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/081-flow-packing-export.png
```
