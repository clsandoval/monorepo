# Stage 012 — Fix Inventory Seed Migration

**Category**: Bug Fix
**Priority**: 2 — FIX
**Finding**: Q06

## Task

Debug and fix the inventory seed migration.

1. Ensure Supabase is running: `cd apps/podplay && npx supabase status || npx supabase start`
2. Read `apps/podplay/supabase/migrations/00015_seed_inventory.sql`
3. Run `npx supabase db reset` and check the error output
4. Common causes: FK constraint violation (referencing non-existent catalog items), duplicate keys, wrong column names
5. Fix the migration SQL
6. Verify with a clean `npx supabase db reset` — all migrations must apply without errors
7. Query the inventory table to confirm 47 rows exist

## Verify

```bash
cd apps/podplay && npx supabase db reset 2>&1 | tail -20
```
