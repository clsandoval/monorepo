# Stage 022 — Wizard Step Locking: Intake Wizard UI

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q11

## Task

Apply step locking UI to the intake wizard.

1. Open the intake wizard route/component
2. Use the step locking logic from stage 021
3. For locked (future) steps:
   - Grey out the step indicator/tab
   - Make it non-clickable (no `onClick` handler, `cursor-not-allowed`)
   - Add `opacity-50` or similar visual treatment
4. For completed steps:
   - Show a checkmark or completed state
   - Keep them clickable (backward navigation allowed)
5. For current step:
   - Normal styling, active indicator

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
