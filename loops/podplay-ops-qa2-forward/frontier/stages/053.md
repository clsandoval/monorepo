# Stage 053 — Desktop Verify: /projects/:id/financials

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/financials` at 1280x800 viewport with Playwright. Assert:
1. Project financials page renders with real financial data
2. Deposit minimum enforcement is visible (minimum deposit amount displayed or enforced in input)
3. Financial summary or breakdown is present (not placeholder)

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/projects/{project_id}/financials
# 3. Assert financial data is visible (dollar amounts, line items)
# 4. Assert deposit minimum enforcement is present
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/053-financials-project-desktop.png
```
