# Stage 013 — Fix "(tier)" Literal in Advance to Deployment

**Category**: Bug Fix
**Priority**: 2 — FIX
**Finding**: Q07

## Task

Fix the confirmation dialog or toast that shows "(tier)" instead of the actual project tier.

1. Search for the literal string "(tier)" or `(tier)` in:
   - `apps/podplay/src/lib/confirmation-dialogs.ts`
   - `apps/podplay/src/lib/toast-messages.ts`
   - `apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`
   - `apps/podplay/src/components/wizard/procurement/` files
2. Find where the tier value should be interpolated but isn't
3. Fix the template to use the actual project tier (e.g., "Pro", "Autonomous", "Autonomous+")
4. Ensure the tier is passed as a parameter to the dialog/toast function

## Verify

```bash
cd apps/podplay && npx tsc --noEmit && grep -rn "(tier)" apps/podplay/src/ | grep -v node_modules | grep -v ".test."
```

The grep should return no matches after the fix.
