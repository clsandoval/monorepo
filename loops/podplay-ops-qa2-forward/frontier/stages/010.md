# Stage 010 — Remove PingPod WiFi Field

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q04

## Task

Remove the `has_pingpod_wifi` field from the venue config step and all references.

1. `apps/podplay/src/components/wizard/intake/VenueConfigStep.tsx` — remove the checkbox field and from the Zod schema
2. `apps/podplay/src/components/wizard/intake/ReviewStep.tsx` — remove the "Has PingPod WiFi" display row
3. `apps/podplay/src/lib/types.ts` — remove `has_pingpod_wifi` from any type definitions
4. `apps/podplay/src/__tests__/wizard/intake/venue-config.test.tsx` — remove tests referencing `has_pingpod_wifi`

## Verify

```bash
cd apps/podplay && npx tsc --noEmit && npm run test -- --reporter=verbose 2>&1 | tail -50
```
