# Stage 061 — Desktop Verify: /settings/catalog

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/settings/catalog` at 1280x800 viewport with Playwright. Assert:
1. Hardware catalog page renders with catalog items
2. iPad 128GB entry is present in the catalog
3. Catalog items show name, price, and other details

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/settings/catalog
# 3. Assert catalog items are visible
# 4. Assert "iPad 128GB" or similar entry exists
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/061-settings-catalog-desktop.png
```
