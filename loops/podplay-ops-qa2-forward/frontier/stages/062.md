# Stage 062 — Desktop Verify: /guide

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/guide` at 1280x800 viewport with Playwright. Assert:
1. Operations guide page renders with all sections
2. Guide content is real (not placeholder text)
3. Print button is visible and clickable
4. All guide sections are accessible (scrollable or navigable)

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/guide
# 3. Assert guide sections are visible
# 4. Assert print button exists
# 5. Assert no placeholder content
# 6. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/062-guide-desktop.png
```
