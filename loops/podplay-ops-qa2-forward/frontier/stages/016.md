# Stage 016 — Delete Project: Dashboard UI + Handler

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q09

## Task

Add delete button and handler to the project dashboard.

1. Open the dashboard component (likely `apps/podplay/src/components/dashboard/` or the projects index route)
2. Add a delete action to each project card (trash icon button or dropdown menu item)
3. On click: show the `deleteProjectDialog` confirmation
4. On confirm: call `supabase.from('projects').delete().eq('id', projectId)`
5. On success: show success toast, refresh project list
6. On error: show error toast

Check if FK constraints cascade deletes to related tables (BOM items, checklist items, invoices, expenses). If not, delete related records first in order.

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
