# Stage 080 — Flow QA: Delete Project

**Category**: Flow QA
**Priority**: 8

## Task

End-to-end flow test with Playwright at 1280x800: Test project deletion flow.

1. Navigate to `/projects` — count current number of project cards
2. Create a new test project via `/projects/new` (name: "QA Delete Test - Stage 080")
3. Assert the new project appears on the dashboard
4. Click the delete button on the newly created project
5. Assert a confirmation dialog appears
6. Confirm the deletion
7. Assert the project is removed from the dashboard
8. Assert the project count is back to the original number
9. Navigate to the deleted project's URL directly — assert it shows 404 or redirects

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to execute the full flow described above
# Assert project is fully removed after deletion
# Take screenshots at key steps

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/080-flow-delete-before.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/080-flow-delete-confirm.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/080-flow-delete-after.png
```
