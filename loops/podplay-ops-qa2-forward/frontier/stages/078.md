# Stage 078 — Flow QA: Inventory Management

**Category**: Flow QA
**Priority**: 8

## Task

End-to-end flow test with Playwright at 1280x800: Test inventory management operations.

1. Navigate to `/inventory`
2. Record current stock levels for an item
3. Adjust stock level for an item (increase or decrease)
4. Assert the display updates to reflect the new stock level
5. Set an on-order quantity for an item
6. Assert the on-order column updates correctly
7. Verify status column reflects changes (e.g. "on-order" when quantity is set)
8. Refresh the page and verify changes persisted in Supabase

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to execute the full flow described above
# Assert inventory data persists across page refresh
# Take screenshots before and after changes

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/078-flow-inventory-before.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/078-flow-inventory-after.png
```
