# Stage 051 — Desktop Verify: /projects/:id/procurement

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/procurement` at 1280x800 viewport with Playwright. Assert:
1. Procurement tabs render (BOM, Purchase Orders, etc.)
2. No CC terminal references exist on the page (removed item)
3. No replay sign references exist on the page (removed item)
4. Project name is displayed in the page header

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/projects/{project_id}/procurement
# 3. Assert procurement tabs are visible
# 4. Assert NO element contains "CC Terminal" or "cc_terminal"
# 5. Assert NO element contains "Replay Sign" or "replay_sign"
# 6. Assert project name appears in header
# 7. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/051-procurement-desktop.png
```
