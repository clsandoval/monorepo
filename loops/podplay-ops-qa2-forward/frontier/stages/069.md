# Stage 069 — Mobile Verify: /projects/:id/financials

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/financials` at 375x812 viewport with Playwright. Assert:
1. Project financials render correctly on mobile
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. Financial data is readable (no truncated columns)
4. All interactive elements have touch targets >= 44px

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/projects/{project_id}/financials
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/069-financials-project-mobile.png
```
