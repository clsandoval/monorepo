# Stage 056 — Desktop Verify: /settings

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/settings` at 1280x800 viewport with Playwright. Assert:
1. Settings index page renders with navigation links to sub-pages
2. Links to team, installers, vendors, pricing, catalog are visible
3. Page is not a stub or placeholder

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/settings
# 3. Assert navigation links to sub-pages are visible
# 4. Assert links for team, installers, vendors, pricing, catalog exist
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/056-settings-desktop.png
```
