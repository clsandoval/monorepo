# Stage 076 — Flow QA: Create Project + Complete Intake

**Category**: Flow QA
**Priority**: 8

## Task

End-to-end flow test with Playwright at 1280x800: Create a new project and complete the full intake process with real Supabase data.

1. Navigate to `/projects/new`
2. Fill in project name (e.g. "QA Test Project - Stage 076"), location, and other required fields
3. Submit the form — assert redirect to the new project's intake page
4. Complete all intake wizard steps in order:
   - Fill each step's required fields
   - Select multiple installers using the multi-select picker
   - Verify step locking: cannot skip ahead to uncompleted steps
   - Advance through each step
5. After all intake steps are complete, advance to procurement phase
6. Verify the project now shows "Procurement" status on the dashboard

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to execute the full flow described above
# Assert each step transition succeeds
# Assert final state: project is in procurement phase
# Take screenshots at key steps

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/076-flow-create-project.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/076-flow-intake-complete.png
```
