# Stage 007 — Remove Replay Sign Types & Labels

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q02

## Task

Remove all replay sign references from shared lib files.

1. `apps/podplay/src/lib/enum-labels.ts` — remove replay sign enum labels
2. `apps/podplay/src/lib/toast-messages.ts` — remove replay sign toast messages
3. `apps/podplay/src/lib/confirmation-dialogs.ts` — remove replay sign confirmation dialogs
4. `apps/podplay/src/lib/validation-messages.ts` — remove replay sign validation messages (if any)
5. `apps/podplay/src/lib/types.ts` — remove replay sign types/interfaces

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
