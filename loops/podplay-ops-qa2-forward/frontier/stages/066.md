# Stage 066 — Mobile Verify: /projects/:id/intake

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/intake` at 375x812 viewport with Playwright. Assert:
1. Intake wizard renders correctly on mobile with step navigation
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. All interactive elements (step buttons, form inputs, multi-select) have touch targets >= 44px
4. Wizard steps are navigable on mobile

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/projects/{project_id}/intake
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px on wizard controls
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/066-intake-mobile.png
```
