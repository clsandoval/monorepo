# Stage 065 — Mobile Verify: /projects/new

**Category**: Mobile Verify
**Priority**: 7

## Task

Navigate to `/projects/new` at 375x812 viewport with Playwright. Assert:
1. New project form renders correctly on mobile
2. No horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth)
3. Form inputs and submit button have touch targets >= 44px
4. Form fields are full-width on mobile

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 375x812
# 2. Navigate to http://localhost:5175/projects/new
# 3. Assert no horizontal overflow: document.documentElement.scrollWidth <= 375
# 4. Assert touch targets >= 44px on form inputs and buttons
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/065-projects-new-mobile.png
```
