# Stage 050 — Desktop Verify: /projects/:id/intake

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/intake` at 1280x800 viewport with Playwright (use a known project ID from Supabase). Assert:
1. Intake wizard renders with multiple steps
2. Step locking is enforced — future steps are disabled until prior steps are completed
3. Multi-select installer picker is present and functional
4. No PingPod WiFi field exists anywhere on the page (this was removed)

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/projects/{project_id}/intake
# 3. Assert wizard steps are visible
# 4. Assert locked steps have disabled/locked state
# 5. Assert multi-select installer picker is present
# 6. Assert NO element contains "PingPod WiFi" or "pingpod_wifi"
# 7. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/050-intake-desktop.png
```
