# Stage 026 — Project Name in Headers

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q14, Q15

## Task

Show project name instead of UUID in procurement and intake page headers.

1. Open `apps/podplay/src/routes/_auth/projects/$projectId/procurement.tsx`
   - Find where the project ID/UUID is displayed in the header
   - Replace with the project name (query from project data)
   - Format: "Procurement — [Project Name]"
2. Open `apps/podplay/src/routes/_auth/projects/$projectId/intake.tsx`
   - Same fix — show project name instead of UUID
   - Format: "Intake — [Project Name]"
3. If the project data is already fetched in these routes, use it. If not, add a query for the project name.

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
