# Stage 032 — Inventory On-Order: Display Columns

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q20

## Task

Update the inventory table to show on-order columns.

1. Open the inventory table component (likely `StockLevelsTable.tsx` or similar)
2. Add columns:
   - **On Hand** — `quantity_on_hand` (existing)
   - **On Order** — `qty_on_order` (new)
   - **Available** — calculated: `quantity_on_hand + qty_on_order` (projected availability)
   - **Status** — `order_status` with appropriate badge/label
3. Update the Supabase query to select the new columns
4. Use existing enum label patterns for `order_status` display

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
