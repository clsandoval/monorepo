# Stage 033 — Inventory On-Order: Tests

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q20

## Task

Add tests for inventory on-order tracking.

1. Update or create tests in `apps/podplay/src/__tests__/inventory/`
2. Test cases:
   - On-order columns render in inventory table
   - Manual on-order adjustment modal works
   - Available quantity calculates correctly (on_hand + on_order)
   - Order status badge displays correct label

## Verify

```bash
cd apps/podplay && npm run test -- --reporter=verbose 2>&1 | tail -50
```
