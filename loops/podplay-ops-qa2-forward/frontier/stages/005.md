# Stage 005 — Remove CC Terminal Tests

**Category**: Removal
**Priority**: 1 — REMOVE
**Finding**: Q01

## Task

Delete or update test files that reference CC terminals.

1. Delete `apps/podplay/src/__tests__/wizard/procurement/cc-terminal*.test.ts*` (if exists)
2. Search all test files for `cc_terminal` / `CcTerminal` references and remove them
3. Update any procurement test that lists CC terminal as a tab

## Verify

```bash
cd apps/podplay && npm run test -- --reporter=verbose 2>&1 | tail -50
```
