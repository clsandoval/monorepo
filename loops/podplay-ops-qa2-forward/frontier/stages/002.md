# Stage 002 — Removal Manifest

**Category**: Scaffold
**Priority**: N/A (audit only)

## Task

Identify ALL files that reference CC terminals, replay signs, travel settings, and PingPod WiFi. Create a removal manifest at `loops/podplay-ops-qa2-forward/frontier/removal-manifest.md`.

Search for:
1. `cc_terminal` / `CcTerminal` / `ccTerminal` — all files
2. `replay_sign` / `ReplaySign` / `replaySign` — all files
3. `travel` in settings context — route file, component, nav link
4. `pingpod_wifi` / `has_pingpod_wifi` / `PingPod WiFi` — all files

For each feature, list:
- Component files to delete
- Route files to delete
- Type/enum references to clean up (in `types.ts`, `enum-labels.ts`, `toast-messages.ts`, `confirmation-dialogs.ts`, `validation-messages.ts`)
- Test files to delete or update
- Navigation/sidebar references to remove

## Verify

```bash
echo "Removal manifest complete — check frontier/removal-manifest.md"
```
