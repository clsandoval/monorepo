# Stage 011 — Fix Vendor Add Failure

**Category**: Bug Fix
**Priority**: 2 — FIX
**Finding**: Q05

## Task

Debug and fix the vendor add failure in `VendorSettings.tsx`.

1. Ensure Supabase is running: `cd apps/podplay && npx supabase status || npx supabase start`
2. Read `apps/podplay/src/components/settings/VendorSettings.tsx`
3. Check the insert query — verify table name, column names, and data shape match the schema in `apps/podplay/supabase/migrations/00017_vendors_table.sql`
4. Check RLS policies — are there any that block inserts?
5. Fix the root cause
6. Verify by running `npx supabase db reset` and then testing the insert via the component logic

## Verify

```bash
cd apps/podplay && npx tsc --noEmit && npm run test -- --reporter=verbose 2>&1 | tail -50
```
