# Stage 018 — Installer Multi-Select: Redesign Component

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q10

## Task

Redesign the installer selection step from single-select to multi-select.

1. Read `apps/podplay/src/components/wizard/intake/InstallerSelectionStep.tsx`
2. Replace the single-select UI with a multi-select dropdown
3. Use the existing `SearchableSelect` component if it supports multi-select, or create a `MultiSelect` variant
4. Selected installers should display as chips/tags below the dropdown
5. Each chip should have an "x" to remove
6. The form value should be an array of installer IDs

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
