# Stage 077 — Flow QA: Procurement to Financial Close

**Category**: Flow QA
**Priority**: 8

## Task

End-to-end flow test with Playwright at 1280x800: Take a project from procurement through deployment to financial close.

1. Navigate to an existing project in procurement phase (`/projects/:id/procurement`)
2. Generate BOM — assert BOM table populates with hardware items
3. Create a Purchase Order — assert PO is created and visible
4. Advance to deployment phase — assert redirect to deployment page
5. Complete all deployment checklist items — check each checkbox
6. Assert advance button becomes enabled when all items are checked
7. Advance to financial close
8. Verify project shows correct final status

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to execute the full flow described above
# Assert each phase transition succeeds
# Assert final state: project reaches financial close
# Take screenshots at key steps

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/077-flow-procurement.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/077-flow-deployment.png
# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/077-flow-financial-close.png
```
