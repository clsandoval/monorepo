# Stage 046 — PDF Export: Add to BOM, Packing List, Invoices

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q19

## Task

Add PDF export buttons to data pages.

1. **BOM Review** (`apps/podplay/src/components/wizard/procurement/BomReviewTable.tsx`):
   - Add `PdfExportButton` wrapping the BOM table
   - Label: "Export BOM as PDF"
2. **Packing List** (`apps/podplay/src/components/wizard/procurement/PackingList.tsx`):
   - Add `PdfExportButton` wrapping the packing list
   - Label: "Export Packing List as PDF"
3. **Invoice/Financial summaries** (in financials wizard components):
   - Add `PdfExportButton` to deposit invoice and final invoice views
   - Label: "Export Invoice as PDF"

Place export buttons in the top-right of each section header.

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
