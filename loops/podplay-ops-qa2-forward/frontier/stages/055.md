# Stage 055 — Desktop Verify: /financials

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/financials` at 1280x800 viewport with Playwright. Assert:
1. Global financials overview page renders
2. Summary metrics or charts are visible (total revenue, costs, etc.)
3. Page shows real aggregated data, not placeholder content

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/financials
# 3. Assert financial overview content is visible
# 4. Assert summary metrics are present
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/055-financials-global-desktop.png
```
