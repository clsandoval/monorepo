# Stage 082 — Discovery: Orphaned Components + Dead Imports

**Category**: Discovery
**Priority**: 9

## Task

Scan the codebase for orphaned references to removed components. These items were intentionally removed and should have zero references:

1. **CC Terminal** — grep for `cc.terminal`, `cc_terminal`, `CCTerminal`, `CC Terminal` in `apps/podplay/src/`
2. **Replay Sign** — grep for `replay.sign`, `replay_sign`, `ReplaySign`, `Replay Sign` in `apps/podplay/src/`
3. **Travel** — grep for travel-related component imports that reference deleted files
4. **PingPod WiFi** — grep for `pingpod.wifi`, `pingpod_wifi`, `PingPodWifi`, `PingPod WiFi` in `apps/podplay/src/`

For each match found:
- Determine if it's a dead import, a string literal, or active code
- Remove the reference or update it as appropriate
- If removing an import causes a build error, fix the downstream code

## Verify

```bash
cd apps/podplay
# Assert zero matches for removed components
grep -ri "cc.terminal\|cc_terminal\|CCTerminal" src/ && echo "FAIL: CC Terminal references found" || echo "PASS: No CC Terminal references"
grep -ri "replay.sign\|replay_sign\|ReplaySign" src/ && echo "FAIL: Replay Sign references found" || echo "PASS: No Replay Sign references"
grep -ri "pingpod.wifi\|pingpod_wifi\|PingPodWifi" src/ && echo "FAIL: PingPod WiFi references found" || echo "PASS: No PingPod WiFi references"
npm run build
```
