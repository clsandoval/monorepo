# Stage 028 — Deposit Minimum Enforcement + Deployment Advance Gate

**Category**: UX Improvement
**Priority**: 3 — IMPROVE
**Finding**: Q16, Q17

## Task

Two related enforcement changes:

### Deposit minimum
1. Open the financials wizard deposit form (likely in `apps/podplay/src/components/wizard/financials/`)
2. Query the `minimum_deposit` from settings
3. Add validation: deposit amount must be >= minimum_deposit
4. Show validation error: "Minimum deposit is $[amount]"

### Deployment advance gate
1. Open `apps/podplay/src/routes/_auth/projects/$projectId/deployment.tsx`
2. Find the "Advance to Financial Close" button
3. Query all `deployment_checklist_items` for this project
4. If any items are unchecked: disable the button
5. Add tooltip on disabled button: "Complete all deployment checklist items before advancing"

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
