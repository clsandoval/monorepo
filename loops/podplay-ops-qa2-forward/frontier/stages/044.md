# Stage 044 — Operations Guide: Print CSS + Styling

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q18

## Task

Add print-friendly CSS and polished styling to the operations guide.

1. Add `@media print` styles to the guide (in `index.css` or a guide-specific CSS):
   - Hide navigation sidebar, header, and non-content elements
   - Clean page breaks between major sections
   - Ensure tables and code blocks don't split across pages
   - Proper margins for printing
2. Style the guide with:
   - Clear typography hierarchy (h1 > h2 > h3)
   - Callout boxes for tips and warnings
   - Numbered steps with visual indicators
   - Table styling for reference data
3. Add a "Print Guide" button at the top of the guide page

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
