# Stage 052 — Desktop Verify: /projects/:id/deployment

**Category**: Desktop Verify
**Priority**: 7

## Task

Navigate to `/projects/:id/deployment` at 1280x800 viewport with Playwright. Assert:
1. Deployment checklist renders with checkable items
2. Advance button is present but disabled when checklist items are incomplete
3. Checklist items are interactive (can be checked/unchecked)

## Setup

```bash
cd apps/podplay && npx supabase status || npx supabase start
cd apps/podplay && npm run dev -- --port 5175 &
sleep 5
```

## Verify

```bash
# Use Playwright to:
# 1. Set viewport to 1280x800
# 2. Navigate to http://localhost:5175/projects/{project_id}/deployment
# 3. Assert checklist items are visible
# 4. Assert advance button exists and is disabled
# 5. Take screenshot

# Screenshot saved to loops/podplay-ops-qa2-forward/screenshots/052-deployment-desktop.png
```
