# Stage 035 — Wizard Navigation: Wire Into Intake

**Category**: New Feature
**Priority**: 4 — BUILD
**Finding**: Q21

## Task

Wire the WizardNavigation component into the intake wizard.

1. Open `apps/podplay/src/routes/_auth/projects/$projectId/intake.tsx`
2. Replace any existing step navigation with `WizardNavigation`
3. Pass the intake steps (Customer Info, Venue Config, ISP Info, Tier Selection, Installer Selection, Financial Setup, Review)
4. Wire `onPrevious` to go back one step (saving current form state)
5. Wire `onNext` to validate + save, then advance
6. Wire `onStepClick` for completed steps (backward navigation)
7. Integrate with step locking from stage 021-022

## Verify

```bash
cd apps/podplay && npx tsc --noEmit
```
