# Analysis Log

| 68 | 2026-03-13 | 8.1.2 — Cross-reference notes for messages/tool_calls bot tables + RLS for website reads | 1 run | CORRECTION: The prior schema.md section incorrectly referred to `messages` and `tool_calls` as "existing bot tables." Investigation confirmed these tables DO NOT exist in the current Decision Orchestrator schema — bot reads Discord history via channel.history() API, never persists to Supabase. Created two NEW SaaS tables: (1) `tenant_messages` — lightweight event log: id UUID PK, tenant_id UUID FK→tenants CASCADE, guild_id TEXT, channel_id TEXT, message_type TEXT CHECK('mention','dm','command'), created_at TIMESTAMPTZ. Composite index on (tenant_id, created_at DESC). RLS: authenticated SELECT via tenant_members EXISTS subquery, INSERT blocked for authenticated (bot uses service role). Migration 20260400000008_create_tenant_messages.sql. Bot instrumentation: fire-and-forget INSERT after message dispatch in guild_handler.py and dm_handler.py; errors logged as WARNING, never block user response. Retention: 90-day rolling DELETE by pg_cron at 03:00 UTC. (2) `tenant_tool_calls` — lightweight tool event log: id UUID PK, tenant_id UUID FK→tenants CASCADE, tool_name TEXT, success BOOLEAN DEFAULT TRUE, duration_ms INTEGER NULL CHECK >=0, created_at TIMESTAMPTZ. Composite index on (tenant_id, created_at DESC). RLS: same pattern as tenant_messages. Migration 20260400000009_create_tenant_tool_calls.sql. Bot instrumentation: fire-and-forget INSERT wrapping ToolRegistry.call_tool() dispatch; asyncio.create_task for non-blocking; duration_ms from time.monotonic(). Retention: 90-day rolling DELETE at 03:05 UTC. Updated dashboard.md: replaced .from('messages')/.from('tool_calls') queries with .from('tenant_messages')/.from('tenant_tool_calls'); removed incorrect "existing bot tables" framing. Updated data-model-reconciliation.md: both rows updated from ⚠️ to ✅ with correct explanation. Added cross-reference table in schema.md listing all 13 dashboard components × tables queried × columns used — all ✅ confirmed. Updated migration sequence in schema.md: 10 migrations total (000–009). Statistics: 74 total, 68 analyzed, 6 pending, 92% convergence. |
| 67 | 2026-03-13 | 8.1.1 — Complete database/schema.md: stripe_webhook_events table + cross-references | 1 run | Added `stripe_webhook_events` table to database/schema.md: full DDL (id UUID PK, stripe_event_id TEXT UNIQUE, event_type TEXT, processed_at TIMESTAMPTZ), column specifications with exact descriptions, UNIQUE constraint rationale (idempotency lock), 2 indexes (processed_at for retention, event_type+processed_at for admin), RLS (enabled, no policies — service role only), TypeScript usage pattern (INSERT ON CONFLICT DO NOTHING RETURNING), complete migration SQL (20260400000007_create_stripe_webhook_events.sql), migration sequence updated to 8 files. Resolved contradiction in api/webhooks.md: "v1 decision: do not implement deduplication table" superseded — updated section with full implementation pattern and rationale. Added cross-references for existing bot tables messages/tool_calls read by dashboard: RLS extension patterns for website JWT reads (tenant_members EXISTS subquery), query patterns for dashboard (message count, tool call count, recent activity feed). Updated data-model-reconciliation.md: stripe_webhook_events ✅, tenant_subscriptions ✅, gap table rows 1-2 marked RESOLVED, verdict updated. Statistics: 74 total, 67 analyzed, 7 pending, 91% convergence. |
| 66 | 2026-03-13 | 8.1 — Data model reconciliation | 1 run | Created database/data-model-reconciliation.md: full audit of 7 schema tables vs frontend queries × 5 pages, 18+ API routes vs api/ directory (critical gap: api/routes.md missing), Stripe/OAuth/Discord/Toggl integration storage (all covered), bot startup query + 4 Realtime channel subscriptions (all covered). Gaps found: (1) tenant_subscriptions in migrations.md only, not schema.md; (2) stripe_webhook_events idempotency table never defined; (3) messages/tool_calls existing bot tables need cross-reference + RLS extension for website reads; (4) api/routes.md entirely missing — 18 routes documented piecemeal; (5) api/auth.md missing; (6) 4 other organizational files missing (multi-tenant/adaptation-plan, api/rate-limiting, database/triggers, database/indexes, database/retention). Added 9 new aspects (8.1.1–8.1.9) to frontier. Statistics updated: 74 total, 66 analyzed, 8 pending, 89% convergence. |
| 65 | 2026-03-13 | 7.4b — Accessibility: keyboard navigation + focus management — tab order per page, focus traps, screen reader announcements | 1 run | Created ui/keyboard-navigation.md: 18 sections — (1) Global keyboard conventions: universal key table (Tab/Shift+Tab/Enter/Space/Escape/Arrow/Home/End/PageUp/PageDown), focus order rules (5 rules including tabindex > 0 never), focusable vs non-focusable element lists; (2) Focus ring styles: global focus ring (outline 2px #00D4E8 offset 2px via focus-visible), 13-row variant table per component (primary/secondary/ghost/destructive button/input/checkbox/toggle/select/link/skip/modal/service card/dropdown/tab — each with color/offset/border-radius), high contrast media query (3px solid black), globals.css implementation with input inset shadow variant; (3) Skip links: full HTML implementation with Tailwind classes (sr-only until focused, z-200, white bg, aqua border), docs pages secondary skip link spec, behavior description (tabindex=-1 on main, first Tab after lands on first focusable inside main); (4–13) Tab order per page: Landing (41 positions — skip/logo/nav/hero/feature-cards/pricing CTAs/FAQ accordion 6 triggers/CTA section/footer links/social icons), Login (7 positions with autoFocus on email, Enter submits, error focus to role=alert div), Signup (10 positions including inline ToS/Privacy links within checkbox label, password strength live region), Reset-password (3 steps: request/confirmation focus-to-heading/new-password form), Dashboard (21 positions — sidebar 7 items/topbar/onboarding checklist CTAs/bot status/stat cards/activity/quick actions, mobile sidebar overlay order), Integrations (tab widget arrow-key behavior, OAuth focus restore post-redirect via sessionStorage pattern, API key modal cross-ref), Billing (inline API key update form sub-sequence with Cancel/Save focus return rules), Settings (tab widget, 4 panels: General/Discord/Team/Danger Zone — each with full sub-sequence), Admin panel (tenant list with search/filter/row actions/pagination, pagination focus-to-heading on page change, tenant detail page), Docs pages (secondary skip link, sidebar links, article heading anchor links, code block copy buttons, TOC links, docs search combobox arrow-key behavior); (14) Focus traps: implementation pattern (focus-trap npm, trigger ref storage/restoration, aria-hidden on background), Service Connection modal (4 positions, focus on close button at open, error stays in modal), Confirmation dialog (3 positions, focus on heading NOT destructive button, Enter defaults to Cancel), User account dropdown (menu item keyboard pattern with arrow/Home/End/Tab-exits/Enter), Mobile nav overlay (focus trap, 7 positions, Escape returns to hamburger), API key reveal (no trap — just type toggle); (15) Focus management after actions: client-side routing (usePathname effect moves focus to #main-content ref), success focus table (13 rows covering all form submissions), error focus table (8 rows: field-level vs banner-level per scenario), async state changes table (10 non-form triggers — none change focus, all use live regions); (16) Component keyboard behavior: Button (Enter/Space activate, disabled removes from tab, loading aria-disabled stays in tab), FormInput (no Escape clear rule), PasswordInput (Tab from field → reveal toggle → next field), Select (native browser), Toggle (Space/Enter, role=switch), Checkbox (Space only, no Enter per ARIA spec), Dropdown menu (full APG pattern with wrap-around), Tabs (auto-activation on arrow keys, Tab exits widget), Accordion (APG pattern), Table (sortable th keyboard), CopyToClipboard (2s label change + live region), Pagination (disabled aria-disabled, focus-to-heading on page change), SearchInput (full combobox pattern); (17) Live regions: live region type table (polite/assertive/off with usage), global toast container HTML (aria-live=polite/atomic=true/role=status, sr-only div, visual toasts separate), 17-row toast message announcements table (every action with exact announcement text), error alert region HTML (role=alert/assertive/tabindex=-1), bot status live region (5 transition messages), onboarding checklist progress, password strength (4 levels), search results count (300ms debounce rule), loading state announcements (user-triggered only, not background polls); (18) Mobile keyboard: virtual keyboard behavior (scroll-into-view native, modal scroll within container), 8-row input type mapping table (email/password/numeric/API key/bot token/search/URL — each with type+inputmode+keyboard type), 9-row touch target table (all interactive elements with visual size vs effective target via padding) |
| 64 | 2026-03-13 | 7.4a — Accessibility: ARIA labels and roles — specific labels per component across all pages | 1 run | Created ui/accessibility.md: 17 sections — (1) Global ARIA landmarks: skip link implementation (sr-only until focused, links to #main-content), public/auth/dashboard page structure with correct role=banner/role=contentinfo/nav/main elements; (2) Layout components: Sidebar (nav aria-label, per-route aria-current=page table, logout button aria-label, loading state aria-busy), DashboardTopbar (header role, hamburger aria-expanded+aria-controls, PlanBadge per-plan aria-label table), MobileNav (role=dialog/aria-modal/aria-label, focus trap, backdrop aria-hidden), AuthCard (role=region+aria-labelledby), AuthFooterLinks (nav aria-label, separator aria-hidden), PageShell (breadcrumb nav+ol with aria-current=page), PublicNavbar (header role=banner, nav aria-label, hamburger aria-expanded+aria-controls, PublicMobileMenu role=dialog), PublicFooter (role=contentinfo with 4 nav sections each aria-labeled, external links with aria-label "(opens in new tab)"); (3) Form components: FormInput (label+htmlFor, aria-describedby combining hint+error IDs, aria-invalid, aria-required, ARIA attributes table for 14 specific fields), PasswordInput (toggle button aria-pressed+aria-label Show/Hide, password strength indicator role=status+aria-live=polite with 4 strength level announcements), ApiKeyInput (connected vs disconnected states with aria-label per state, table for all 4 services), Select (aria-invalid, aria-required), Toggle (role=switch+aria-checked, Toggle instances table with 3 toggles across Settings and Admin), Checkbox (aria-label+aria-required, 2 checkbox instances), SearchInput (role=search, sr-only results count span aria-live=polite, 2 instances); (4) Feedback components: AlertBanner (role/aria-live per variant table: error→alert/assertive, warning/success/info→status/polite, dismiss button aria-label with context, 3 dismiss aria-label examples), Toast (toast container role=region, individual toast role/aria-live per variant, 21-row toast message ARIA table), ConfirmDialog (role=dialog+aria-modal+aria-labelledby+aria-describedby, confirm input aria-describedby with sr-only instruction, 10-row complete dialog inventory with exact title/description/confirm/cancel aria-labels for every destructive action), Modal (aria-labelledby+aria-describedby, close button aria-label "Close {title} dialog", 6-row modal instance table), EmptyState (role=region+aria-label, 4-row instance table), ErrorState (role=alert+aria-live=assertive, retry aria-label patterns, 5-row instance table), SkeletonLoader (role=status+aria-busy=true+aria-label, sr-only text, 8-row instance table); (5) Data display components: Badge (aria-label per badge type — 14-row badge instance table covering bot status/plan/subscription/admin/service type), StatusIndicator (role=status+aria-label, 7-row instance table), Table (caption+th scope=col+aria-sort on sortable headers+sort button aria-label, row aria-label pattern, 3-row table instance table), Pagination (prev/next button aria-label, page button aria-current=page, aria-live sr-only "Page X of Y"), StatCard (role=region+aria-label, dl/dt/dd structure with aria-label, trend aria-label, 4-row instance table), ActivityFeed (section aria-live=polite/aria-atomic=false, time dateTime+aria-label, event aria-label patterns table), CopyToClipboard (button aria-label changes on copy state, 4-row instance table); (6) Action components: Button (aria-label only when text is insufficient — 30-row Button usage table, loading state aria-label table with 8 patterns), IconButton (aria-label REQUIRED rule, 14-row usage table), DropdownMenu (aria-haspopup=menu+aria-expanded+aria-controls on trigger, ul role=menu+aria-labelledby, li role=none, button role=menuitem), Tabs (role=tablist+aria-label, role=tab+aria-selected+aria-controls+tabIndex, role=tabpanel+aria-labelledby+hidden, 3-row instance table); (7–13) Per-page ARIA annotations: landing (section aria-labelledby for each section, FAQ dl/dt/dd pattern with button aria-expanded+aria-controls, pricing plan article aria-label, testimonial blockquote+cite structure), login/signup/reset/confirm (form aria-label, AlertBanner role=alert, all field aria-invalid/aria-required/aria-describedby, password strength role=status, terms checkbox aria-required), dashboard (onboarding checklist ol with step aria-label complete/incomplete patterns, stat cards role=group, bot status aria-label, Realtime subscription announce pattern), integrations (service grid ul role=list, article per service aria-label with connected state, connect/disconnect button aria-labels), billing (current plan region aria-label, plan comparison role=list with aria-current=true for active plan, API keys section aria-labelledby+aria-describedby, invoice Table), settings (Tabs for sections, general form aria-label, discord form aria-label, danger zone section aria-labelledby+aria-describedby+role=group), admin (search role=search, table th scope=col with sr-only "Actions", row aria-label pattern, DropdownMenu per-row with tenant name in aria-label, admin detail dl aria-label, Toggle aria-label with active/suspended state, admin action group aria-label); (14) Docs pages: docs nav aria-label, submenu button aria-expanded+aria-controls, aria-current=page per link, article aria-labelledby, code block figure+figcaption+code aria-label; (15) Live region announcements: global live regions in root layout (2 divs: polite + assertive with IDs), 25-row announcement table (trigger/region/exact message string for every integration/billing/settings/session action), bot status Realtime update pattern with announce() TypeScript helper function; (16) Image and icon alt text: aria-hidden=true rule for all decorative icons paired with text, service logo alt text (8 services), OG image alt text, user avatar aria-hidden; (17) Color contrast ratios: 14 foreground/background combinations with ratios and WCAG level, 3 actionable failures identified (AuthFooterLinks rgba(12,31,64,0.45) fails AA → use 0.55, focus ring Aqua on white 2.5:1 fails 1.4.11 → use #82D4C8 3.2:1, default input border rgba(12,31,64,0.20) fails 3:1 → use 0.30), focus ring color table per background context (4 rows) |

| 63 | 2026-03-13 | 7.3b — Edge cases: integrations + billing — Stripe down, OAuth revoked externally, bot crash mid-operation, slow connections | 1 run | Created frontend/edge-cases-integrations-billing.md: 9-part exhaustive spec — (1) Stripe failures: 1.1 Checkout Session creation fails (Stripe 5xx → AlertBanner "billing_unavailable" + Try Again, stays on page, no DB change), 1.2 webhook delivery failure (3-day retry schedule, 6hr reconciliation Edge Function SQL with stripe.subscriptions.retrieve, mismatched status update, admin_audit_log), 1.3 signature verification failure (raw body req.text() requirement — JSON.parse must NOT run first — return 400), 1.4 Customer Portal unavailable (error toast 8s), 1.5 payment method declined → past_due (AlertBanner persistent warning on every dashboard page, bot continues in grace period, customer.subscription.deleted triggers Free downgrade, excess connections → pending_disconnect, bot enforces on Realtime update, Stripe retry schedule default 7 days), 1.6 Pro→Starter downgrade (sort by created_at oldest kept, status=pending_disconnect, localStorage dismiss key for banner), 1.7 subscription canceled (info AlertBanner, service connections NOT deleted, bot gating at invocation time), 1.8 duplicate webhook idempotency (stripe_webhook_events table, INSERT ON CONFLICT DO NOTHING, return 200 on conflict); (2) OAuth revocation: 2.1 GitHub externally revoked (tool 401 detection → update status=error, Discord message, daily validate-oauth-tokens Edge Function for proactive detection), 2.2 Google invalid_grant on refresh (earlier detection at 1h access token expiry), 2.3 Linear revoked, 2.4 partial scope revocation → status=degraded (amber "Limited permissions" badge, Reconnect re-requests full scopes), 2.5 CSRF attack (state mismatch → clear all oauth_* cookies → /integrations?error=invalid_state); (3) Bot crash: 3.1 process crash mid-tool-call (no Discord response, heartbeat stale 90s → disconnected 5min, Fly.io restart_policy=always 10–30s, no in-progress tool resumption, all tenants affected simultaneously — known shared-infra limitation), 3.2 Discord WebSocket disconnect (on_disconnect event, status=reconnecting, toast appears after 5 failed reconnects, 10-attempt max then status=failed, manual Reconnect from Settings), 3.3 Anthropic API unavailable (4 retry attempts, in-Discord fallback message, bot stays running), 3.4 bot removed from guild (on_guild_remove handler, status=kicked, re-add + Reconnect flow, connection NOT deleted), 3.5 Discord token invalidated close code 4004 (LoginFailure exception, status=auth_failed, Realtime event payload, Update Token modal pre-populated with guild ID), 3.6 guild deleted (identical to kicked from bot's perspective); (4) API key invalidation: 4.1 Anthropic revoked mid-session (bot sends Discord message, all commands return upgrade-key message, dashboard AlertBanner, Billing page pre-expanded form, Realtime hot-reload key), 4.2 Toggl rotated (tool 403, status=error, Discord message), 4.3 Anthropic usage limit vs transient rate limit (limit_reached permanent vs 429 transient 4-attempt backoff); (5) Slow connections: 5.1 Stripe 30s Vercel timeout (504 → "Request timed out" AlertBanner, safe to retry), 5.2 OAuth callback expired (10min cookie TTL, /integrations?error=expired_state) or browser closed (no error — service shows not connected), 5.3 Supabase Realtime browser disconnect (amber pulsing dot in TopBar, info toast after 5s, "Live updates restored" on reconnect, refetchDashboardData on SUBSCRIBED), 5.4 Supabase query timeout (10s → skeleton replaced with ErrorState), 5.5 bot token Discord API validation timeout (10s abort, error below input, connection not saved), 5.6 30s client-side safety valve on all loading buttons (setTimeout setIsLoading false); (6) Multi-tab: 6.1 duplicate checkout prevention (check stripe_subscription_status + in-flight session ID in DB before creating), 6.2 concurrent settings save last-writer-wins, 6.3 OAuth completed in background tab (Realtime INSERT event updates connected state in other tabs); (7) Billing lifecycle: 7.1 no trial at launch (documented for future), 7.2 proration mechanics (Stripe handles automatically, no custom code), 7.3 annual→monthly switch at period end, 7.4 annual renewal failure (AlertBanner shows dollar amount); (8) State machine summary: Discord 11 states + transitions table, OAuth 5 states, API key 4 states, subscription 4 states — all transitions enumerated; (9) Notification matrix: 20 events × dashboard AlertBanner/Toast/Discord message/email (Stripe handles billing emails) |

| 62 | 2026-03-13 | 7.3a — Edge cases: auth + session — expired sessions, invalid tokens, concurrent logins, password reset mid-session | 1 run | Created frontend/edge-cases-auth-session.md: 46-scenario exhaustive auth/session edge case spec across 12 sections — (1) Session expiry: 1.1 access token expired/refresh succeeds (transparent, middleware cookie write pattern, must return res not new NextResponse); 1.2 both tokens expired (silent redirect preserving ?next= path, safe path validation); 1.3 session expired mid-form (SERVER_EXPIRED return + toast + 2s redirect, form state lost); 1.4 cookie tampered (identical UX to 1.2); 1.5 server-side session revocation (getUser() vs getSession() distinction — getSession() is local-only, getUser() does server roundtrip — required for privileged routes); (2) Invalid tokens: 2.1 Discord format (client-side regex before API call), 2.2 Discord API 401 (Discord users/@me call), 2.3 bot not in guild (403/404 from Discord guilds/{id}, invite URL modal with client_id extraction from token first segment), 2.4 Anthropic key (4 response codes: 401 invalid/403 permission/429 rate-limited treated as success/402 no-credits saveable with warning), 2.5 OpenAI key (OpenAI models endpoint), 2.6 OAuth token revoked externally (bot detects 401, updates status column, website shows Revoked badge + Reconnect button per service); (3) Concurrent logins: 3.1 two browsers (both valid, no conflict), 3.2 sign out one tab (auth state listener via onAuthStateChange broadcasts SIGNED_OUT across tabs via localStorage, TypeScript AuthStateListener component), 3.3 concurrent edit same resource (last-write-wins documented, no optimistic locking), 3.4 admin impersonation + active user (read-only enforced, user unaware), 3.5 OAuth in two tabs (state cookie one-time-use → second tab gets state mismatch); (4) Password reset: 4.1 while logged in (middleware redirect to /settings?tab=security), 4.2 link expired (error only detectable on submit not page load, "Link expired" message + request new link), 4.3 link used twice (same error as 4.2), 4.4 mid-session interruption (Supabase invalidates other sessions on password change), 4.5 non-existent email (always show success — prevents email enumeration), 4.6 email in spam (resend button, 3-attempt client-side rate limit, no countdown for resend); (5) Account states: 5.1 email verification (confirm-email page, resend, /api/auth/callback PKCE route TypeScript, ?error=confirmation_failed handling), 5.2 duplicate email signup (always show "check email" — prevents enumeration, existing user gets "someone tried" security email), 5.3 tenant setup incomplete (no tenant_members row → redirect /onboarding, idempotent createWorkspaceAction TypeScript with existing-check), 5.4 account suspended mid-session (natural expiry path up to 1h, immediate revocation via Admin API, "User not allowed" detection at login); (6) Admin impersonation: 6.1 user deleted during impersonation (soft-delete 30-day grace, hard delete cascade), 6.2 admin token expiry during impersonation (60-min TTL, pg_cron cleanup every 15min, "Impersonation ended" on next load), 6.3 read-only enforcement (all write buttons disabled with tooltip, server action check pattern, impersonating_admin_id in SessionContext); (7) Multi-tab: 7.1 sign-out one tab broadcasts to all via onAuthStateChange, 7.2 token refresh shared via cookie, 7.3 concurrent Realtime subscriptions (independent, correct, one WebSocket each); (8) Rate limiting: 8.1 login 15-min countdown with client-side timer (retryAfterMs in response, useEffect re-enables button), 8.2 signup 429, 8.3 password reset 3-resend client counter; (9) Email change (deferred to future, read-only with support contact); (10) Deep links: 10.1 bookmarked path preserved in ?next=, 10.2 open redirect prevention (must start with / not //), 10.3 invalid next in email confirmation link (404 acceptable post-auth); (11) Supabase Auth outage: 11.1 at login (catch 5xx/network → SERVICE_UNAVAILABLE → "Temporarily unavailable" + status page link), 11.2 during session refresh in middleware (catch error → allow through with cached session, log to monitoring, do NOT logout user); (12) 46-row summary table (scenario/trigger/user sees/system does) |
| 61 | 2026-03-13 | 7.2 — Micro-interactions, transitions, toasts, confirmation dialogs | 1 run | Created ui/micro-interactions.md: 25 sections — (1) Global animation token system: 8 CSS custom property durations (instant 100ms through collapse 400ms), 3 easings (standard/enter/exit), Tailwind config with keyframe definitions (shimmer/status-pulse/status-fade/spin/progress-drain), prefers-reduced-motion override block (all duration vars → 0ms, animation: none on pulse/skeleton); (2) Page navigation loading bar: NProgress via next-nprogress-bar, 2px Aqua bar, showSpinner: false, when-triggers table (Link/router.push/Back/not-forms/not-Realtime), reduced motion behavior; (3) Button micro-interactions: state table (default/hover/active/focus/loading/disabled with triggers/durations/easings), width stability during loading; LoadingSpinner SVG cross-reference; (4) Sidebar nav: active indicator instant swap rationale, nav item hover 200ms ease, sidebar collapse animation 240px→56px 250ms cubic-bezier with label fade, mobile nav slide-in translateX(-100%)→0 250ms cubic-bezier + backdrop 200ms, nav item tap flash, collapsed sidebar tooltip (300ms delay/150ms enter/100ms exit); (5) Form field transitions: focus ring 150ms (border-color+box-shadow), error state appearance (field error CSS keyframes fieldErrorAppear translateY(-4px)→0), password eye toggle, PasswordStrengthBar (width+color 300ms), Toggle thumb translateX 200ms, Checkbox stroke-dasharray animation, ApiKeyInput validation badge (500ms debounce, cross-fade between Validating/Valid/Invalid, 150ms); (6) StatusIndicator animations: connected pulse ring (2s ease-out CSS keyframes), connecting fade oscillation (1.5s ease-in-out), real-time status change cross-fades (dot color 300ms, label cross-fade 150ms, ring delayed start 300ms); (7) Onboarding checklist: progress bar width 400ms, step row completion (icon swap + strikethrough + row flash 300ms), full completion sequence (T+0 all checks/T+1500ms fade-out opacity+height 400ms/T+1900ms unmount+toast); (8) Dashboard real-time status transitions: cross-fade table, bot→offline (red transition+persistent toast), disconnected→connecting (amber+info toast), connecting→connected (green+success toast at T+300ms); (9) Dropdown menu: open 150ms ease-out (translateY(-4px)→0+fade), close 100ms ease-in (fade only), Radix UI data-state CSS patterns, item hover 100ms; (10) Modal/ConfirmDialog: backdrop 150ms fade, panel scale(0.96)+opacity enter 200ms cubic-bezier, scale exit 150ms ease-in, scroll lock, focus management (cancel first / first focusable), ConfirmDialog loading state; (11) Toast animations cross-reference; (12) Skeleton shimmer cross-reference; (13) Alert banner dismiss: opacity+max-height+padding 200ms ease-in CSS block, transitionend unmount rule; (14) Copy button: state table (default/0–2ms success/2000ms reset), cross-fade pattern, no-size-jump rule; (15) Table row hover: 100ms ease, admin panel right-arrow indicator; (16) Tabs: underline instant swap rationale, pills 150ms ease; (17) Landing page: blob animations (two CSS keyframes blob-drift-1/2 12s and 15s), hero CTA pulse (ctaPulse once 1.5s 400ms delay), scroll reveal (IntersectionObserver threshold 0.1, stagger table per section, CSS reveal-section classes); (18) Service card transitions: OAuth connect flow timeline, API key connect flow timeline, disconnect flow timeline, status badge cross-fade 200ms; (19) Billing transitions: API key save flow, plan toggle cross-fade, Stripe redirect sequence; (20) Settings transitions: workspace save, danger zone enable/disable transition (opacity 200ms), delete workspace flow; (21) Global cross-cutting: session expiry modal, free plan gate modal, network error persistent toast, optimistic updates policy (none — all wait for server with rationale); (22) ConfirmDialog complete inventory (8 rows: disconnect Discord/delete account/delete tenant/disconnect service/downgrade to Free/suspend/unsuspend/impersonate — each with variant/title/description/confirmLabel/cancelLabel/confirmationText); (23) Modal complete inventory (6 rows: connect API key/OAuth connecting/audit log entry/impersonate/update API key/update Discord token); (24) 54-row animation reference table (all animations consolidated: element/keyframes/duration/easing/trigger); (25) prefers-reduced-motion checklist (10 categories, useReducedMotion TypeScript hook implementation) |
| 60 | 2026-03-13 | 7.1b — Error states — audit every page, ensure every fetch/mutation has a specific error UI, fill gaps | 1 run | Created frontend/error-states.md: 13 sections — (1) Overview: error category table (7 rows: page load/card/mutation/field validation/OAuth callback/auth/network/rate limit × when/UI pattern); (2) Global patterns: Pattern 1 full-page error.tsx (visual spec with exact dimensions/colors/typography, required error.tsx file list for 6 route segments, boilerplate TSX with useEffect logging, per-page body text customization table 5 rows), Pattern 2 card-level error (visual spec with AlertTriangle/title/body/text-button, when-to-use card vs full-page rule), Pattern 3 mutation error inline vs toast (rule: inline for modal/form contexts, toast for standalone buttons; inline AlertBanner spec: #FEE2E2 bg/#991B1B text/1px #FCA5A5 border/0 radius/12px 16px padding, role=alert/aria-live=assertive; toast spec: #DC2626 bg/white text/6000ms/always-visible dismiss button); Pattern 4 server field validation error (red border #DC2626/inline 12px error text/aria-describedby/aria-invalid); Pattern 5 network/offline error (toast message/30s timeout rule); Pattern 6 session expiry (401 → toast → 1500ms → redirect to /login?redirect=path); Pattern 7 rate limit (429 → inline or toast, specific copy); (3) Landing page: static, no data errors; (4) Auth pages: login (9 scenarios with exact error messages/UI locations including wrong-password/unconfirmed/suspended/rate-limited/network/generic, AlertBanner visual spec, button re-enable behavior), signup (8 scenarios), reset-password request (5 scenarios + note on email-not-found security), reset-password confirm (7 scenarios + expired-link full-page visual spec); (5) Dashboard: page load/bot status 3 variants (query fail card-level spec, status=error record spec with fallback copy, Realtime disconnect reconnecting banner with amber colors), subscription card error, activity feed error (non-blocking), onboarding checklist error; (6) Integrations: page load/individual service card (5-row default error messages per service), OAuth callback (7 error param values + toast messages/durations, implementation note: clear URL params after show), OAuth initiation (3 scenarios), API key modal (6 scenarios, button re-enable + retain input value + onFocus clear error behavior), disconnect service (3 scenarios); (7) Billing: page load, Stripe checkout (4 scenarios including canceled return), Customer Portal (3 scenarios), Anthropic key (6 error types including rate_limited/provider_error), OpenAI key (4 scenarios), delete API key (2 scenarios with dialog-stays-open rule), partial stale data AlertBanner (warning variant with amber colors); (8) Settings: page load, workspace name (5 scenarios), Discord connection add/update/remove (9+4+3 scenarios), display name (3 scenarios), change password (7 scenarios), delete workspace (4 scenarios incl. Stripe failure strategy), member management invite/remove/change-role (5+2+2 scenarios); (9) Admin: page load, tenant list pagination error, tenant detail page load, suspend/unsuspend/force-plan/impersonate mutations (3+2+2+2 scenarios each); (10) Docs: static, 404 not-found.tsx spec; (11) Global 500 page root error.tsx visual spec; (12) Cross-cutting scenarios: session expiry mid-action (toast → 1500ms → redirect, form data not preserved), concurrent modification (revert optimistic update + toast), bot token revoked while open (Realtime update + AlertBanner above status card), Stripe webhook failure (no auto UI — admin panel handles), Realtime WebSocket failure (60s delay → amber reconnecting badge → auto-dismiss on reconnect), free plan gate errors (upgrade prompts not generic errors, visual spec for upgrade prompt component); (13) Visual specifications summary table for 5 error components (ErrorState/AlertBanner/Toast/card-level/field-level — every property: dimensions/colors/fonts/ARIA); accessibility requirements table (8 rules) |
| 59 | 2026-03-13 | 7.1a — Loading and empty states — audit every page (landing, auth, dashboard, integrations, billing, settings, admin, docs), fill gaps | 1 run | Created frontend/loading-and-empty-states.md: 12 sections — (1) Global patterns: shimmer CSS keyframes (`.skeleton` class with 1.5s ease-in-out gradient animation), client-side mutation spinner pattern (Loader2 icon, 30s timeout rule, aria-busy), EmptyState pattern overview; (2) Landing page: static/CDN, no loading states, no empty states; (3) Auth pages: signup/login/reset-password — per-phase table for every form submission button (before click/in-progress/success/error/timeout states, exact copy for each phase); Google OAuth redirect pattern; (4) Dashboard: loading.tsx full 6-section skeleton (header/onboarding/bot-status/api-keys/integrations/stats/activity with exact dimensions for every block), real-time update transitions (Aqua tint fade, cross-fade stats, slide-in feed items), section-level loading for Reconnect Bot (5 phases) and Load More (4 phases), empty states for new user (7 components each specified), returning user bot disconnected, returning user no recent activity; (5) Integrations: loading.tsx with ServiceCardSkeleton HTML (exact element-by-element spec with dimensions), OAuth connect phases (button→server→redirect→callback→reload→card update), OAuth cancelled/error banners with copy, API key modal loading phases, disconnect modal phases, real-time status update CSS transition spec, empty state only for data fetch error; (6) Billing: loading.tsx 3-section skeleton (header/plan-card/plan-grid/api-keys with exact heights), plan upgrade phases (7 steps: click→checkout session→Stripe redirect→payment→return→plan update), success/cancelled banners with copy, Customer Portal loading phases, Anthropic API key save/delete phases (6 each), OpenAI key phases with different copy, empty states (no API key warning banner, data fetch error); (7) Settings: loading.tsx 5-section skeleton, workspace save phases, Discord add-bot phases (5 steps including async bot Realtime note), Discord remove phases, Account display name update, Account change password phases (wrong-password error), Danger Zone delete workspace phases (5s loading note, cascade delete duration note), empty states (no Discord connection shows form inline, solo team member shows "coming soon" placeholder text); (8) Admin: tenant list loading.tsx (header/stats/search/10-row table skeleton), tenant detail loading.tsx (back+header/4 cards), audit log loading.tsx (header/filters/15 rows), admin action phases (Suspend/Reactivate/Override Plan/Impersonate each with confirm dialog → loading → success/error), empty states (no tenants, search no results, audit log no entries — all with exact icon/title/subtitle/action props); (9) Docs pages: static generation from Next.js generateStaticParams, no loading.tsx needed, table of contents IntersectionObserver no loading state, 404 page spec; (10) Cross-cutting global states: navigation progress bar (2px Aqua, nprogress pattern, sidebar link opacity transition), sign out loading (sidebar spinner, signOut phases), session expiry non-dismissible modal (Lock icon, "Session expired" title, /login?next= redirect), global error boundary error.tsx (AlertTriangle, "Something went wrong", reset() + return-to-dashboard link); (11) Pattern matrix table (12 rows covering every page × loading.tsx/mutations/empty states); (12) Skeleton component TypeScript spec (Skeleton.tsx props interface, aria-hidden, cn() usage); EmptyState component TypeScript spec (EmptyStateProps, role=status, aria-label, icon/title/subtitle/action rendering with href vs onClick discrimination) |

| 58 | 2026-03-13 | 6.5b — Content strategy — blog topics, comparison pages, landing page keyword targeting | 1 run | Created seo-and-growth/content-strategy.md: (1) Landing page keyword targeting — 16 target keywords by intent/priority (primary: discord ai bot/ai bot for discord/discord automation bot/discord productivity bot/ai assistant discord; secondary: claude ai discord/discord github integration/discord project management bot; long-tail and brand terms); keyword groupings by landing section (hero/features/pricing/CTA with LSI keywords); (2) Blog strategy — 4 content pillars (how-to/use-cases/vs-comparisons/concepts) with URL prefix scheme; (3) How-to posts ×8 (set up AI bot/connect GitHub/Toggl time tracking/Google Analytics/manage Linear/deploy Fly/custom persona/security) — each with slug/target keyword/secondary keywords/search intent/word count/full section outline/CTA/internal links; (4) Use case posts ×5 (indie hacker stack/remote standup/freelancer time audit/startup ops/daily driver) — each with slug/target keyword/narrative description/tools highlighted/CTA; (5) Comparison pages ×5 (vs Zapier/vs MEE6/vs DIY bot/vs Slack AI/vs ChatGPT) — each with slug/target keyword/angle/full 10–12 row comparison table/conclusion/CTA/length target/FAQ questions per page; (6) Concept posts ×4 (what is discord AI bot/BYOK explained/discord command center/how Daimon works/50+ things you can ask) with outlines; (7) 6-month content calendar (32 posts, 2/week, week-by-week schedule for months 1–3 + rolling guidance for months 4–6); (8) On-page SEO requirements per post (metadata fields table/Schema.org Article JSON-LD/content requirements table: word counts/H1 rules/internal links/external links/CTA/images/TOC/reading time); (9) Comparison page spec (8 required sections + FAQ questions table per page + FAQPage schema); (10) Blog page technical spec (routes, index page with category filter tabs + pagination + sidebar, post page with TOC + footer, MDX format with frontmatter schema 10 fields, 5 custom MDX components); (11) XML sitemap addition for /blog/* (priorities 0.7–0.9); robots.txt rules for /blog; (12) Social/distribution strategy (Twitter posts/threads, Discord community channels, HN/Reddit/IH, ProductHunt); (13) Internal linking rules table (10 source→destination→anchor patterns); (14) Keyword tracking tiers (7 Tier 1 weekly/8 Tier 2 monthly/brand quarterly); (15) Success metrics (6-month targets: organic sessions/posts/keyword rankings/blog→signup conversion/comparison page session duration/backlinks) |

| 57 | 2026-03-13 | 6.5a — SEO — meta tags for every page, OG image specs, schema.org markup per page | 1 run | Created seo-and-growth/seo-strategy.md: global SEO config (Next.js Metadata API, metadataBase, title template, default description, keywords, robots, OG/twitter defaults, icons, manifest, themeColor, viewport, siteverification, canonical); per-page metadata for all 16 routes (landing/login/signup/reset-password/reset-password/confirm/dashboard/integrations/billing/settings/admin/admin-tenant-detail/docs-root/docs-quickstart/docs-tools/docs-billing/docs-faq) with title+description+keywords+OG images+twitter cards+robots directives; Schema.org JSON-LD for landing (WebSite+Organization+SoftwareApplication with 3 Offers+FAQPage with 5 Q&As), docs root (WebPage+BreadcrumbList), docs/quickstart (HowTo with 6 steps+estimatedCost+totalTime+BreadcrumbList), docs/tools (TechArticle+BreadcrumbList), docs/billing (WebPage+BreadcrumbList), docs/faq (FAQPage with 16 Q&As+BreadcrumbList); OG image inventory (8 files in public/og/): specs for default/landing/auth/docs/quickstart/tools/billing/faq — every image has exact dimensions (1200×630), layer-by-layer design (background color/gradient blobs/logo position+size/text content+font+size+weight+color+position/border bars); @vercel/og route stub (Edge Runtime, dynamic title+description params); robots.txt (Allow for public pages, Disallow for /dashboard /admin /api /auth pages); sitemap.ts (Next.js MetadataRoute.Sitemap for 6 URLs with changeFrequency+priority); site.webmanifest (name/short_name/start_url/display/colors/icons/categories/lang); favicon & icon asset inventory (8 files with sizes+formats+usage); icon design spec (SVG rocket on transparent/white-circle/navy-square depending on context); canonical URL rules table; structured data injection pattern (script tag with dangerouslySetInnerHTML, getSchemaForSlug helper, lib/schemas/structured-data.ts module) |
| 56 | 2026-03-13 | 6.4b — Legal: Privacy Policy — actual complete privacy policy text ready to publish | 1 run | Created legal/privacy-policy.md: 16 sections — introduction (data controller identification, GDPR notice); Section 1 Information We Collect (3 subsections: directly provided: email/password hash/name/tenant config/Discord Guild ID/Bot Token/Anthropic key/OpenAI key/OAuth tokens/Toggl key/Stripe IDs; automatically collected: log data, bot operational metrics, cookies; from third parties: Supabase Auth, Stripe, GitHub/Google/Linear OAuth with exact scopes per provider); Section 2 How We Use (5 subsections with table of purpose/data used/legal basis per GDPR article for every processing activity; explicit "What We Do NOT Do" list: no sell, no message training, no persistent message storage, no ad targeting); Section 3 How We Share (sub-processor table with 8 entries: Supabase/Stripe/Vercel/Fly.io/Anthropic/Sentry/Langfuse/Postmark-Resend with data shared and location; OAuth providers; Discord; business transfers 30-day notice; legal requirements; aggregated data); Section 4 Credentials Special Handling (table of 8 credential types; AES-256-GCM Vault encryption explanation; TLS 1.3; RLS + application access controls; human access policy; transmission table listing exactly what is sent to which third party and when for every credential type; credential deletion timeline per action); Section 5 Discord Bot Message Data (4-step message flow: receipt→in-memory→Anthropic→response; no persistent storage statement; message metadata only logged: timestamp/Guild ID/tool category/latency/success; Guild members as third-party data subjects; user is data controller for Guild members; DPA available at daimon.bot/dpa); Section 6 Cookies (5-row cookie table with name/type/purpose/duration; no advertising or analytics cookies; no consent banner rationale; local storage entries); Section 7 Data Retention (4 tables: account data/tenant config/billing/operational logs with specific retention periods and rationale; backup retention: 7-day PITR + 30-day snapshots); Section 8 Data Security (TLS 1.2+/HSTS; AES-256-GCM Vault; bcrypt passwords; 1h JWT access tokens; RLS; MFA for company staff; SOC 2 sub-processors; incident response process: containment→assessment→72h notification→regulatory notification; security@daimon.bot); Section 9 International Transfers (primary storage US; EEA/UK transfer mechanisms: DPF + SCCs + UK Addendum; Anthropic API transfer note); Section 10 Children's Privacy (under 13 prohibition; 13-17 parental consent); Section 11 Third-Party Links and Services; Section 12 Your Rights and Choices (access/correction/deletion/marketing opt-out/cookies/data portability); Section 13 GDPR Rights (legal bases table; 8 GDPR rights; DPO contact dpo@daimon.bot; Article 27 representative note with forward-loop instruction); Section 14 CCPA/CPRA (categories table with CCPA definitions; 6 rights; opt-out: no sale; sensitive PI (API keys) limited use; authorized agent process); Section 15 Changes; Section 16 Contact (6-row contact table: privacy/GDPR/CCPA/DPO/security/legal emails); Appendix A DPA summary (controller vs processor roles; sub-processors; data subject rights assistance); Appendix B sub-processor change notice process (30-day notice + compliance notification opt-in in Settings) |
| 55 | 2026-03-13 | 6.4a — Legal: Terms of Service — actual complete ToS text ready to publish | 1 run | Created legal/terms-of-service.md: 18 sections of publish-ready ToS text. Key sections: Definitions (20 defined terms including BYOK, Bot Token, Guild ID, Platform Fee, Tenant, Credentials, Third-Party Service); Eligibility (18+ age, legal capacity, Discord account required, Anthropic account required); Account Registration (accuracy obligations, security responsibilities, one-account-per-person rule); Service Description (BYOK model — Anthropic key required/OpenAI optional, both third-party costs entirely user responsibility; Discord Bot Token responsibilities — user creates own bot, must comply with Discord Dev ToS; Integration types: OAuth for GitHub/Google/Linear, API key for Toggl); Subscription Plans (Free $0/Starter $9mo or $79yr/Pro $29mo or $249yr; billing cycles; auto-renewal; failed payment dunning — 3 retries over 14 days then downgrade to Free; disputed charges must contact us within 60 days before chargeback); Uptime SLA (Pro only, 99.9% monthly, credit table: 1 day per full % below 99.9% up to 15 days/month, exclusions: Discord/Anthropic outages/invalid tokens/configuration issues); Cancellation (no refund for unused period; downgrade connection-limit enforcement: oldest connections disabled last); Acceptable Use Policy (22 prohibited uses covering illegal activity, IP infringement, spam, malware, reverse engineering, Discord ToS violations, reselling without consent); Credential Storage (AES-256 via Supabase Vault, used only for operating Service); IP Rights (user retains User Data ownership, company retains Service IP, Feedback license grant); Privacy (no persistent storage of Discord message content, 30-day data export SLA); Termination (mutual termination rights, effect of termination — Bot disconnects/creds deleted/integrations disconnected, survival clause); Disclaimers (AS IS, AI output disclaimer, Third-Party Services, Discord-specific disclaimer, No Professional Advice); Limitation of Liability (consequential damages excluded; liability cap = 12-month fees paid or $100); Indemnification; Governing Law (Delaware); Dispute Resolution (60-day informal resolution → AAA binding arbitration; class action waiver; small claims exception; EU ODR platform mention); Changes to Terms (30 days notice for material changes); Confidentiality; General Provisions (17 clauses including force majeure, US export laws, no third-party beneficiaries); Contact (support/legal/billing/security emails at daimon.bot); Appendix A (Discord Developer Terms compliance summary); Appendix B (Anthropic+OpenAI Terms compliance notice) |

| 54 | 2026-03-13 | 6.3 — Monitoring & alerting — health checks, error tracking, Langfuse integration, alert thresholds | 1 run | Created deployment/monitoring.md: Bot /health endpoint (FastAPI route returning 200+uptime_seconds, what it checks vs doesn't check, Fly.io health check config in fly.toml with 10s interval/5s timeout/30s grace_period + TCP check); Fly.io machine monitoring (restart_policy=always, 6-row Fly.io Prometheus metrics table with alert thresholds, CLI commands for status/logs/machine-list/SSH, built-in email alert behavior); Langfuse observability (9-field trace schema with tenant_id tag for per-tenant filtering, span hierarchy diagram for each request: routing_decision→system_prompt_build→claude_agent_run→generation+tool_calls→discord_response_send, create_langfuse_client() Python code with no-op fallback for missing keys, flush_at=15/flush_interval=60, 5-step Langfuse dashboard setup with saved views table, multi-tenant filtering via tags, admin panel reads from Supabase not Langfuse, Langfuse free tier limits table: 50K traces/month/30-day retention/2 members/3 projects); website monitoring (Vercel built-in analytics, Next.js structured JSON logger with logError+logInfo functions, API route error logging pattern, Vercel log drain config with Supabase Edge Function sink TypeScript code, optional Sentry with sentry.client.config.ts + 4 env vars + v1 decision to defer); Supabase monitoring (5 manual SQL queries: stale connections/error tenants/active count/recent subscriptions/table sizes, automated stale connection alerting Edge Function TypeScript Deno implementation with Discord webhook embed, pg_cron schedule SQL every 5 minutes, ADMIN_DISCORD_WEBHOOK_URL setup, connection pool monitoring SQL); alert thresholds (Section 6.1 critical 4 rows: bot down/crash loop/DB unreachable/Stripe webhook failures; Section 6.2 warning 5 rows: stale connections/high Langfuse error rate/elevated Claude latency/memory pressure/new tenants with no connection; Section 6.3 informational 4 rows: error-status tenants/key validation failures/approaching row limits/long-running transactions); incident runbook (4 scenarios: bot process not responding with step-by-step CLI commands + rollback fly deploy command, all connections stale with likely causes + resolution, Stripe webhooks not processing with signature verification debugging, DB migration failure with manual rollback guidance); daily checklist (4 items), weekly checklist (7 items), monthly checklist (6 items) |
| 53 | 2026-03-13 | 6.2 — CI/CD pipeline — GitHub Actions workflow, test suite, deploy triggers, preview deployments | 1 run | Created deployment/ci-cd.md: 3 GitHub Actions workflows (daimon-web-ci.yml / daimon-bot-deploy.yml / daimon-db-migrate.yml); web CI: 7 jobs (install→typecheck+lint+unit-tests in parallel→build→deploy-vercel→e2e-tests); full YAML for all jobs including pnpm/node setup, cache restore, Vercel CLI deploy with prod/preview branching, PR comment with preview URL; bot deploy: ruff lint + pyright type check + pytest + flyctl deploy --remote-only + health verification; DB migrations: Supabase CLI link + validate naming convention + db push (dry-run + apply modes) + failure issue creation; 10-row GitHub Secrets table (VERCEL_TOKEN/VERCEL_ORG_ID/VERCEL_PROJECT_ID/E2E_TEST_USER_EMAIL/E2E_TEST_USER_PASSWORD/FLY_API_TOKEN/SUPABASE_ACCESS_TOKEN/SUPABASE_PROJECT_REF/GH_PAT); Vitest unit test suite: vitest.config.ts full TypeScript (jsdom env, v8 coverage, 70/65% thresholds, path aliases), test setup.ts (jest-dom, next/navigation mock, Supabase client mock, console.error filter), 15-row test file inventory with exact scenarios per file; E2E tests (Playwright): playwright.config.ts full TypeScript (serial, 1 worker, JUnit reporter, trace on retry, chromium only), 9 E2E test files with all scenarios per file (signup/login/reset-password/onboarding/dashboard-status/discord/api-keys/billing-upgrade/settings/sidebar), auth fixture (authenticatedPage for shared session), test npm scripts; complete package.json scripts block (12 scripts: dev/build/start/lint/lint:fix/format/format:check/typecheck/test/test:unit variants/test:e2e variants/ci:all); 8-row branch protection rules table; preview deployment: how previews work (URL format, PR comment, E2E runs against preview), env var overrides table (Stripe test vs live keys only, Supabase same for both), cleanup policy; 3-section release process (normal deploy/hotfix/bot deploy/DB migration flows); workflow dependency diagram (ASCII tree showing parallel vs sequential job execution) |

| 52 | 2026-03-13 | 6.1 — Vercel deployment — config, env vars, build commands, domains | 1 run | Created deployment/environment.md: website env vars (4 Supabase, 8 Stripe, 3 app, 2 GitHub, 2 Google, 2 Linear, 2 admin), complete .env.local template, Vercel environment scoping table (17 vars × 3 envs), bot env vars (2 Supabase, 2 Fly.io, 2 Onyx, 3 Langfuse, 5 optional config), fly.toml [env] block + fly secrets set command, shared variables cross-reference, lib/env.ts Zod validation schema (TypeScript), 5 secret rotation procedures (service role/webhook/OAuth/JWT). Created deployment/infrastructure.md: Vercel config (framework preset, Root Directory, Node.js version, build command, install command, Turborepo alternative), next.config.ts full TypeScript (security headers X-Content-Type-Options/X-Frame-Options/XSS-Protection/Referrer-Policy/Permissions-Policy/HSTS, image domains, redirects), Vercel project settings (region, timeout, ignored build step script), middleware.ts full TypeScript (session refresh, /dashboard auth guard, /admin admin-claim guard, matcher pattern), Vercel tier recommendation (Pro, reasons), Fly.io fly.toml (auto_stop=off, min_machines=1, shared-cpu-2x 512MB, health checks), Fly.io machine config table, Dockerfile, deployment commands (first/subsequent/rolling-restart/logs/scale-up), health server endpoints 3 routes, Supabase setup steps (9 one-time steps: extensions/Vault/Realtime ALTER PUBLICATION/Auth settings/email templates/migrations/pg_cron/SMTP), Supabase local dev (CLI commands, local service URLs, local env vars), architecture diagram (4-layer ASCII: browser → Vercel → Supabase + Stripe + OAuth → Fly.io → external services), cost estimates (Vercel Pro $20 + Supabase Pro $25 + Fly.io $5–15 = ~$50–60/mo), Edge Functions deployment (3 functions, deploy commands). Created deployment/domains.md: domain table (daimon.ai/www/wildcard/reserved), DNS records table (A/AAAA/CNAME apex + www + wildcard), Cloudflare notes (proxy mode/SSL Full Strict), Vercel domain setup steps, SSL properties (Let's Encrypt auto, TLS 1.2+, HSTS, Cloudflare compatibility note), routing table (28 URL patterns → file paths), redirect rules (7 redirects), CORS policy (same-origin only, Edge Function CORS headers with preview-compatible origin), cookie table (5 cookies with scope/SameSite/Secure/TTL), Supabase Auth redirect URL configuration, preview deployment OAuth handling (3 approaches, getAppUrl() helper), custom email domain DNS records (SPF/DKIM/DMARC) |

| 51 | 2026-03-13 | 5.7 — Supabase Realtime contract — channel config, payload shapes, reconnection | 1 run | Created integrations/supabase-realtime.md: architecture diagram (website→Postgres→bot via Realtime, no direct API); publication setup SQL (ALTER PUBLICATION for 4 tables: discord_connections/tenant_api_keys/tenant_service_connections/tenants); verification query; RLS+Realtime interaction (anon key with JWT enforces RLS; service role bypasses for bot); supabase/config.toml realtime section; TypeScript types for all 4 table row shapes (DiscordConnectionRow/TenantApiKeyRow/TenantServiceConnectionRow/TenantRow); note: bot_token always null in CDC, Vault UUIDs returned not plaintext; website channel subscriptions: useDiscordConnectionStatus hook (initial fetch + UPDATE filter per connectionId, refetch on SUBSCRIBED, status→UI badge color table 6 rows); useTenantStatus hook (UPDATE filter per tenantId, spread merge on update); useServiceConnections hook (INSERT/UPDATE/DELETE handlers, safe column selection without encrypted fields); channel names+filter table (3 website channels, 4 bot channels); 4 complete payload shapes with concrete JSON examples (connect success, token error, api key insert, tenant suspend); reconnection: JS client auto-reconnects with exponential backoff (1s→30s), no event replay — always call fetchInitial() in SUBSCRIBED callback; 5-row subscription status table (SUBSCRIBED/TIMED_OUT/CLOSED/CHANNEL_ERROR/CONNECTING); bot reconnection via TenantConnectionManager.reconnect() + full state reload from DB; Realtime outage fallback (stale banner, manual refresh); 5 security rules (never subscribe to api keys, never expose vault UUIDs in browser, never service role in browser, never expose bot_token, RLS enforced server-side); RLS policy SQL for 3 tables; channel isolation (name collision harmless due to RLS); 4 env vars table; integration test pattern (Vitest); 5 known limitations: no event replay, concurrency limits (200 Free/500 Pro/3k Team → ~250 concurrent dashboard users on Pro), filter syntax constraints (eq only, no IN/OR), Vault column exclusions table (5 columns), multi-tab behavior (independent state, converges on events) |

| 50 | 2026-03-13 | 5.6 — API key services (Toggl) — validation endpoints, key format, error handling | 1 run | Created integrations/api-key-services.md: Toggl as sole API key service at launch (1 service, `auth_type='api_key'`); key format (exactly 32 chars, `[a-f0-9]` hex, example, validation regex `/^[a-z0-9]{32}$/` server-side, find at `track.toggl.com/profile`); validation endpoint `GET https://api.track.toggl.com/api/v9/me` with Basic Auth (`api_key:api_token` base64); success response fields used (id/email/fullname/default_workspace_id); 5-row HTTP error table (200/403/401/429/5xx+network); workspace metadata lookup via `GET /me/all` (non-fatal if fails, finds default workspace by ID, extracts workspace name/org_id/org_name/role); complete `TogglServiceConnectionMetadata` TypeScript interface (8 fields); UPSERT SQL for `tenant_service_connections` with ON CONFLICT behavior; Vault secret lifecycle (create before INSERT, delete old after UPSERT, naming convention `toggl_{tenant_id}`); vault_delete_secret idempotency on reconnect; `upsert-service-connection` Edge Function logic (full TypeScript Deno implementation with access+refresh Vault handling, rollback on upsert failure); complete `POST /api/integrations/api-key/validate` route (9 error paths: body parse/service validation/key presence/format/membership/role/Toggl 401/Toggl 429/Toggl 5xx+timeout + AbortSignal.timeout(10000)); bot key injection into `UserContext.credentials[CredentialPlatform.TOGGL]` as tenant-level fallback when Discord user has no personal Toggl cred; ToolContext.toggl_workspace_id/toggl_organization_id override; 7 error scenarios (wrong token/externally revoked/Toggl down/wrong format/member role/concurrent tabs last-write-wins/bot marks error); bot-side error marking SQL (status=error); Toggl rate limit analysis + mitigation; disconnect flow (DELETE route → `disconnect-service` Edge Function with vault cleanup); reconnection UPSERT replaces revoked row; complete error message reference table (12 rows); future API key service extension guide (5-step template per service) |

| 47–49 | 2026-03-13 | 5.3/5.4/5.5 — GitHub OAuth, Google OAuth, Linear OAuth — scopes, redirect URI, token exchange, refresh, revocation | 1 run | Created integrations/oauth-services.md: GitHub OAuth App setup (OAuth App vs GitHub App rationale, GitHub Dev Portal config, `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` env vars); GitHub scopes (`repo read:org gist` — why each; NOT `write:org`/`workflow`/`delete_repo`); auth URL construction (TypeScript, `allow_signup=true`); token exchange (POST `github.com/login/oauth/access_token`, JSON response, no refresh token, no expiry, error table 5 rows); user identity fetch (`GET /user`, metadata fields `github_login`/`github_user_id`/`github_name`/`github_avatar_url`/`github_email`, non-blocking on failure); token storage (Vault naming `tenant_service_connections:{id}:github:access`, upsert pattern, old secret deletion on re-auth); bot usage (`GITHUB_TOKEN` env var injection into `gh` CLI, scope-per-command table 5 rows); token revocation detection (401 → `status=error`, ToolError message); disconnect flow (vault delete, no revocation call, reasoning); scope validation on callback (missing-scope warning stored in metadata, partial connection allowed); re-authorization flow; 3 API route signatures (start/callback/delete with all error codes); Google OAuth Web App setup (GCP Console steps: enable Analytics Data API, OAuth consent screen fields, test users, authorized redirect URIs for prod+dev); Google scopes (`openid email profile analytics.readonly` — rationale for each, NOT calendar/gmail/full-analytics); auth URL with `access_type=offline` + `prompt=consent` (why both are required, what happens without them); token exchange (`oauth2.googleapis.com/token`, response fields including `refresh_token`/`expires_in`/`id_token`); user identity via id_token JWT decode (faster than userinfo endpoint, claims used: `sub`/`email`/`name`/`picture`); token storage (two Vault secrets: access + refresh, expiry timestamp calculation); token refresh cron (full TypeScript `app/api/cron/refresh-tokens/route.ts`, `CRON_SECRET` verification, SQL for expiring tokens, `prompt=consent` effect, `invalid_grant` → `status=expired`, vault.update_secret pattern); 401 in bot marks `status=expired` (bot cannot refresh — no client_secret); disconnect deletes both Vault secrets; Linear OAuth App setup (linear.app/settings/api, fields, callback URL); Linear scopes (`read write issues:create comments:create` — why explicit action scopes needed); auth URL (no `access_type`/`prompt` — tokens don't expire); token exchange (`api.linear.app/oauth/token`, no `refresh_token`, no `expires_in`); user identity via GraphQL `viewer` + `teams` query; team selection logic (0 teams → block, 1 team → auto-select, 2+ teams → `linear_team_select=true` redirect + team picker UI); Linear token storage (no refresh vault ID, no expiry); bot usage (decrypted token → `TenantConfig.linear_api_key` + `TenantConfig.linear_team_id` → `ToolContext`); `AUTHENTICATION_ERROR` → `status=error`; disconnect flow; shared infrastructure: OAuth start route full TypeScript (service validation, session check, tenant membership owner/admin gate, CSRF state + 3 HttpOnly cookies, `crypto.randomUUID()`, 10-min TTL); OAuth callback route full TypeScript (state CSRF verify, cookie clear, provider error pass-through, code exchange, identity fetch, old Vault secret deletion, two-secret storage, upsert, Linear multi-team redirect); token refresh cron (Google only); 9-row OAuth error code table; 6-row env vars table; `lib/integrations/oauth.ts` module contract (OAuthTokens interface, OAuthError class, 4 function signatures) |

| 46 | 2026-03-13 | 5.2 — Discord token handling — format validation, storage, error scenarios, what happens on invalid token | 1 run | Created integrations/discord.md: BYOBT model overview; token format (3-part dot-separated structure, base64 prefix + 6-char timestamp + 27-char HMAC, 59–77 chars, "Bot " prefix stripping); guild ID format (17–20 digit snowflake regex); client-side + server-side format validation (TypeScript regex DISCORD_TOKEN_REGEX); /api/discord/validate-token route (full TypeScript implementation — auth check, sanitization, format checks, rate limit, Discord GET /api/v10/users/@me call with 8-second timeout, 401/429/5xx handling, bot vs user token check, bot_user_id + bot_username extraction, display name construction for legacy vs new-style usernames, 8 response shapes); Vault storage flow (5-step: validate → create_secret → INSERT/UPSERT → delete old secret → Realtime event); Vault secret naming convention; token retrieval by bot (SQL + Python); token masking rules (no hint column — display via bot_username only, never shown after save); 9 connection lifecycle scenarios (initial connect, token update with guild_id lock explanation, manual disconnect flow with 5s timeout, token revoked on Discord side/close code 4004 behavior, transient reconnect backoff table 5 rows + close code table 8 retriable + 6 non-retriable, disallowed intents/4014 with full Developer Portal instructions, bot not in guild + invite URL construction, account suspended via admin action, plan downgrade excess connection suspension); complete UI error messages (client validation 4 rows, server API errors 9 rows, connection status error cards 8 rows, toast notifications 4 rows); 5 API routes (/validate-token, POST /discord-connections, PATCH /:id, DELETE /:id, POST /:id/reconnect — each with full request/response/error tables); bot-side token handling (startup SQL + TenantConfig dict structure, token isolation guarantees, hot-reload logic Python pseudocode, heartbeat write loop); security properties table 11 rows; cross-references table 10 rows |
| 45 | 2026-03-13 | 5.1 — Stripe integration — products, prices, Checkout Sessions, webhooks, Customer Portal, subscription lifecycle | 1 run | Created integrations/stripe.md: Stripe env vars (6 vars), Dashboard setup (2 products, 4 prices with exact amounts, webhook endpoint registration with 10 events, Customer Portal config); SDK setup (lib/stripe.ts); plan→price mapping helper (lib/stripe-prices.ts with getPriceId + getPlanFromPriceId); Checkout Session route (POST /api/billing/checkout — full TypeScript with auth/role check/plan collision prevention/sessionParams/customer reuse/error responses); Customer Portal route (POST /api/billing/portal — full TypeScript with auth/role check/stripe_customer_id check); webhook handler (POST /api/stripe/webhook — runtime=nodejs/signature verification/event dispatch/7 handlers: handleCheckoutCompleted/handleSubscriptionCreated/upsertSubscription/handleSubscriptionUpdated/handleSubscriptionDeleted/handleInvoicePaymentSucceeded/handleInvoicePaymentFailed/handleCustomerDeleted); subscription lifecycle state machine (6 states: FREE/TRIALING/ACTIVE/CANCELING/PAST_DUE/UNPAID; complete transition table 14 rows); idempotency strategy; test setup (CLI + 4 test cards); customer→tenant mapping rules (5 rules); security considerations (5 rules); admin force-plan route. Created premium/tiers.md: tier definitions (Free/Starter/Pro with DB states, feature lists, target users, cost); connection limit enforcement (SQL + TypeScript API route code); plan downgrade behavior (excess connections, bot enforcement Python logic); upgrade flows (Free→paid via Checkout; lateral upgrade via Portal); trial logic (disabled at launch). Created premium/pricing.md: price points table; Stripe Price IDs env vars; billing cycle toggle spec (pill toggle colors); currency (USD); proration (Stripe default); promotion codes; invoicing; tax; dunning config. Created premium/features-by-tier.md: complete feature matrix (21 rows); gating rules (Rule 1: connection limit only; Rule 2: no other gates); bot enforcement logic (Python nameddict MAX_CONNECTIONS); support tier SLAs; uptime SLA spec (99.9% definition, measurement, credit table, exclusions, remedy process). Created api/webhooks.md: route config (runtime=nodejs, dynamic=force-dynamic); middleware exclusion patterns; request headers; response codes; retry behavior; idempotency (v1: no deduplication table, rely on SQL); logging patterns; admin client setup |

| 43 | 2026-03-13 | 4.11 — Validation rules — every form field validation with specific error messages | 1 run | Created frontend/validation-rules.md; 8 Zod schema names (LoginSchema, SignupSchema, ResetPasswordRequestSchema, ResetPasswordConfirmSchema, AnthropicApiKeySchema, OpenAIApiKeySchema, WorkspaceNameSchema, DiscordConnectionSchema, DisplayNameSchema, ChangePasswordSchema, DeleteWorkspaceConfirmSchema); every field constraint documented with exact error messages; password strength indicator 4-segment bar (Weak red/Fair amber/Good Aqua/Strong green); client-side Zod snippets for Discord token regex pattern, API key prefix checks, whitespace detection; server-side validation flow for Anthropic (test call to /v1/messages), OpenAI (GET /v1/models), Toggl (GET /api/v9/me), Discord (GET /api/v10/users/@me); complete server error mapping tables per form; OAuth callback error handling for GitHub/Google/Linear (query param→alert banner); 7 general patterns section (required field indicator, email format pattern, password complexity, visibility toggle, submit-during-loading, error persistence, accessible error association via aria-describedby + role=alert); shared sub-schemas (emailSchema, newPasswordSchema, apiKeyBaseSchema); SERVER_ERROR_MESSAGES record with 14 error codes; 3 display components (FieldError/FormErrorBanner/CharacterCounter) with full TypeScript interfaces and styling specs; discrepancy resolution table (workspace name max 100→64 chars, full name error messages) |
| 42 | 2026-03-13 | 4.10 — Complete copy inventory — every user-facing string across all pages | 1 run | Created frontend/copy.md; 17 sections covering every user-facing string in the application: (1) Global/Shared — brand name/tagline, sidebar nav labels, public nav labels, mobile nav ARIA, 30 common button labels, 20+ common labels (plan/role/status), footer links for auth and landing; (2) Landing Page — hero headline/subheadline/CTAs, how-it-works 3-step text, 8 feature card titles+descriptions, integrations strip items, 3-tier pricing with all plan features, 7 FAQ Q&A pairs, final CTA banner, footer copyright; (3) Auth Pages — login (card title/subtitle, 2 field labels+placeholders, forgot-password text, footer prompt+link, 6 auth error messages, 3 field validation errors), signup (card title/subtitle, 4 field labels+placeholders+helpText, terms text, post-signup success state with resend, 12 validation errors), reset-password (form + success state + 4 errors), reset-confirm (3 states + errors), email confirmation page; (4) Dashboard Home — page title, onboarding checklist (4 step titles+descriptions+CTAs+progress label+completion toast), bot status card (6 states: connected/connecting/error/disconnected/null/suspended with all copy per state including stale heartbeat), API keys card (2 providers × 5 status variants), service integrations summary, 3 quick stats labels, activity feed (10 event types, empty state), page error state; (5) Integrations Page — page header, 4 service cards with all states (active/expired/error for each + disconnect confirm dialogs), Toggl API key modal (all fields/buttons/errors), role restriction notice; (6) Billing Page — page header, subscription section (8 billing states, Stripe status banners, upgrade/portal buttons), plan comparison table (6 features × 3 plans), API key section (2 providers × all states), API key input modal (Anthropic+OpenAI versions, all error messages), delete confirmation dialogs, Stripe checkout banners, role restriction; (7) Settings Page — page header, workspace section (all field labels+validation errors+success/error toasts), Discord connection section (no-connection state, add/update modal with all fields/errors/success, remove confirmation), team members section, account section (display name + change password with all error states), danger zone (delete workspace with confirmation dialog including typed-name requirement); (8) Admin Panel — page header, tenant list (search, filters, columns, actions, pagination), tenant detail (sections, quick action buttons), impersonation banner, suspend/unsuspend confirmation dialogs; (9) Toast Notifications — 19 success toasts, 8 error toasts, 3 info toasts with triggers for each; (10) Confirmation Dialogs — 11 dialogs with exact title/body/cancel/confirm label copy; (11) Empty States — 5 empty state variants; (12) Error States — full-page, page-specific (dashboard/integrations/billing/settings), OAuth callback errors (4 types), 404 page, 500 page; (13) Loading/Skeleton — 8 generic loading strings, 6 skeleton ARIA labels, 8 inline loading states; (14) Tooltips — 16 tooltip strings; (15) Form Validation Errors — 9 forms with complete field-level rules and error messages (login, signup, reset-password, reset-confirm, workspace name, discord connection, Anthropic key, OpenAI key, Toggl key, account display name, change password, delete workspace confirmation); (16) Page Titles — 20 routes with exact HTML title strings; (17) Meta Descriptions — 8 page meta descriptions all ≤160 chars |
| 40 | 2026-03-13 | 4.9d — Component library: Data display components — Badge, StatusIndicator, Table, Pagination, StatCard, ActivityFeed, CopyToClipboard | 1 run | Appended Section 4 to frontend/component-library.md; updated TOC to link section 4; 7 components: Badge (components/ui/Badge.tsx — BadgeVariant union type 22 variants: plan-free/starter/pro, status-pending/configured/active/suspended, key-valid/invalid/unconfigured/validating, connection-connected/connecting/error/disconnected, neutral/info/success/warning/danger; base styles: font Inter weight 600, border-radius 0px always sharp, display inline-flex; 2 sizes sm(11px/2px 8px/0.05em) and md(13px/3px 10px/0.03em); plan badge table 3 rows with background/text/border exact values; status badge table 4 rows with semantic background colors (yellow-100/blue-100/green-12%/red-12%); key status badge table 4 rows including validating animated ellipsis CSS keyframes; connection status badge table 4 rows; generic semantic table 5 rows; implementation TSX snippet; where-used table 5 rows; accessibility: aria-label required, no hover states, wrap in button if clickable); StatusIndicator (components/ui/StatusIndicator.tsx — IndicatorStatus type 5 values; props: status/label/dotSize default 12/labelSize default 14/showLabel default true/className; dot specs table: connected=22C55E+pulse ring animation/connecting=F59E0B+fade animation/error=EF4444+static/disconnected=rgba navy 25% static/suspended=rgba navy 40% static; pulse keyframe CSS: 0%=0 0 0 0 rgba(34,197,94,0.50) / 70%=0 0 0 8px 0 / 100%=0 0 0 0; fade keyframe CSS: 0%,100%=opacity 0.4 / 50%=opacity 1; default labels table 5 rows; layout: flex items-center gap-10px; label typography: Archivo Semi-Expanded 28px 500 Navy in Bot Status Card context; implementation TSX snippet with role=img aria-label on dot; where-used table 3 rows: Bot Status Card dot 12px label 28px / admin tenant detail 10px 14px / DashboardTopbar compact 8px no label); Table (components/ui/Table.tsx — generics T; Column<T> interface: key/header/width/align/sortable/render; TableProps<T>: columns/rows/keyExtractor/onRowClick/sortKey/sortDirection/onSort/emptyState/loading/rowClassName/stickyHeader/caption/className; container: overflow-x-auto + 1px #E5E7EB border; thead: #F9FAFB bg + sticky when stickyHeader; th: 12px Inter 500 #374151 uppercase 0.05em; sort indicator: ChevronUpDownIcon gray-400 / ChevronUp Navy / ChevronDown Navy, gap 4px inline; tr hover: #F9FAFB; clickable rows: role=button tabIndex=0 focus-visible Aqua outline; td: 14px Inter Navy 12px 16px padding border-b #F3F4F6 last:no-border; empty state: full-width td 48px padding + default DefaultTableEmptyState; loading: 5 skeleton rows with Skeleton 80% 14px per cell; implementation TSX snippet full; where-used table 3 rows: admin tenant list/audit log/tenant detail connections); Pagination (components/ui/Pagination.tsx — props: currentPage/totalPages/totalItems/itemsPerPage/onPageChange/className; layout: flex justify-between align-center; item range text: 14px #6B7280 "Showing {start}–{end} of {N} {entity}"; page display algorithm: current±2 + always first+last + ellipsis; button styles table 5 states: default Transparent #374151 1px E5E7EB / current Navy White / hover #F9FAFB / disabled Transparent #D1D5DB / ellipsis not-a-button; button dims: 36px height/36px min-width for numbers/auto for Prev-Next/0px radius; Prev/Next text label hides to ← → on mobile <480px; mobile fallback: hide range text + show Page N of M center; URL param pattern: router.push with params.set('page', N); accessibility: nav aria-label/aria-current=page/aria-disabled/Prev+Next specific aria-labels/page number aria-labels; where-used 2 rows: admin tenants 50/page + audit log 100/page); StatCard (components/ui/StatCard.tsx — props: label/value/subValue/icon/accentStripe default true/variant default/compact/loading/className; default variant: White bg/1.5px rgba border/20px 24px padding/3px rgba(180,231,221,0.6) left stripe; compact variant: White bg/1px #E5E7EB border/16px 20px padding/no stripe; icon 20px rgba(12,31,64,0.45); label: 12px Inter 500 uppercase 0.06em Navy 55% (default) / 12px 400 #6B7280 (compact); value: Archivo Expanded wdth:125 32px 700 (default) / Archivo Semi-Expanded wdth:112.5 28px 600 (compact); subValue: 12px rgba(12,31,64,0.45) (default) / 11px #9CA3AF (compact); loading skeleton: 4 skeleton elements per variant; implementation TSX snippet with fontVariationSettings; where-used table 2 rows: dashboard 3 default stats + admin 4 compact stats; Peach Orange restriction note re: data-viz only); ActivityFeed (components/ui/ActivityFeed.tsx — ActivityEventType union 10 values; ActivityEvent interface: id/type/timestamp ISO 8601/description/metadata(serviceName/planName/errorMessage); props: events/maxItems default 10/showHeader default true/loading/className; container: White/1.5px rgba border/24px 28px; header: ClockIcon 16px + "Recent Activity" Inter 14px 600 + border-b; event row: flex items-start gap-3 py-3 divide-y rgba 4%; EventIcon: 32px×32px flex center per-type bg; event type config table 10 rows: bot_connected ZapIcon green/bot_disconnected ZapOffIcon red/bot_error AlertTriangle red/api_key_added Key Aqua/api_key_invalid KeyRound red/service_connected Plug Aqua/service_expired PlugZap amber/plan_upgraded CreditCard Aqua/plan_downgraded CreditCard amber/account_created User periwinkle; EventText: flex-1 14px Navy; timestamp: 12px Navy 45% nowrap + title ISO + relative format table 5 age buckets: <60s "just now"/1-59m "Nm ago"/1-23h "Nh ago"/1-6d "Nd ago"/≥7d "Mar D"; client-side refresh every 60s; empty state: ActivityIcon 40px + "No activity yet" + body; loading: 5 skeleton rows; Realtime note: events prepend+trim to maxItems; where-used 1 row: dashboard Section 6 max 10); CopyToClipboard (components/ui/CopyToClipboard.tsx — props: value/displayValue/masked default false/size sm|md default md/variant inline|block/label/className; block variant: flex container #F9FAFB bg 1px #E5E7EB border 8px 12px padding; value display: flex-1 font-mono 14px truncate; masked=true shows 20 bullets, clipboard always copies real value; Show/Hide toggle: EyeIcon→EyeOffIcon 16px Navy 45% hover 75% 28/32px; copy button in block: CopyIcon 14px / CheckIcon success rgba(34,197,94,0.12) / XIcon error rgba(239,68,68,0.12) — 28/32px; inline variant: icon-only CopyIcon 14/16px 24/28px with 4px padding; clipboard API implementation with navigator.clipboard.writeText + fallback execCommand('copy') + setTimeout 2000ms reset; toast notification "Copied to clipboard" fires on success; accessibility: copy aria-label cycles "Copy to clipboard"/"Copied!"/"Failed to copy" + aria-live=polite; Show/Hide aria-label + aria-pressed; where-used table 4 rows: billing Anthropic key/billing OpenAI key/admin bot token (all block masked=true)/settings guild ID inline/docs inline) |
| 39 | 2026-03-13 | 4.9c — Component library: Feedback components — AlertBanner, Toast, ConfirmDialog, Modal, EmptyState, ErrorState, SkeletonLoader | 1 run | Appended Section 3 to frontend/component-library.md; Semantic feedback color table (error #FEF2F2/#DC2626/#7F1D1D, warning #FFFBEB/#D97706/#78350F, success #F0FDF4/#16A34A/#14532D, info rgba(180,231,221,0.20)/#B4E7DD/#0C1F40); AlertBanner (components/ui/AlertBanner.tsx — props: variant/title/description/dismissible/onDismiss/action/icon/className; 4 variant color tables with bg+border+icon+text per variant; 3px left border accent distinguishing inline banners from toasts; dismiss animation: opacity 0 + max-height 0 + padding 0 over 200ms; action as inline text link not button; complete usage inventory 5 rows: dashboard bot offline/email unverified, settings post-disconnect, billing no-subscription, auth server error); Toast (components/ui/Toast.tsx + ToastProvider.tsx + lib/toast.ts — ToastItem props: id/variant/title/description/duration default 4000/action; imperative API: toast.success/error/warning/info + full object form; viewport: fixed bottom-24px right-24px z-100 w-360px flex column-reverse gap-8px; individual toast: white bg + 1px navy border + 3px left variant border + 0 radius + shadow 0 4px 16px 10% navy; enter animation: translateX(100%+24px)→0 + opacity 0→1, 250ms cubic-bezier(0.22,1,0.36,1); exit: slide-right + fade, 200ms ease-in; progress bar: 2px variant color 30% opacity, width 100%→0 over duration ms, pauses on hover; hover pause: mouseover pauses timer, mouseleave resumes from remaining time; stacking: newest at bottom, maxToasts oldest force-dismissed; mobile: fixed left-16px right-16px bottom-16px full-width; complete trigger inventory 22 rows covering all success/error/warning/info toasts in the app); ConfirmDialog (components/ui/ConfirmDialog.tsx — props: open/onOpenChange/variant/title/description/confirmLabel/cancelLabel/onConfirm/loading/confirmationText/confirmationPlaceholder; 3 variants: danger=Trash2+DC2626+red confirm, warning=AlertTriangle+D97706+amber confirm, default=HelpCircle+Navy+primary Aqua confirm; overlay: fixed inset-0 navy 55% + blur 4px z-50; panel: 440px max-calc(100vw-32px) fixed centered z-51 shadow 0 20px 60px 18% navy, scale(0.96)→scale(1) + opacity 0→1 200ms cubic-bezier; structure: Header(IconContainer 36px×36px 8% variant bg + Title Archivo Semi-Expanded 18px) + Body(Description Inter 14px 65% navy + ConfirmationInput?) + Footer(Cancel secondary + Confirm variant-dependent); ConfirmationInput: typed confirmation string, case-sensitive exact match, confirm disabled until match, code element styling navy 6% bg mono font; loading: Loader2 spin replaces label, opacity 0.75, pointer-events none; keyboard: Escape closes (if not loading), focus trap, initial focus on CancelButton (safe default); scroll lock on open; 7 ARIA attributes; complete usage inventory 5 rows: disconnect Discord/delete account(confirmationText "delete my account")/admin delete tenant(confirmationText=slug)/disconnect integration/downgrade to Free); Modal (components/ui/Modal.tsx — props: open/onOpenChange/title/description/size/children/footer/showClose/closeOnBackdrop/closeOnEscape/loading; 3 sizes: sm 380px, md 520px, lg 720px; identical overlay to ConfirmDialog; panel: max-height calc(100vh-80px) flex column, same shadow+animation; structure: ModalHeader(sticky, TitleGroup + CloseButton 28px×28px X icon) + ModalBody(flex-1 overflow-y-auto, children) + ModalFooter?(sticky); loading overlay: absolute inset-0 white 75% + Loader2 24px Navy spinning; CloseButton: 28px×28px X 16px rgba(12,31,64,0.45)→0.80 hover; body scroll: scrolls internally when content overflows; initial focus: first focusable in body, fallback CloseButton; complete usage inventory 5 rows: add API key service sm/OAuth connecting sm/audit log entry md/impersonate tenant sm/payment method md); EmptyState (components/ui/EmptyState.tsx — props: icon/title/description/action{label,onClick,href,variant}/size/className; 3 sizes: sm(48px icon container/20px icon/14px Inter 600 title/24px padding), md(64px/28px/16px/40px), lg(80px/36px/Archivo Semi-Expanded 20px/60px); layout: flex column center max-width 360px margin auto; icon container: Aqua 20% bg + Aqua 50% border + 0 radius + Navy 45% icon; default icon InboxIcon; action uses brand Button component; complete usage inventory 8 rows: integrations no services(PlugZap)/admin no tenants(Users)/admin no audit log(FileText)/dashboard no activity(Activity)/dashboard bot never connected(Bot+href settings)/tool reference no search(Search+onClick clear)/integration search empty(Search+onClick)/billing no invoices(Receipt)); ErrorState (components/ui/ErrorState.tsx — props: title default "Something went wrong"/description/onRetry/error/size/className; identical layout to EmptyState but error colors; icon: AlertCircle always not overridable; icon container: DC2626 6% bg + DC2626 20% border; retry button: secondary variant compact 38px + RotateCw 14px leading icon; dev-mode error detail: pre block 11px mono DC2626 75% max-height 120px overflow-y auto, completely suppressed in production NODE_ENV!==development; complete usage inventory 8 rows: dashboard/integrations/admin-tenant-list/admin-audit-log/billing/settings/bot-status(silent retry 3 attempts before showing)/docs); SkeletonLoader (components/ui/SkeletonLoader.tsx base + components/ui/skeletons/ composites — base props: width/height/className/style; shimmer: linear-gradient 90deg navy 6%→10%→6% background-size 200% animated keyframes shimmer 200%→-200% position over 1.5s ease-in-out infinite; 0 border-radius; 5 composite skeletons: DashboardSkeleton(4-col status cards grid + TwoColumn 7fr/5fr activity feed 5 rows + checklist 4 rows), IntegrationsSkeleton(page header + 9-card 3-col service grid), BillingSkeleton(plan card + 2 API key rows), SettingsSkeleton(3 sections × 2 field rows each), AdminTenantListSkeleton(header + 5-col table header + 8 data rows)); updated TOC entry for Section 3; summary table 7 components |
| 38 | 2026-03-13 | 4.9b — Component library: Form components — FormInput, PasswordInput, Select, Toggle, Checkbox, ApiKeyInput, SearchInput | 1 run | Appended Section 2 to frontend/component-library.md; shared form tokens table (44px height, border-color state table for default/hover/focus/error/disabled, focus ring = 1.5px Navy border + 0 0 0 3px rgba(180,231,221,0.30) Aqua shadow, error = #DC2626 red, 0 border-radius throughout); FormInput (components/ui/FormInput.tsx — full props interface: id/label/type/value/onChange/placeholder/hint/error/disabled/required/autoComplete/autoFocus/maxLength/readOnly/className/inputRef; markup with .form-field wrapper + label + .input-wrapper + optional AlertCircleIcon + aria-describedby/aria-invalid/role=alert wiring; 7-row state table default/hover/focus/error/error+focus/disabled/read-only with exact border+background+box-shadow; 44px height, 0 12px padding, error-icon right-12px absolute; required asterisk #DC2626; disabled: pointer-events none + label 50% opacity; read-only: default cursor + no hover border change); PasswordInput (components/ui/PasswordInput.tsx — extends FormInput + showStrengthMeter/minLength props; internal useState(showPassword); toggle button absolute right-0 44px×44px with EyeIcon/EyeOffIcon 16px rgba(12,31,64,0.45) → 0.80; autoComplete defaults current-password; PasswordStrengthMeter sub-component: scoring function with 4 criteria: length≥12/uppercase/digit/special each +1 point, 5 tiers 0=Too-short-red/1=Weak-red/2=Fair-amber/3=Good-emerald/4=Strong-dark-emerald; 4px bar height, score/4*100% width, color+width transition 0.3s ease, label 12px right-aligned same color); Select (components/ui/Select.tsx — appearance:none + custom ChevronDown right-12px absolute pointer-events-none; native <select> 44px height 0px 40px 0px 12px padding; placeholder option disabled empty-value; CSS :has(option:checked:disabled) for placeholder color; 5-row state table; disabled: opacity 0.60 + not-allowed cursor); Toggle (components/ui/Toggle.tsx — role=switch aria-checked, pill-shaped track exception to PyMC no-radius documented; sm: 32×18px track 14px thumb 14px travel; md: 44×24px track 18px thumb 20px travel; track bg: off=rgba(12,31,64,0.15)/on=Aqua #B4E7DD; thumb: white circle box-shadow; thumb translateX animation 0.2s ease; 8-row state table including hover/focus for both on/off + disabled on/off; labelPosition left|right prop; label 14px 500 + description 13px 400; keyboard: Space/Enter toggle; exception documented: toggles are always pill-shaped because sharp toggle tracks are non-standard UX); Checkbox (components/ui/Checkbox.tsx — visually-hidden native input technique (position:absolute w-px h-px clip); custom .checkbox-box 18×18px div with 0 border-radius; CheckIcon 11px / MinusIcon 11px inside; indeterminate prop with ref callback; 8-row state table: unchecked/hover/focus/checked/checked-hover/checked-focus/indeterminate/disabled-checked/error-unchecked; focus detected via .checkbox-input:focus-visible ~ .checkbox-box CSS selector; label may contain JSX links with specific link style: Navy text + Aqua underline; error ml-[26px] to align with label text); ApiKeyInput (components/ui/ApiKeyInput.tsx — 2 modes: entry mode (hasExistingValue=false: password-type input + show/hide toggle + Save button when value.length>0 + isValidating spinner) and existing key mode (hasExistingValue=true: masked code display + Copy/Edit/Delete icon buttons); masked format: [prefix][first-4]••••••• never reveals full server key; .api-key-masked-display: font-mono 14px rgba(12,31,64,0.04) bg 1px solid rgba(12,31,64,0.10) border; 3 action icon btns 32×32px with border 0 border-radius; delete btn: #DC2626 icon → #FEF2F2 hover; copy: CheckIcon 2s then back + #059669 green; ApiKeyValidationBadge sub-component: valid=emerald/invalid=red/unknown=amber with CheckCircle/XCircle/AlertCircle icons + exact badge styles); SearchInput (components/ui/SearchInput.tsx — no label prop, aria-label instead; type=search with -webkit-search-cancel-button hidden; SearchIcon absolute left-12px pointer-events-none; clear button absolute right-0 only when value.length>0; sm=36px/md=44px height variants; focus-within: 1.5px Navy border + Aqua shadow via CSS; isLoading: Loader2 spin replaces SearchIcon; debounce NOT built-in — caller's responsibility; disabled: F7F7F7 bg + 0.60 opacity); summary table 7 components with files and usage |
| 37 | 2026-03-13 | 4.9a — Component library: Layout components | 1 run | Created frontend/component-library.md; 15 layout components specced: DashboardLayout (app/(dashboard)/layout.tsx — auth guard, flex shell CSS, responsive ml-[240px]/ml-[56px]/ml-0 at 3 breakpoints); Sidebar (240px fixed navy, logo area 64px, NavItem sub-component with 5 interaction states including active 12% Aqua bg + 2px Aqua left border, 5 nav items with Lucide icons, SidebarFooter absolute bottom with UserAvatar circle+initials+email truncated+LogoutButton with loading spinner during signOut); DashboardTopbar (56px sticky white, Archivo Semi-Expanded 20px page title left, tenant name + PlanBadge right, hamburger on mobile, PlanBadge sub-component with 3 plan variants: free=20% Periwinkle / starter=30% Aqua / pro=solid Aqua, 12px Inter 600 uppercase 0.05em); MobileNav (full-screen overlay, 280px panel translateX animation 250ms, backdrop 50% black, focus trap, Escape key, body scroll lock, aria-modal, identical NavItems, header with CloseButton, footer with logout); AuthLayout (app/(auth)/layout.tsx — inverse auth guard, centered 440px container, White Soft bg, p-4 page padding); AuthCard (white bg, 1px navy 10% border, 0 border-radius, 40px padding, CardHeader with Archivo Semi-Expanded 24px title + Inter 14px description, mobile reduces to 24px padding); AuthLogo (centered flex, 32px rocket SVG Navy, Archivo 20px 700 wordmark, opacity hover, dark variant for Navy bg); AuthFooterLinks (Privacy Policy · Terms of Service · Help, Inter 13px, 45% navy opacity, · separator aria-hidden); PageShell (page-level heading container: Archivo Semi-Expanded 28px h1, Inter 15px description, Breadcrumbs sub-component with ChevronRight separator + current/linked link styles, actions slot right-aligned, mobile stacks to flex-col); PublicNavbar (64px/56px sticky, rgba(255,255,255,0.92) + blur(12px), 28px logo, 4 NavLinks center with 28px gap + active 2px Aqua underline, Ghost "Sign in" + Compact Primary "Get started" CTAs, hamburger at ≤900px, PublicMobileMenu full-screen Navy overlay with logo + CloseButton + vertical navlinks 56px + divider + full-width CTA buttons); PublicFooter (Navy bg, 48px padding, 5-column grid 1.5fr 1fr 1fr 1fr 1fr desktop / 1fr 1fr mobile, Brand col with Aqua logo, 4 link columns: PRODUCT/RESOURCES/LEGAL/COMPANY each with header Inter 14px uppercase 0.08em 45% opacity + links Inter 14px 70% opacity + hover 100%, divider + copyright row + "Built with ❤ on Claude"); PublicLayout (app/(public)/layout.tsx); AdminLayout (app/(admin)/layout.tsx — admin role check via user_profiles.role, AdminSidebar with 6 nav items, Peach #F6AE72 ADMIN badge in logo area — documented exception to data-viz-only rule); summary table of all 15 components with file paths |
| 36 | 2026-03-13 | 4.8h — Docs: Billing & Plans docs page — plan comparison, feature gating rules, upgrade/downgrade flows explained | 1 run | Appended to frontend/docs-pages.md; /docs/billing page spec (route app/(docs)/billing/page.tsx, static page, title + meta description "Understand how Daimon plans work, how BYOK billing saves you money, and how to upgrade, downgrade, or cancel your subscription."); page header with breadcrumb "Account & Billing"; in-page TOC linking to 9 section anchors (plans-overview/byok-model/billing-cycles/upgrading/downgrading/canceling/managing-billing/api-keys/payment-failures); Section 1: Plans Overview — full comparison table (13 rows × 4 cols: monthly price Free $0/Starter $9/Pro $29; annual price $0/$79/$249; annual savings —/$29/$99; Discord connections 1/3/unlimited; all tools ✓ all tiers; BYOK ✓ all tiers; optional OpenAI ✓ all tiers; all integrations ✓ all tiers; community support ✓ all; email support —/✓48h/✓24h; priority support —/—/✓; 99.9% SLA —/—/✓; payment required no/yes/yes) + tip callout "all plans include full tool catalog" + prose per-plan descriptions covering ideal use cases; Section 2: BYOK Model — why BYOK (transparency/control/fairness/privacy), what Daimon charges vs what Anthropic charges, typical Anthropic cost estimate ($1–5/month), link to console.anthropic.com, OpenAI key optional explanation (classification optimization), info callout about AES-256 encryption; Section 3: Billing Cycles — monthly billing (same date each month, cancel keeps access through paid period), annual billing (once/year, Starter $79/yr save $29, Pro $249/yr save $99), switching intervals (via Customer Portal, takes effect next period), billing date behavior (set on first subscribe, proration on upgrade), currency (USD, bank converts), invoices (Stripe-hosted, sent by email, all invoices in portal); Section 4: Upgrading — 6-step walkthrough (Billing page → plan grid → billing toggle → Upgrade button → Stripe Checkout → success redirect), what happens immediately (plan updated, connection slots available, bot uninterrupted), proration explanation with example ($9/mo Starter → $29/mo Pro on day 15 → ~$10 charge), owner-only restriction callout, tip callout "upgrades are instant"; Section 5: Downgrading — downgrade is scheduled (takes effect at period end not immediately), 4-step walkthrough with confirmation dialog copy (exact text for Pro-to-Free dialog: lists 3 consequences: 1-connection limit + extra connections suspended + email/priority support/SLA lost), reversal flow (click Reactivate), connection limit enforcement (oldest connections kept, excess suspended, data preserved), how to choose which to keep (disconnect manually before downgrade date), downgrading to Free cancels Stripe subscription (customer record preserved), owner-only, warning callout about suspended connections; Section 6: Canceling — cancel = revert to Free at period end (not immediate), 2 options (Billing page → Manage Billing → Stripe Portal → Cancel plan; or billing.stripe.com directly), what "canceled" means (bot runs until date, Free reverts, extra connections suspended, account NOT deleted, data preserved), reactivation flows (before period end: Reactivate button; after: upgrade again, no penalty), account deletion vs cancellation distinction (separate Danger Zone action), refund policy (no refunds except where required by law, contact support@daimon.ai); Section 7: Managing Billing — what the Stripe Customer Portal can do (payment methods: add/remove/set default; billing info: email/address/company; invoices: view/download PDF/line items; subscription: change interval/cancel/reactivate), what portal cannot do (change tier — use Daimon page instead), returning from portal (redirect to /dashboard/billing?portal_return=1 with welcome banner), billing contact vs account owner (billing email updated in portal independently); Section 8: API Keys — Anthropic required (where to get: console.anthropic.com → API Keys → Create Key → starts sk-ant-, 7-step add walkthrough, 3 validation errors with cause/fix table, update flow, delete flow with confirmation dialog, what happens if key becomes invalid), OpenAI optional (when to add: cost optimization, where to get: platform.openai.com/api-keys → starts sk-, add walkthrough, 3 validation errors with cause/fix table), security note (Vault AES-256, never logged, partial hint only sk-ant-a...b12c, decrypted only in memory at point of use), key validation frequency (on save / on auth error / no manual re-validation); Section 9: Payment Failures — past_due status (Stripe retries: +3d/+5d/+7d), grace period behavior (bot keeps running, plan intact, warning banner on Billing + Dashboard), suspension on unpaid (bot offline, tenant.status=suspended, suspension banner), reactivation 5-step flow (Billing → Update Payment → portal → Pay now → webhook unsuspends within seconds), locked-out fallback (contact support@daimon.ai), danger callout; footer nav (← FAQ / no next page); accessibility: 9 ARIA specs (h1 id, all h2 section ids match TOC anchors, plan comparison table caption sr-only, 2 validation error table captions sr-only, 3 external links with sr-only "(opens in new tab)", TOC aria-label, footer nav aria-label, callout boxes role=note with aria-label) |
| 35 | 2026-03-13 | 4.8g — Docs: FAQ page — complete Q&A (billing, security, bot setup, troubleshooting, limits) | 1 run | Appended to frontend/docs-pages.md; /docs/faq page spec (route app/(docs)/faq/page.tsx, static page, title + meta description); page header with breadcrumb "Account & Billing"; in-page TOC linking to 7 section anchors (billing-pricing/security/bot-setup-discord/integrations-tools/troubleshooting/limits-quotas/account-teams); accordion component spec (details/summary HTML, CSS for open/closed states, "+" and "−" toggle, all open by default); 42 questions documented across 7 sections: Billing & Pricing (10 Q): what plans exist (Free $0/1 conn, Starter $9/mo/$79/yr/3 conn, Pro $29/mo/$249/yr/unlimited), credit card requirement, how billing works (Stripe flat fee + separate Anthropic bill), BYOK model explained, plan switching (upgrade=immediate pro-rated, downgrade=cancel_at_period_end), payment failure flow (retry + grace period + suspension), refund policy (no refunds for partial periods), annual billing (Starter $79/yr save $29, Pro $249/yr save $99), Anthropic API charges not included in Daimon fee, downgrade to Free consequences (1 conn remains, extras disabled but config preserved); Security (6 Q): API key storage (Supabase Vault AES-256-GCM, hint only shown), Discord token security (same Vault encryption, hint only, regenerate flow), who can see keys (no one, not staff, ciphertext only), team members cannot see keys (read-only hint visibility), Discord message access disclaimer with privacy policy reference, tenant data isolation (tenant_id tagging + RLS at DB level), account deletion data handling (keys+connections deleted immediately, message history within 30 days, backups 90 days); Bot Setup & Discord (8 Q): how bot connects (Developer Portal → bot token + guild ID → Settings page), must create own bot (not shared), Discord permissions required (9 permissions listed in table + 2 privileged intents: Server Members + Message Content), multiple servers by plan (Free 1, Starter 3, Pro unlimited), invalid token causes and fixes (3 causes: copied wrong/regenerated/app deleted), bot online but not responding (4 checks: Message Content Intent/channel perms/@mention requirement/Anthropic key), bot status "connecting" meaning and timeout behavior, bot status "error" causes table (4 causes), changing bot token after setup (Settings → gear → Update Token); Integrations & Tools (6 Q): full tool list table (13 categories, 90+ tools, all tools on all plans), how to connect services (OAuth: GitHub/Google/Linear/LinkedIn redirect flow; API key: Toggl/Fly/ACP/Onyx/Bluedot/Dub paste modal), removing integration consequences (token deleted, tools fail gracefully, data preserved), GitHub not authorized after connecting (token expired/revoked → Reconnect flow), using bot without integrations (Discord + Decision Hub work immediately), no custom tools in current version; Troubleshooting (7 Q): bot went offline causes (5 causes: Discord disconnect/token regenerated/payment failed/Discord outage/Daimon disruption), tool errors table (6 error message × cause × resolution rows), workspace-not-found error, reporting bugs + support SLAs (Starter 2 business days, Pro 4 business hours), Anthropic key shows Invalid (4 causes: no credits/revoked/copy error/rate limit), no verification email (5 steps), forgot password (7 steps); Limits & Quotas (5 Q): Discord connections by plan table (Free 1, Starter 3, Pro unlimited), message processing limits (none from Daimon, Anthropic rate limits apply), storage limits (no hard limits, fair-use for extreme volumes), Daimon API rate limits table (6 endpoint categories with exact limits), service integration limits (none), message history retention (lifetime of account, deleted within 30 days after account deletion); Account & Teams (6 Q): team invites (planned future feature), multiple workspaces (planned future feature), changing workspace name (Settings → Workspace), changing email (not self-serve, contact support), deleting account (Settings → Danger Zone → name confirmation → 5 listed consequences), workspace transfer (not self-serve, contact support); footer nav (← Linear Tools / Billing & Plans →); accessibility: 7 ARIA specs (details/summary native keyboard, sr-only table captions, external link sr-only "(opens in new tab)", mailto links, in-page TOC aria-label, footer nav aria-labels) |
| 34 | 2026-03-13 | 4.8f — Docs: Tool Reference — Linear (6 remote MCP) tools + tool index/navigation structure | 1 run | Appended to frontend/docs-pages.md; /docs/tool-reference/linear page spec (route: app/(docs)/tool-reference/linear/page.tsx, static page, title + meta description); page header with breadcrumb "Tool Reference"; in-page TOC (6 tools: linear_list_issues/search_issues/get_issue/create_issue/update_issue/list_teams); Remote MCP callout (info type, explains streamable HTTP proxy, 1-3s latency); Requires Linear API key callout (warning type, explains error message text "Linear API key not configured. Set LINEAR_API_KEY in environment."); "Setting Up Linear" section (2-step walkthrough: generate API key in linear.app Settings→API + connect in Daimon Integrations page); 6 tools documented in full: linear_list_issues (team_id optional + first default 50, returns identifier/title/state/assignee/priority per issue, 4 error cases, usage example), linear_search_issues (query required + first default 20, searches titles+descriptions, 3 error cases), linear_get_issue (issue_id required accepts UUID or BAI-42 format, returns full details+comments with author+timestamp+body, 3 error cases), linear_create_issue (title required + description/team_id/priority 0-4/assignee_id/label_ids optional, 6 error cases, disambiguation note for assignee-by-name flow), linear_update_issue (issue_id required + title/description/priority/state_id/assignee_id all optional; assignee="" to unassign; 5 error cases; requires linear_list_teams to resolve state name to state_id), linear_list_teams (no params, returns 3-level hierarchy: team-key/name → states with state-type enum: backlog/unstarted/started/completed/cancelled, 3 error cases, tip to use for all ID lookups); Remote MCP architecture note (6-step flow: tool selection → proxy.py → MCP Streamable HTTP → remote GraphQL → stream back → return string); 6-row quick-reference table (tool/category/auth/parameters/description); Complete Tool Index section (master index of all 90 tools across all 5 platforms: 11 Discord&Core, 34 Toggl, 21 LinkedIn&GA (note: 17 LinkedIn + 4 GA), 23 Fly&Infra, 6 Linear remote MCP — all with description + anchor link to doc page); footer nav (← Fly & Infrastructure Tools / FAQ →); loading/empty/error states (static page, error.tsx fallback, auth CTA slot client-side); accessibility (9 ARIA specs: main role, in-page TOC aria-label, aria-current=location, 2 callout role=note labels, tool heading ids as anchor targets, parameter table sr-only captions, code block tabindex=0, complete tool index table captions) |
| 33 | 2026-03-13 | 4.8e — Docs: Tool Reference — Fly (9) + ACP (4) + Decision Hub (4) + Onyx (2) + Bluedot (4) tools | 1 run | Appended to frontend/docs-pages.md; /docs/tool-reference/fly page spec (route, file, render mode static, layout shared docs 2-col); page title + intro paragraph covering all 5 platforms; in-page TOC tree (all 23 tools nested under 5 platform sections); Fly.io (9): platform auth note (system-level token, no user account needed); Sessions subsection: fly_launch_session (5 params: template required + region/cpu_kind/cpus/memory_mb with defaults, example output: app_name mmm-abc12345 + status+region+urls.notebook+urls.acp+machine_id+cpu_kind+cpus+memory_mb, usage notes: mmm-{random} naming pattern, ephemeral delete-on-stop), fly_stop_session (app_name required, XML result with status=deleted, irreversible warning), fly_get_session_status (app_name required, XML with state+region+urls+machine_id+created_at, 4 machine state values: started/stopped/created/destroyed), fly_list_sessions (no params, XML sessions wrapper with template field, empty list note); Images subsection: fly_list_images (no params, XML with repository+tag+digest+size_mb+created_at); Templates subsection: fly_list_templates (discord_user_id optional, XML with is_system+is_public+owner_discord_id/name fields), fly_save_template (9 params: slug+name+fly_app required + description/source_repos/framework/is_public/discord_user_id/discord_user_name optional, records Docker image reference, source app need not stay running), fly_delete_template (slug required + discord_user_id optional, 3 error scenarios: not-owner/system-template/not-found), fly_launch_builder (region+memory_mb optional, hardcoded template=decision-pack-compiler cpu_kind=performance cpus=4 note, beefier cost note, use fly_stop_session to terminate); ACP (4): platform intro (Claude-to-Claude over HTTP, always health_check first then optionally list_tools); acp_health_check (app_name required, XML with status=healthy+acp_url+response_ms, 2 error scenarios: not-reachable / app-not-found), acp_list_tools (app_name required, XML tools list with server+name+description, total count), acp_send_message (3 params: app_name+message required, timeout default 120 range 10-600, XML with status+response+duration_ms, session Claude has full tool access, increase timeout for long ops), acp_call_tool (4 params: app_name+server+tool required + params default '{}', XML tool_result with status+result, params must be valid JSON string, use acp_list_tools for server/tool names, bypasses session Claude); Decision Hub (4): platform intro (skills = prompt engineering packages injected into system prompt); decision_hub_search_skills (query required, XML skills with org+name+description+version, empty list OK), decision_hub_activate_skill (org+skill_name required, XML with status=activated+version+description+message, multiple skills accumulate, use list to see active), decision_hub_list_active_skills (no params, XML active_skills with activated_at, empty list when none), decision_hub_deactivate_skill (org+skill_name required, XML status=deactivated, error if not currently active); Onyx (2): platform intro (RAG, AI-generated answers with citations, system-level Onyx key); onyx_list_agents (no params, XML agents with id+name+description+document_sets array), onyx_query (message required + persona_id default 0, XML with answer+sources list: title+url+score, accuracy caveat, use persona_id for specialized domain); Bluedot (4): platform intro (meeting transcripts via webhook sync, no user Bluedot account needed, manual re-export note for missing meetings); bluedot_list_meetings (start_date+end_date optional ISO 8601, XML meetings with id+title+date+duration_minutes+attendees+has_transcript+has_summary), bluedot_get_transcript (meeting_id required + max_lines optional, XML transcript with line elements: speaker+timestamp+text, total_lines, speaker identification note), bluedot_get_summary (meeting_id required, XML summary with overview+key_points+action_items: owner+task, AI-generated caveat), bluedot_search_transcripts (query required + start_date+end_date optional + limit 1-100 default 25, XML results with match_context snippet, always-pass-dates instruction, use get_transcript/get_summary for full content); 23-row quick-reference table (tool/category/auth/parameters/description); footer nav (← LinkedIn & Google Analytics Tools / Linear Tools →); loading/empty/error states (static page, error.tsx fallback, auth CTA slot); accessibility table (10 ARIA specs: 2 destructive action role=note warnings, platform section heading IDs, parameter table captions, code block tabindex) |
| 32 | 2026-03-13 | 4.8d — Docs: Tool Reference — LinkedIn (17) + Google Analytics (4) tools | 1 run | Appended to frontend/docs-pages.md; /docs/tool-reference/linkedin page spec; LinkedIn auth note (2 tokens: Ads Token App 1 for campaigns/analytics/conversions/events/leads; Community Token App 2 for posts/org-stats); in-page TOC (17 tools nested under 7 categories); Posts (4): linkedin_list_posts (count/start params, XML posts wrapper with commentary/visibility/lifecycle_state, empty hint, pagination hint), linkedin_create_post (commentary required + visibility/article_url/article_title/article_description optional, article post example), linkedin_update_post (post_urn+commentary required, commentary-only note), linkedin_delete_post (post_urn required, permanent deletion warning); Ads & Campaigns (4): linkedin_list_ad_accounts (page_size optional, XML ad_accounts with name/status), linkedin_list_campaigns (ad_account_id required, status_filter optional, valid status values documented), linkedin_create_campaign (ad_account_id+campaign_data object required, required fields documented), linkedin_update_campaign (3 params, patch semantics documented, common patch fields); Ad Analytics & Library (2): linkedin_get_ad_analytics (11 params: account_urns+pivot required, date as 6 int fields, time_granularity/fields/campaign_urns optional; pivot values: CAMPAIGN/CREATIVE/ACCOUNT/MEMBER_INDUSTRY/MEMBER_SENIORITY/MEMBER_JOB_TITLE/MEMBER_COMPANY_SIZE/MEMBER_COUNTRY/MEMBER_REGION; output: clicks/impressions/cost/shares/likes/comments/follows/videoViews per row), linkedin_search_ad_library (query/advertiser_name/page_size optional, public ad transparency data); Conversions (1): linkedin_send_conversions (conversion_events list with conversion URN+timestamp+userInfo, count output); Events (2): linkedin_list_events (no params, XML events with name+id), linkedin_create_event (event_data object, required fields: name/organizer/startAt/endAt); Leads (1): linkedin_get_lead_form_responses (sponsored_account_urn required, start_ms/end_ms/count optional, XML with submitted_at+text answers); Org Stats (3): linkedin_get_share_stats (start_ms/end_ms/granularity DAY|WEEK|MONTH + share_urns filter, output: clicks/likes/comments/impressions/shares), linkedin_get_follower_stats (start_ms/end_ms/granularity, output by function: organic+paid counts), linkedin_get_page_stats (start_ms/end_ms/granularity, page_views total); 17-row summary table; GA tools section: auth note (service account JSON + property ID, not configured message); output format note (ga-report XML, 50 row cap, overflow hint); GA (4): ga_run_report (dimensions+metrics+start_date+end_date required, limit 1-1000 optional, example with country+sessions+newUsers), ga_get_traffic_overview (fixed dims: channelGroup+sourceMedium, fixed metrics: sessions+activeUsers+bounceRate+engagementRate, start_date+end_date+limit params), ga_get_top_pages (fixed dims: pagePath+pageTitle, fixed metrics: screenPageViews+activeUsers+userEngagementDuration, start_date+end_date+limit), ga_get_campaign_performance (fixed dims: campaignName+sourceMedium, fixed metrics: sessions+activeUsers+conversions+totalRevenue, start_date+end_date+limit); 4-row GA summary table; footer nav (← Toggl Tools / Fly ACP Decision Hub Onyx Bluedot →); loading/empty/error states; accessibility table (8 ARIA specs including delete warning role=note) |
| 31 | 2026-03-13 | 4.8c — Docs: Tool Reference — Toggl (34 tools) | 1 run | Appended to frontend/docs-pages.md; /docs/tool-reference/toggl page spec; page header with subtitle; prerequisites banner (Aqua bg, border); Admin Only badge spec (Amber 100 bg); tool entry format (section with h3+code id, description, meta row, parameter table with sr-only caption, details/summary example block); in-page TOC (34 tools nested under 6 category anchors); 6 categories documented in full: Time Entry (7): toggl_get_my_time_entries (start_date/end_date both optional ISO 8601, 90-day max error message), toggl_get_my_time_entry (time_entry_id required), toggl_get_my_current_time_entry (no params, returns running entry object or "No time entry is currently running." string), toggl_create_my_time_entry (7 params: start required + description/project_id/task_id/duration/stop/tags/billable all optional, running timer = duration:-1 + omit stop note), toggl_update_my_time_entry (8 params all optional except time_entry_id, tags:[] removes all tags), toggl_stop_my_time_entry (time_entry_id required, must be running or error), toggl_bulk_edit_time_entries (ids max 100 + JSON Patch operations array, supported paths: /description /project_id /task_id /tags /billable, example output with updated count + errors array); Projects (4): toggl_get_projects (active bool optional filter), toggl_get_project (project_id), toggl_update_project (7 optional params), toggl_create_project (name required + 4 optional); Tasks (5): toggl_get_tasks (no params), toggl_get_task (project_id+task_id), toggl_get_project_tasks (project_id), toggl_create_task (project_id+name required, estimated_seconds+active optional), toggl_update_task (project_id+task_id required, 3 optional); Workspace Member (1): toggl_get_workspace_members (name optional substring filter, returns user IDs for add-to-project); Project User (3): toggl_add_user_to_project (project_id+user_id required, manager bool, note about already-on-project error), toggl_get_project_users (returns project_user_id distinct from user_id), toggl_remove_user_from_project (project_user_id required, not user_id — common mistake called out); Workspace Report (14 all Admin Only): admin-only section notice (Amber 100 bg, 403 error message text), toggl_search_workspace_time_entries (start+end required, user_ids/project_ids optional, auto-chunked >1yr), toggl_get_workspace_time_summary (6 params, grouping=users/projects/clients, sub_grouping 5 values), toggl_workspace_project_summary (start+end only, per-project + per-member breakdown), toggl_workspace_time_totals (granularity=day/week/month optional), toggl_weekly_report (daily per-user breakdown), toggl_export_detailed_csv (returns CSV string, column list documented), toggl_export_summary_csv (grouping+sub_grouping+filter params), toggl_project_trends (previous_period_start required, Premium note + 403 error message), toggl_employee_profitability (currency required, Premium note), toggl_project_profitability (currency required, Premium note), toggl_list_report_users (no params, returns ids for filter), toggl_list_report_projects (no params), toggl_list_report_clients (no params), toggl_list_project_user_rates (project_ids optional, returns rate+currency per user per project); 34-row quick-reference table (tool/category/access/admin/description); page footer nav (← Discord & Core Tools / LinkedIn & Analytics →); loading/empty/error states; accessibility table (7 ARIA specs including admin-badge role=img + section notice role=note) |
| 30 | 2026-03-13 | 4.8b — Docs: Tool Reference — Discord & Core Tools | 1 run | Appended to frontend/docs-pages.md; /docs/tool-reference/discord page spec; in-page TOC (sticky right gutter, desktop only, anchor links to each tool); 11 tools documented across 4 categories; Discord (7): discord_read_thread (thread_id param, up to 100 messages, XML format with author role/id/username, 3 error conditions), discord_read_channel (channel_id + limit 1–100, same XML format, separate get_channel call for name), discord_parse_link (URL → guild_id/channel_id/message_id/link_type, supports discord.com+ptb+canary, link_type="channel"|"message_or_thread" with decision tree for next action), discord_search_messages (8 params: content/author_ids/author_types/mentions/channel_ids/has/limit 1–25/offset 0–9975, total+showing+guild-id in result wrapper, 202 index-building error), discord_get_message (channel_id+message_id, single message element), discord_send_message (channel_id+content max 2000+silent, client-channel safety guard: checks name contains "client" + thread parent check, 3 API calls for threads, format: "Message sent to #name (message ID: ...)"), discord_create_thread (channel_id+thread_name max 100+content max 2000+silent, 3 sequential API calls: get_channel → create_thread → create_message, partial failure note); Dub.co (2): dub_list_links (7 params: search/domain/tag_names/sort_by 4 values/sort_order/page/page_size max 100, XML links wrapper with url+short_link+utm_source/medium/campaign when non-null+clicks+leads+sales, DUB_API_KEY required check, 429+timeout errors), dub_get_analytics (22 params: event 4 values/group_by 16 values/link_id/domain/interval 9 values/start/end/timezone/country/city/device/browser/os/referer/url/tag_id/utm_source/medium/campaign/term/content, count response vs record-list response by group_by type, all fields passed through from Dub API); Credentials (1): get_credential (platform: "toggl"|"github" from CredentialPlatform enum, returns raw token string, no external API call, sources from user_context.credentials already-loaded); GitHub (1): github_run_gh (command string, strips leading "gh ", shlex.split(), GH_TOKEN injection, 30s timeout with process.kill(), XML gh-result with stdout+stderr+exit-code attr on failure, requires_credential=CredentialPlatform.GITHUB); page footer nav (← Quick Start / Toggl Tools →); full page metadata; responsive at 3 breakpoints (parameter table overflow-x on mobile); accessibility (parameter table captions sr-only, tool heading ids as anchor targets, in-page TOC aria-label, aria-current="location" for active item) |
| 29 | 2026-03-13 | 4.8a — Docs: Quick Start guide | 1 run | Created frontend/docs-pages.md; Docs layout: fixed 260px sidebar (nav sections: Getting Started / Tool Reference 5 sub-pages / Account & Billing 2 sub-pages), DocsTopbar (56px sticky, breadcrumb + auth-aware CTA button), article max-width 780px; full typography spec for docs content (h1–h4, p, a, code, pre, ul, blockquote, table, hr — all with exact font/size/weight/color/margin); callout box system (info/warning/tip/danger with background+border-left colors); step numbering blocks (28px Navy circle, flex layout); sidebar nav link active/inactive states (Aqua left border when active); Quick Start page (/docs/quick-start): page header with estimated time (8–12 min); Prerequisites section with info callout; 8 numbered steps: (1) Create account — signup form fields, submit behavior, 7 error scenarios, success redirect + toast; (2) Create Discord Bot — 7 sub-steps covering Developer Portal navigation, New Application, Bot creation, token copy (danger callout), enable 3 privileged intents, OAuth2 invite URL generation with all 9 bot permissions, finding Guild ID in Developer Mode; (3) Get Anthropic API Key — 3 sub-steps: create account, add billing, create API key (warning callout about one-time display); (4) Add API Key to Daimon — billing page navigation, API Keys section wireframe, 9-scenario input behavior (valid/format-invalid/invalid-with-Anthropic/Anthropic-unreachable), optional OpenAI callout; (5) Connect Discord Bot — settings page navigation, Add Connection form (2 fields: bot_token password input + guild_id text), 9-scenario connect behavior (validation → pending → 10-30s realtime update → connected/error), connected row wireframe with effective_status; (6) Verify Bot Is Live — dashboard navigation, Bot Status card layout (Aqua circle + Connected text + bot username + heartbeat), "Connecting…" vs "Error" cases; (7) Talk to Bot — Discord message format, @mention example block, Discord-dark styled code block spec (#36393F), "if no integrations yet" copy with bot's exact error response text; (8) Connect Integrations (Optional) — 4 service cards wireframe (GitHub/Google/Linear/Toggl), per-service: "what it enables" + connect flow steps (OAuth vs API key for Toggl); Troubleshooting section: 4 subsections (Bot Not Connecting — 4-row error×cause×fix table, API Key Issues — 4-row problem×fix table, Tool access error, Billing/plan limits, Still Stuck with support email); What's Next section (4 links); page metadata (title/description/OG/canonical); responsive behavior at 3 breakpoints (desktop 2-col, tablet horizontal tab bar, mobile hamburger slide-over with full focus trap spec); loading state (static page, topbar CTA auth-state rendering); accessibility (12 ARIA specs, focus management for mobile menu, keyboard nav table) |
| 28 | 2026-03-13 | 4.7 — Admin panel | 1 run | Created frontend/admin-panel.md; Admin auth: JWT custom claim `is_admin: true` in `raw_app_meta_data` (set via SQL, not a separate table); middleware returns 404 (not 403) for non-admins to avoid info disclosure; service role Supabase client for all admin queries (bypasses RLS); Admin shell: 220px navy sidebar (Tenants/Audit Log/Exit Admin nav + shield icon logo), 52px topbar with admin badge (Aqua bg, Navy text); Tenant List: 4 stat cards (total/active/paying/suspended; server-side COUNT queries), filter bar (search 300ms debounce + Plan/Status/Sort dropdowns + Reset Filters ghost button), 8-column table (tenant name+owner email, plan badge, status badge, discord bot username+guild, created, View button) with pagination 50/page; plan badge colors (free=gray, starter=20% aqua, pro=solid aqua); status badge colors (pending=yellow, configured=blue, active=green, suspended=red); Tenant Detail page: breadcrumb, header with tenant name + badges + quick actions (Impersonate/Suspend/Unsuspend/Override Plan); 8 sections: Tenant Info (8 fields with copy-to-clipboard and View in Stripe link), Discord Connections (7 status badge colors, error_message inline, Reset modal, Disconnect modal), API Keys (provider+key_hint+status+validated_at, Revoke with reason modal), Service Connections (connected-as identity from metadata, revoke), Subscription (plan+stripe_status+sub_id+period+cancel_at_period_end+trial), Recent Admin Actions (last 10 from audit_log with expandable JSONB), Danger Zone (Delete with 2-step modal: impact summary + type-name confirmation); Suspend flow: PATCH suspend endpoint, updates tenants.status + all discord_connections.status to suspended, writes audit log; Unsuspend: sets status=configured + connections=pending; Plan Override: DB-only, requires reason ≥10 chars, shows out-of-sync warning when tenants.plan ≠ tenant_subscriptions.plan; Impersonation: JWT claim is_impersonated:true, 48px yellow fixed banner with End Impersonation button, all mutation routes return 403 when impersonation detected, 30-min session expiry, start/end audit log entries; Audit Log page: 5 filters (tenant_id/action/admin/date-from/date-to), 6-column table with row expansion for JSONB metadata, 100/page; 13 admin API routes documented with request/response shapes; security: 10 properties including 404-not-403, service role never exposed to browser, atomic audit log writes, IP address logged; all loading states (shimmer), empty states, error states; desktop-only (<1024px shows "not available on mobile") |
| 27 | 2026-03-13 | 4.6 — Settings page | 1 run | Created frontend/settings-page.md; 5 sections stacked as cards (0px radius, #E5E7EB border); Section 1 Workspace: name edit form (input+Save inline row, onChange disables Save when unchanged, 3 validation rules), workspace ID (monospace + copy-to-clipboard with 2s checkmark feedback), creation date (Intl.DateTimeFormat long format); Section 2 Discord Connection: connection list rows (32px Discord icon, bot_username + guild_id label, effective_status badge with 7 state×color matrix, relative last_heartbeat time using relativeTime() util, error_message inline, Update Token + Remove buttons with role-gating); plan limit banner (yellow, links to /dashboard/billing); Add Connection form (collapsible toggle when connections exist, always open when zero; bot_token password input with Discord token format regex /^[A-Za-z0-9_\-]{23,28}\.[A-Za-z0-9_\-]{6}\.[A-Za-z0-9_\-]{27,38}$/, guild_id with [0-9]{17,20} pattern; 8 validation rules including server-side Discord API rejection and already-in-use token; Connect Bot button → POST /api/discord-connections → 201 + 3s auto-refresh); Update Token modal (480px, native <dialog>, focus trap, Escape closes, 5 validation rules, PATCH /api/discord-connections/{id}); Remove confirmation dialog (alertdialog role, DELETE /api/discord-connections/{id}); Section 3 Team Members: member list rows (circular avatar with initial, email + joined date, role badge 3 variants: owner=navy/white admin=aqua/navy member=gray), Invite Member button disabled with "coming soon" tooltip; Section 4 Account: display name form (saves to user_metadata.full_name via supabase.auth.updateUser), email readonly with hint, change password form (3 fields: current/new/confirm; server-side verification sequence: signInWithPassword to verify current → admin.updateUserById to set new; 7 validation rules including mismatch + same-as-current); Section 5 Danger Zone: owner-only (not rendered for admin/member, not CSS-hidden, not in JSX tree); delete workspace row with description; Delete Workspace modal (alertdialog, name-confirmation input activates delete button on exact case-sensitive match; consequences list enumerates deleted items dynamically; 6-step ordered SQL deletion: service_connections→discord_connections→api_keys→subscriptions→members→tenant; Stripe subscription cancel + Customer del; signOut; auth.users row preserved; redirect to /?deleted=1); SettingsPageData TypeScript interface; page-level error state; role restriction matrix 5×3; responsive at 3 breakpoints (desktop max-width 800px, tablet full-width, mobile stacked input rows + reduced padding 16px/20px); loading skeleton with shimmer animation; 14 toast scenarios; accessibility: keyboard nav table, ARIA labels for every interactive element, focus management on modal open/close/delete-row, WCAG AA contrast table (7 pairs) |
| # | Timestamp | Aspect | Duration | Key Findings |
| 26 | 2026-03-13 | 4.5 — Billing page | 1 run | Created frontend/billing-page.md; Route /dashboard/billing; SSR data fetch (3 queries: tenant+subscription JOIN, user role, api_keys non-sensitive); BillingPageProps interface with tenant/subscription/apiKeys/userRole/checkoutSuccess/checkoutCanceled; 4 URL params handled (?success=1, ?canceled=1, ?portal_return=1 — all cleared via router.replace after toast); 4 conditional banners: CheckoutSuccessBanner (green, 5s auto-dismiss), CheckoutCanceledBanner (blue, 5s), SuspendedAccountBanner (red, no-dismiss), PastDueBanner (amber, no-dismiss with Customer Portal CTA); CurrentPlanCard: plan name + price (Archivo 24px) + feature list (checkmark icons in Aqua) + billing period line (8 conditional states: free/active/cancel-at-period-end/trialing/past_due/canceled/unpaid) + CTA button (7 state × plan combinations); Billing cycle toggle (44×24px sharp corners, Aqua track when on, monthly/annual); PlanComparisonGrid 3-col desktop / 2-col tablet / 1-col mobile: 3 plan cards (Free $0, Starter $9/mo or $79/yr saves $29, Pro $29/mo or $249/yr saves $99); per-plan CTA matrix (upgrade/current-disabled/downgrade-ghost-red); feature lists all 4 Free, 4 Starter, 5 Pro; downgrade triggers ConfirmDialog with cancel_at_period_end mechanics; Pricing table: 4 Stripe price IDs as env vars; role restriction: owner-only for mutations, member sees read-only; Section 2 API Keys: 2 ApiKeyRow components (Anthropic REQUIRED, OpenAI OPTIONAL); key hint box (monospace, gray-50, no-radius); status indicator (green/red/gray) with validated_at date; 5 inline state variants per row (active/invalid/no-key-anthropic/no-key-openai/revoked); Add/Update Key Modal (480px, password input with show/hide toggle, client-side format validation, server-side test API call to Anthropic/OpenAI, 5 error response types per provider, loading state with "Verifying…" note, unsaved-changes guard without browser confirm()); Delete Key Dialog (alertdialog role, Anthropic warns "bot stops immediately", OpenAI warns "falls back to Haiku", DELETE /api/billing/api-keys/:id, vault.delete_secret()); Downgrade dialogs (2: to-Free and to-Starter-from-Pro, both explain cancel_at_period_end + lists lost features + connection count); 11 toast scenarios; 5 API routes used; loading skeletons; 5 error state types; responsive 3 breakpoints; accessibility: 22 ARIA label/role specs, focus trapping in modals, focus management on open/close; WCAG AA contrast table (7 pairs verified) |
| 25 | 2026-03-13 | 4.4 — Integrations page | 1 run | Created frontend/integrations-page.md; 4 services at launch: GitHub (OAuth), Google (OAuth), Linear (OAuth), Toggl (API key); ServiceGrid: 2-col desktop/tablet, 1-col mobile; ServiceCard component: 5 status states (connected/expired/error/revoked/not-connected), connected=3px aqua left border, error/expired=3px red, status badge (22px, 0 radius, 5 color variants), card-details section with connected-at/last-used-at/scopes, error-banner for error+expired, footer with Connect/Reconnect/Disconnect buttons; SERVICE_META constant with displayName/description/defaultErrorMessage/oauthScopes/keyFormat per service; Server Component data fetching (SELECT from tenant_service_connections, index by service name); OAuth flow A: button click → /api/integrations/oauth/start?service={s} → buildAuthorizationUrl() (GitHub: repo+read:org+gist, Google: analytics.readonly + access_type=offline + prompt=consent, Linear: read+write+issues:create+comments:create) → provider redirect → /api/integrations/oauth/callback → state CSRF validation (UUID stored in httpOnly SameSite=Lax cookie, 10min expiry) → exchangeCodeForTokens() (GitHub: no expiry/no refresh, Google: expires_in+refresh_token, Linear: no expiry/no refresh) → upsert-service-connection Edge Function; API key flow B (Toggl): button click → ApiKeyModal → password input + help link to toggl.com/app/profile → /^[a-z0-9]{32}$/ format validation → POST /api/integrations/api-key/validate → live Toggl API validation (GET /api/v9/me with Basic auth) → metadata capture (toggl_user_id/email/workspace_id) → upsert-service-connection Edge Function; disconnect flow: button → ConfirmDialog (alertdialog role, "bot will lose access" copy) → DELETE /api/integrations/{service} → delete-service-connection Edge Function (SELECT ids → delete vault secrets → UPDATE status=revoked); 2 Edge Functions specced: upsert-service-connection (old secret delete + vault create + UPSERT row) + delete-service-connection (vault deletes + status=revoked update); Realtime hook: useServiceConnectionsRealtime subscribes to postgres_changes UPDATE on tenant_service_connections, updates card status badge in place; Toast notifications: 10 success/error scenarios with exact messages; loading states: page-level skeleton (4 ServiceCardSkeleton with animate-pulse), card-level "Redirecting..." spinner, modal "Validating..." state (all inputs disabled), disconnect "Disconnecting..." state; empty state: data fetch error (AlertTriangle + refresh CTA); member-role: all buttons disabled with tooltip; error state: OAuth error query params read on mount, router.replace() to remove after showing toast; accessibility: role=list/listitem, aria-label on cards, role=dialog, role=alertdialog, aria-modal, focus trap (Tab/Shift+Tab cycles), focus-on-open (modal→input, dialog→Cancel), focus-on-close (back to trigger button), aria-live=polite on status badge; security: Vault-only storage, service role for Vault writes (RLS blocks JWT), CSRF state param, redirect URI validation, member-role server-side enforcement, no tokens exposed to client; file map (14 files: page/loading/client + 4 routes + 5 components + 1 hook + 1 lib + 2 Edge Functions) |
| 24 | 2026-03-13 | 4.3 — Dashboard home | 1 run | Created frontend/dashboard.md; dashboard shell: Sidebar (240px navy, 5 nav items with active/hover/focus states, logo area, footer with email+logout), DashboardTopbar (56px sticky white, page title + tenant name + PlanBadge), mobile bottom nav (5 icons, safe-area inset); 6 page sections: (1) OnboardingChecklist (4 ordered steps: create Discord bot / connect server / add Anthropic key / wait for bot, progress bar with completedSteps/totalSteps, step status icons: completed CheckCircle aqua / current circle-with-number / pending faded, completion animation with 1.5s delay + fade-out + success toast, welcome banner for created_at < 5min), (2) BotStatusCard with Supabase Realtime updates (6 states: connected with pulse animation + relative heartbeat time + stale warning at 2min, connecting with dots animation, error with error_message + settings CTA, disconnected with CTA, null no-connection empty state with dashed border, suspended with red border + support CTA), (3) ApiKeysCard (Anthropic+OpenAI rows with provider icons, 4 KeyStatusBadge states: valid green/invalid red/not-configured gray/validating amber, required-key warning banner, invalid-key red row tint, OpenAI labeled optional), (4) IntegrationsSummaryCard (7-service grid with Aqua bg when connected / faded when not, 48px icon cells, tooltip on hover after 300ms, connected count string, empty state with 4 placeholder boxes), (5) QuickStatsRow (3 stat cards: messages today / tool uses today / uptime formatted as 3d 14h, all degrade gracefully to em dash, Archivo Expanded 32px values, CI accent stripe on left), (6) RecentActivityFeed (10 event types with icons + colored circle bg, empty state); server-side data fetching via Promise.all (5 parallel Supabase queries, full TypeScript queries documented); 7 TypeScript interfaces (Tenant/DiscordConnection/ApiKey/ServiceConnection/TenantSubscription/DashboardStats/ActivityEvent + DashboardPageData); Realtime subscription code (3 channels: discord_connections + tenant_api_keys + tenants, channel name format dashboard:{tenantId}, cleanup on unmount); loading states (Next.js Suspense skeleton with shimmer CSS animation, card-level 1px aqua spinner for real-time updates); error states (page-level error.tsx with refresh CTA, card-level dashed border + retry button, Realtime disconnect banner with 5-retry escalation); responsive at 1280px (sidebar fixed, 3fr/2fr bot+keys grid, 3-col stats), 768px (sidebar 200px, stacked cards, 2-col stats), 375px (sidebar hidden, mobile bottom nav, all stacked, 44px touch targets); ARIA: nav aria-label, aria-current, role=status, aria-live=polite, progressbar with aria-valuenow/min/max, aria-busy skeleton, every service icon aria-label; file structure (12 component files in components/dashboard + components/layout + components/ui) |
| 23 | 2026-03-13 | 4.2 — Auth pages | 1 run | Created frontend/auth-pages.md; 4 pages: /login (email+password, forgot-password link, mapAuthError for 6 error types, ?next= param handling, ?passwordUpdated=true banner, rate limit display), /signup (fullName+email+password+confirmPassword+terms checkbox, 4-segment password strength bar with Weak/Fair/Good/Strong colors, createTenantForUser server action creates tenants+tenant_members+tenant_subscriptions rows, email-already-exists links to /login?email=, mapSignupError for 6 error types), /reset-password (request form + State B success with 60s resend cooldown, security: success shown even for unknown emails), /reset-password/confirm (State A token valid with new password form, State B token expired with link to request new, State C loading/validating spinner, PASSWORD_RECOVERY auth state event handler, signOut after successful update); shared AuthCard component with 6px CI left stripe (3 bands: Aqua 30% / Periwinkle 35% / Aqua 60%), FormInput with 4 states (default/focus/error/disabled), PasswordInput with show/hide toggle, AlertBanner with 3 types (error/success/info), 44px touch targets everywhere; /api/auth/callback route for PKCE code exchange; responsive at ≤900px (card padding 40px→32px, touch targets ≥44px maintained); accessibility: focus management on error (focus moves to AlertBanner or first error field), all ARIA labels enumerated, screen reader text for loading spinner and password strength; WCAG AA contrast ratios verified (all pass); toast spec (sonner, 4000ms, border-radius 0); 18 edge cases documented; complete file structure (app/(auth)/layout.tsx + 4 page files + 6 component files + 1 server action) |
| 22 | 2026-03-13 | 4.1 — Landing page | 1 run | Created frontend/landing-page.md; 9 sections: nav bar (64px sticky frosted glass, logo+links+CTA), hero (Tier 1 animated blobs with 4 keyframe animations + dots texture overlay, content at bottom, headline "Your Discord server, powered by Claude.", subheadline, 2 CTA buttons + social proof line), how it works (3-step grid with step numbers in 80px Archivo Expanded aqua, CI accent stripe cards), features grid (9 cards: Developer Tools, Time & Tasks, Knowledge & Research, Calendar, Files & Docs, Memory, Communication, Media, BYOK card with periwinkle tint), integrations strip (11 service logos at 50% opacity, scrollable), pricing (billing toggle monthly/annual, 3 cards: Free $0, Starter $12/mo navy bg with "Most Popular" badge, Pro $39/mo), FAQ (12 questions with full answers), final CTA banner (navy bg, aqua blob decoration), footer (5-col grid, 4 link columns, legal, social); all exact copy written; blob CSS keyframes (drift-teal/drift-periwinkle/drift-navy-center/drift-navy-right) with pixel ranges; prefers-reduced-motion media query; complete OG image spec (1200×630, navy bg); performance requirements (LCP<2.5s, CLS<0.1); smooth scroll with scroll-margin-top:80px offset; scroll-triggered animation spec with IntersectionObserver; all responsive breakpoints (≤900px) per section |
| 21 | 2026-03-13 | 3.7 — Migration plan | 1 run | Created database/migrations.md; 7 migrations in order: 20260400000000_create_enums (8 enum types with IF NOT EXISTS guards + rollback), 20260400000001_create_tenants (DDL + 3 indexes + updated_at trigger + 4 RLS policies + Realtime publication), 20260400000002_create_tenant_members (DDL + 3 indexes + 4 RLS policies), 20260400000003_create_discord_connections (DDL + 4 indexes + updated_at trigger + get_decrypted_secret SECURITY DEFINER function with REVOKE from anon/authenticated + 4 RLS policies + Realtime), 20260400000004_create_tenant_api_keys (DDL + 2 partial indexes + updated_at trigger + 1 SELECT-only RLS policy with INSERT/UPDATE/DELETE intentionally blocked + Realtime), 20260400000005_create_tenant_service_connections (DDL + 3 indexes including partial for Google token expiry + updated_at trigger + 4 RLS policies + Realtime + pg_cron Google token refresh job using net.http_post), 20260400000006_create_tenant_subscriptions (DDL + 5 indexes including partial for dunning states + updated_at trigger + sync_tenant_plan_from_subscription() SECURITY DEFINER trigger function + AFTER INSERT OR UPDATE OF status/plan trigger + 1 SELECT-only RLS with writes blocked + no Realtime); all migrations additive-only (no existing table modifications); prerequisites documented (Vault enable, pg_cron extension, update_updated_at_column() pre-existence); verification SQL (7 queries: tables exist, enums exist, RLS enabled, function exists, trigger exists, Realtime publications, cron job); full rollback procedure (reverse order, all objects); migration file placement path documented |
|---|-----------|--------|----------|--------------|
| 20 | 2026-03-13 | 3.6 — Complete RLS policies | 1 run | Created database/rls-policies.md; 7 tables covered: tenants (4 policies: select_member/insert_owner/update_admin/delete_owner), tenant_members (4 policies: select/insert_admin/update_owner/delete_owner with self-removal prevention), discord_connections (4 policies: select/insert/update/delete for JWT; bot writes service-role bypass; column-level restriction at API route level not DB), tenant_api_keys (1 policy: select only; INSERT/UPDATE/DELETE blocked for all JWT users; all writes via Edge Functions + service role), tenant_service_connections (4 policies: select/insert/update/delete; owner+admin for writes), tenant_subscriptions (1 policy: select only; UPDATE blocked for all JWT — most critical security constraint, prevents self-upgrade), admin_audit_log (no policies — full default deny for JWT; service role only via admin panel); RLS architecture overview: 2 trust levels (JWT vs service role); membership subquery pattern; default deny semantics; complete SQL blocks for all ENABLE ROW LEVEL SECURITY statements; complete SQL block for all policy CREATE statements in dependency order; service role bypass table (12 operations); performance analysis of membership subquery; EXISTS vs IN pattern tradeoffs |
| 19 | 2026-03-13 | 3.5 — tenant_subscriptions + admin_audit_log tables | 1 run | tenant_subscriptions: 14 columns (id/tenant_id/stripe_customer_id/stripe_subscription_id/stripe_price_id/plan/status/cancel_at_period_end/current_period_start/current_period_end/canceled_at/trial_start/trial_end/created_at/updated_at); UNIQUE(tenant_id) + UNIQUE(stripe_subscription_id); CHECK constraint enforces free-tier sentinel (NULL sub_id + plan='free' + status='active'); 3 triggers: updated_at, sync_tenant_plan (propagates plan→tenants.plan via AFTER UPDATE OF plan, only when OLD.plan IS DISTINCT FROM NEW.plan), suspend_tenant_on_unpaid (unpaid→tenants.status='suspended'; recovery path: unpaid→active→tenants.status='configured'); 5 indexes including partial for cancel_at_period_end and non-active status; Stripe webhook handler specs for all 7 events (checkout.session.completed, customer.subscription.created/updated/deleted, invoice.payment_failed/succeeded, customer.subscription.trial_will_end); STRIPE_PRICE_TO_PLAN map; complete TypeScript implementations for 3 critical handlers; past_due does NOT suspend (grace period); only unpaid suspends; RLS: SELECT for JWT members, INSERT/UPDATE/DELETE blocked (service role only); row pre-created at signup with plan='free' sentinel; admin_audit_log: 9 columns (id/admin_user_id/action CHECK IN(10 values)/tenant_id NULL no-FK/target_user_id NULL/metadata JSONB/ip_address/user_agent/created_at); append-only; tenant_id intentionally NOT a FK (preserves records after tenant deletion); 8 action types with per-action metadata schemas; 3 indexes; RLS blocks all JWT access (service role admin panel only); impersonation session lifecycle documented |
| 18 | 2026-03-13 | 3.4 — tenant_service_connections table | 1 run | 4 services at launch: github (oauth), google (oauth), linear (oauth), toggl (api_key); 2 new enums: service_auth_type ('oauth','api_key'), service_connection_status ('connected','expired','revoked','error'); UNIQUE (tenant_id, service); CHECK constraint locks service↔auth_type pairing; vault_secret_id (access/api key) + refresh_vault_secret_id (google refresh token only); token_expires_at for Google OAuth only; scopes TEXT[] for OAuth audit; metadata JSONB with per-service schemas (github: login/user_id/name; google: email/user_id/name; linear: team_id/team_name/viewer_id/org_id; toggl: workspace_id/org_id/workspace_role); 3 indexes; updated_at trigger; 4 RLS policies (SELECT: all members; INSERT/UPDATE/DELETE: owner+admin); vault name patterns with :access/:refresh/:key suffixes; orphan cleanup query updated; Google token refresh logic (30-min pg_cron job, /api/integrations/google/refresh route); bot hot-reload via Realtime on INSERT/UPDATE; bot startup LEFT JOINs for all 4 services; TenantConfig extended for github_token, toggl_api_key, google_token fields |
| 1 | 2026-03-13 | 1.1 — Supabase schema extraction | 1 run | 31 migrations read; 10 live tables (discord_channel_mapping, session_templates, bluedot_transcripts, user_identity_discord, user_credentials, user_profiles, admin_impersonation_sessions, direct_message_sessions, conversation_skills, scheduled_tasks); 1 view (bluedot_accessible_meetings); 1 storage bucket (discord-archive); 2 roles (bluedot_webhook_writer, discord_bot_reader with LOGIN); 6 tables dropped in 20260224; Vault pattern established in user_credentials; no existing tenant_id columns; new tables (tenants, tenant_members, discord_connections, tenant_api_keys, tenant_service_connections, tenant_subscriptions) required |
| 2 | 2026-03-13 | 1.2 — MCP tool catalog extraction | 1 run | Bot codebase at projects/decision-orchestrator/apps/bot/src_v2/; 89 tools in ALL_TOOLS + 6 remote Linear tools = 95 total; platforms: Dub(2), Discord(7), Fly(9), Bluedot(4), Onyx(2), GitHub(1), Credentials(1), ACP(4), LinkedIn(17), Decision Hub(4), Toggl(34), Google Analytics(4); Linear registered via remote MCP proxy not in ALL_TOOLS; per-user credentials for GitHub+Toggl; system credentials for all others; complete ToolContext, UserContext, DatabaseContext documented; multi-tenant implications for each ToolContext field documented |
| 3 | 2026-03-13 | 1.3 — Auth model extraction | 1 run | Bot source not in CI; reconstructed from schema + tools catalog + design spec; 4 auth tables: user_identity_discord (Supabase↔Discord link), user_credentials (Vault refs), user_profiles (is_admin), admin_impersonation_sessions; UserContext built per-request from discord_id lookup; CredentialPlatform enum: GITHUB + TOGGL (only two requires_credential gates); Vault decryption via vault.decrypted_secrets; website auth: Supabase Auth email/password, @supabase/ssr cookies, Next.js middleware, tenant resolution via tenant_members; admin gate: user_profiles.is_admin in middleware + component; credential lifecycle change: per-user (user_credentials) → per-tenant (tenant_api_keys, tenant_service_connections); all auth error scenarios documented |
| 4 | 2026-03-13 | 1.4 — Bootstrap config & client factories | 1 run | Bot source not in CI; reconstructed from ToolContext in existing-tools.md + design spec; 19 env vars total (14 required, 5 optional); ToolContext frozen dataclass fully documented with all 19 fields; UserContext construction 5-step lookup chain; DatabaseContext + SQLAlchemy session factory; 4 client factories (Supabase service-role, Discord with intents, Anthropic, SQLAlchemy engine); FastAPI health endpoint; 10-step startup sequence; 11-step message handler pipeline; per-tenant vs system-level classification for every ToolContext field; BYOK implication: anthropic_api_key MUST come from tenant DB; Langfuse env vars documented; complete env var list for Fly.io deployment |
| 5 | 2026-03-13 | 1.5 — Bot startup, Discord connection, message handler pipeline | 1 run | Source verified from actual files in projects/decision-orchestrator/; run_discord_bot() full sequence (NullPool critical detail); create_bot() signature and cog loading order; Discord intents (message_content=True, dm_messages=True); setup_hook sequence (AuthCog+AdminCog conditional on signing_key, DmCog always, ScheduleCog always, guild sync strategy); on_ready background tasks (archive_sync_loop + scheduler_loop); on_message decision tree 8-branch fully documented; execute_with_routing 7-step pipeline with thread creation patterns; handle_dm_message DM session boundary via started_at; all slash commands (/connect 3 flows, /impersonate 3 subcommands, /new, /schedule); status embed lifecycle (CancelView button, update/delete pattern); message history collection (included vs skipped types, thread starter quirk); GateRegistry eviction (30min idle, 100 access check); preprocessing mention resolution XML format; reply context XML format; execute_message() full pipeline (tool selection, skill download, prompt variant, SDK dispatch); ExecutionResult dataclass; scheduler_loop 60s poll; DM delivery via LayoutView Components v2 with 3950 char truncation; web entrypoint FastAPI routes documented |
| 6 | 2026-03-13 | 1.6 — ORM models and repository patterns | 1 run | 9 ORM models: UserProfileORM, UserIdentityDiscordORM, UserCredentialORM (metadata_ alias), DirectMessageSessionORM, ScheduledTaskORM, AdminImpersonationSessionORM, ConversationSkillORM, SessionTemplateORM, DiscordChannelMappingORM + 2 Bluedot models (BluedotTranscriptORM + BluedotMeetingORM for view); 9 repository modules all returning Pydantic at boundaries; Vault repo uses raw SQL (separate schema); DM upsert uses pg INSERT ON CONFLICT; credential upsert uses Python select-first; Bluedot upsert uses COALESCE merge for multi-webhook; TogglCredentialMetadata typed metadata model; table-by-table multi-tenant migration implications documented; 5 new tables needed: tenants, tenant_members, tenant_api_keys, tenant_service_connections, tenant_subscriptions |
| 7 | 2026-03-13 | 1.7 — PyMC brand guidelines extraction | 1 run | 6 colors with hex values (Navy #0C1F40, White #FFFFFF, White Soft #F7F7F7, Aqua #B4E7DD, Periwinkle #9FAAE2, Peach #F6AE72); 60-70/20-30/5-10 ratio rule; Peach restricted to data viz only; 3 fonts (Archivo variable wdth:62.5-125, Inter, Lora); full type scale with clamp() values; spacing system (12/16/24/32/48/64px); button specs (44px/38px heights, 0 radius, 1.5px border); card CI accent stripe (6px, 3 bands with Aqua/Periwinkle at specific opacities); nav specs (64px desktop, 56px mobile, frosted glass); footer (Navy bg, 5-col grid); blob animations (5 named drift keyframes); Tier 1/2 gradient system; Tailwind config mapping; Daimon-specific application notes |
| 8 | 2026-03-13 | 1.8 — Design spec seeding | 1 run | 10 core product decisions; full tech stack; 5 new DB tables with column details; 4 user flows (Sign Up→Live, Service Connection, Billing, Dashboard); 8 routes (5 public + 3 authenticated + 2 admin); bot integration contract (7 website→bot triggers, 5 bot→website writes); tenant lifecycle state machine (4 states); security model (6 concerns); deprecated systems (Workflows, Composio); docs structure outline |
| 9 | 2026-03-13 | 2.1 — Multi-token connection manager | 1 run | Why single-tenant can't scale (blocking run(), frozen ToolContext, no tenant isolation); TenantConnectionManager class with full interface; TenantConfig + ActiveTenant dataclasses; TenantToolContext (per-tenant discord_token + anthropic_api_key + tenant_id) + SystemEnv (platform-level vars); per-tenant supervisor task with exponential backoff reconnect (10 max); on_ready handler writes bot_user_id + bot_username to DB; on_message handler with guild scope check; Supabase Realtime INSERT/UPDATE handlers for dynamic tenant add/reconnect; heartbeat loop 30s interval; stale detection SQL (120s threshold, effective_status computed in frontend); startup query (joins discord_connections + tenants + tenant_api_keys); startup staggering >100 tenants; graceful SIGTERM shutdown; all error scenarios (LoginFailure non-retry, PrivilegedIntentsRequired non-retry, transient retry, max retries, missing Anthropic key, Vault failure, guild not found, bot kicked); concurrency limits (500 tenants, pool_size=30); 4 new files documented |
| 10 | 2026-03-13 | 2.2 — Per-tenant tool scoping | 1 run | ToolContext partitioned into 4 categories: (A) per-tenant (discord_token, guild_id, anthropic_api_key), (B) per-tenant optional (linear_api_key/team_id from tenant_service_connections), (C) platform-level (fly, onyx, toggl, supabase), (D) platform-admin only (linkedin, ga, dub); TenantConfig + SystemConfig namedtuples; build_tenant_tool_context() function; per-tenant ToolRegistry in dict keyed by tenant_id; 3 new Scope tags (PLATFORM_ADMIN, PLAN_STARTER, PLAN_PRO); gate order in call_tool(); plan-based gating (free=discord only, starter=fly+onyx+github+toggl, pro=linear+bluedot+acp+decision_hub); UserContext gains tenant_id field; multi-tenant user_credentials design (global per user, cross-tenant by design); DB table tenant scoping documented; credential update propagation via Realtime → refresh_tenant_context(); 8 isolation guarantees documented; full end-to-end execution flow; 6 files to create/modify |
| 11 | 2026-03-13 | 2.3 — BYOK key routing per tenant | 1 run | Current single-tenant: ANTHROPIC_API_KEY env var → ToolContext.anthropic_api_key → injected into Fly Machine env at session launch; why BYOK is impossible single-tenant (5 problems); storage: tenant_api_keys table with Vault encryption + key_hint (8+4 chars); TenantConfig expanded with anthropic_api_key + openai_api_key fields; _load_tenant_config() full async implementation; _decrypt_vault_secret() via get_decrypted_secret() Postgres SECURITY DEFINER function; _build_tool_context() injects BYOK key + system-level keys from env; Fly Machine injection: ANTHROPIC_API_KEY + OPENAI_API_KEY as Machine env vars; hot-reload via Supabase Realtime on tenant_api_keys INSERT/UPDATE; _on_api_key_change() handler replaces TenantConfig + ToolContext atomically; key lifecycle state machine (active→invalid via async 401 detection, invalid→active via replacement); 2 validation endpoints (POST /api/keys/validate-anthropic + POST /api/keys/validate-openai); buildKeyHint function; Edge Function store-tenant-api-key with JWT ownership check; all error messages (7 Anthropic + 2 OpenAI); Replace Key modal flow; OpenAI optional BYOK (falls back to Haiku if absent); security properties table (7 properties); async invalidity detection via fly session launch error handler |
| 12 | 2026-03-13 | 2.4 — Supabase Realtime subscription design | 1 run | 4 channels total: tenant-lifecycle (discord_connections INSERT/UPDATE), tenant-api-keys (tenant_api_keys INSERT/UPDATE), tenant-status (tenants UPDATE), tenant-service-connections (tenant_service_connections INSERT/UPDATE/DELETE); exact JSON payload shapes for all 8 event types; REPLICA IDENTITY FULL requirement for all 4 tables (SQL provided); complete handler logic for all 16 event cases; idempotency rules per handler; duplicate event handling (at-least-once delivery); reconciliation pass after WebSocket reconnect; channel health monitoring loop (60s); channel state values (joined/joining/leaving/closed/errored); service role bypasses RLS; Realtime publication verification SQL; supabase_realtime publication add table SQL; complete event table (16 rows: table × event × trigger → bot action); _subscribe_to_all_realtime() sequence; _monitor_realtime_health() background coroutine; _on_realtime_reconnect() reconciliation |
| 13 | 2026-03-13 | 2.5 — Heartbeat + health monitoring | 1 run | ConnectionStatus enum (4 values + computed 'stale'); effective_status state machine with 2 stale thresholds (120s warning, 600s critical); heartbeat_writer() full coroutine spec (30s interval, non-fatal on failure); real-time dashboard update via client-side Realtime subscription; stale detection algorithm (computed at query time, not stored); stale does NOT trigger auto-reconnect (by design); Force Reconnect API route (POST /api/discord/reconnect); reconnect backoff table (10 attempts, 5s→300s schedule, ~25 min total); 3 non-retriable exceptions with exact error messages; recovery after bot restart (fresh retries for error-state tenants); health_server.py full FastAPI spec (3 endpoints: /health, /health/tenants, /health/tenant/{id}); TenantHealthItem + HealthResponse Pydantic models; fly.toml health check config; get_status() implementation; Langfuse per-tenant trace metadata; admin panel aggregate health query SQL; admin alert thresholds (5 severity levels); admin force reconnect API route; Realtime subscription health tracking (_realtime_connected flag); all health data points table (10 rows: data point × location × writer × reader × purpose); constants reference file |
| 15 | 2026-03-13 | 3.1 — tenants + tenant_members tables | 1 run | Created final-mega-spec/database/schema.md; 5 new enum types (tenant_plan, tenant_status, tenant_member_role, discord_connection_status, service_connection_status, subscription_status, api_key_type); tenants table: 8 columns fully specified (id/name/owner_id/plan/status/stripe_customer_id/created_at/updated_at), 4 indexes, 1 trigger, 4 RLS policies, notes on plan denormalization + status lifecycle + tenant deletion + bot startup query; tenant_members table: 5 columns (tenant_id/user_id/role/invited_by/created_at), composite PK, 3 FKs (ON DELETE CASCADE + SET NULL), 3 indexes (user_id + tenant_id + composite role), 4 RLS policies, notes on owner row creation + RLS self-reference + uniqueness + owner transfer + single seat enforcement; cross-references to rls-policies/triggers/migrations/multi-tenant docs |
| 14 | 2026-03-13 | 2.6 — Tenant isolation boundaries | 1 run | Logical multi-tenancy model (shared infra, logical isolation); isolation dimension table (11 rows: Discord/Anthropic key/conversation/DB/RLS/service role/tool execution/platform infra/platform creds/platform-admin tools/Langfuse); shared-across-tenants inventory: code/event loop/SQLAlchemy pool/Supabase client/Fly.io app+org+IP/Vercel/Stripe/Langfuse; platform-level credentials table (9 env vars same for all); platform-admin-only credentials (6: LinkedIn×3, GA×2, Dub); defense-in-depth hardened build_tenant_tool_context() (empty strings for non-admin creds); per-tenant isolated: Discord client (physical), BYOK key (logical), DB rows (RLS+tenant_id), conversation context (per-channel/ephemeral), ToolRegistry (dict keyed by tenant_id), Langfuse traces (tenant_id tag); failure isolation: _safe_supervisor() exception boundary; Fly session Machine crash isolation; Anthropic rate limit isolation; shared asyncio event loop risk (low); service role trust model (bot bypasses RLS, enforces in app code); DB audit: 12 service-role operations documented; website uses anon key + JWT + RLS; platform-admin tenant identification (PLATFORM_ADMIN_TENANT_ID + PLATFORM_ADMIN_GUILD_ID env vars); admin capabilities table; 10-row isolation boundary checklist; known limitations table (6 rows: shared event loop, pool contention, shared Fly IP, memory, Langfuse content, Onyx/Toggl shared); |
| 17 | 2026-03-13 | 3.3 — tenant_api_keys table | 1 run | 9 columns: id (UUID PK), tenant_id (FK→tenants CASCADE), key_type (api_key_type ENUM: anthropic/openai), vault_secret_id (UUID NOT NULL, logical FK vault.secrets), key_hint (TEXT NOT NULL, masked 8-30 chars), status (TEXT: active/invalid/revoked), validated_at (TIMESTAMPTZ NULL), created_at/updated_at; UNIQUE(tenant_id, key_type) enables UPSERT pattern; status is TEXT+CHECK (not ENUM) for easier future extension; 2 partial indexes (invalid keys, active by type); Realtime on INSERT+UPDATE for hot-reload; RLS: SELECT for JWT members, INSERT/UPDATE/DELETE BLOCKED for all JWT (service role only via Edge Functions); 3 Edge Functions specced: store-tenant-api-key, revoke-tenant-api-key + store-discord-token (Discord token counterpart); full revocation flow (confirm dialog → API route → Edge Function → vault.delete_secret → Realtime → bot disconnect → dashboard update); bot startup JOIN query (inner on anthropic, left on openai); 9 notes (row count, no hard delete, no delete on revocation, Vault naming, hint safety, async invalidity, TEXT vs ENUM, admin visibility, audit log hint); created vault-encryption.md: Vault schema (vault.secrets + vault.decrypted_secrets), 3 Vault functions (create_secret/update_secret/delete_secret), 3 wrapper functions (get_decrypted_secret/create_tenant_secret/delete_tenant_secret with SECURITY DEFINER, REVOKE from authenticated), both tables using Vault documented, orphan cleanup detection query, what NOT in Vault table, security properties summary |
| 16 | 2026-03-13 | 3.2 — discord_connections table | 1 run | 10 columns: id (UUID PK), tenant_id (FK→tenants CASCADE), guild_id (TEXT NOT NULL, snowflake format CHECK '^[0-9]{17,20}$'), vault_secret_id (UUID NOT NULL, logical FK to vault.secrets), status (discord_connection_status NOT NULL DEFAULT 'pending'), last_heartbeat (TIMESTAMPTZ NULL), error_message (TEXT NULL, length ≤ 500), bot_user_id (TEXT NULL, snowflake CHECK), bot_username (TEXT NULL, length ≤ 100), created_at/updated_at (TIMESTAMPTZ); 2 constraints: UNIQUE(tenant_id, guild_id) prevents duplicate guild per tenant; 4 indexes (tenant_id, tenant+status, active partial, tenant+last_heartbeat WHERE connected); Realtime on INSERT (new tenant connects) and UPDATE (disconnect/token update); RLS: select/insert/delete for owner+admin JWT, update restricted to guild_id+vault_secret_id fields (bot-only columns protected at route level), service role bypasses for bot writes; pending→connecting→connected status transitions; bot writes bot_user_id+bot_username on on_ready; complete connection lifecycle state machine; 8 edge case notes (Vault FK, multi-connection limits by plan, pending vs connecting distinction, vault_secret_id never-null, bot identity persistence, admin suspension flow, guild ID validation, startup query reference); updated Cross-References section in schema.md |

| 41 | 2026-03-13 | 4.9e — Component library: Action components — Button, IconButton, Link, DropdownMenu, Tabs + brand compliance matrix | 1 run | Appended Section 5 (Action Components) + Section 6 (Brand Compliance Matrix) to frontend/component-library.md. Button: props interface with variant/size/isLoading/disabled/leftIcon/rightIcon/fullWidth/type; 3 sizes (sm 32px, md 44px, lg 52px) with padding/font/weight/icon-size table; 5 variants (primary/secondary/ghost/danger/danger-secondary) with full color tables for both light and dark context; all states (default/hover/focus-visible/active/disabled/loading); LoadingSpinner sub-component TSX; where-used table 21 rows; accessibility table. IconButton: required aria-label (TypeScript enforced); 4 sizes (xs/sm/md/lg 24px-48px); 4 variants (primary/secondary/ghost/danger); all states; where-used table 14 rows. Link: 5 variants (default/nav/muted/underline/unstyled); 3 sizes; external link behavior (auto ExternalLinkIcon, target blank, rel noopener); disabled renders as span; where-used table 10 rows. DropdownMenu: DropdownMenuItem union type (item/separator/label); panel styling (white bg, 1px border navy 10%, box-shadow navy-tinted, 0px radius); item dimensions (36px height, 0 radius); danger variant (red text/icon); all states table; open/close keyboard nav (arrows/home/end/enter/space/escape); animation (150ms fade+shift); Radix UI implementation note; where-used table 5 rows; ARIA roles (menu/menuitem/aria-haspopup/aria-expanded). Tabs: 3 variants (underline/pills/bordered); TabItem type with badge support; underline (bottom 2px Aqua active border, margin-bottom -1px); pills (gray container bg, white active bg, box-shadow); bordered (full border, active Aqua 12% bg); badge pill (Aqua 30% tint, 0 radius); fullWidth via flex:1; controlled component design (no internal state); URL tab persistence example; where-used table 4 rows; full ARIA roles (tablist/tab/tabpanel/aria-selected/aria-controls/aria-labelledby). Brand Compliance Matrix: 8 brand rules enumerated (R1-R8: 0 radius/Aqua primary/no Peach/Inter/Archivo/Navy/0.2s transition/Aqua focus ring); 32-component matrix across all 5 library sections; common violations table (9 violation types with symptom + correction); Tailwind config verification (borderRadius overrides, ringColor default Aqua, peach-orange intentionally excluded); CSS custom properties definition block (20 design tokens). |

| 44 | 2026-03-13 | 4.12 — Responsive behavior — every page at mobile (375px), tablet (768px), desktop (1280px) | 1 run | Created frontend/responsive-behavior.md; breakpoint system: mobile <768px / tablet 768-1279px / desktop ≥1280px (auth+landing use 900px custom); touch target rules (44px min for all interactive elements); typography scaling table (all elements with clamp() values); global dashboard shell (sidebar 240px desktop / 56px icon-only tablet / hidden+bottom-nav mobile); bottom nav bar spec (56px, 5 icons, active indicator, item states); MobileNav slide-over spec; public shell nav; page-by-page: landing (9 sections with exact property tables: nav, hero, how-it-works, features, integrations strip, pricing, FAQ, CTA, footer); auth pages (≤900px single breakpoint, card sizing, form behavior); dashboard home (3-breakpoint grid specs with ASCII diagrams); integrations page (2-col→1-col + bottom sheet on mobile); billing page (3-col→2-col→1-col plans, ApiKeyRow stacking); settings page (horizontal rows→stacked on mobile); admin panel (desktop-only, blocking message at <1024px); docs pages (sidebar→horizontal tabs→hamburger slide-over); 10 component-level responsive rules (Sidebar/DashboardTopbar/Table/Modal/AlertBanner/StatCard/Pagination/Button/DropdownMenu/Toast); Tailwind CSS breakpoint config (sm:640/md:768/lg:1024/xl:1280); viewport meta tag (maximum-scale=5, no user-scalable=no); content reflow at 200% zoom; summary table (all 8 pages × 4 nav/mobile/tablet/desktop columns) |
