[package]
name = "irontide-core"
version = "0.1.0"
edition = "2021"
description = "Iron Tide RTS - Core game simulation engine"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
