---
phase: 08-remaining-unblocked-legal-tax-bridge-defects
plan: 08
type: execute
wave: 5
depends_on: ["08-06", "08-07"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LAW-05, LAW-08, LAW-09, LAW-10, LAW-11]
user_setup: []

must_haves:
  truths:
    - "The frontend suite was measured against a WASM binary rebuilt from the fixed engine, not the pre-phase one"
    - "The frontend failure set is compared line by line against the five Phase 5 left behind, and any growth is reported rather than absorbed"
    - "Each of the five requirements is ticked only after the command that proves it was run and its output read"
    - "The runner's halt at G3 is reported as Phase 5's unresolved decision, never as a Phase 8 result"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAW-05, LAW-08, LAW-09, LAW-10 and LAW-11 marked complete with their traceability rows updated"
      contains: "LAW-11"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "engine/tests/integration.rs"
      via: "each ticked LAW requirement is proved by a named vector observed passing in this plan"
      pattern: "LAW-05"
    - from: "frontend/src/wasm/pkg/inheritance_engine_bg.wasm"
      to: "frontend/src/wasm/__tests__/wasm-real.test.ts"
      via: "the rebuilt binary the frontend suite loads through readFileSync"
      pattern: "build-wasm"
---

<objective>
Close the phase against commands whose output was read, not against plan summaries.

Seven plans changed engine behaviour and frontend behaviour and proved each change against its own
narrow suite. Three things are still unproved at phase level. First, the frontend suite has been
running against a WASM binary built before any of this phase's engine changes, so its judgement of the
engine is stale. Second, the five frontend tests Phase 5 left red are the only failures the gate should
report, and nobody has yet compared this phase's failure set against that list as a whole. Third,
`.planning/REQUIREMENTS.md` still shows all five requirements as pending.

`08-RESEARCH.md` §0 records the inherited state: `bash scripts/ci-gates.sh` has halted at `G3`, gate 8
of 13, since Phase 5, on `src/__tests__/integration.test.tsx`, `src/wasm/__tests__/bridge.test.ts` and
`src/wasm/__tests__/wasm-real.test.ts` — none of which appears in `frontend/test-baseline.json`. Phase 8
neither touches those tests nor resolves the OBS-05 and OBS-06 product decision behind them.

Output: a rebuilt WASM artifact, a measured comparison of the frontend failure set, a full gate run
with every exit code recorded, and five requirements ticked on the strength of that evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PLAN-STANDARD.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-VALIDATION.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-01-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-02-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-03-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-04-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-05-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-06-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-07-SUMMARY.md
@.planning/REQUIREMENTS.md
@frontend/test-baseline.json
@gates.manifest.json
@GATES.md
@engine/build-wasm.sh
@frontend/.gitignore
</context>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. Commit arguments are
   repo-root-relative; every other path in this plan is relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken or loosen a test, an assertion or a gate. A gate that cannot legitimately
   pass is reported BLOCKED with the real pasted command output.
4. **A newly failing frontend test is a BLOCKED condition, not a licence to edit anything.** Do not
   append an entry to `frontend/test-baseline.json`, do not edit a frontend test, and do not edit
   `gate-skips.lock`. If the failure set grew, stop and report BLOCKED with the pasted output naming
   every new failure.
5. **No point of Philippine law may be decided.** This plan runs commands and ticks checkboxes. If a
   question arises, stop, report BLOCKED, and append it to `.planning/LAWYER-AGENDA.md` without
   answering it.
6. Only one file may be edited: `.planning/REQUIREMENTS.md`. Do not edit anything under `engine/`,
   `frontend/`, `specs/`, `scripts/`, `gates.manifest.json`, `gates.manifest.lock`, `gate-skips.lock`,
   `frontend/test-baseline.json`, `engine/defect-baseline.json`, `assertion-baseline.json` or
   `GATES.md`.
7. `frontend/src/wasm/pkg/inheritance_engine_bg.wasm` is a build artifact ignored by
   `frontend/.gitignore` and must not be staged. Rebuilding it is a verification step, not a
   deliverable.
8. Do not tick any checkbox other than the five named in task 3. In particular `OBS-05` and `OBS-06`
   remain unticked; this phase did not touch them. `LAW-06`, `LAW-07` and `LAW-12` also remain
   unticked; all three are blocked on lawyer answers and owned by Phase 14.
9. `bash scripts/ci-gates.sh` is expected to halt at `G3` for Phase 5's unresolved OBS-05 and OBS-06
   product decision. **`ALL GATES PASSED (13/13)` is not achievable in this phase and must not be
   claimed in any summary.** The five gates the runner never reaches must be run directly and their
   exit codes recorded.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the WASM artifact and measure the frontend against the fixed engine</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>engine/build-wasm.sh, frontend/.gitignore, frontend/test-baseline.json, gates.manifest.json, GATES.md</read_first>
  <action>
The frontend suite loads a compiled WASM binary from `frontend/src/wasm/pkg/`. Until it is rebuilt, the
frontend is testing the engine as it stood before plans 08-01, 08-03, 08-05 and 08-07. Run:

    bash engine/build-wasm.sh

Confirm it exits 0 and that `frontend/src/wasm/pkg/inheritance_engine_bg.wasm` has a newer modification
time than the most recent edit under `engine/src/`. The script itself verifies existence, a 100 KB size
floor and the `0061736d` WebAssembly magic number, so wasm-pack exiting 0 is not accepted as proof on
its own. Do not stage the binary.

Then run the frontend gate exactly as gate `G3` runs it:

    cd frontend && npm run test:gate

Record its exit code and its complete failure list. Compare the observed set against the five failures
Phase 5 left behind:

- `src/__tests__/integration.test.tsx`
- `src/wasm/__tests__/bridge.test.ts`
- `src/wasm/__tests__/wasm-real.test.ts`

Write the comparison into the plan summary as a table: the observed failing file and test name, and
whether it is one of the five. State plainly whether the set is identical, and if it is not, name every
test that newly fails.

Also record the reported total test count. `frontend/scripts/check-test-baseline.mjs` treats
`min_total_tests` as a floor of 2416, and this phase added tests, so the total must be at least 2416
and is expected to be higher. A total **below** 2416 means tests were lost and is a BLOCKED condition.

Then run the two gates that sit ahead of `G3` in the runner and the engine suite, recording each exit
code:

    node scripts/check-gate-manifest.mjs
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs
    bash scripts/coverage-report.sh && node scripts/check-coverage.mjs
    node scripts/check-assertion-discipline.mjs
    cd engine && cargo test
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && bash engine/build-wasm.sh; echo "G2_EXIT=$?"; ls -l frontend/src/wasm/pkg/inheritance_engine_bg.wasm; (cd frontend && npm run test:gate); echo "G3_EXIT=$?"; node scripts/check-gate-manifest.mjs; echo "G5=$?"; node scripts/check-plan-closed-world.mjs; echo "G6=$?"; node scripts/check-commit-discipline.mjs; echo "G7=$?"; node scripts/check-assertion-discipline.mjs; echo "G13=$?"; (cd engine && cargo test 2>&1 | tail -12); git status --porcelain frontend/
  </verify>
  <acceptance_criteria>
    - `bash engine/build-wasm.sh` exits 0 and the rebuilt `inheritance_engine_bg.wasm` is newer than the most recent edit under `engine/src/`
    - The frontend failure set is compared against the five Phase 5 failures and the comparison table is pasted into the plan summary
    - The reported total test count is at least 2416
    - `node scripts/check-gate-manifest.mjs`, `check-plan-closed-world.mjs`, `check-commit-discipline.mjs` and `check-assertion-discipline.mjs` each exit 0
    - `git status --porcelain frontend/` shows no modified tracked file under `frontend/`
    - `frontend/test-baseline.json` and `gate-skips.lock` are unmodified, shown by an empty `git status --porcelain` for both
  </acceptance_criteria>
  <done>The frontend judges the fixed engine, and any regression it would reveal is surfaced rather than absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full gate set and the five gates the runner never reaches</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>gates.manifest.json, GATES.md, gate-results.json, .planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md</read_first>
  <action>
Run the full runner and record where it stops and why:

    bash scripts/ci-gates.sh

The expected outcome, unchanged since Phase 5, is a halt at `G3`, gate 8 of 13, on the five frontend
tests recorded in task 1. Record the exit code, the gate id it halted on, and the gates it reported as
passing before the halt.

Because the runner halts, gates ordered after `G3` are never executed and `gate-results.json` will
report them as `not-run`. Run each of them directly and record its exit code:

    cd frontend && npx tsc -b --force
    node scripts/check-lawyer-agenda.mjs
    node scripts/check-observability.mjs
    node scripts/check-gate-skips.mjs
    node scripts/check-gate-results.mjs

`node scripts/check-gate-results.mjs` is expected to fail with `RESULTS INCOMPLETE` while the run
halted early, because it rejects a run in which any gate other than itself is `not-run`. Record that as
a cascade of the halt rather than as an independent failure, exactly as Phases 5, 6 and 7 recorded it.

Write into the plan summary a table with one row per gate id, its order, the command, its exit code,
and whether it ran inside the runner or directly. State explicitly that
`ALL GATES PASSED (13/13)` was not reached and why.

If the runner halts at any gate **before** `G3`, stop and report BLOCKED with the pasted output. If
`G3`'s failure set has grown beyond the five, stop and report BLOCKED naming the new failures.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && bash scripts/ci-gates.sh; echo "RUNNER_EXIT=$?"; (cd frontend && npx tsc -b --force); echo "G4=$?"; node scripts/check-lawyer-agenda.mjs; echo "G10=$?"; node scripts/check-observability.mjs; echo "G11=$?"; node scripts/check-gate-skips.mjs; echo "G8=$?"; node scripts/check-gate-results.mjs; echo "G9=$?"
  </verify>
  <acceptance_criteria>
    - `bash scripts/ci-gates.sh` halts at `G3` and at no earlier gate, and its exit code is recorded
    - `cd frontend && npx tsc -b --force` produces zero output and exits 0
    - `node scripts/check-lawyer-agenda.mjs` and `node scripts/check-observability.mjs` each exit 0
    - The plan summary carries a per-gate table with an exit code for every one of the thirteen gates
    - The summary states that `ALL GATES PASSED (13/13)` was not reached, and attributes the halt to Phase 5's unresolved OBS-05 and OBS-06 decision
  </acceptance_criteria>
  <done>Every gate has a recorded exit code, and the one red gate is attributed to the phase that owns it.</done>
</task>

<task type="auto">
  <name>Task 3: Mark the five requirements complete and commit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, scripts/safe-commit.sh, .planning/PLAN-STANDARD.md</read_first>
  <action>
Only after tasks 1 and 2 have been run and their output read, edit `.planning/REQUIREMENTS.md`.

Each of the five is ticked only on the strength of a named command whose output was observed in this
phase. Record the proving evidence for each in the plan summary before ticking:

| Requirement | Proving evidence |
|---|---|
| LAW-05 | `test_law05a_preterition_preserves_a_non_inofficious_legacy`, `test_law05a_inofficious_legacy_is_reduced_not_dropped`, `test_law05b_collated_donation_defeats_preterition` and `test_law05b_exempt_donation_still_preterites_and_flags` observed passing in `cargo test --test integration` |
| LAW-08 | `npx vitest run src/lib/estate-tax-engine/__tests__/special-deductions.test.ts` observed passing with the medical deduction at 0 for a TRAIN death, and spec TV-02 stating ₱135,000 |
| LAW-09 | the two worked-example tests in `__tests__/ordinary-deductions.test.ts` observed asserting `item5g_vanishing_deduction.total` of `800_000_000` in both regimes |
| LAW-10 | the ₱30,000,000 worked-example test in `src/lib/__tests__/tax-bridge.test.ts` observed asserting charges `1524000000` and base `1476000000` |
| LAW-11 | `test_law11_reserva_troncal_fact_raises_a_flag` observed passing, together with the two `EstateStep.test.tsx` tests |

Then make the edits:

- In the Legal Conformance list, change `- [ ] **LAW-05**` to `- [x] **LAW-05**` and do the same for
  `LAW-08`, `LAW-09`, `LAW-10` and `LAW-11`. Do not change the requirement text on any of the five
  lines.
- In the traceability table further down, change the `Status` cell from `Pending` to `Complete` on the
  five rows whose first cell is `LAW-05`, `LAW-08`, `LAW-09`, `LAW-10` and `LAW-11`. Leave every other
  row untouched, including the `LAW-06`, `LAW-07` and `LAW-12` through `LAW-15` rows.
- Update the trailing `*Last updated: ...*` line to name Phase 8 execution and today's date, in the
  same shape the existing line uses.

Do not tick any other checkbox anywhere in the file. `OBS-05` and `OBS-06` stay unticked.

Then stage and commit the single edited file:

    bash scripts/safe-commit.sh -m "docs(08-08): close LAW-05, LAW-08, LAW-09, LAW-10 and LAW-11 against observed gate output" apps/inheritance/.planning/REQUIREMENTS.md
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -n "LAW-05\|LAW-08\|LAW-09\|LAW-10\|LAW-11" .planning/REQUIREMENTS.md | head -20; grep -c "^- \[ \] \*\*OBS-0[56]\*\*" .planning/REQUIREMENTS.md; grep -n "LAW-06\|LAW-07\|LAW-12" .planning/REQUIREMENTS.md | head -8; git log -1 --stat --format="%s"; node scripts/check-commit-discipline.mjs; echo "G7_EXIT=$?"; node scripts/check-plan-closed-world.mjs; echo "G6_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `LAW-05`, `LAW-08`, `LAW-09`, `LAW-10` and `LAW-11` each show `- [x]` in the Legal Conformance list and `Complete` in the traceability table
    - `OBS-05`, `OBS-06`, `LAW-06`, `LAW-07` and `LAW-12` are still shown as unticked and still `Pending`, verified by `git diff .planning/REQUIREMENTS.md`
    - `git log -1 --stat` shows exactly one changed file
    - `node scripts/check-commit-discipline.mjs` and `node scripts/check-plan-closed-world.mjs` each exit 0
    - The plan summary carries the proving-evidence table with one row per requirement
  </acceptance_criteria>
  <done>Five requirements are recorded complete on the strength of observed command output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine source → frontend test suite | The compiled WASM binary is the only path by which engine changes reach the frontend suite; a stale binary makes the suite's verdict about the engine false |
| plan summaries → `.planning/REQUIREMENTS.md` | A requirement checkbox is the project's durable claim that a defect is closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Spoofing | `frontend/src/wasm/pkg/inheritance_engine_bg.wasm` | mitigate | The binary is rebuilt and its modification time is compared against the most recent `engine/src/` edit, so a stale artifact cannot silently certify the frontend against the old engine |
| T-08-16 | Repudiation | `.planning/REQUIREMENTS.md` | mitigate | Each checkbox is ticked only after a named command was run and its output read, and the proving-evidence table naming those commands is written into the plan summary before the edit |
| T-08-17 | Tampering | `frontend/test-baseline.json` and `gate-skips.lock` | mitigate | Both are shrink-only ledgers and both are outside this plan's `files_modified`; a grown failure set is a BLOCKED condition with no path to a ledger append |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package. `frontend/package.json`, `frontend/package-lock.json`, `engine/Cargo.toml` and `engine/Cargo.lock` are not edited |
</threat_model>

<verification>

Run from `/home/clsandoval/cs/monorepo/apps/inheritance`:

    bash engine/build-wasm.sh
    cd engine && cargo test 2>&1 | tail -25
    cd frontend && npm run test:gate
    cd frontend && npx tsc -b --force
    bash scripts/ci-gates.sh
    node scripts/check-lawyer-agenda.mjs
    node scripts/check-observability.mjs
    node scripts/check-gate-skips.mjs
    node scripts/check-gate-results.mjs
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs

Expected: the WASM build exits 0; `cargo test` reports `0 failed` with at least 543 passing; `tsc`
produces zero output; `check-lawyer-agenda.mjs`, `check-observability.mjs`,
`check-plan-closed-world.mjs` and `check-commit-discipline.mjs` each exit 0.

`bash scripts/ci-gates.sh` is expected to reach `G3` and halt there with the same five frontend
failures Phase 5 left behind, and `node scripts/check-gate-results.mjs` is expected to report
`RESULTS INCOMPLETE` as a cascade of that halt. Both are the recorded, unchanged state of the runner and
are not Phase 8 regressions. **`ALL GATES PASSED (13/13)` is not achievable in this phase and must not
be claimed.** If the runner halts anywhere **before** `G3`, or if `G3`'s failure set has grown, stop and
report BLOCKED with the pasted output.

</verification>

<success_criteria>

- [ ] The WASM artifact was rebuilt and is newer than the most recent `engine/src/` edit
- [ ] The frontend failure set was compared against the five Phase 5 left behind and the comparison is
      in the plan summary
- [ ] The reported total test count is at least 2416
- [ ] Every one of the thirteen gates has a recorded exit code, whether it ran inside the runner or
      directly
- [ ] `frontend/test-baseline.json`, `gate-skips.lock`, `engine/defect-baseline.json` and
      `assertion-baseline.json` are all unmodified
- [ ] LAW-05, LAW-08, LAW-09, LAW-10 and LAW-11 are marked complete and no other checkbox moved
- [ ] OBS-05, OBS-06, LAW-06, LAW-07 and LAW-12 are still pending
- [ ] The commit lists exactly one file and `node scripts/check-commit-discipline.mjs` exits 0
- [ ] `ALL GATES PASSED (13/13)` was not claimed, and the halt at `G3` is reported as Phase 5's
      unresolved decision rather than as a Phase 8 result

</success_criteria>

<output>
Create `.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-08-SUMMARY.md` when done
</output>
