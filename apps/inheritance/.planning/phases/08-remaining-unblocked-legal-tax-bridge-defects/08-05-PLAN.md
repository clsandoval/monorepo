---
phase: 08-remaining-unblocked-legal-tax-bridge-defects
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - engine/tests/integration.rs
autonomous: true
requirements: [LAW-05]
user_setup: []

must_haves:
  truths:
    - "Each half of the preterition defect has a named regression vector asserting exact centavos"
    - "The vectors assert values measured before the fix landed, not values read back out of the fixed engine"
    - "No pre-existing expected value anywhere in the vector suite was edited"
    - "The whole 173-file committed corpus still conserves the estate and holds all sixteen named invariants"
  artifacts:
    - path: "engine/tests/integration.rs"
      provides: "Four named LAW-05 regression vectors with exact centavo assertions"
      contains: "test_law05a_preterition_preserves_a_non_inofficious_legacy"
  key_links:
    - from: "engine/tests/integration.rs"
      to: ".planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md"
      via: "each vector's comment cites the measured before-value from section 1.2 and the derivation from section 1.5"
      pattern: "08-RESEARCH"
    - from: "engine/tests/integration.rs"
      to: "engine/src/step7_distribute.rs"
      via: "the vectors exercise the preterition arm plans 08-01 and 08-03 installed"
      pattern: "IntestateByPreterition"
---

<objective>
Lock both halves of the preterition defect behind named regression vectors with exact centavo
assertions.

Plans 08-01 and 08-03 changed engine behaviour and proved each change against unit tests inside the
source files. A unit test inside `engine/src/` exercises a single step; it does not prove that the
whole ten-step pipeline produces the right per-heir peso figures. This plan converts the four
before-and-after pairs measured in `08-RESEARCH.md` §1.2 into named functions in
`engine/tests/integration.rs`, in that file's established style: one function per vector, exact
centavo assertions through `assert_total_centavos`, the sum invariant through `check_sum_invariant`,
and a header comment naming the governing article, the measured before-value, and the
`.planning/research/LEGAL-CONFORMANCE.md` §2a row the vector closes.

Every expected value below was measured in this tree before any fix landed, and is restated here so
the executor copies rather than derives. `08-RESEARCH.md` §1.2 records the four measured runs.

Output: four named vectors, plus a corpus re-measurement that proves the 36 committed preterition
inputs still conserve the estate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PLAN-STANDARD.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-VALIDATION.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-01-SUMMARY.md
@.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-03-SUMMARY.md
@.planning/research/LEGAL-CONFORMANCE.md
@engine/tests/integration.rs
@engine/tests/common/invariants.rs
@engine/src/types.rs
</context>

<interfaces>
Helpers already defined in `engine/tests/integration.rs`. Reuse them; do not add new ones.

```rust
fn person(id: &str, rel: Relationship) -> Person;
fn find_share(output: &EngineOutput, heir_id: &str) -> Option<&HeirShare>;
fn assert_total_centavos(share: Option<&HeirShare>, expected: i64, label: &str);
fn assert_vector_shape(
    output: &EngineOutput,
    scenario: ScenarioCode,
    succession: SuccessionType,
    row_count: usize,
    label: &str,
);
fn check_sum_invariant(output: &EngineOutput, estate: &Money);
fn check_scenario_consistency(output: &EngineOutput, expected: ScenarioCode);
```

That file holds its **own inline copy** of the ten-step pipeline, defined near `integration.rs:27` and
constructing `Step6Input` and `Step7Input` directly. Plans 08-01 and 08-03 added no field to either
struct, so the inline copy compiles unchanged and must not be edited.

From `engine/src/types.rs`, the disposition shapes these vectors build:

```rust
pub struct Legacy { pub id: DispositionId, pub legatee: HeirReference, pub property: LegacySpec, pub conditions: Vec<Condition>, pub substitutes: Vec<Substitute>, pub is_preferred: bool }
pub enum LegacySpec { FixedAmount(Money), SpecificAsset(AssetId), GenericClass(String, Money) }
pub enum ShareSpec { Fraction(Frac), EntireEstate, EntireFreePort, EqualWithOthers, Unspecified, Residuary }
```
</interfaces>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. Commit arguments are
   repo-root-relative; every other path in this plan is relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken or loosen a test, an assertion or a gate. **No pre-existing expected
   value anywhere in `engine/tests/integration.rs` may be edited.** `08-RESEARCH.md` §1.3 established
   that no committed vector in that file has a preterition shape with a legacy, so none of them moves.
   If one nevertheless fails, that is a defect in plan 08-01 or 08-03: stop, report BLOCKED with the
   pasted failure, and do not adjust the expectation.
4. **No point of Philippine law may be decided.** Every expected value below is either a measured
   before-value from `08-RESEARCH.md` §1.2 or an arithmetic consequence derived in §1.5 from Art. 854,
   Art. 888 and Art. 911. If a question arises that those sentences do not answer, stop, report
   BLOCKED, and append it to `.planning/LAWYER-AGENDA.md` without answering it.
5. **Pin no expected centavo value for a case whose only donation is one the Code exempts from
   collation and whose preterition outcome therefore turns on the open question.** The fourth vector
   below asserts the *flag* and the *succession type*, and asserts the sole heir's amount only because
   that amount is unchanged from the pre-fix measurement. It must not be presented as the answer to
   `LAWYER-09`, whose status in `.planning/lawyer-decisions.json` is `awaiting-answer`.
6. Only one file may be edited: `engine/tests/integration.rs`. Do not edit anything under
   `engine/src/`, `engine/examples/`, `engine/defect-baseline.json`, `frontend/`, `specs/`,
   `gates.manifest.json`, `gates.manifest.lock`, `gate-skips.lock`, `frontend/test-baseline.json` or
   `GATES.md`.
7. `cd engine && cargo test` must report **0 failed** with at least **542 passing** at the end of every
   task.
8. `bash scripts/ci-gates.sh` still halts at `G3` for Phase 5's unresolved OBS-05 and OBS-06 product
   decision, which this plan does not touch. `ALL GATES PASSED (13/13)` is not achievable and must not
   be claimed in any summary.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Two named vectors for the surviving-legacy half of LAW-05</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/tests/integration.rs, .planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md, .planning/research/LEGAL-CONFORMANCE.md</read_first>
  <action>
Append two vectors to `engine/tests/integration.rs`, after the existing `test_law04_*` block and before
the cross-cutting invariant tests, each with a banner comment in the file's established style naming
the requirement, the governing articles, the measured before-value, and the `LEGAL-CONFORMANCE.md` §2a
Art. 854 row it closes.

**`test_law05a_preterition_preserves_a_non_inofficious_legacy`** — estate
`Money::from_pesos(30_000_000)`, that is 3000000000 centavos. Family tree: two living
`Relationship::LegitimateChild` persons `ana` and `ben`. No spouse, no ascendants, no donations. A
will whose `institutions` holds one entry instituting a stranger with `person_id: None`, `name`
`"Kevin"`, and `share: ShareSpec::EntireFreePort`; whose `legacies` holds one entry `L1` with
`legatee.person_id` `Some("carlos")`, `property: LegacySpec::FixedAmount(Money::from_pesos(3_000_000))`
and `is_preferred: false`; and whose `devises` and `disinheritances` are empty.

Assertions:
- `check_sum_invariant` against the estate
- an assertion that `output.succession_type == SuccessionType::IntestateByPreterition`
- `assert_total_centavos(find_share(&output, "carlos"), 300000000, "LAW-05a carlos")`
- `assert_total_centavos(find_share(&output, "ana"), 1350000000, "LAW-05a ana")`
- `assert_total_centavos(find_share(&output, "ben"), 1350000000, "LAW-05a ben")`
- an assertion that no row has `heir_id == "Kevin"`, since Art. 854 annulled his institution
- an assertion that `output.warnings` contains an entry whose `category` is `preterition`

Comment the arithmetic: Art. 854 annuls the institution of heir but keeps devises and legacies valid
insofar as they are not inofficious; Art. 888 gives two legitimate children a collective legitime of
one half, so the free portion on a ₱30,000,000 estate is ₱15,000,000 and a ₱3,000,000 legacy is not
inofficious; the residue 3000000000 − 300000000 = 2700000000 divides equally between the two children
under Art. 980. Record the before-value from `08-RESEARCH.md` §1.2: `ana=1500000000`,
`ben=1500000000`, and **no row for `carlos` at all**.

**`test_law05a_inofficious_legacy_is_reduced_not_dropped`** — the same family and the same will with
the legacy amount changed to `Money::from_pesos(20_000_000)`.

Assertions:
- `check_sum_invariant` against the estate
- an assertion that `output.succession_type == SuccessionType::IntestateByPreterition`
- `assert_total_centavos(find_share(&output, "carlos"), 1500000000, "LAW-05a reduced carlos")`
- `assert_total_centavos(find_share(&output, "ana"), 750000000, "LAW-05a reduced ana")`
- `assert_total_centavos(find_share(&output, "ben"), 750000000, "LAW-05a reduced ben")`
- an assertion that `output.warnings` contains an entry whose `category` is `inofficiousness`

Comment the arithmetic: Σ legacies 2000000000 exceeds the ₱15,000,000 disposable free portion by
500000000; Art. 911 reduces the single non-preferred legacy pro rata, which on one legacy is a
quarter, leaving 1500000000; the residue 1500000000 divides equally, so each child receives exactly the
₱7,500,000 legitime Art. 888 guarantees — which is the assertion that proves the reduction stopped in
the right place. Record the before-value: `ana=1500000000`, `ben=1500000000`, no `carlos` row.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && cargo test --test integration 2>&1 | grep -E "law05|test result" | tail -12; cargo test 2>&1 | grep -E "^test result|^error"; cargo test 2>&1 | awk '/^test result/{s+=$4} END {print "TOTAL_PASSED="s}'
  </verify>
  <acceptance_criteria>
    - `test_law05a_preterition_preserves_a_non_inofficious_legacy` and `test_law05a_inofficious_legacy_is_reduced_not_dropped` both appear by name in the `cargo test --test integration` output and pass
    - The first vector asserts 300000000 / 1350000000 / 1350000000 and the second asserts 1500000000 / 750000000 / 750000000
    - `git diff engine/tests/integration.rs` shows only additions and no change to any pre-existing `assert_total_centavos` or `assert_total_pesos` line
    - `cd engine && cargo test` reports `0 failed` with at least 540 passing
    - Both vectors carry a comment citing Art. 854, the measured before-value, and the `LEGAL-CONFORMANCE.md` row
  </acceptance_criteria>
  <done>The surviving-legacy rule cannot silently regress.</done>
</task>

<task type="auto">
  <name>Task 2: Two named vectors for the donation half of LAW-05</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/tests/integration.rs, .planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md, .planning/LAWYER-AGENDA.md</read_first>
  <action>
Append two more vectors in the same place and the same style.

**`test_law05b_collated_donation_defeats_preterition`** — estate `Money::from_pesos(30_000_000)`.
Family tree: one living `Relationship::LegitimateChild` `ben`, and nothing else. A will whose
`institutions` holds one entry instituting a stranger with `person_id: None`, `name` `"Kevin"` and
`share: ShareSpec::EntireFreePort`, and whose `legacies`, `devises` and `disinheritances` are empty.
One donation `don1` with `recipient_heir_id: Some("ben")`, `recipient_is_stranger: false`,
`value_at_time_of_donation: Money::from_pesos(10_000_000)`, and **every** exemption flag `false`.

Assertions:
- `check_sum_invariant` against the estate
- an assertion that `output.succession_type == SuccessionType::Testate`
- `assert_total_centavos(find_share(&output, "ben"), 1500000000, "LAW-05b ben")`
- `assert_total_centavos(find_share(&output, "Kevin"), 1500000000, "LAW-05b Kevin")`
- an assertion that `output.warnings` contains no entry whose `category` is `preterition`

Comment the source of the expected values: they are the measured output of the `b-after-proxy` input in
`08-RESEARCH.md` §1.2, which is the identical family with `ben` named in the will, so the post-fix
figures were measured rather than predicted. Record the before-value from the same table: the engine
returned `IntestateByPreterition` and gave `ben` the whole 3000000000, destroying the will. Cite
Art. 1061, which brings a donation to a compulsory heir into the mass "in order that it may be computed
in the determination of the legitime", and *Morales v. Olondriz*, which requires the omission to be
total.

**`test_law05b_exempt_donation_still_preterites_and_flags`** — the same estate, family and will, with
`don1.is_expressly_exempt` set to `true`.

Assertions:
- `check_sum_invariant` against the estate
- an assertion that `output.succession_type == SuccessionType::IntestateByPreterition`
- `assert_total_centavos(find_share(&output, "ben"), 3000000000, "LAW-05b exempt ben")`
- an assertion that `output.warnings` contains an entry whose `category` is
  `preterition_exempt_donation` and whose `related_heir_id` is `Some("ben".to_string())`

Comment that Art. 1062 lets the donor provide expressly against collation, so this donation is not an
advance on the legitime, and that whether it nevertheless defeats *Morales*' total-omission test is the
recorded question `LAWYER-09` in `.planning/LAWYER-AGENDA.md`, whose status is `awaiting-answer`. State
in the comment that the 3000000000 figure is the **unchanged pre-fix measurement** and is asserted for
that reason, not because this vector takes a position on `LAWYER-09`. The load-bearing assertion of
this vector is the presence of the flag.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && cargo test --test integration 2>&1 | grep -E "law05|test result" | tail -14; cargo test 2>&1 | grep -E "^test result|^error"; cargo test 2>&1 | awk '/^test result/{s+=$4} END {print "TOTAL_PASSED="s}'; cd .. && node scripts/check-lawyer-agenda.mjs; echo "G10_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `test_law05b_collated_donation_defeats_preterition` and `test_law05b_exempt_donation_still_preterites_and_flags` both appear by name in the `cargo test --test integration` output and pass
    - The first asserts `Testate` with `ben=1500000000` and `Kevin=1500000000`
    - The second asserts `IntestateByPreterition` and a `preterition_exempt_donation` flag naming `ben`
    - The second vector's comment states that its centavo figure is the unchanged pre-fix measurement and that it takes no position on `LAWYER-09`
    - `node scripts/check-lawyer-agenda.mjs` exits 0 and `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` returns 0
    - `cd engine && cargo test` reports `0 failed` with at least 542 passing
  </acceptance_criteria>
  <done>Both halves of the defect are pinned, and the open question is cited rather than answered.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure the committed corpus and commit</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/tests/common/invariants.rs, .planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-RESEARCH.md, scripts/safe-commit.sh</read_first>
  <action>
`08-RESEARCH.md` §1.3 measured that 36 of the 173 committed inputs produce `IntestateByPreterition` and
that exactly 7 of those have a non-empty `will.legacies`. Those 7 are the only committed inputs whose
per-heir amounts move under this phase, and no test or gate pins their amounts. Prove that the move is
a conservation-preserving one rather than a regression.

Build the release binary and re-run the whole corpus, reading the exit code of each run. Exit code 2
means the runtime conservation or uniqueness check in `engine/src/output_check.rs` rejected the output,
which is a BLOCKED condition:

    cargo build --release
    for f in examples/cases/*.json examples/testate-cases/*.json examples/fuzz-cases/*.json examples/coverage-cases/*.json examples/defect-cases/*.json; do ./target/release/inheritance-engine "$f" > /dev/null 2>&1 || echo "NONZERO $? $f"; done

The two `examples/defect-cases/` files are expected to exit 2; they are the two `LAW-06` entries in
`engine/defect-baseline.json`, blocked on `LAWYER-06` and owned by Phase 14. **Any other nonzero exit
is a BLOCKED condition.** Record the full list in the plan summary.

Then run the three corpus-wide test binaries and read their output:

    cargo test --test fuzz_invariants
    cargo test --test observability
    cargo test --test defect_ledger

`defect_ledger` printing `STALE DEFECT DECLARATION` means a `LAW-06` case stopped reproducing, which a
Phase 8 change should not have caused: stop and report BLOCKED rather than editing
`engine/defect-baseline.json`.

Record in the plan summary the observed per-heir totals for the seven moved inputs
(`examples/testate-cases/08.json` and `examples/fuzz-cases/061`, `062`, `066`, `067`, `068`, `069`),
each with its estate figure, so a later reader can see that the legatee now appears and the sum still
matches.

Then stage and commit the single edited file:

    bash scripts/safe-commit.sh -m "test(08-05): named regression vectors for the preterition surviving-legacy and donation rules (LAW-05)" apps/inheritance/engine/tests/integration.rs

Do not stage anything under `engine/target/`, `frontend/src/wasm/pkg/`, or `.planning/REQUIREMENTS.md`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && cargo test --test fuzz_invariants --test observability --test defect_ledger 2>&1 | grep -E "^test result|STALE|panicked" ; cd .. && node scripts/check-observability.mjs; echo "G11_EXIT=$?"; node scripts/check-commit-discipline.mjs; echo "G7_EXIT=$?"; git log -1 --stat --format="%s"; git status --porcelain engine/
  </verify>
  <acceptance_criteria>
    - Every committed input outside `engine/examples/defect-cases/` exits 0 from the release binary, and the two defect cases exit 2
    - `cargo test --test fuzz_invariants` reports 0 failed, so all sixteen named invariants hold
    - `cargo test --test observability --test defect_ledger` reports 0 failed and prints no `STALE DEFECT DECLARATION`
    - The plan summary lists the observed per-heir totals for the seven moved inputs alongside each estate figure
    - `git log -1 --stat` shows exactly one changed file and `node scripts/check-commit-discipline.mjs` exits 0
  </acceptance_criteria>
  <done>The corpus is re-measured against the fixed engine rather than assumed unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed corpus → regression suite | `engine/examples/` is the evidence base every invariant test reads; a silently moved output there would go unnoticed without a re-measurement |
| plan text → executor | The expected centavo values in this plan are the only figures the executor may assert; reading a value back out of the fixed engine would make the vector vacuous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Repudiation | `engine/tests/integration.rs` | mitigate | Every asserted value is stated in this plan and traced to a measurement in `08-RESEARCH.md` section 1.2, so a vector cannot be written to match whatever the engine happens to produce |
| T-08-12 | Denial of Service | the 173-file committed corpus | accept | Re-running the release binary over 173 inputs takes seconds and runs only in this plan's task 3; no gate is slowed |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package. `engine/Cargo.toml` and `engine/Cargo.lock` are not edited |
</threat_model>

<verification>

Run from `/home/clsandoval/cs/monorepo/apps/inheritance`:

    cd engine && cargo test 2>&1 | tail -25
    cd engine && cargo test --test integration 2>&1 | grep -E "law05|test result"
    cd engine && cargo test --test fuzz_invariants --test observability --test defect_ledger
    node scripts/check-observability.mjs
    node scripts/check-lawyer-agenda.mjs
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs

Expected: `cargo test` reports `0 failed` with at least 542 passing; all four `law05` vectors pass by
name; the three corpus binaries report 0 failed; the four static checks exit 0.

`bash scripts/ci-gates.sh` is expected to reach `G3` and halt there on the same five frontend failures
Phase 5 left behind. If the runner halts anywhere **before** `G3`, stop and report BLOCKED with the
pasted output.

</verification>

<success_criteria>

- [ ] Four named vectors exist in `engine/tests/integration.rs`, two per half of the defect, each
      citing its governing article and its measured before-value
- [ ] The surviving-legacy vector asserts `carlos=300000000`, `ana=1350000000`, `ben=1350000000`
- [ ] The reduction vector asserts `carlos=1500000000` with both children at their exact ₱7,500,000
      legitime
- [ ] The collated-donation vector asserts `Testate` with `ben=1500000000` and `Kevin=1500000000`
- [ ] The exempt-donation vector asserts the `preterition_exempt_donation` flag and states that it
      takes no position on `LAWYER-09`
- [ ] No pre-existing expected value in the vector suite was edited
- [ ] Every committed input outside `defect-cases/` exits 0, and both defect cases still exit 2
- [ ] `cd engine && cargo test` reports 0 failed with at least 542 passing
- [ ] `ALL GATES PASSED (13/13)` was not claimed

</success_criteria>

<output>
Create `.planning/phases/08-remaining-unblocked-legal-tax-bridge-defects/08-05-SUMMARY.md` when done
</output>
