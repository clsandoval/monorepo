---
phase: 14
plan: 14-06
wave: 4
depends_on: ["14-01", "14-02", "14-03", "14-04", "14-05"]
files_modified:
  - gates.manifest.json
  - gates.manifest.lock
  - GATES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LAW-06, LAW-07, LAW-12, LAW-13, LAW-14, LAW-15]
must_haves:
  truths:
    - "Every commit stages explicit file paths through bash scripts/safe-commit.sh; git add -A, git add . and git commit -a are prohibited because a concurrent auto-committer runs on this monorepo."
    - "No test, assertion, or gate may be weakened, deleted, skipped or loosened to make anything pass; a check that cannot legitimately pass is reported BLOCKED with the real pasted command output."
    - "No point of Philippine law may be decided by the executing agent. LAW-06, LAW-07 and LAW-12 stay open, blocked on LAWYER-06, LAWYER-04 and LAWYER-08."
    - "The gate set may only grow. No locked command string changes, no gate is removed, and no blocking gate becomes non-blocking; only the unlocked order field moves."
    - "A requirement is marked complete only when a gate proves it; a blocked requirement stays unchecked in .planning/REQUIREMENTS.md."
---

<objective>
Register the four checks the earlier plans in this phase built as gates G26 through G29, document
them in `GATES.md`, update the requirement status records to what the gates actually prove, and run
the full runner end to end.

| Gate | Command | Order | Proves | Built by |
|---|---|---|---|---|
| G26 | `node scripts/check-blocked-requirements.mjs` | 21 | LAW-06, LAW-07 and LAW-12 are recorded as blocked on LAWYER-06, LAWYER-04 and LAWYER-08, the record agrees with the decision registry, and an arriving answer turns the gate red instead of passing silently | `14-03` |
| G27 | `node scripts/check-spec-legal-text.mjs` | 22 | the spec's four misstatements of law stay corrected | `14-04` |
| G28 | `node scripts/check-legal-traceability.mjs` | 23 | every article the engine's production code cites is either mapped to exactly one named test vector or declared in a shrink-only ledger | `14-05` |
| G29 | `node scripts/check-bugs-ledger.mjs` | 24 | `engine/BUGS.md` keeps the fixed entry shape, every open entry keeps a runnable reproduction, and every legal claim in it stays attributed | `14-02` |

Four gates join at orders 21 to 24; G10, G11, G8 and G9 shift to 25, 26, 27 and 28 with G9 still
last. The set goes from 24 gates to 28. `order` is the only field that changes on any pre-existing
gate, and `order` is deliberately unlocked, which is the same move Phase 13 made when it inserted
G22 through G25.

The phase ends when `bash scripts/ci-gates.sh` prints `ALL GATES PASSED (28/28)` and exits 0.
</objective>

<constraints>
- The gate set may only grow. `gates.manifest.lock` gains exactly four entries and loses none. No
  locked `command` string is edited, no gate id is renamed or reused, and no `blocking: true` becomes
  `false`. `node scripts/check-gate-manifest.mjs` enforces all of that and has no flag that rewrites
  the lock.
- `G14` stays reserved and unregistered. It belongs to Phase 9's plan `09-06` and this plan does not
  claim it.
- LAW-06, LAW-07 and LAW-12 stay unchecked in `.planning/REQUIREMENTS.md`. Marking any of them `[x]`
  makes gate G26 raise `REQUIREMENT CLAIMED COMPLETE`, which is the correct behavior: those three
  requirements are blocked, not delivered, and this plan delivers a record of the blockage plus its
  gate, never the fix.
- Only LAW-13, LAW-14 and LAW-15 become `[x]`, and only after their gates have been observed passing
  inside the full runner rather than only in isolation.
- If `bash scripts/ci-gates.sh` exits 1, the failing gate is fixed at its cause. Editing a gate, a
  precondition, the manifest, a test or an assertion to clear it is prohibited. If it exits 2, that
  is a HALT: stop and report BLOCKED with the five-field template in `.planning/PLAN-STANDARD.md`
  section 3 and the real pasted output.
- Every commit stages explicit paths through `bash scripts/safe-commit.sh`.
- No point of Philippine law is decided.
</constraints>

<tasks>

<task id="1" name="Confirm the four checks pass in isolation before registering them">
<read_first>
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/check-blocked-requirements.mjs`
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/check-spec-legal-text.mjs`
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/check-legal-traceability.mjs`
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/check-bugs-ledger.mjs`
</read_first>
<action>
From `/home/clsandoval/cs/monorepo/apps/inheritance`, run each of the four checks with no flags and
record its exit code and its complete output. All four must exit 0 and each must print exactly one
`GATE-SKIPS` line. A check that does not exit 0 here means its own plan is not complete, and this
plan stops with a BLOCKED report naming that plan rather than registering a red gate.
</action>
<verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && for s in check-blocked-requirements check-spec-legal-text check-legal-traceability check-bugs-ledger; do echo "--- $s"; node scripts/$s.mjs; echo "EXIT=$?"; done
</verify>
<acceptance_criteria>
- All four runs print `EXIT=0`.
- Each of the four prints exactly one line matching `GATE-SKIPS total=[0-9]+ skipped=0`.
- `node scripts/check-legal-traceability.mjs` prints the coverage line
  `LEGAL TRACEABILITY COVERAGE 63/79 articles traced, 16 declared untraced`.
- `node scripts/check-blocked-requirements.mjs` prints `all awaiting-answer`, confirming no lawyer
  answer arrived while this phase ran; if it prints `ANSWER ARRIVED`, stop and report BLOCKED,
  because LAW-06, LAW-07 or LAW-12 would then need implementing through
  `.planning/LEGAL-CORRECTION-WORKFLOW.md` rather than recording.
</acceptance_criteria>
<done>
All four checks are green in isolation and none of them reports a lawyer answer having arrived.
</done>
</task>

<task id="2" name="Append G26 to G29 to gates.manifest.json and gates.manifest.lock">
<read_first>
- `/home/clsandoval/cs/monorepo/apps/inheritance/gates.manifest.json`
- `/home/clsandoval/cs/monorepo/apps/inheritance/gates.manifest.lock`
- `/home/clsandoval/cs/monorepo/apps/inheritance/GATES.md` section 1, `### Adding a gate — the only legitimate path`
- `## Reference A — the four gate objects` in this plan file
</read_first>
<action>
Append the four gate objects in `## Reference A` to the `gates` array in `gates.manifest.json`, each
with all nine keys `id`, `name`, `order`, `command`, `cwd`, `precondition`, `blocking`, `proves`,
`requirements`. Then change the `order` field — and only the `order` field — of the four existing
gates: G10 from 21 to 25, G11 from 22 to 26, G8 from 23 to 27, and G9 from 24 to 28. Then append the
four matching `{"id", "command", "blocking"}` entries to `locked_gates` in `gates.manifest.lock`,
removing nothing.
</action>
<verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && node scripts/check-gate-manifest.mjs; echo "EXIT=$?"; node -e "const m=require('./gates.manifest.json');const l=require('./gates.manifest.lock');console.log('gates',m.gates.length,'locked',l.locked_gates.length);console.log(m.gates.slice().sort((a,b)=>a.order-b.order).map(g=>g.id+':'+g.order).join(' '))"
</verify>
<acceptance_criteria>
- `node scripts/check-gate-manifest.mjs` prints `MANIFEST OK` and `EXIT=0`.
- The counts print `gates 28 locked 28`.
- The order listing ends with `G26:21 G27:22 G28:23 G29:24 G10:25 G11:26 G8:27 G9:28`, so G9 is
  still last.
- `git diff gates.manifest.lock` shows four added entries and zero removed or modified entries.
- `git diff gates.manifest.json` shows no change to any `command` or `blocking` value on any
  pre-existing gate.
</acceptance_criteria>
<done>
Four gates are registered and frozen, four pre-existing gates moved only in `order`, and the manifest
integrity check is green.
</done>
</task>

<task id="3" name="Document the four gates in GATES.md">
<read_first>
- `/home/clsandoval/cs/monorepo/apps/inheritance/GATES.md`
- `/home/clsandoval/cs/monorepo/apps/inheritance/.planning/BLOCKED-REQUIREMENTS.md`
- `/home/clsandoval/cs/monorepo/apps/inheritance/engine/legal-traceability.lock`
</read_first>
<action>
Update the `### Current gates` table in `GATES.md` section 1 so it lists all 28 gates in `order`,
matching the manifest exactly. Then add four numbered sections at the end of the document, one per
new gate, each naming the command, every violation marker the check can raise, the committed fixture
that drives each marker, and what the gate does not check.

Section 16, G26, additionally records that this is the one gate in the set that can turn red on good
news: when a blocking decision stops being `awaiting-answer` while its requirement is still open, the
gate raises `ANSWER ARRIVED` and exits 1, and the remedy is to run the five steps of
`.planning/LEGAL-CORRECTION-WORKFLOW.md`, never to edit the gate.

Section 18, G28, additionally records the two shrink-only ledgers this project now carries alongside
`gate-skips.lock` and `engine/defect-baseline.json`, and states that `engine/legal-traceability.lock`
currently holds 16 articles and that the number may only fall.
</action>
<verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -c "^| G" GATES.md && grep -n "^## 1[6-9]\." GATES.md && node -e "const m=require('./gates.manifest.json');const t=require('fs').readFileSync('GATES.md','utf8');for(const g of m.gates){if(!t.includes(g.command))throw new Error('GATES.md is missing the command for '+g.id)}console.log('every gate command documented')"
</verify>
<acceptance_criteria>
- The current-gates table has one row per manifest gate, and the node check prints
  `every gate command documented`.
- `grep -n "^## 1[6-9]\." GATES.md` prints four section headings, numbered 16 through 19, one per new
  gate.
- Section 16 contains the literal `ANSWER ARRIVED` and the literal
  `.planning/LEGAL-CORRECTION-WORKFLOW.md`.
- Section 18 contains the literal `may only shrink` and the number `16`.
- Every violation marker named in the four checker scripts appears somewhere in `GATES.md`, verified
  by grepping `GATES.md` for each marker string in turn.
</acceptance_criteria>
<done>
`GATES.md` lists 28 gates and documents each new gate's markers, fixtures and limits.
</done>
</task>

<task id="4" name="Run the full gate runner three times">
<read_first>
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/ci-gates.sh`
- `/home/clsandoval/cs/monorepo/apps/inheritance/gates.manifest.json`
</read_first>
<action>
Run `bash scripts/ci-gates.sh` from `/home/clsandoval/cs/monorepo/apps/inheritance` and capture the
complete output. Repeat twice more, once immediately and once after the commit in task 6, so the
result is shown to be stable rather than a single lucky run. Record the wall-clock duration of each
run. Exit code 1 means a gate ran and failed: fix its cause. Exit code 2 is a HALT: stop and report
BLOCKED with the pasted output.
</action>
<verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && time bash scripts/ci-gates.sh; echo "RUNNER=$?"
</verify>
<acceptance_criteria>
- The runner prints `ALL GATES PASSED (28/28)` and `RUNNER=0`, observed on at least two runs before
  the commit and once after it.
- `.gate-runs/logs/` holds one log per gate, all stamped with the same `GSD-RUN` timestamp as
  `.gate-runs/logs/RUN.stamp`, and each of `G26.log`, `G27.log`, `G28.log` and `G29.log` contains a
  `GATE-SKIPS` line.
- `node scripts/check-gate-skips.mjs` exits 0 with no `UNDECLARED SKIP` and no
  `STALE SKIP DECLARATION`, proving the four new gates did not require an addition to
  `gate-skips.lock`.
- The wall-clock duration of each run is recorded in the plan summary.
</acceptance_criteria>
<done>
The full 28-gate runner has been observed exiting 0 with `ALL GATES PASSED (28/28)` on three separate
runs, with per-gate logs and clean skip accounting.
</done>
</task>

<task id="5" name="Update the requirement and roadmap records to what the gates prove">
<read_first>
- `/home/clsandoval/cs/monorepo/apps/inheritance/.planning/REQUIREMENTS.md`
- `/home/clsandoval/cs/monorepo/apps/inheritance/.planning/ROADMAP.md` section `### Phase 14`
- `/home/clsandoval/cs/monorepo/apps/inheritance/.planning/BLOCKED-REQUIREMENTS.md`
</read_first>
<action>
In `.planning/REQUIREMENTS.md`, change the checkbox for LAW-13, LAW-14 and LAW-15 from `- [ ]` to
`- [x]`, and leave LAW-06, LAW-07 and LAW-12 unchecked. In the status table at the bottom of the same
file, set LAW-13, LAW-14 and LAW-15 to `Complete`, and set LAW-06, LAW-07 and LAW-12 to
`Blocked on LAWYER-06`, `Blocked on LAWYER-04` and `Blocked on LAWYER-08` respectively, with a
pointer to `.planning/BLOCKED-REQUIREMENTS.md`.

In `.planning/ROADMAP.md`, update the Phase 14 bullet in the `## Phases` list so it records the
outcome in the same style the earlier phase bullets use: which requirements closed, which stayed
blocked and on which recorded decision, the four gates added at orders 21 to 24, the shift of G10,
G11, G8 and G9 to 25 to 28, the observed `ALL GATES PASSED (28/28)`, and the traceability coverage
figure of 63 of 79 articles with 16 declared untraced. Mark the phase `[ ]` rather than `[x]`,
because three of its six requirements remain open on a lawyer's answer.
</action>
<verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -n "LAW-0[67]\|LAW-1[2345]" .planning/REQUIREMENTS.md && node scripts/check-blocked-requirements.mjs; echo "EXIT=$?"
</verify>
<acceptance_criteria>
- LAW-13, LAW-14 and LAW-15 show `- [x]` and their status rows read `Complete`.
- LAW-06, LAW-07 and LAW-12 still show `- [ ]` and their status rows name their blocking decision.
- `node scripts/check-blocked-requirements.mjs` exits 0 and does not raise
  `REQUIREMENT CLAIMED COMPLETE`.
- The Phase 14 bullet in `.planning/ROADMAP.md` names all six requirement ids, the four new gate ids,
  and the literal `ALL GATES PASSED (28/28)`.
</acceptance_criteria>
<done>
The requirement and roadmap records state exactly what the gates prove, with the three blocked
requirements visibly open.
</done>
</task>

<task id="6" name="Commit, then re-run the runner one final time">
<read_first>
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/safe-commit.sh`
- `/home/clsandoval/cs/monorepo/apps/inheritance/scripts/check-commit-discipline.mjs`
</read_first>
<action>
Commit the five paths in `files_modified` in one commit through `bash scripts/safe-commit.sh` with
the message `feat(14-06): register gates G26-G29 and close LAW-13/14/15 (LAW-06/07/12 stay blocked)`.
Then run `bash scripts/ci-gates.sh` once more against the committed tree, because gates G6 and G7
read plan files and commit history and must be seen passing after the commit rather than only before
it.
</action>
<verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && node scripts/check-commit-discipline.mjs && git status --porcelain apps/inheritance && bash scripts/ci-gates.sh; echo "RUNNER=$?"
</verify>
<acceptance_criteria>
- `node scripts/check-commit-discipline.mjs` exits 0 and `git status --porcelain apps/inheritance`
  prints nothing.
- `git show --stat --name-only HEAD` lists exactly the five paths in `files_modified`, all under
  `apps/inheritance/`.
- The post-commit runner prints `ALL GATES PASSED (28/28)` and `RUNNER=0`.
- `node scripts/check-plan-closed-world.mjs` exits 0, confirming this phase's own six plan files pass
  the closed-world lint, including rule 9's requirement that a plan claiming LAW-06, LAW-07 or LAW-12
  cites LAWYER-06, LAWYER-04 or LAWYER-08.
</acceptance_criteria>
<done>
One commit holding exactly the five files is on the branch and the full runner is green against the
committed tree.
</done>
</task>

</tasks>

## Reference A — the four gate objects

Each object is appended to the `gates` array in `gates.manifest.json` with all nine keys. `cwd` is
`apps/inheritance` on all four, matching every other gate in the manifest. `blocking` is `true` on
all four. The `proves` text below is the short form; the executor writes it out in the manifest and
expands it in the matching `GATES.md` section.

| Key | G26 | G27 | G28 | G29 |
|---|---|---|---|---|
| `id` | `G26` | `G27` | `G28` | `G29` |
| `name` | `blocked requirements` | `spec legal text` | `legal traceability` | `bugs ledger` |
| `order` | `21` | `22` | `23` | `24` |
| `command` | `node scripts/check-blocked-requirements.mjs` | `node scripts/check-spec-legal-text.mjs` | `node scripts/check-legal-traceability.mjs` | `node scripts/check-bugs-ledger.mjs` |
| `precondition` | `test -f .planning/BLOCKED-REQUIREMENTS.md` | `test -f specs/inheritance-engine-spec.md` | `test -f engine/legal-rules.json` | `test -f engine/BUGS.md` |
| `requirements` | `["LAW-06","LAW-07","LAW-12"]` | `["LAW-13"]` | `["LAW-14"]` | `["LAW-15"]` |

`proves`, short form:

- **G26** — the three lawyer-blocked requirements are recorded against the decision each waits on,
  the record agrees with `.planning/lawyer-decisions.json`, none of them is marked complete while
  still blocked, and an arriving answer raises `ANSWER ARRIVED` rather than passing. See `GATES.md`
  section 16.
- **G27** — the four passages of law named in `.planning/research/LEGAL-CONFORMANCE.md` section 2b
  read correctly, checked as literal strings at named anchors in named files, with the superseded
  wording proven absent. See `GATES.md` section 17.
- **G28** — every article cited by the engine's production code is either mapped to exactly one named
  test function carrying its `LEGAL-VECTOR` marker or declared in the shrink-only
  `engine/legal-traceability.lock`, with `implemented_in` recomputed from source on every run. See
  `GATES.md` section 18.
- **G29** — `engine/BUGS.md` keeps its fixed entry shape, every open entry keeps a runnable
  reproduction, every closed entry keeps a stated reason, and every legal claim stays attributed to
  `.planning/research/LEGAL-CONFORMANCE.md`. The behavioral half lives in
  `engine/tests/bugs_ledger.rs` under gate G1. See `GATES.md` section 19.

<verification>
Run from `/home/clsandoval/cs/monorepo/apps/inheritance`.

1. `node scripts/check-gate-manifest.mjs` prints `MANIFEST OK` and exits 0.
2. `gates.manifest.json` holds 28 gates and `gates.manifest.lock` holds 28 locked entries; the four
   pre-existing gates G10, G11, G8 and G9 changed only in `order`, and G9 is still last.
3. `bash scripts/ci-gates.sh` prints `ALL GATES PASSED (28/28)` and exits 0, observed three times,
   including once after the final commit.
4. `node scripts/check-gate-skips.mjs` exits 0 and `gate-skips.lock` gained no entry.
5. `.planning/REQUIREMENTS.md` shows LAW-13, LAW-14 and LAW-15 checked and LAW-06, LAW-07 and LAW-12
   unchecked with their blocking decision named.
6. `node scripts/check-plan-closed-world.mjs` exits 0 over all six of this phase's plans.
7. `node scripts/check-commit-discipline.mjs` exits 0 and `git status --porcelain apps/inheritance`
   is empty.
</verification>

<success_criteria>
- Gates G26, G27, G28 and G29 are registered in both `gates.manifest.json` and `gates.manifest.lock`
  at orders 21 through 24, blocking, with all nine manifest keys, and `G14` remains reserved and
  unregistered.
- The gate set grew from 24 to 28 with no command string changed, no gate removed, and no blocking
  gate weakened; the only field that moved on a pre-existing gate is `order`.
- `bash scripts/ci-gates.sh` prints `ALL GATES PASSED (28/28)` and exits 0 on three observed runs,
  one of them against the committed tree.
- `GATES.md` lists all 28 gates and carries a section per new gate naming every violation marker and
  the committed fixture that drives it.
- LAW-13, LAW-14 and LAW-15 are marked complete and are each proven by a named gate; LAW-06, LAW-07
  and LAW-12 remain open, are marked as blocked on LAWYER-06, LAWYER-04 and LAWYER-08, and no reading
  of any of the three was implemented, defaulted or stubbed anywhere in the tree.
</success_criteria>
