---
phase: 01-gate-foundations-suites-execute-at-all
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - scripts/ci-gates.sh
  - README.md
  - frontend/.gitignore
  - ../../.github/workflows/inheritance-ci.yml
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04]
user_setup: []

must_haves:
  truths:
    - "One script runs all four gates in order and exits nonzero if any fails"
    - "The identical script is what CI executes, so CI behavior is reproducible on a developer machine"
    - "A CI workflow triggers on push and on pull_request and is scoped to apps/inheritance"
    - "No gate step can pass while failing — no continue-on-error, no || true, no if: always() on a gate"
    - "A reader can go from clean checkout to all four gates green by following README.md"
  artifacts:
    - path: "scripts/ci-gates.sh"
      provides: "The single fail-closed runner for cargo test, WASM build, test gate, and typecheck"
      min_lines: 40
      contains: "set -euo pipefail"
    - path: "../../.github/workflows/inheritance-ci.yml"
      provides: "GitHub Actions workflow running the gate script on every push and pull request"
      contains: "pull_request"
    - path: "README.md"
      provides: "Documented install and gate commands for a clean checkout"
      min_lines: 40
      contains: "npm ci"
  key_links:
    - from: "../../.github/workflows/inheritance-ci.yml"
      to: "scripts/ci-gates.sh"
      via: "the workflow's only gate step invokes the committed script"
      pattern: "ci-gates\\.sh"
    - from: "scripts/ci-gates.sh"
      to: "frontend/package.json"
      via: "npm run test:gate"
      pattern: "test:gate"
    - from: "scripts/ci-gates.sh"
      to: "engine/build-wasm.sh"
      via: "invokes the WASM build before running the frontend suite"
      pattern: "build-wasm\\.sh"
---

<objective>
Make all four gates run as one fail-closed command, have CI execute that exact command on every push and pull request, and document the clean-checkout sequence.

Purpose: RESEARCH.md §1 measured that no CI gate exists today — `.github/workflows/inheritance.yml` is `workflow_dispatch` only and invokes zero test commands. Roadmap criterion 4 requires a workflow that runs `cargo test`, the WASM build, `npm test`, and `npx tsc -b` on every push and PR and fails when any of them fails. RESEARCH.md §3.2 decided the mechanism: the gate logic lives in a committed shell script, not inside YAML, so it is debuggable and provable on a developer machine rather than only after a push.

Output: `apps/inheritance/scripts/ci-gates.sh`, `.github/workflows/inheritance-ci.yml` at the monorepo root, and `apps/inheritance/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gate-foundations-suites-execute-at-all/01-RESEARCH.md
@.planning/phases/01-gate-foundations-suites-execute-at-all/01-01-SUMMARY.md
@.planning/phases/01-gate-foundations-suites-execute-at-all/01-03-SUMMARY.md
@frontend/package.json
@engine/Cargo.toml
</context>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. GSD's project root is `/home/clsandoval/cs/monorepo/apps/inheritance`, but the git repository root and `.github/workflows/` are at `/home/clsandoval/cs/monorepo`. The new workflow file goes at `/home/clsandoval/cs/monorepo/.github/workflows/inheritance-ci.yml` — an absolute path outside the GSD project root. Everything else in this plan stays under `apps/inheritance/`.
2. Do NOT modify, rename, or delete the existing `/home/clsandoval/cs/monorepo/.github/workflows/inheritance.yml`. It is the Ralph forward-loop runner and is unrelated. Create a **new, separate** file.
3. Do NOT touch any other workflow in `.github/workflows/` — `daimon-forward.yml`, `podplay-ops.yml`, `pymc-content-loop.yml`, `ralph-loops.yml`, `taxklaro.yml` all belong to unrelated apps.
4. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only.
5. NEVER delete, skip, weaken, or loosen a test or an assertion. Do not modify any `*.test.ts`/`*.test.tsx` file, `frontend/vitest.config.ts`, `frontend/test-baseline.json`, or `frontend/scripts/check-test-baseline.mjs`.
6. **No gate step may be allowed to pass while failing.** `continue-on-error`, `|| true`, `|| echo`, `set +e`, `if: always()` on a gate step, and `--passWithNoTests` are all prohibited anywhere in the script or the workflow.
7. Do NOT add a `paths-ignore` that could exclude a gate-relevant file, and do NOT restrict the workflow to a single branch. It must fire on push to any branch and on every pull_request.
8. This plan makes no product change. If a gate fails during this plan, that is a real signal — report BLOCKED with the raw output rather than adjusting the gate to accommodate it.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/ci-gates.sh, the single fail-closed gate runner</name>
  <files>scripts/ci-gates.sh</files>
  <read_first>engine/build-wasm.sh, frontend/package.json, frontend/scripts/check-test-baseline.mjs, .planning/phases/01-gate-foundations-suites-execute-at-all/01-RESEARCH.md</read_first>
  <action>
Create `apps/inheritance/scripts/ci-gates.sh`, executable, starting with `#!/usr/bin/env bash` and `set -euo pipefail`.

Resolve the app root from the script's own location: `APP_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`, so the script works from any working directory. This matters because CI and a developer will invoke it from different places.

Run these four gates **in this order**, each preceded by a banner line containing the literal string `=== GATE n/4:` and the gate's name:

- **Gate 1/4 — engine tests.** `cd "$APP_DIR/engine" && cargo test`. Expected: exit 0, 442 passed / 0 failed. Runs first because it is the fastest gate with the strongest signal and needs no Node or WASM toolchain.
- **Gate 2/4 — WASM build.** `bash "$APP_DIR/engine/build-wasm.sh"`. Must come before gate 3, because the frontend suite loads `inheritance_engine_bg.wasm` from disk (`bridge.ts:330`) and that file is gitignored, so on a clean checkout it does not exist until this gate produces it.
- **Gate 3/4 — frontend test gate.** `cd "$APP_DIR/frontend" && npm run test:gate`. This runs the complete 2,416-test suite and diffs it against `test-baseline.json`. Do NOT substitute plain `npm test` here — that would be red on the 46 known failures and would tell CI nothing.
- **Gate 4/4 — typecheck.** `cd "$APP_DIR/frontend" && npx tsc -b --force`. Use `--force`, never bare `tsc -b`: RESEARCH.md §4 measured that `frontend/tsconfig.tsbuildinfo` is committed, so an incremental build can no-op against a stale cache and report clean when it is not.

Because `set -e` is active and no gate is wrapped in `||`, any nonzero exit aborts the script immediately with that gate's status. Do not add explicit `if` / `||` error handling around the gates — that is how a `|| true` slips in later. Add a comment saying exactly this.

Add an `--only <n>` option accepting `1`, `2`, `3`, or `4` that runs a single gate, for fast local iteration. When absent, run all four. Do not add a `--skip` option — there must be no way to run "all gates except one" and still get a success message.

On success, print a final line containing the literal string `ALL GATES PASSED (4/4)`.

Also add, before gate 1, a preflight that fails fast with an actionable message if any required tool is missing: `cargo`, `rustup`, `wasm-pack`, `node`, `npm`. Each missing tool must print a line containing `MISSING TOOL:` and the install command, then `exit 1`. Also fail with `MISSING DEPS: run npm ci in frontend/` if `$APP_DIR/frontend/node_modules` does not exist — this is the single most likely clean-checkout failure and a bare `npm run test:gate` error would be far less legible.

`chmod +x apps/inheritance/scripts/ci-gates.sh`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo && bash apps/inheritance/scripts/ci-gates.sh; echo "EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `scripts/ci-gates.sh` exists and `test -x apps/inheritance/scripts/ci-gates.sh` succeeds
    - The script contains the literal strings `set -euo pipefail`, `cargo test`, `build-wasm.sh`, `npm run test:gate`, `tsc -b --force`, `ALL GATES PASSED (4/4)`, `MISSING TOOL:`, `MISSING DEPS: run npm ci in frontend/`
    - The script contains all four banners `=== GATE 1/4:`, `=== GATE 2/4:`, `=== GATE 3/4:`, `=== GATE 4/4:`
    - `grep -cE "\|\| true|\|\| echo|continue-on-error|set \+e|--passWithNoTests|--skip" apps/inheritance/scripts/ci-gates.sh` returns 0
    - The script does NOT contain the bare string `tsc -b` without `--force` (every occurrence of `tsc -b` is followed by ` --force`)
    - `bash apps/inheritance/scripts/ci-gates.sh` exits 0 and its output contains `ALL GATES PASSED (4/4)`
    - `bash apps/inheritance/scripts/ci-gates.sh --only 1` exits 0 and its output contains `=== GATE 1/4:` but not `=== GATE 3/4:`
  </acceptance_criteria>
  <done>All four gates run as one fail-closed command that a developer can execute locally.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the runner is fail-closed</name>
  <files></files>
  <read_first>scripts/ci-gates.sh, frontend/test-baseline.json</read_first>
  <action>
This task changes no committed file. It exists so "CI fails the check when any gate fails" is demonstrated rather than asserted — roadmap criterion 4 is a claim about failure behavior, and a runner nobody has seen fail is not known to fail.

Perform this single reversible experiment, then restore:

1. Copy the ledger aside: `cp apps/inheritance/frontend/test-baseline.json /tmp/phase01-ledger-backup.json`.
2. Remove exactly one entry from `known_failures` in `apps/inheritance/frontend/test-baseline.json` (leaving 45). This simulates a regression: a test that fails but is no longer covered by the ledger.
3. Run `bash apps/inheritance/scripts/ci-gates.sh`. It MUST exit 1, and the output MUST contain `UNKNOWN FAILURE` and MUST NOT contain `ALL GATES PASSED`.
4. Restore immediately: `cp /tmp/phase01-ledger-backup.json apps/inheritance/frontend/test-baseline.json`.
5. Re-run `bash apps/inheritance/scripts/ci-gates.sh` and confirm it is back to exit 0 with `ALL GATES PASSED (4/4)`.
6. Confirm `git status --porcelain apps/inheritance/frontend/test-baseline.json` is empty — the ledger is byte-identical to what plan 01-03 committed.

Do NOT perform this experiment by breaking an engine source file, a test file, or an assertion. Editing the ledger is the safe, fully reversible way to inject a synthetic regression.

If step 3 exits 0, the runner is not fail-closed. STOP and report BLOCKED — do not proceed to the workflow, because a workflow wrapping a non-failing runner is worse than no workflow at all.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo && git status --porcelain apps/inheritance/frontend/test-baseline.json; echo "PORCELAIN_EXIT=$?"; bash apps/inheritance/scripts/ci-gates.sh >/dev/null 2>&1; echo "RESTORED_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - With one ledger entry removed, `bash apps/inheritance/scripts/ci-gates.sh` exited 1 and its output contained `UNKNOWN FAILURE`
    - With one ledger entry removed, the output did NOT contain `ALL GATES PASSED`
    - After restore, `bash apps/inheritance/scripts/ci-gates.sh` exits 0 with `ALL GATES PASSED (4/4)`
    - After restore, `git status --porcelain apps/inheritance/frontend/test-baseline.json` produces empty output
    - `git diff --name-only -- 'apps/inheritance/frontend/src/**/*.test.ts' 'apps/inheritance/frontend/src/**/*.test.tsx' 'apps/inheritance/engine/src/'` produces empty output
  </acceptance_criteria>
  <done>The runner has been observed failing on an injected regression and recovering afterward, with the tree left byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Create the GitHub Actions workflow</name>
  <files>../../.github/workflows/inheritance-ci.yml</files>
  <read_first>/home/clsandoval/cs/monorepo/.github/workflows/inheritance.yml, scripts/ci-gates.sh, .planning/phases/01-gate-foundations-suites-execute-at-all/01-RESEARCH.md</read_first>
  <action>
Create a NEW file at the absolute path `/home/clsandoval/cs/monorepo/.github/workflows/inheritance-ci.yml`. Do not modify the existing `inheritance.yml` (constraint 2) — read it only to see the toolchain-setup actions and versions it already uses, so the new workflow stays consistent with them.

Contents:

- `name: Inheritance CI`
- Triggers — both are required by GATE-04:
  - `on.push` with `paths:` limited to `apps/inheritance/**` and `.github/workflows/inheritance-ci.yml`. No `branches` filter: it must fire on every branch. The `paths:` scope is necessary because this monorepo has a concurrent auto-committer producing frequent unrelated commits (RESEARCH.md §5) — without it, every `fitness log` commit would trigger a full Rust + WASM + Node run.
  - `on.pull_request` with the same `paths:` list.
- One job, `gates`, `runs-on: ubuntu-latest`, `timeout-minutes: 30`.
- Steps, in order:
  1. `actions/checkout@v4`.
  2. `dtolnay/rust-toolchain@stable` with `targets: wasm32-unknown-unknown`. This is the same action the existing workflow uses.
  3. Install wasm-pack: `curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh`. Matches the existing workflow. Do NOT pin a wasm-bindgen version — RESEARCH.md §4 measured that wasm-pack resolves `0.2.114` from the committed `Cargo.lock` automatically.
  4. `actions/setup-node@v4` with `node-version: 20` and `cache: npm` and `cache-dependency-path: apps/inheritance/frontend/package-lock.json`.
  5. Install frontend dependencies: `npm ci` with `working-directory: apps/inheritance/frontend`. Use `npm ci`, NOT `npm install` and NOT `pnpm` — RESEARCH.md §1.1 measured that this tree has a committed npm `package-lock.json` (lockfileVersion 3) and no pnpm lockfile. The existing workflow's `npm install -g pnpm` step is a red herring; do not copy it.
  6. The single gate step, named `Run all gates`: `run: bash apps/inheritance/scripts/ci-gates.sh`.

The workflow must contain exactly one step that runs project checks. All gate logic lives in the committed script, so what CI runs and what a developer runs are the same bytes (RESEARCH.md §3.2).

Prohibited anywhere in this file (constraint 6): `continue-on-error`, `|| true`, `if: always()`, `--passWithNoTests`. Also do not add `fail-fast: false` — there is only one job, and a matrix here would only create ways for a red gate to be reported green.

Add a top-of-file comment stating: this workflow is the enforcement point for GATE-01 through GATE-04; all gate logic is in `apps/inheritance/scripts/ci-gates.sh` so it can be run and debugged locally; and no step here may be made non-blocking.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo && python3 -c "
import yaml,sys
d=yaml.safe_load(open('.github/workflows/inheritance-ci.yml'))
on=d.get('on', d.get(True))
print('name:', d['name'])
print('has push:', 'push' in on)
print('has pull_request:', 'pull_request' in on)
print('push paths:', on['push']['paths'])
print('pr paths:', on['pull_request']['paths'])
steps=d['jobs']['gates']['steps']
print('steps:', [s.get('name') or s.get('uses') for s in steps])
"; echo "YAML_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `/home/clsandoval/cs/monorepo/.github/workflows/inheritance-ci.yml` exists and parses as valid YAML via `python3 -c "import yaml; yaml.safe_load(open(...))"`
    - The parsed `on` mapping contains both the key `push` and the key `pull_request`
    - Both `on.push.paths` and `on.pull_request.paths` contain `apps/inheritance/**`
    - Neither `on.push` nor `on.pull_request` contains a `branches` key
    - The file contains the literal string `bash apps/inheritance/scripts/ci-gates.sh`
    - The file contains `npm ci` and does NOT contain `pnpm` or `npm install -g`
    - The file contains `wasm32-unknown-unknown` and `node-version: 20`
    - `grep -cE "continue-on-error|\|\| true|if: always\(\)|--passWithNoTests" .github/workflows/inheritance-ci.yml` returns 0
    - The existing `.github/workflows/inheritance.yml` is unchanged: `git diff --stat -- .github/workflows/inheritance.yml` produces empty output
    - The `gates` job has exactly one step whose `run` invokes a project check, and that step is `bash apps/inheritance/scripts/ci-gates.sh`
  </acceptance_criteria>
  <done>Every push and pull request touching `apps/inheritance/**` runs all four gates, and no step can pass while failing.</done>
</task>

<task type="auto">
  <name>Task 4: Write README.md with the clean-checkout sequence, and stop tracking tsconfig.tsbuildinfo</name>
  <files>README.md, frontend/.gitignore</files>
  <read_first>scripts/ci-gates.sh, engine/build-wasm.sh, frontend/package.json, frontend/.gitignore, CLAUDE.md</read_first>
  <action>
**Part A — README.md.** Create `apps/inheritance/README.md`. There is currently no README anywhere in this app. GATE-01 says the suite must run "from a clean checkout (after documented install steps)" — this file is those documented steps.

Sections, in this order:

1. *What this is* — two sentences: a Rust succession engine compiled to WASM plus a React frontend and a TypeScript estate-tax engine. Point at `CLAUDE.md` for full project context; do not duplicate it.
2. *Clean checkout to green gates* — a numbered sequence of the exact commands, each in its own fenced block, with the working directory stated:
   - `rustup target add wasm32-unknown-unknown`
   - `cargo install wasm-pack`
   - `cd apps/inheritance/frontend && npm ci`
   - `cd apps/inheritance && bash scripts/ci-gates.sh`
   State that the fourth command runs all four gates and that a green result there is what CI checks.
3. *The four gates* — a table with columns Gate / Command / What it proves, one row each for: engine tests (`cd engine && cargo test`), WASM build (`bash engine/build-wasm.sh`), frontend suite (`cd frontend && npm run test:gate`), typecheck (`cd frontend && npx tsc -b --force`). Note in the typecheck row that `--force` is required because `tsconfig.tsbuildinfo` was historically committed and an incremental run can no-op.
4. *Known test failures* — explain that `frontend/test-baseline.json` records the currently-known-failing tests, that `npm run test:gate` fails on any failure not in that ledger and also on any test that is skipped or on the ledger becoming stale, and state the two rules explicitly: **the ledger may only shrink**, and **adding an entry to make the build pass is prohibited — fix the regression instead**. Give the current count (46) and name `src/routes/settings/__tests__/team.test.tsx` (12 entries) as the largest single cluster and a genuine product bug rather than a test-authoring artifact.
5. *Committing in this repo* — one short paragraph: this app lives inside a monorepo with a concurrent auto-committer, so every commit must stage explicit file paths; `git add -A`, `git add .`, and `git commit -a` are prohibited.

Keep it factual. Do not invent commands that were not created in this phase, and do not document local Supabase or seed data — that is Phase 3's reproducible-environment work and does not exist yet.

**Part B — stop tracking the build cache.** `frontend/tsconfig.tsbuildinfo` is currently committed. It is a build artifact, and a stale committed cache is a live false-negative source for GATE-02. Remove it from the index without deleting it from disk:

`git rm --cached apps/inheritance/frontend/tsconfig.tsbuildinfo`

Then add the line `tsconfig.tsbuildinfo` to `apps/inheritance/frontend/.gitignore`, after the existing `.vite/` line. Leave `node_modules/`, `dist/`, `.vite/`, and `src/wasm/pkg/*.wasm` unchanged.

CI already uses `--force` (Task 1), so this does not change CI behavior — it removes the possibility of a stale cache being distributed to a fresh clone and masking a type error for a local developer.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo && git ls-files apps/inheritance/frontend/tsconfig.tsbuildinfo; echo "TRACKED_LINES=$(git ls-files apps/inheritance/frontend/tsconfig.tsbuildinfo | wc -l)"; cat apps/inheritance/frontend/.gitignore; wc -l apps/inheritance/README.md
  </verify>
  <acceptance_criteria>
    - `apps/inheritance/README.md` exists and is at least 40 lines
    - `README.md` contains all five literal strings `rustup target add wasm32-unknown-unknown`, `cargo install wasm-pack`, `npm ci`, `bash scripts/ci-gates.sh`, `npm run test:gate`
    - `README.md` contains `npx tsc -b --force` and contains `tsconfig.tsbuildinfo`
    - `README.md` contains the literal string `may only shrink` and the literal string `test-baseline.json`
    - `README.md` contains the literal string `git add -A` in the context of it being prohibited
    - `README.md` does NOT mention `supabase db`, `seed.sql`, or a local Supabase setup (those are Phase 3 and do not exist yet)
    - `git ls-files apps/inheritance/frontend/tsconfig.tsbuildinfo` produces empty output (no longer tracked)
    - `test -f apps/inheritance/frontend/tsconfig.tsbuildinfo` still succeeds (file still on disk, only untracked)
    - `frontend/.gitignore` contains the line `tsconfig.tsbuildinfo` and still contains `node_modules/`, `dist/`, `.vite/`, `src/wasm/pkg/*.wasm`
  </acceptance_criteria>
  <done>A reader can go from clean checkout to four green gates by following the README, and no stale type-check cache ships in the repo.</done>
</task>

<task type="auto">
  <name>Task 5: Final full-gate run and commit</name>
  <files>scripts/ci-gates.sh, README.md, frontend/.gitignore, ../../.github/workflows/inheritance-ci.yml</files>
  <read_first>scripts/ci-gates.sh, README.md, /home/clsandoval/cs/monorepo/.github/workflows/inheritance-ci.yml</read_first>
  <action>
Run the complete gate suite one final time from a clean state and confirm every roadmap Phase 1 success criterion holds:

`cd /home/clsandoval/cs/monorepo && rm -f apps/inheritance/frontend/src/wasm/pkg/inheritance_engine_bg.wasm && bash apps/inheritance/scripts/ci-gates.sh`

Deleting the WASM artifact first is deliberate: it proves gate 2 genuinely regenerates it and that gate 3 is not passing on a leftover binary. Expected: exit 0, output contains `ALL GATES PASSED (4/4)`.

Then commit. From `/home/clsandoval/cs/monorepo`, stage exactly these five paths:

`git add apps/inheritance/scripts/ci-gates.sh apps/inheritance/README.md apps/inheritance/frontend/.gitignore .github/workflows/inheritance-ci.yml`

plus the staged deletion of `apps/inheritance/frontend/tsconfig.tsbuildinfo` already produced by `git rm --cached` in Task 4.

Verify with `git diff --cached --name-only` that exactly those five paths appear and nothing else. Unstage anything else with `git restore --staged <path>`. In particular confirm that no file under `apps/inheritance/engine/src/`, no `*.test.ts(x)`, and no other app's workflow is staged.

Commit message: `ci(01): push/PR workflow running all four gates via one local-runnable script`

NEVER run `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo && git log -1 --name-only --format="%s" && rm -f apps/inheritance/frontend/src/wasm/pkg/inheritance_engine_bg.wasm && bash apps/inheritance/scripts/ci-gates.sh | tail -3; echo "EXIT=${PIPESTATUS[0]}"
  </verify>
  <acceptance_criteria>
    - After deleting the WASM artifact, `bash apps/inheritance/scripts/ci-gates.sh` exits 0 and outputs `ALL GATES PASSED (4/4)`
    - `frontend/src/wasm/pkg/inheritance_engine_bg.wasm` exists again after the run, with size > 100000 bytes
    - `git log -1 --format=%s` outputs `ci(01): push/PR workflow running all four gates via one local-runnable script`
    - `git log -1 --name-only --format=` lists exactly: `.github/workflows/inheritance-ci.yml`, `apps/inheritance/README.md`, `apps/inheritance/frontend/.gitignore`, `apps/inheritance/frontend/tsconfig.tsbuildinfo`, `apps/inheritance/scripts/ci-gates.sh`
    - `git log -1 --name-only` lists no file under `apps/inheritance/engine/src/`, no `*.test.ts` or `*.test.tsx`, and no workflow other than `inheritance-ci.yml`
    - `git status --porcelain apps/inheritance/` shows no unexpected leftover modifications
  </acceptance_criteria>
  <done>All four gates pass from a WASM-less starting state, and the phase's CI enforcement is committed.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|---|---|---|---|
| T-1-04 | A gate step is silently made non-blocking (`continue-on-error`, `\|\| true`, `if: always()`), so CI reports green on a failing gate | High | Constraint 6 prohibits each by name in both script and workflow; acceptance criteria grep both files and require zero matches; Task 2 empirically observes the runner exiting 1 on an injected regression. |
| T-1-04b | The workflow exists but never fires — wrong trigger, over-narrow `paths`, or a `branches` filter | High | Task 3's verify parses the YAML and asserts both `push` and `pull_request` exist, that `paths` includes `apps/inheritance/**`, and that no `branches` key is present. |
| T-1-04c | CI logic diverges from what a developer can run, so failures are only reproducible by pushing | Medium | All gate logic lives in one committed script; the workflow's only project-check step invokes it; the phase gate is that same script exiting 0 locally. |
| T-1-04d | Gate 3 passes on a stale WASM binary left over from an earlier run rather than on a freshly built one | Medium | Task 5 deletes `inheritance_engine_bg.wasm` before the final run; gate 2 must regenerate it; `engine/build-wasm.sh` independently verifies size and magic number. |
| T-1-04e | A committed stale `tsconfig.tsbuildinfo` makes `tsc -b` no-op and report clean on a tree with type errors | Medium | Gate 4 uses `--force`; Task 4 untracks the file and adds it to `.gitignore`. |
| T-1-04f | A push-triggered workflow at the monorepo root fires on the auto-committer's unrelated commits, burning minutes and training everyone to ignore the check | Medium | Both triggers are `paths:`-scoped to `apps/inheritance/**` plus the workflow file itself. |
| T-1-04g | A broad `git add` sweeps the auto-committer's in-flight work or another app's workflow into this commit | High | Task 5 stages five explicit paths and verifies `git diff --cached --name-only` before committing; constraints 2, 3, and 4 prohibit touching other workflows and broad staging. |
| T-1-04h | The workflow leaks or requires a secret | Low | The workflow declares no `secrets`, no `env` credentials, and uses the default `GITHUB_TOKEN` implicitly for checkout only. It runs tests; it does not deploy, push, or publish. Do not add `permissions: write` or any token. |

The new workflow executes only checked-in code plus two well-known toolchain installers (`dtolnay/rust-toolchain@stable`, the official wasm-pack init script) already used by the existing workflow in this repo. `npm ci` installs strictly from the committed lockfile, so dependency resolution is pinned.
</threat_model>

<verification>
Before declaring plan complete, run each and confirm the stated result:
- [ ] `cd /home/clsandoval/cs/monorepo && rm -f apps/inheritance/frontend/src/wasm/pkg/inheritance_engine_bg.wasm && bash apps/inheritance/scripts/ci-gates.sh` → exit 0, `ALL GATES PASSED (4/4)`
- [ ] `cd apps/inheritance/engine && cargo test` → exit 0, 442 passed / 0 failed
- [ ] `cd apps/inheritance/frontend && npm run test:gate` → exit 0, `GATE OK`, suite still reports 2416 total tests
- [ ] `cd apps/inheritance/frontend && npx tsc -b --force` → exit 0, no output
- [ ] `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/inheritance-ci.yml')); on=d.get('on', d.get(True)); assert 'push' in on and 'pull_request' in on"` → exit 0
- [ ] `grep -cE "continue-on-error|\|\| true|if: always\(\)|--passWithNoTests" .github/workflows/inheritance-ci.yml apps/inheritance/scripts/ci-gates.sh` → 0 for both files
- [ ] `git diff --stat -- .github/workflows/inheritance.yml` → empty (the pre-existing Ralph workflow is untouched)
- [ ] `git ls-files apps/inheritance/frontend/tsconfig.tsbuildinfo` → empty
- [ ] `git diff --name-only -- 'apps/inheritance/frontend/src/**/*.test.ts' 'apps/inheritance/frontend/src/**/*.test.tsx' 'apps/inheritance/engine/src/'` → empty
- [ ] Task 2's injected-regression experiment was performed and the runner exited 1 with `UNKNOWN FAILURE`
</verification>

<success_criteria>
- All five tasks completed
- Roadmap Phase 1 success criterion 1: `npm test` executes the full Vitest suite and reports real counts from a clean checkout after the documented install steps in README.md
- Roadmap Phase 1 success criterion 2: `npx tsc -b --force` completes with zero type errors
- Roadmap Phase 1 success criterion 3: one documented command builds `inheritance_engine_bg.wasm` into `frontend/src/wasm/pkg/` and the WASM-dependent test files pass against it
- Roadmap Phase 1 success criterion 4: a CI workflow triggers on every push and pull request, runs `cargo test`, the WASM build, the frontend suite, and the typecheck, and fails when any of them fails — the last of which was demonstrated, not assumed
- No test, assertion, engine source file, or unrelated workflow was modified
- Exactly one commit was made, naming exactly five explicit paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-gate-foundations-suites-execute-at-all/01-04-SUMMARY.md`.
Record: the full `ci-gates.sh` output from the final clean run, the parsed YAML trigger assertions, the exit code and message from the Task 2 injected-regression experiment, and the commit SHA.
Note as carried-forward debt for later phases: the 46 known failures in `frontend/test-baseline.json`, of which the 12 in `src/routes/settings/__tests__/team.test.tsx` are a genuine undefined-import product bug rather than a test-authoring artifact.
</output>
