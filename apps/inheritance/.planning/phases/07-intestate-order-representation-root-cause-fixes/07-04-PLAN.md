---
phase: 07-intestate-order-representation-root-cause-fixes
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - engine/tests/integration.rs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LAW-01, LAW-02, LAW-03, LAW-04]
user_setup: []

must_haves:
  truths:
    - "Each of the four defects has a named test vector that cites its governing articles and asserts exact centavos"
    - "Every previously committed expected value in the vector suite is unchanged"
    - "The rebuilt WASM binary carries the fixed engine, so the frontend suite tests the new code"
    - "The four requirements are marked complete only after a command was run and its output read"
    - "No test, assertion or gate is deleted, skipped, weakened or loosened"
  artifacts:
    - path: "engine/tests/integration.rs"
      provides: "Four named regression vectors, one per requirement, with exact centavo assertions"
      contains: "test_law01_grandparents_inherit_under_regime_b"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAW-01 through LAW-04 marked complete with their traceability rows updated"
      contains: "LAW-04"
  key_links:
    - from: "engine/tests/integration.rs"
      to: ".planning/research/LEGAL-CONFORMANCE.md"
      via: "each vector's comment cites the row of section 2a that recorded the defect"
      pattern: "LEGAL-CONFORMANCE"
    - from: "engine/tests/integration.rs"
      to: "engine/src/step2_lines.rs"
      via: "the vectors exercise the anchor rule the three prior plans installed"
      pattern: "run_pipeline"
---

<objective>
Lock the four closed defects behind named regression vectors, rebuild the WASM artifact so the
frontend tests the fixed engine, and record the four requirements as complete against commands whose
output was read.

Plans 07-01 through 07-03 changed engine behaviour and proved each change against a scratch input and
the committed corpus. A scratch input under `/tmp` protects nothing. This plan converts each of the
four measured before-and-after pairs into a named vector in `engine/tests/integration.rs`, in that
file's established style: one function per vector, exact centavo assertions through
`assert_total_centavos`, a shape assertion through `assert_vector_shape`, the sum invariant through
`check_sum_invariant`, and a header comment naming the governing articles and the
`.planning/research/LEGAL-CONFORMANCE.md` §2a row the vector closes.

`engine/tests/integration.rs` holds its own inline copy of the pipeline and its own person helpers
(`person`, `parent`, `sibling`, `find_share`, `assert_total_centavos`, `assert_total_pesos`,
`assert_vector_shape`, `check_sum_invariant`, `check_scenario_consistency`). Read those helpers before
writing, and reuse them rather than adding new ones. The degree mapping at
`engine/tests/integration.rs:437-440` already assigns degree 1 to `LegitimateParent`, degree 2 to
`LegitimateAscendant` and `Sibling`, and degree 3 to `NephewNiece`, so the new vectors do not need to
set `degree` by hand except where a heir sits deeper than that mapping provides.

The four expected outputs are fixed here and are not open for the executor to choose. Each is derived
from a codal sentence quoted in the constraints and from an estate figure stated in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PLAN-STANDARD.md
@.planning/phases/07-intestate-order-representation-root-cause-fixes/07-RESEARCH.md
@.planning/phases/07-intestate-order-representation-root-cause-fixes/07-VALIDATION.md
@.planning/research/LEGAL-CONFORMANCE.md
@.planning/REQUIREMENTS.md
@engine/tests/integration.rs
@engine/tests/defect_ledger.rs
@engine/tests/fuzz_invariants.rs
@engine/build-wasm.sh
@GATES.md
@gates.manifest.json
</context>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. Commit arguments are
   repo-root-relative; every other path is relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken or loosen a test, an assertion or a gate. **No existing expected value
   in `engine/tests/integration.rs` may be edited.** `07-RESEARCH.md` section 6.1 read TV-15, TV-19,
   TV-20 and TV-23 line by line and established that none of their families moves under this phase's
   four rules. If one of them nevertheless fails, that is a defect in plans 07-01 through 07-03: stop,
   report BLOCKED with the pasted failure, and do not adjust the expectation.
4. **No point of Philippine law may be decided.** The four expected outputs below are each derived
   from a quoted codal sentence: Art. 987 ¶1 and ¶2 for the grandparent split; Arts. 1005, 1006 and
   1008 for the collateral split; Art. 969 with Art. 962 ¶1 for the promoted degree; Art. 972 ¶1 with
   Art. 986 ¶2 for the ascending-line prohibition. If a question arises that these do not answer, stop
   and report BLOCKED and append the question to `.planning/LAWYER-AGENDA.md` without answering it.
5. **Pin no expected centavo value for a nephews-alone case with mixed full and half blood.** That
   shape is governed by the recorded question LAWYER-03, whose status in
   `.planning/lawyer-decisions.json` is `awaiting-answer`. None of the four vectors below has that
   shape, and none may be given it.
6. Only two files may be edited: `engine/tests/integration.rs` and `.planning/REQUIREMENTS.md`. Do not
   edit anything under `engine/src/`, `engine/examples/`, `engine/defect-baseline.json`, `frontend/`,
   `gates.manifest.json`, `gates.manifest.lock`, `gate-skips.lock`, `frontend/test-baseline.json` or
   `GATES.md`.
7. `frontend/src/wasm/pkg/inheritance_engine_bg.wasm` is a build artifact ignored by
   `frontend/.gitignore` and must not be staged. Rebuilding it is a verification step, not a
   deliverable.
8. `cd engine && cargo test` must report **0 failed**, and the passing count must be at least 485 —
   481 before this phase, plus the four vectors added here, plus every unit test added by plans 07-01
   through 07-03.
9. Do not mark a requirement complete in `.planning/REQUIREMENTS.md` on the strength of a plan
   summary. Each of the four is ticked only after the vector that proves it has been observed passing
   in a `cargo test` run performed in this plan.
10. `bash scripts/ci-gates.sh` still halts at `G3` because of Phase 5's unresolved OBS-05 and OBS-06
    product decision, which this phase does not touch. `ALL GATES PASSED (13/13)` is not achievable
    and must not be claimed in any summary.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Named vectors for LAW-01 and LAW-04, the ascendant tier</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/tests/integration.rs, .planning/phases/07-intestate-order-representation-root-cause-fixes/07-RESEARCH.md, .planning/research/LEGAL-CONFORMANCE.md</read_first>
  <action>
Append two vectors to `engine/tests/integration.rs`, after the existing TV-23 block and before the
cross-cutting invariant tests, each with a banner comment in the file's established style naming the
requirement, the governing articles, and the `LEGAL-CONFORMANCE.md` §2a row it closes.

**`test_law01_grandparents_inherit_under_regime_b`** — estate `Money::from_pesos(12_000_000)`, that
is 1200000000 centavos. Family tree: three `Relationship::LegitimateAscendant` persons at degree 2,
all alive — `gp1` and `gp2` with `line: Some(LineOfDescent::Paternal)`, `gp3` with
`line: Some(LineOfDescent::Maternal)`. No spouse, no descendants, no will, no donations.

Assertions:
- `check_sum_invariant` against the estate
- `assert_vector_shape(&output, ScenarioCode::I5, SuccessionType::Intestate, 3, "LAW-01")`
- `check_scenario_consistency(&output, ScenarioCode::I5)`
- `assert_total_centavos(find_share(&output, "gp1"), 300000000, "LAW-01 gp1")`
- `assert_total_centavos(find_share(&output, "gp2"), 300000000, "LAW-01 gp2")`
- `assert_total_centavos(find_share(&output, "gp3"), 600000000, "LAW-01 gp3")`
- an assertion that no row in `output.per_heir_shares` has `heir_id == "STATE"`

Comment the arithmetic: Art. 987 ¶1 puts the estate in the grandparent tier in default of the
parents; Art. 987 ¶2 gives one half to the paternal line and one half to the maternal, per capita
within each line, so the two paternal grandparents take 300000000 each and the sole maternal
grandparent takes 600000000. Record the before-value from `07-RESEARCH.md` section 1.1 in the same
comment: scenario `I15` with a single `STATE` row of 1200000000.

**`test_law04_no_representation_in_the_ascending_line`** — estate `Money::from_pesos(12_000_000)`.
Family tree: a `Relationship::LegitimateParent` `fa` with `is_alive_at_succession: false`,
`children: vec!["sib1".into()]` and `line: Some(LineOfDescent::Paternal)`; a living
`Relationship::LegitimateParent` `mo` with `line: Some(LineOfDescent::Maternal)`; a living
`Relationship::Sibling` `sib1` with `blood_type: Some(BloodType::Full)`.

Assertions:
- `check_sum_invariant` against the estate
- `check_scenario_consistency(&output, ScenarioCode::I5)`
- `assert_total_centavos(find_share(&output, "mo"), 1200000000, "LAW-04 mo")`
- `assert_total_centavos(find_share(&output, "fa"), 0, "LAW-04 fa")`
- an assertion that no row in `output.per_heir_shares` has `heir_id == "sib1"`

Comment the arithmetic: Art. 972 ¶1 states that representation "takes place in the direct descending
line, but never in the ascending", so the predeceased father is not represented by his own children,
who are the decedent's siblings; Art. 986 ¶2 gives the whole estate to the sole surviving parent; and
Art. 985 excludes collaterals while an ascendant survives. Record the before-value from
`07-RESEARCH.md` section 1.4: `fa=6000000000` and `mo=6000000000`, against a sibling-free control of
`fa=0` and `mo=12000000000`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && cargo test --test integration 2>&1 | grep -E "law01|law04|test result" | tail -10; cargo test 2>&1 | tail -20; echo "CARGO_TEST_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `test_law01_grandparents_inherit_under_regime_b` and `test_law04_no_representation_in_the_ascending_line` both appear by name in the `cargo test --test integration` output and pass
    - `cd engine && cargo test` reports `0 failed`
    - `git diff engine/tests/integration.rs` shows only additions after the TV-23 block and no change to any pre-existing `assert_total_centavos` or `assert_total_pesos` line
    - Both new vectors carry a comment citing their governing articles and the before-values from research
  </acceptance_criteria>
  <done>The two ascendant-tier defects cannot silently return.</done>
</task>

<task type="auto">
  <name>Task 2: Named vectors for LAW-02 and LAW-03</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/tests/integration.rs, engine/examples/defect-cases/01-collateral-halfblood-nephews.json, .planning/research/LEGAL-CONFORMANCE.md</read_first>
  <action>
Append two more vectors in the same place and the same style.

**`test_law02_collateral_representation_conserves_the_estate`** — estate
`Money::from_pesos(6_000_000)`, that is 600000000 centavos. Family tree, mirroring the committed input
`engine/examples/defect-cases/01-collateral-halfblood-nephews.json`: a living `Relationship::Sibling`
`sib1` with `blood_type: Some(BloodType::Full)`; a `Relationship::Sibling` `sib2` with
`is_alive_at_succession: false`, `blood_type: Some(BloodType::Half)` and
`children: vec!["n1".into(), "n2".into()]`; two living `Relationship::NephewNiece` heirs `n1` and `n2`,
each with `blood_type: Some(BloodType::Half)`.

Assertions:
- `check_sum_invariant` against the estate
- `assert_vector_shape(&output, ScenarioCode::I13, SuccessionType::Intestate, 3, "LAW-02")`
- `check_scenario_consistency(&output, ScenarioCode::I13)`
- `assert_total_centavos(find_share(&output, "sib1"), 400000000, "LAW-02 sib1")`
- `assert_total_centavos(find_share(&output, "n1"), 100000000, "LAW-02 n1")`
- `assert_total_centavos(find_share(&output, "n2"), 100000000, "LAW-02 n2")`
- an assertion that the `heir_id` values across `output.per_heir_shares` are all distinct
- an assertion that no row has `heir_id == "sib2"`

Comment the arithmetic: Art. 1005 gives surviving siblings per capita and the children of siblings
per stirpes; Art. 1006 gives the full blood double the half blood; Art. 1008 carries that ratio to the
representatives. One full-blood line at 2 units plus one half-blood line at 1 unit is 3 units of
200000000 centavos, so `sib1` takes 400000000 and the two nephews split their line's 200000000. Record
the before-value from `07-RESEARCH.md` section 1.2: five rows, `n1` and `n2` each duplicated, a sum of
480000000 against a 600000000 estate, and a CLI exit code of 2.

**`test_law03_total_repudiation_promotes_the_following_degree`** — estate
`Money::from_pesos(120_000_000)`, that is 12000000000 centavos. Family tree: three
`Relationship::LegitimateChild` heirs `lc1`, `lc2`, `lc3`, all alive, all with `has_renounced: true`,
each naming exactly one child in `children` — `gc1`, `gc2`, `gc3` respectively; three living
`Relationship::LegitimateChild` heirs `gc1`, `gc2`, `gc3` with `degree` set to 2.

Assertions:
- `check_sum_invariant` against the estate
- `assert_vector_shape(&output, ScenarioCode::I1, SuccessionType::Intestate, 6, "LAW-03")`
- `check_scenario_consistency(&output, ScenarioCode::I1)`
- `assert_total_centavos(find_share(&output, "gc1"), 4000000000, "LAW-03 gc1")`
- `assert_total_centavos(find_share(&output, "gc2"), 4000000000, "LAW-03 gc2")`
- `assert_total_centavos(find_share(&output, "gc3"), 4000000000, "LAW-03 gc3")`
- `assert_total_centavos(find_share(&output, "lc1"), 0, "LAW-03 lc1")`
- an assertion that no row has `heir_id == "STATE"`

If the row count that `assert_vector_shape` observes is not 6, read the actual row list, use the
observed count in the assertion, and state in the plan summary which repudiating heirs carry a
zero-valued row. The three grandchild amounts and the absence of a `STATE` row are the load-bearing
assertions and may not be relaxed.

Comment the arithmetic: Art. 969 gives the following degree the inheritance "in their own right",
Art. 962 ¶1 divides among relatives of the same degree "in equal shares", and Art. 977 bars the
grandchildren from representing the repudiators, so 12000000000 divides three ways into 4000000000
each. Record the before-value from `07-RESEARCH.md` section 1.3: scenario `I15` with a single `STATE`
row of 12000000000 and the grandchildren absent.

This family is deliberately symmetric — one grandchild per repudiating child — so that an own-right
per-capita division and a per-stirpes division produce identical amounts. The vector therefore proves
the promotion without pinning a division the plan has not derived from a quoted article.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && cargo test --test integration 2>&1 | grep -E "law0[1-4]|test result" | tail -12; cargo test 2>&1 | tail -20; echo "CARGO_TEST_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `test_law02_collateral_representation_conserves_the_estate` and `test_law03_total_repudiation_promotes_the_following_degree` both appear by name in the `cargo test --test integration` output and pass
    - The LAW-02 vector asserts three rows summing to 600000000 with all `heir_id` values distinct
    - The LAW-03 vector asserts three grandchild rows of 4000000000 each and the absence of a `STATE` row
    - `cd engine && cargo test` reports `0 failed` with at least 485 passing
    - No pre-existing expected value anywhere in `engine/tests/integration.rs` was edited, shown by `git diff`
  </acceptance_criteria>
  <done>All four defects are locked behind named vectors with exact centavo assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the WASM artifact and measure the frontend against its known-failure ledger</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/build-wasm.sh, frontend/.gitignore, frontend/test-baseline.json, gates.manifest.json, GATES.md</read_first>
  <action>
The frontend suite loads a compiled WASM binary from `frontend/src/wasm/pkg/`. Until it is rebuilt,
the frontend is testing the pre-fix engine. Run:

    bash engine/build-wasm.sh

Confirm it exits 0 and that `frontend/src/wasm/pkg/inheritance_engine_bg.wasm` has a newer
modification time than the last `engine/src/` edit. Do not stage that binary; `frontend/.gitignore`
ignores it deliberately.

Then run the frontend gate exactly as gate `G3` runs it:

    cd frontend && npm run test:gate

Record its exit code and its full failure list. The expected state, unchanged by this phase, is that
`G3` fails on the five tests that Phase 5 left red for the unresolved OBS-05 and OBS-06 product
decision. Compare the observed failure set against `frontend/test-baseline.json` and report in the
plan summary whether the set is identical, and if it is not, name every test that newly fails.

**A newly failing frontend test is a BLOCKED condition, not a licence to edit anything.** Do not add
an entry to `frontend/test-baseline.json`, do not edit a frontend test, and do not edit
`gate-skips.lock`. If the failure set grew, stop and report BLOCKED with the pasted output naming the
new failures, so the engine change that caused them can be corrected in a follow-up.

Finally, run the gates that sit ahead of `G3` in the runner and record each exit code:

    node scripts/check-gate-manifest.mjs
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs
    bash scripts/coverage-report.sh && node scripts/check-coverage.mjs
    node scripts/check-assertion-discipline.mjs
    cd engine && cargo test
    bash engine/build-wasm.sh

and the two that sit after it and must therefore be run directly:

    node scripts/check-lawyer-agenda.mjs
    node scripts/check-observability.mjs
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && bash engine/build-wasm.sh; echo "G2_EXIT=$?"; ls -l frontend/src/wasm/pkg/inheritance_engine_bg.wasm; (cd frontend && npm run test:gate); echo "G3_EXIT=$?"; node scripts/check-gate-manifest.mjs; echo "G5=$?"; node scripts/check-plan-closed-world.mjs; echo "G6=$?"; node scripts/check-commit-discipline.mjs; echo "G7=$?"; node scripts/check-assertion-discipline.mjs; echo "G13=$?"; node scripts/check-lawyer-agenda.mjs; echo "G10=$?"; node scripts/check-observability.mjs; echo "G11=$?"; git status --porcelain frontend/
  </verify>
  <acceptance_criteria>
    - `bash engine/build-wasm.sh` exits 0 and the rebuilt `inheritance_engine_bg.wasm` is newer than the last `engine/src/` edit
    - The frontend failure set is compared against `frontend/test-baseline.json` and the comparison is pasted into the plan summary
    - `frontend/test-baseline.json` and `gate-skips.lock` are unmodified, shown by an empty `git status --porcelain` for both
    - `node scripts/check-gate-manifest.mjs`, `check-plan-closed-world.mjs`, `check-commit-discipline.mjs`, `check-assertion-discipline.mjs`, `check-lawyer-agenda.mjs` and `check-observability.mjs` each exit 0
    - `git status --porcelain frontend/` shows no staged or modified tracked file under `frontend/`
  </acceptance_criteria>
  <done>The frontend is measured against the fixed engine, and any regression it would reveal is surfaced rather than absorbed.</done>
</task>

<task type="auto">
  <name>Task 4: Mark the four requirements complete and commit</name>
  <files>.planning/REQUIREMENTS.md, engine/tests/integration.rs</files>
  <read_first>.planning/REQUIREMENTS.md, scripts/safe-commit.sh, .planning/PLAN-STANDARD.md</read_first>
  <action>
Only after task 2's `cargo test --test integration` run was observed passing with all four named
vectors, edit `.planning/REQUIREMENTS.md`:

- In the Legal Conformance list, change `- [ ] **LAW-01**` to `- [x] **LAW-01**` and do the same for
  LAW-02, LAW-03 and LAW-04. Do not change the requirement text on any of the four lines.
- In the traceability table further down, change the `Status` cell from `Pending` to `Complete` on
  the four rows whose first cell is `LAW-01`, `LAW-02`, `LAW-03` and `LAW-04`. Leave every other row
  untouched, including the LAW-05 through LAW-15 rows.
- Update the trailing `*Last updated: ...*` line to name Phase 7 execution and today's date, in the
  same shape the existing line uses for Phase 3.

Do not tick any other checkbox anywhere in the file. In particular OBS-05 and OBS-06 remain unticked;
this phase did not touch them.

Then stage and commit exactly the two edited files:

    bash scripts/safe-commit.sh -m "test(07-04): named regression vectors for the intestate order and representation fixes (LAW-01, LAW-02, LAW-03, LAW-04)" apps/inheritance/engine/tests/integration.rs apps/inheritance/.planning/REQUIREMENTS.md
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -n "LAW-0[1-4]" .planning/REQUIREMENTS.md | head -20; grep -c "^- \[ \] \*\*OBS-0[56]\*\*" .planning/REQUIREMENTS.md; git log -1 --stat --format="%s"; node scripts/check-commit-discipline.mjs; echo "G7_EXIT=$?"; cd engine && cargo test 2>&1 | tail -20
  </verify>
  <acceptance_criteria>
    - LAW-01, LAW-02, LAW-03 and LAW-04 each show `- [x]` in the Legal Conformance list and `Complete` in the traceability table
    - OBS-05 and OBS-06 are still shown as unticked, and no other checkbox changed, verified by `git diff .planning/REQUIREMENTS.md`
    - `git log -1 --stat` shows exactly two changed files
    - `node scripts/check-commit-discipline.mjs` exits 0
    - `cd engine && cargo test` reports `0 failed` with at least 485 passing
  </acceptance_criteria>
  <done>The four requirements are recorded complete on the strength of observed test output.</done>
</task>

</tasks>

<verification>

Run from `/home/clsandoval/cs/monorepo/apps/inheritance`:

    cd engine && cargo test 2>&1 | tail -25
    cd engine && cargo test --test integration 2>&1 | grep -E "law0[1-4]|test result"
    cd engine && cargo test --test fuzz_invariants --test defect_ledger --test observability
    bash engine/build-wasm.sh
    node scripts/check-lawyer-agenda.mjs
    node scripts/check-observability.mjs
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs
    bash scripts/ci-gates.sh

Expected: `cargo test` reports `0 failed` with at least 485 passing; all four `law0N` vectors pass by
name; the three other engine binaries report `0 failed`; the WASM build exits 0; the four static
checks exit 0.

`bash scripts/ci-gates.sh` is expected to reach `G3` and halt there with the same five frontend
failures Phase 5 left behind. That is the recorded, unchanged state of the runner and is not a Phase 7
regression. `ALL GATES PASSED (13/13)` is not achievable in this phase and must not be claimed. If the
runner halts anywhere **before** `G3`, or if `G3`'s failure set has grown, stop and report BLOCKED
with the pasted output.

</verification>

<success_criteria>

- [ ] Four named vectors exist in `engine/tests/integration.rs`, one per requirement, each citing its
      governing articles and the `LEGAL-CONFORMANCE.md` row it closes
- [ ] The grandparent vector asserts `I5` with 300000000, 300000000 and 600000000 and no `STATE` row
- [ ] The collateral vector asserts `I13` with three distinct rows of 400000000, 100000000 and
      100000000 summing to 600000000
- [ ] The repudiation vector asserts `I1` with three grandchild rows of 4000000000 and no `STATE` row
- [ ] The ascending-line vector asserts the sole surviving parent at 1200000000, the predeceased
      parent at 0, and no sibling row
- [ ] No pre-existing expected value in the vector suite was edited
- [ ] The WASM artifact was rebuilt and the frontend failure set was compared against
      `frontend/test-baseline.json`, which was not modified
- [ ] LAW-01 through LAW-04 are marked complete in `.planning/REQUIREMENTS.md` and no other checkbox
      moved
- [ ] `cd engine && cargo test` reports 0 failed with at least 485 passing
- [ ] `ALL GATES PASSED (13/13)` was not claimed, and the runner's halt at `G3` is reported as Phase
      5's unresolved decision rather than as a Phase 7 result

</success_criteria>
