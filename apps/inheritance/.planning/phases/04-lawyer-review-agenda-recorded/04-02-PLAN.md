---
phase: 04-lawyer-review-agenda-recorded
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/LAWYER-AGENDA.md
autonomous: true
requirements: [LAWYER-05, LAWYER-06, LAWYER-07, LAWYER-08]
user_setup: []

must_haves:
  truths:
    - "Entries LAWYER-05 through LAWYER-08 exist, completing all eight interpretive choices"
    - "LAWYER-06 states that it blocks LAW-06 and that answering it changes the output schema"
    - "LAWYER-08 states that it blocks LAW-12 and offers an explicit refusal option"
    - "All eight entries carry the identical eight-heading structure, verified by counting metadata lines"
    - "No entry states which reading is correct, and no entry is marked answered"
  artifacts:
    - path: ".planning/LAWYER-AGENDA.md"
      provides: "All eight recorded decisions, LAWYER-01 through LAWYER-08"
      min_lines: 240
      contains: "## LAWYER-08"
  key_links:
    - from: ".planning/LAWYER-AGENDA.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the three blocking entries name the LAW-* requirement each blocks"
      pattern: "LAW-06|LAW-12"
---

<objective>
Complete the agenda: append the remaining four recorded decisions, including the two that block
Phase 14 code changes.

Purpose: ROADMAP Phase 4 criterion 1 requires all eight interpretive choices from
`.planning/research/LEGAL-CONFORMANCE.md` section 3 to be recorded. Plan 04-01 landed four. This
plan lands `LAWYER-05` through `LAWYER-08`, which includes `LAWYER-06` (blocking `LAW-06`, the
₱30,000,000-distributed-from-a-₱10,000,000-estate defect) and `LAWYER-08` (blocking `LAW-12`, the
RA 11642 adoption regime).

Two of these four carry a property the first four do not: answering them changes an output schema or
enables a refusal path, not just a fraction. `LAWYER-06`'s Reading B introduces a second output —
a claim against a named donee — and `LAWYER-08` offers an explicit "refuse to compute" option the
audit put on the table. Both are stated in the entry so the lawyer knows what a "confirm" costs.

Output: `.planning/LAWYER-AGENDA.md` extended to eight entries, with a closing status table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/PLAN-STANDARD.md
@.planning/research/LEGAL-CONFORMANCE.md
@.planning/LAWYER-AGENDA.md
@.planning/phases/04-lawyer-review-agenda-recorded/04-RESEARCH.md
@.planning/phases/04-lawyer-review-agenda-recorded/04-01-SUMMARY.md
</context>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. All paths in this plan are
   relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken, or loosen a test, an assertion, or a gate. If a check cannot
   legitimately pass, stop and report BLOCKED per `.planning/PLAN-STANDARD.md` section 3, pasting
   the real command output.
4. **No point of Philippine law may be decided in this plan.** Every entry ships
   `**Status:** awaiting-answer` with all three Answer boxes unticked. No entry may state that a
   reading is correct, preferred, better supported, or settled.
5. The legal content of each entry is copied from `.planning/research/LEGAL-CONFORMANCE.md`
   section 3, questions Q5 through Q8 (lines 110–132). Article numbers, statute sections, peso
   figures and case names are transcribed exactly as they appear there.
6. The eight-heading entry structure written by plan 04-01 is reused byte-for-byte. Read the
   existing `LAWYER-01` entry first and match its heading text, metadata line order, and Answer
   block exactly. Do not introduce a ninth heading, rename one, or reorder them.
7. This plan edits exactly one file: `.planning/LAWYER-AGENDA.md`. Do not create the registry JSON,
   do not edit any source file, and do not edit `specs/estate-tax-engine-spec.md`. Plan 04-03 owns
   the registry and every marker, including the `LAWYER-07` spec edit.
8. Do not modify `.planning/research/LEGAL-CONFORMANCE.md`.
9. Do not alter entries `LAWYER-01` through `LAWYER-04`. This plan appends.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write entries LAWYER-05 and LAWYER-06</name>
  <files>.planning/LAWYER-AGENDA.md</files>
  <read_first>.planning/LAWYER-AGENDA.md, .planning/research/LEGAL-CONFORMANCE.md, .planning/REQUIREMENTS.md</read_first>
  <action>
Append two entries, matching the structure of the existing `LAWYER-01` entry exactly. Source content
is `.planning/research/LEGAL-CONFORMANCE.md` section 3, questions Q5 and Q6 (lines 110–120).

**`## LAWYER-05 — Q5: Art. 907 reduction, self-executing or a claim the heir must assert`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `A` |
| `**Blocks:**` | `nothing scheduled` |
| `**Governing code:**` | `engine/src/step6_validation.rs` — `pub fn reduce_inofficious` |

`### The question` — when a compulsory heir's legitime is impaired, does the engine compute the
corrected partition directly, or report the impairment as a claim the heir must assert.
`### Reading A` — compute the corrected partition. Art. 907 reduction is applied and the output
presents the result as if it were the will's own disposition.
`### Reading B` — Art. 907 reduction happens "on petition of the same," and Art. 906 gives the heir
a right to "demand" completion. Neither is self-executing. The output should show the will's
figures, the legitime, the shortfall, and who must petition.
`### What the engine does today` — Reading A, silently. Reproduced: estate ₱30,000,000, two children
each given 1/10 with a stranger given 8/10 → the engine returns the corrected 250,000,000 /
250,000,000 / 500,000,000 centavos with `warnings: []` and a narrative that never mentions that the
stranger's 80% bequest was cut by ₱3,000,000 or that the first child's own gift was increased. The
disappointed voluntary heir has no notice of the fact he would litigate.
`### What I need from you` — decide whether the deliverable is a computed partition, or a computed
partition plus a disclosure of the procedural posture. Record the audit's own framing verbatim: this
is a product decision with legal consequences and the auditor declined to make it.

**`## LAWYER-06 — Q6: An heir's entitlement exceeding the estate because of a donation *inter vivos*`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `A` |
| `**Blocks:**` | `LAW-06` |
| `**Governing code:**` | `engine/src/step4_estate_base.rs` — `pub fn step4_compute_estate_base`; `engine/src/step8_collation.rs` — `pub fn step8_collation_adjustment` |

`### The question` — should the output show one number or two.
`### Reading A` — one number, drawn from the collated base.
`### Reading B` — two distinct outputs: payable-from-estate, and a reduction or return claim against
a named donee. Art. 771 is explicit that reduction "shall not prevent the donations from taking
effect during the life of the donor, nor shall it bar the donee from appropriating the fruits," so
the heir gets an *accion de reduccion*, not estate property. Only compulsory heirs may sue, and only
for the excess.
`### What the engine does today` — Reading A, and the audit records it as wrong as implemented: a
₱10,000,000 estate with one child and a ₱20,000,000 stranger donation produces ₱30,000,000 to that
child, three times the entire estate. A ₱10,000,000 estate with two children and a ₱20,000,000
donation to the first produces ₱15,000,000 to the second, ₱5,000,000 more than the whole estate.
State plainly that the `LAW-06` fix requires Reading B's data model, so an answer of "confirm
Reading A" changes what the fix looks like rather than removing the need for one.
`### What I need from you` — confirm that modelling the heir's remedy as a claim against the donee
rather than as estate pesos is the right shape, since it changes the output schema.

Both entries close with the same unticked three-checkbox Answer block and the same provenance footer
naming `.planning/research/LEGAL-CONFORMANCE.md` section 3 and the question number.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md && grep -n "reduce_inofficious\|accion de reduccion\|LAW-06\|step8_collation_adjustment" .planning/LAWYER-AGENDA.md && grep -c "\[x\]" .planning/LAWYER-AGENDA.md
  </verify>
  <acceptance_criteria>
    - `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` returns `6`
    - The file contains the headings `## LAWYER-05 — Q5:` and `## LAWYER-06 — Q6:`
    - The `LAWYER-06` entry's `**Blocks:**` line reads `LAW-06`
    - The `LAWYER-06` entry contains the literal strings `accion de reduccion`, `Art. 771`, `₱30,000,000` and `step8_collation_adjustment`
    - The `LAWYER-05` entry contains the literal strings `reduce_inofficious`, `Art. 906` and `Art. 907`
    - `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` returns `0`
    - `grep -c "^\*\*Status:\*\*" .planning/LAWYER-AGENDA.md` returns `6`
  </acceptance_criteria>
  <done>The question blocking LAW-06 is recorded, including the fact that answering it changes the output schema.</done>
</task>

<task type="auto">
  <name>Task 2: Write entries LAWYER-07 and LAWYER-08</name>
  <files>.planning/LAWYER-AGENDA.md</files>
  <read_first>.planning/LAWYER-AGENDA.md, .planning/research/LEGAL-CONFORMANCE.md, specs/estate-tax-engine-spec.md, engine/src/types.rs</read_first>
  <action>
Append the final two entries, matching the existing structure exactly. Source content is
`.planning/research/LEGAL-CONFORMANCE.md` section 3, questions Q7 and Q8 (lines 122–132).

**`## LAWYER-07 — Q7: Family home deduction on a conjugal home, half of FMV or full FMV`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `A` |
| `**Blocks:**` | `nothing scheduled` |
| `**Governing code:**` | `frontend/src/lib/estate-tax-engine/special-deductions.ts` — `familyHome.ownershipType === 'conjugal'`; `specs/estate-tax-engine-spec.md` — `min(fmv * 0.5, cap)` |

`### The question` — is the deduction half of fair market value, or full fair market value up to the
cap.
`### Reading A` — `min(fmv × 0.5, ₱10,000,000)`, the decedent's interest. RR 12-2018 §7.2.3: "the
current fair market value... or the extent of the decedent's interest (whether conjugal/community or
exclusive property), whichever is lower, but not exceeding P10,000,000."
`### Reading B` — full fair market value up to the cap. The engine's own spec hedges at
`specs/estate-tax-engine-spec.md:1008`: "Some commentary uses full FMV for conjugal."
`### What the engine does today` — Reading A. A conjugal ₱12,000,000 home yields a ₱6,000,000
deduction; an exclusive ₱12,000,000 home yields ₱10,000,000, capped.
`### What I need from you` — one line confirming Reading A, so the spec hedge can be replaced with a
recorded decision and a regression test can pin it. ₱6,000,000 of deduction turns on it, roughly
₱360,000 of tax. Record that the auditor reads §7.2.3 as settling this in the engine's favour, and
record it as the auditor's reading rather than as a conclusion.

**`## LAWYER-08 — Q8: RA 11642 Sec. 41 retroactivity to adoption decrees issued before 2022`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `neither` |
| `**Blocks:**` | `LAW-12` |
| `**Governing code:**` | `engine/src/types.rs` — `pub retroactive_ra_11642: bool` |

`### The question` — does RA 11642 Sec. 41 apply retroactively to adoption decrees issued before
2022.
`### Reading A` — yes. Sec. 41 extends legitimate filiation to "the adopter's parents, adopter's
legitimate siblings, and legitimate descendants," and Sec. 43 gives reciprocal succession "without
distinction from legitimate filiations." Applied to a pre-2022 decree this reverses *Sayson v. CA*
("The relationship created by the adoption is between only the adopting parents and the adopted
child and does not extend to the blood relatives of either party"), letting an adoptee represent the
adopter in the adopter's parent's estate.
`### Reading B` — no. Succession rights vest under the law at the decedent's death, and Sec. 41 is
prospective as to decrees.
`### What the engine does today` — neither. `config.retroactive_ra_11642` and `AdoptionRegime` exist
and are provably dead: the audit ran the identical family tree as `{Ra8552, retroactive: false}` and
as `{Ra11642, retroactive: true}` and the diff of the two complete outputs is empty. The engine
silently computes every case under the pre-2022 *Sayson* rule. Record also that
`engine/src/step1_classify.rs` justifies its one operative adoption rule by citing "RA 8552 Sec. 20",
a provision repealed by RA 11642 Sec. 62.
`### What I need from you` — the answer becomes the default for a flag that currently does nothing.
Record the audit's explicit alternative: an answer of "I would rather not decide" is itself an
acceptable answer, and the engine is then built to refuse to compute Sec. 41 fact patterns rather
than guessing.

`LAWYER-08`'s `### Answer` block carries one extra line beneath `Notes:`, matching the pattern
`LAWYER-04` established for a sub-question:
`Refuse to compute Sec. 41 fact patterns instead of answering? yes / no:`
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md && grep -n "RR 12-2018\|Sayson\|retroactive_ra_11642\|LAW-12\|Refuse to compute" .planning/LAWYER-AGENDA.md && grep -c "\[x\]" .planning/LAWYER-AGENDA.md
  </verify>
  <acceptance_criteria>
    - `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` returns `8`
    - The file contains the headings `## LAWYER-07 — Q7:` and `## LAWYER-08 — Q8:`
    - The `LAWYER-07` entry contains the literal strings `RR 12-2018`, `whichever is lower`, `₱6,000,000` and `specs/estate-tax-engine-spec.md:1008`
    - The `LAWYER-08` entry's `**Blocks:**` line reads `LAW-12`
    - The `LAWYER-08` entry contains the literal strings `Sayson`, `RA 11642 Sec. 62`, `retroactive_ra_11642` and `Refuse to compute Sec. 41 fact patterns`
    - `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` returns `0`
    - `specs/estate-tax-engine-spec.md` is unchanged, confirmed by `git status --porcelain specs/` producing no output
  </acceptance_criteria>
  <done>All eight interpretive choices are recorded, and both remaining blocking questions name what they block.</done>
</task>

<task type="auto">
  <name>Task 3: Add the closing status table, prove structural uniformity, and commit</name>
  <files>.planning/LAWYER-AGENDA.md</files>
  <read_first>.planning/LAWYER-AGENDA.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
Append a final section `## Status at a glance` after the eighth entry. It is a table with columns
`Decision`, `Question`, `Engine implements`, `Blocks`, `Status`, holding exactly eight rows in id
order, with values copied from each entry's own metadata lines. The `Status` column reads
`awaiting-answer` in all eight rows on the day this plan runs.

Below the table, three lines:
- a count line stating that eight of eight decisions are awaiting an answer;
- a line stating that `.planning/lawyer-decisions.json` is the machine-readable form of this table
  and that gate `G10` fails the build when the two disagree;
- a line stating that a status is changed only through
  `.planning/LEGAL-CORRECTION-WORKFLOW.md`, never by editing this table alone.

Then prove the eight entries are structurally uniform. Every one of these counts must equal `8`:
occurrences of `^## LAWYER-`, `^\*\*Status:\*\*`, `^\*\*Engine implements:\*\*`, `^\*\*Blocks:\*\*`,
`^\*\*Governing code:\*\*`, `### The question`, `### Reading A`, `### Reading B`,
`### What the engine does today`, `### What I need from you`, `### Answer`, `Confirm Reading A`,
`Change to Reading B`, `Neither — see notes`, `Answered by:`, `Date:`, `Notes:`.

If any count differs from `8`, repair the entry that is short. Do not repair the count by deleting a
heading from the other entries.

Then commit, staging explicit paths only:

`bash scripts/safe-commit.sh -m "docs(04): complete the lawyer review agenda with entries LAWYER-05 through LAWYER-08" .planning/LAWYER-AGENDA.md`

NEVER run `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && for p in '^## LAWYER-' '^\*\*Status:\*\*' '^\*\*Engine implements:\*\*' '^\*\*Blocks:\*\*' '^\*\*Governing code:\*\*' '### The question' '### Reading A' '### Reading B' '### What the engine does today' '### What I need from you' '### Answer' 'Confirm Reading A' 'Change to Reading B' 'Answered by:'; do printf '%s = ' "$p"; grep -cE "$p" .planning/LAWYER-AGENDA.md; done; grep -c "awaiting-answer" .planning/LAWYER-AGENDA.md; bash scripts/ci-gates.sh; echo "EXIT=$?"; cd /home/clsandoval/cs/monorepo && git log -1 --format="%s" && git log -1 --name-only --format=""
  </verify>
  <acceptance_criteria>
    - Every one of the fourteen greps in the verify block returns exactly `8`
    - The file contains the heading `## Status at a glance` with a table of exactly eight data rows
    - The status table's `Blocks` column names `LAW-07`, `LAW-06` and `LAW-12` on the rows for `LAWYER-04`, `LAWYER-06` and `LAWYER-08`
    - The file contains the literal strings `.planning/lawyer-decisions.json` and `.planning/LEGAL-CORRECTION-WORKFLOW.md`
    - `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` returns `0`
    - `bash scripts/ci-gates.sh` exits 0 and prints `ALL GATES PASSED (9/9)`
    - `git log -1 --format=%s` outputs `docs(04): complete the lawyer review agenda with entries LAWYER-05 through LAWYER-08`
    - `git log -1 --name-only --format=""` lists only `apps/inheritance/.planning/LAWYER-AGENDA.md`
  </acceptance_criteria>
  <done>The agenda is complete, internally consistent, and parseable by the gate plan 04-04 builds.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|---|---|---|---|
| T-4-02a | An entry endorses a reading, deciding a point of Philippine law | Critical | Constraint 4; all three boxes ship unticked; every task acceptance greps `\[x\]` and requires `0` |
| T-4-02b | `LAWYER-06` is answered without the lawyer knowing that Reading B changes the output schema | High | The entry states the schema consequence explicitly and records that `LAW-06`'s fix requires Reading B's data model |
| T-4-02c | `LAWYER-08` is answered under pressure when the honest answer is "I would rather not decide" | High | The entry carries an explicit refusal line, `Refuse to compute Sec. 41 fact patterns instead of answering? yes / no:`, transcribed from the audit's own offer |
| T-4-02d | The four new entries drift structurally from the first four, so the gate cannot parse the set | High | Constraint 6 requires byte-for-byte reuse of the existing structure; task 3 counts fourteen structural markers and requires each to equal `8` |
| T-4-02e | The `LAWYER-07` spec hedge is edited here as a convenience, silently converting a recorded disagreement into an assertion | Critical | Constraint 7 forbids editing `specs/estate-tax-engine-spec.md` in this plan; task 2's acceptance criteria assert `git status --porcelain specs/` is empty |
| T-4-02f | The commit absorbs the concurrent auto-committer's in-flight work | High | Constraint 2 and `scripts/safe-commit.sh` with one explicit path; the acceptance criterion asserts the commit's file list |

No dependency is added. No source file, gate, manifest or test is touched by this plan.
</threat_model>

<verification>
Before declaring plan complete, run each and confirm the stated result:
- [ ] `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "^\*\*Status:\*\*" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "^\*\*Engine implements:\*\*" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "^\*\*Blocks:\*\*" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "^\*\*Governing code:\*\*" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "### Answer" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "Confirm Reading A" .planning/LAWYER-AGENDA.md` → `8`
- [ ] `grep -c "awaiting-answer" .planning/LAWYER-AGENDA.md` → at least `16`, one per entry and one per status-table row
- [ ] `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` → `0`
- [ ] `grep -n "LAW-07" .planning/LAWYER-AGENDA.md` → matches inside the `LAWYER-04` entry and the status table
- [ ] `grep -n "LAW-06" .planning/LAWYER-AGENDA.md` → matches inside the `LAWYER-06` entry and the status table
- [ ] `grep -n "LAW-12" .planning/LAWYER-AGENDA.md` → matches inside the `LAWYER-08` entry and the status table
- [ ] `git status --porcelain specs/` → empty output
- [ ] `git status --porcelain engine/` → empty output
- [ ] `node scripts/check-plan-closed-world.mjs` → exit 0
- [ ] `bash scripts/ci-gates.sh` → exit 0, `ALL GATES PASSED (9/9)`
- [ ] `git log -1 --name-only --format=""` → only `apps/inheritance/.planning/LAWYER-AGENDA.md`
</verification>

<success_criteria>
- All three tasks completed
- All eight interpretive choices are recorded, each stating the engine's current reading and the exact question posed
- `LAWYER-04`, `LAWYER-06` and `LAWYER-08` each name the `LAW-*` requirement they block
- Fourteen structural markers each occur exactly eight times, proving the entries are uniform
- No reading is endorsed anywhere in the file, and all eight statuses read `awaiting-answer`
- No source file, spec file, gate or manifest was touched
- `bash scripts/ci-gates.sh` still exits 0
- Exactly one commit was made, naming one explicit path
</success_criteria>

<output>
After completion, create `.planning/phases/04-lawyer-review-agenda-recorded/04-02-SUMMARY.md`.
Record: the fourteen structural counts, the final line count of the agenda, the status table as
written, the `ALL GATES PASSED` line, and the commit SHA. Plan 04-03 mirrors this table into
`.planning/lawyer-decisions.json`, so the exact metadata values for all eight entries must appear in
this summary.
</output>
