---
phase: 04-lawyer-review-agenda-recorded
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/LAWYER-AGENDA.md
autonomous: true
requirements: [LAWYER-01, LAWYER-02, LAWYER-03, LAWYER-04]
user_setup: []

must_haves:
  truths:
    - ".planning/LAWYER-AGENDA.md exists and opens with a fixed how-to-answer section naming the three blocking questions first"
    - "Entries LAWYER-01 through LAWYER-04 each state the engine's current reading and the exact question posed"
    - "Every entry carries the identical eight-heading structure, so a machine can parse the set"
    - "Every entry ends with an Answer block a lawyer completes by ticking one of three boxes"
    - "LAWYER-04 states that it blocks LAW-07 and carries the flag sub-question the audit raised"
    - "No entry states which reading is correct, and no entry is marked answered"
  artifacts:
    - path: ".planning/LAWYER-AGENDA.md"
      provides: "The lawyer-facing review agenda, entries LAWYER-01 through LAWYER-04"
      min_lines: 120
      contains: "## LAWYER-04"
  key_links:
    - from: ".planning/LAWYER-AGENDA.md"
      to: ".planning/research/LEGAL-CONFORMANCE.md"
      via: "each entry cites the audit section its content was copied from"
      pattern: "LEGAL-CONFORMANCE"
---

<objective>
Create the lawyer review agenda and write the first four recorded decisions into it.

Purpose: ROADMAP Phase 4 criterion 1 requires each of the eight interpretive choices in
`.planning/research/LEGAL-CONFORMANCE.md` section 3 to have a recorded-decision entry stating the
engine's current reading and the exact question posed to the lawyer. Criterion 3 requires the three
questions that block later code changes to be answerable "confirm" or "change to B" in one sitting.
This plan builds the file and lands four of the eight entries, including the highest-stakes one.

The content is a restructuring of an existing verified document, not a new legal analysis.
`LEGAL-CONFORMANCE.md` lines 81–132 already carry all eight questions in a uniform shape: a title,
`Reading A`, `Reading B`, `Engine implements:`, and `What I need:`. Every article citation there is
a live reference-style link into the link table at lines 209–222. This plan copies that material
into a fixed per-entry structure and adds an answer block. It does not restate the law in new words
and it does not evaluate either reading.

`.planning/PLAN-STANDARD.md:180-183` already routes every future legal question to this file and
says Phase 4 owns its structure. Until this plan lands, that instruction points at a file that does
not exist.

Output: `.planning/LAWYER-AGENDA.md` with a header, a how-to-answer section, a blocking-questions
index, and entries `LAWYER-01`, `LAWYER-02`, `LAWYER-03`, `LAWYER-04`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/PLAN-STANDARD.md
@.planning/research/LEGAL-CONFORMANCE.md
@.planning/phases/04-lawyer-review-agenda-recorded/04-RESEARCH.md
</context>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. All paths in this plan are
   relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken, or loosen a test, an assertion, or a gate. If a check cannot
   legitimately pass, stop and report BLOCKED per `.planning/PLAN-STANDARD.md` section 3, pasting
   the real command output.
4. **No point of Philippine law may be decided in this plan.** Every entry records what the engine
   does today and what is being asked. No entry may state that a reading is correct, preferred,
   better supported, or settled. Every entry's `**Status:**` line reads `awaiting-answer` and every
   Answer block ships with all three boxes unticked.
5. The legal content of each entry is copied from `.planning/research/LEGAL-CONFORMANCE.md`
   section 3. Article numbers, case names, peso figures, and scenario codes are transcribed exactly
   as they appear there. Do not add a citation that document does not contain, and do not drop one
   it does.
6. This plan writes exactly one file: `.planning/LAWYER-AGENDA.md`. Do not create the registry JSON,
   do not create the correction workflow document, do not edit any source file, and do not edit
   `gates.manifest.json`. Plans 04-03, 04-04 and 04-05 own those.
7. Do not modify `.planning/research/LEGAL-CONFORMANCE.md`. It is the audited source this file is
   derived from, and editing it would break the provenance the entries cite.
8. Entries LAWYER-05 through LAWYER-08 are not written here. Plan 04-02 appends them to this same
   file.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create the agenda file with its header, answering instructions, and blocking index</name>
  <files>.planning/LAWYER-AGENDA.md</files>
  <read_first>.planning/research/LEGAL-CONFORMANCE.md, .planning/PLAN-STANDARD.md, .planning/ROADMAP.md, .planning/phases/04-lawyer-review-agenda-recorded/04-RESEARCH.md</read_first>
  <action>
Create `.planning/LAWYER-AGENDA.md`. It has four parts before the first entry, in this order.

**1. Title and standing note.** Level-one heading `# Lawyer Review Agenda`. The exact string
`# Lawyer Review Agenda` is required verbatim, because `.planning/PLAN-STANDARD.md:180-183` instructs
an executor hitting a point of law to create this file with that single heading line and append
below it. Matching it means such an append lands in a file that already exists in the right shape.

Below the heading, a short standing note stating four things:
- what this file is — eight interpretive choices the engine has already made, each needing a
  lawyer's signature rather than a bug fix;
- that none of them is a defect, and that each current reading is defensible;
- that the source is `.planning/research/LEGAL-CONFORMANCE.md` section 3, an audit in which every
  legal proposition was verified against primary text and every code claim reproduced by running
  the engine;
- that three of the eight block code changes scheduled for Phase 14, and are listed first.

**2. `## How to answer`.** State the mechanics literally:
- Each entry ends with an `### Answer` block holding three checkboxes. Tick exactly one.
- Ticking `Confirm Reading A` means the engine's current behaviour stands.
- Ticking `Change to Reading B` means the engine changes to the stated alternative.
- Ticking `Neither — see notes` means the answer is written in the `Notes:` field below it.
- Fill `Answered by:` and `Date:` on every entry answered.
- Answering three entries is enough to unblock the scheduled work; answering all eight closes the
  agenda.
- Nothing in the codebase changes because a box was ticked. An answer becomes a code change only
  through the procedure in `.planning/LEGAL-CORRECTION-WORKFLOW.md`.

**3. `## Answer these three first`.** A table with columns `Decision`, `Question`, `Blocks`,
`One-line ask`, containing exactly three rows in this order:

| Decision | Question | Blocks | One-line ask |
|---|---|---|---|
| `LAWYER-04` | Q4 — the reach of *Aquino v. Aquino* into the collateral line | `LAW-07` | Confirm the collateral bar survives, so it can be implemented |
| `LAWYER-06` | Q6 — an heir's donation-excess entitlement, estate pesos or a claim against the donee | `LAW-06` | Confirm the claim-against-donee shape, which changes the output schema |
| `LAWYER-08` | Q8 — RA 11642 Sec. 41 retroactivity to pre-2022 decrees | `LAW-12` | Answer, or say the engine should refuse the fact pattern |

Immediately after the table, one sentence stating that the remaining five entries can be answered
at any time and that no scheduled work waits on them.

**4. `## Entry format`.** State the eight headings every entry carries, in order, so a reader
knows the shape is uniform and a machine can parse it:

`## LAWYER-0N — QN: <short title>`, then the four bold metadata lines `**Status:**`,
`**Engine implements:**`, `**Blocks:**`, `**Governing code:**`, then the level-three headings
`### The question`, `### Reading A`, `### Reading B`, `### What the engine does today`,
`### What I need from you`, `### Answer`.

State the fixed vocabularies literally: `**Status:**` is one of `awaiting-answer`, `confirmed`,
`changed`; `**Engine implements:**` is one of `A`, `B`, `neither`. State that `neither` means the
engine implements no rule on the point at all, which is itself an unrecorded position.

Do not write any entry in this task.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && head -1 .planning/LAWYER-AGENDA.md && grep -c "^## " .planning/LAWYER-AGENDA.md && grep -n "LAWYER-04\|LAWYER-06\|LAWYER-08" .planning/LAWYER-AGENDA.md
  </verify>
  <acceptance_criteria>
    - The first line of `.planning/LAWYER-AGENDA.md` is exactly `# Lawyer Review Agenda`
    - The file contains the level-two headings `## How to answer`, `## Answer these three first` and `## Entry format`
    - The blocking table has exactly three data rows, naming `LAWYER-04`, `LAWYER-06` and `LAWYER-08` in that order
    - The blocking table names `LAW-07`, `LAW-06` and `LAW-12` in its `Blocks` column
    - The file contains the literal strings `awaiting-answer`, `confirmed` and `changed`
    - The file contains the literal string `.planning/LEGAL-CORRECTION-WORKFLOW.md`
    - `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` returns `0`, proving no entry was written in this task
  </acceptance_criteria>
  <done>The agenda exists, states how it is answered, and puts the three blocking questions first.</done>
</task>

<task type="auto">
  <name>Task 2: Write entries LAWYER-01, LAWYER-02 and LAWYER-03</name>
  <files>.planning/LAWYER-AGENDA.md</files>
  <read_first>.planning/LAWYER-AGENDA.md, .planning/research/LEGAL-CONFORMANCE.md, .planning/REQUIREMENTS.md</read_first>
  <action>
Append three entries to `.planning/LAWYER-AGENDA.md`, each using the eight-heading structure defined
in task 1. Source content for all three is `.planning/research/LEGAL-CONFORMANCE.md` section 3,
questions Q1, Q2 and Q3 (lines 85–102).

**`## LAWYER-01 — Q1: One legitimate child plus surviving spouse, intestate`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `A` |
| `**Blocks:**` | `nothing scheduled` |
| `**Governing code:**` | `engine/src/step7_distribute.rs` — `I2: n LC + Spouse (Art. 996)` |

`### The question` — is the split ½/½, or ½ to the child and ¼ to the spouse.
`### Reading A` — Art. 996: the spouse takes "the same share as that of each of the children," so
the lone child and the spouse each take one share, ½/½. This is *Santillon v. Miranda*,
G.R. No. L-19281 (1965).
`### Reading B` — apply the testate legitime table: child ½ under Art. 888, spouse ¼ under
Art. 892 ¶1, remainder intestate.
`### What the engine does today` — Reading A. 1 legitimate child plus spouse, estate ₱12,000,000 →
₱6,000,000 each, scenario code I2, legal basis `["Art. 996"]`. Add the audit's note that this is the
single most common intestate family shape in the Philippines and that everything downstream inherits
it.
`### What I need from you` — confirm *Santillon* is still how you would advise, and that no
post-1965 decision has unsettled it.

**`## LAWYER-02 — Q2: One legitimate child plus spouse plus illegitimate children, testate`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `A` |
| `**Blocks:**` | `nothing scheduled` |
| `**Governing code:**` | `engine/src/step5_legitimes.rs` — `"Art. 892 ¶1".into()` |

`### The question` — is the spouse's legitime ¼, or equal to the child's ½.
`### Reading A` — Art. 892 ¶1 controls: "If only one legitimate child... survives, the widow or
widower shall be entitled to one-fourth."
`### Reading B` — Art. 897's literal text: where the spouse survives with "legitimate children or
descendants, and acknowledged natural children," the spouse takes "a portion equal to the legitime
of each of the legitimate children," which is ½ where there is one child.
`### What the engine does today` — Reading A, ¼, following the prevailing doctrinal table. The spec
cites Art. 897 only for the multi-child case T5b, so the choice is unrecorded.
`### What I need from you` — confirm Art. 897 is read as addressing the plural case only, so that
reading can be written into the spec with your name on it.

**`## LAWYER-03 — Q3: Nephews and nieces alone surviving, per capita under Art. 975 ¶2`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `neither` |
| `**Blocks:**` | `nothing scheduled` |
| `**Governing code:**` | `engine/src/step7_distribute.rs` — `Branch 3: Nephews/nieces only — per capita (Art. 975)` |

`### The question` — when nephews and nieces alone succeed per capita, does the Art. 1006 full/half
blood 2:1 ratio still apply.
`### Reading A` — Art. 975 ¶2: "if they alone survive, they shall inherit in equal portions," flat
per capita, ratio gone.
`### Reading B` — Art. 1008: children of half-blood siblings succeed "in accordance with the rules
laid down for brothers and sisters of the full blood," importing Art. 1006's doubling. Tolentino and
Jurado are cited for this reading.
`### What the engine does today` — neither. The per-capita branch is unreachable dead code, so the
engine currently produces per stirpes regardless. `specs/inheritance-engine-spec.md:1400-1412` flags
the debate and proposes a config flag.
`### What I need from you` — an answer before the dead branch is enabled, so the fix does not
hardcode an unreviewed position. A config flag defaulting to the answer given is acceptable.

Each entry's `### Answer` block is identical in shape:

```
- [ ] Confirm Reading A
- [ ] Change to Reading B
- [ ] Neither — see notes

Answered by:
Date:
Notes:
```

All three boxes ship unticked, and `Answered by:`, `Date:` and `Notes:` ship empty. Close each entry
with a one-line provenance footer naming `.planning/research/LEGAL-CONFORMANCE.md` section 3 and the
question number it was copied from.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md && grep -c "Confirm Reading A" .planning/LAWYER-AGENDA.md && grep -n "Santillon\|Art. 897\|Art. 1008" .planning/LAWYER-AGENDA.md && grep -c "\[x\]" .planning/LAWYER-AGENDA.md
  </verify>
  <acceptance_criteria>
    - `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` returns `3`
    - The file contains the headings `## LAWYER-01 — Q1:`, `## LAWYER-02 — Q2:` and `## LAWYER-03 — Q3:`
    - `grep -c "Confirm Reading A" .planning/LAWYER-AGENDA.md` returns `3`, one Answer block per entry
    - Each of the three entries contains all four metadata lines `**Status:**`, `**Engine implements:**`, `**Blocks:**` and `**Governing code:**`
    - `grep -c "awaiting-answer" .planning/LAWYER-AGENDA.md` returns at least `3`
    - The file contains the literal strings `Santillon`, `Art. 897`, `Art. 1008` and `₱12,000,000`
    - `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` returns `0`, proving no box was ticked
    - `git diff --name-only` lists only `.planning/LAWYER-AGENDA.md`
  </acceptance_criteria>
  <done>Three of the eight interpretive choices are recorded with the engine's position and the exact ask.</done>
</task>

<task type="auto">
  <name>Task 3: Write entry LAWYER-04, the highest-stakes question, and commit</name>
  <files>.planning/LAWYER-AGENDA.md</files>
  <read_first>.planning/LAWYER-AGENDA.md, .planning/research/LEGAL-CONFORMANCE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
Append the fourth entry. Source content is `.planning/research/LEGAL-CONFORMANCE.md` section 3
question Q4 (lines 104–108), plus the two supporting rows in section 2a (the Art. 992 critical row)
and section 4 (the Art. 992 missing-rule row).

**`## LAWYER-04 — Q4: How far *Aquino v. Aquino* reaches into the collateral line`**

| Metadata line | Value |
|---|---|
| `**Status:**` | `awaiting-answer` |
| `**Engine implements:**` | `neither` |
| `**Blocks:**` | `LAW-07` |
| `**Governing code:**` | `engine/src/step1_classify.rs` — `pub fn check_eligibility` |

`### The question` — does Art. 992's iron curtain still bar a nonmarital claimant in the collateral
line after *Aquino v. Aquino*, G.R. Nos. 208912 and 209018, En Banc, 7 December 2021.

`### Reading A` — the narrow reading, and what the Court said about itself: *Aquino* limited its own
holding — "this ruling will only apply when the nonmarital child has a right of representation to
their parent's share in her grandparent's legitime. It is silent on collateral relatives where the
nonmarital child may inherit by themself." Art. 992 therefore survives intact for collaterals, and
*Diaz v. IAC* remains operative there.

`### Reading B` — the broad reading: the Court's equal-protection reasoning about birth status
cannot coherently stop at the direct line, so lower courts should extend it.

`### What the engine does today` — neither as a rule. No iron curtain exists anywhere:
`grep -rn "IronCurtain\|iron_curtain" engine/src` returns zero hits. That happens to match Reading A
in the direct line and contradicts Reading A in the collateral line. Two reproduced consequences,
transcribed from the audit:
- An illegitimate decedent whose sole survivor is a legitimate half-blood sibling, estate ₱8,000,000
  → the sibling takes ₱8,000,000, scenario I13, warnings `[]`. The `is_illegitimate=false` control is
  bit-identical, proving no rule runs. Under Reading A the sibling is barred and the estate escheats.
- A nonmarital nephew currently inherits from a marital uncle.
Also record that the suite's only Art. 992 test, `engine/tests/integration.rs:1336`, passes with the
rule entirely absent, and that `specs/inheritance-v2-spec.md:524` defines an `ExclusionReason::IronCurtain`
that nothing implements.

`### What I need from you` — two things, both stated as separate asks so they can be answered
separately:
1. Confirm Reading A, so the collateral barrier can be implemented.
2. State whether every case where the barrier is decisive should carry a `LAWYER_REVIEW` flag in the
   output rather than being a silent computation.

`### Answer` — the same three-checkbox block as the other entries, plus one extra line beneath the
`Notes:` field: `Flag every barrier-decisive case for manual review? yes / no:`.

Close with the same provenance footer, and add one line recording that this is the
highest-stakes item on the agenda and that `LAW-07` in Phase 14 cannot start without it.

Then commit, staging explicit paths only:

`bash scripts/safe-commit.sh -m "docs(04): record lawyer review agenda entries LAWYER-01 through LAWYER-04" .planning/LAWYER-AGENDA.md`

NEVER run `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md && grep -n "LAW-07\|Aquino\|check_eligibility\|LAWYER_REVIEW" .planning/LAWYER-AGENDA.md && node scripts/check-plan-closed-world.mjs; echo "LINT=$?"; cd /home/clsandoval/cs/monorepo && git log -1 --format="%s" && git log -1 --name-only --format=""
  </verify>
  <acceptance_criteria>
    - `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` returns `4`
    - The `LAWYER-04` entry contains the literal strings `Aquino`, `It is silent on collateral relatives`, `LAW-07`, `check_eligibility` and `LAWYER_REVIEW`
    - The `LAWYER-04` entry's `**Blocks:**` line reads `LAW-07`
    - The `LAWYER-04` entry contains the literal string `₱8,000,000`
    - `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` returns `0`
    - `grep -c "awaiting-answer" .planning/LAWYER-AGENDA.md` returns at least `4`
    - `node scripts/check-plan-closed-world.mjs` exits 0
    - `git log -1 --format=%s` outputs `docs(04): record lawyer review agenda entries LAWYER-01 through LAWYER-04`
    - `git log -1 --name-only --format=""` lists only `apps/inheritance/.planning/LAWYER-AGENDA.md`
  </acceptance_criteria>
  <done>The question that blocks LAW-07 is written down in a form answerable in one sitting.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|---|---|---|---|
| T-4-01a | An entry states which reading is correct, deciding a point of Philippine law by prose | Critical | Constraint 4 prohibits it; every entry ships `**Status:** awaiting-answer` with three unticked boxes; task acceptance greps `\[x\]` and requires `0` |
| T-4-01b | An entry's citations drift from the audited source, so the lawyer is answering a question about law nobody verified | High | Constraint 5 requires transcription from `LEGAL-CONFORMANCE.md` section 3; each entry carries a provenance footer naming the section and question number; acceptance criteria grep for specific citation strings |
| T-4-01c | The file heading differs from the one `PLAN-STANDARD.md` tells an executor to create, so a future BLOCKED append creates a second divergent file | High | Task 1 requires the first line to be exactly `# Lawyer Review Agenda`, and the acceptance criterion reads line 1 |
| T-4-01d | Entries are structurally uneven, so plan 04-04's gate cannot parse them and the machine-readable link degrades into prose | High | Task 1 fixes the eight-heading structure before any entry is written; tasks 2 and 3 apply it; acceptance criteria count the metadata lines per entry |
| T-4-01e | The three blocking questions are buried among the five non-blocking ones, so a time-boxed lawyer answers the wrong ones first | Medium | `## Answer these three first` is placed before the entries with a fixed three-row table naming the blocked requirement ids |
| T-4-01f | The commit sweeps unrelated concurrent work from the auto-committer into this change | High | Constraint 2 and the `scripts/safe-commit.sh` invocation stage one explicit path; the acceptance criterion asserts the commit's file list is exactly that path |

No dependency is added. No source file, gate, manifest or test is touched by this plan.
</threat_model>

<verification>
Before declaring plan complete, run each and confirm the stated result:
- [ ] `head -1 .planning/LAWYER-AGENDA.md` → exactly `# Lawyer Review Agenda`
- [ ] `grep -c "^## LAWYER-" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "Confirm Reading A" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "Change to Reading B" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "awaiting-answer" .planning/LAWYER-AGENDA.md` → at least `4`
- [ ] `grep -c "\[x\]" .planning/LAWYER-AGENDA.md` → `0`
- [ ] `grep -c "^\*\*Status:\*\*" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "^\*\*Engine implements:\*\*" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "^\*\*Blocks:\*\*" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "^\*\*Governing code:\*\*" .planning/LAWYER-AGENDA.md` → `4`
- [ ] `grep -c "LEGAL-CONFORMANCE" .planning/LAWYER-AGENDA.md` → at least `5`
- [ ] `node scripts/check-plan-closed-world.mjs` → exit 0
- [ ] `bash scripts/ci-gates.sh` → exit 0, `ALL GATES PASSED (9/9)`
- [ ] `git log -1 --name-only --format=""` → only `apps/inheritance/.planning/LAWYER-AGENDA.md`
</verification>

<success_criteria>
- All three tasks completed
- `.planning/LAWYER-AGENDA.md` exists, opens with the exact heading `PLAN-STANDARD.md` names, and explains how it is answered
- Four recorded decisions exist, each stating the engine's current reading and the exact question posed
- Every entry carries the identical eight-heading structure and an unticked three-box Answer block
- `LAWYER-04` names `LAW-07` as blocked and carries the manual-review-flag sub-question
- No reading is endorsed anywhere in the file, and no entry is marked answered
- `bash scripts/ci-gates.sh` still exits 0
- Exactly one commit was made, naming one explicit path
</success_criteria>

<output>
After completion, create `.planning/phases/04-lawyer-review-agenda-recorded/04-01-SUMMARY.md`.
Record: the file's line count, the output of every grep in the verification checklist, the
`ALL GATES PASSED` line, and the commit SHA. Also record the exact eight-heading entry structure as
written, because plans 04-02, 04-03 and 04-04 all depend on parsing it.
</output>
