---
phase: 03-reproducible-environment-gate-reporting
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/supabase/seed.sql
  - frontend/supabase/fixtures.json
  - scripts/check-seed-fixture.mjs
  - scripts/fixtures/seed-drift.sql
  - scripts/fixtures/seed-ok.sql
  - scripts/fixtures/fixtures-drift.json
autonomous: true
requirements: [GATE-06]
user_setup: []

must_haves:
  truths:
    - "frontend/supabase/seed.sql exists and is applied automatically by supabase db reset"
    - "The seed creates two organizations, two users, two clients and two cases with fixed UUIDs"
    - "frontend/supabase/fixtures.json publishes every seeded id so later gates reference rows by id"
    - "Both seeded users can sign in with a password through the local auth API"
    - "The seeded case input_json is copied byte-for-byte from engine/examples/cases/02-married-3lc.json"
    - "node scripts/check-seed-fixture.mjs exits 0 with SEED OK on the current tree"
    - "A uuid present in one of seed.sql and fixtures.json but not the other exits 1 with FIXTURE DRIFT"
    - "Running supabase db reset twice leaves exactly the same rows"
  artifacts:
    - path: "frontend/supabase/seed.sql"
      provides: "The known org, user and case fixture later gates reference by id"
      min_lines: 90
      contains: "organization_members"
    - path: "frontend/supabase/fixtures.json"
      provides: "The machine-readable registry of every seeded id"
      contains: "org_alpha"
    - path: "scripts/check-seed-fixture.mjs"
      provides: "Static agreement between the seed SQL and the id registry"
      min_lines: 90
      contains: "FIXTURE DRIFT"
  key_links:
    - from: "frontend/supabase/fixtures.json"
      to: "frontend/supabase/seed.sql"
      via: "every id in the registry appears literally in the seed SQL"
      pattern: "seed\\.sql"
    - from: "frontend/supabase/seed.sql"
      to: "engine/examples/cases/02-married-3lc.json"
      via: "the seeded case input_json is a verbatim copy of that committed engine fixture"
      pattern: "02-married-3lc"
---

<objective>
Seed a known, two-tenant fixture that later gates can reference by id, without authoring any legal content.

Purpose: GATE-06 requires `supabase/seed.sql` to exist and to produce a known org, user and case fixture that later gates can reference by ID. The file does not exist today, while `frontend/supabase/config.toml` already declares `[db.seed] enabled = true` with `sql_paths = ["./seed.sql"]` — so the moment the file lands, `supabase db reset` applies it, and no config change is needed.

Two tenants rather than one. ROADMAP Phase 11 success criterion 4 reads: a test run against a real local Supabase proves a user in org A cannot read, write, or enumerate org B's cases, PDFs, or shared links. A single-org fixture cannot express that, and extending a seed that later gates already reference by id is exactly the churn a fixture exists to prevent. Two orgs is a transcription of a roadmap criterion, not a scope increase.

The seeded case needs a valid `EngineInput`. Authoring one means choosing a family structure, which is choosing which succession rules the fixture exercises — the beginning of a legal judgment, which no agent in this project may make. The seed therefore copies `engine/examples/cases/02-married-3lc.json` byte-for-byte: a committed engine fixture already exercised by the engine's own test suite. Copying it introduces no new legal content.

Scope note: this plan does not register `check-seed-fixture.mjs` as a gate; plan 03-04 owns the next manifest edit. It does not modify `scripts/ci-gates.sh`, `gates.manifest.json`, or `gates.manifest.lock`. It does not add fixture rows for invitations, deadlines or PDFs — Phase 10 owns whatever seeding seams the journey gates need beyond this.

Output: `frontend/supabase/seed.sql`, `frontend/supabase/fixtures.json`, `scripts/check-seed-fixture.mjs`, and three fixtures proving the drift check fires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/PLAN-STANDARD.md
@.planning/phases/03-reproducible-environment-gate-reporting/03-RESEARCH.md
@.planning/phases/03-reproducible-environment-gate-reporting/03-02-SUMMARY.md
@frontend/supabase/migrations/001_initial_schema.sql
@frontend/supabase/config.toml
@engine/examples/cases/02-married-3lc.json
</context>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. All paths in this plan are relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken, or loosen a test, an assertion, or a gate to make something pass. If a check cannot legitimately pass, stop and report BLOCKED per `.planning/PLAN-STANDARD.md` section 3, pasting the real command output.
4. Do not modify `scripts/ci-gates.sh`, `gates.manifest.json`, or `gates.manifest.lock`. Plans 03-04 and 03-05 own those files.
5. Do not modify any existing migration file, and do not modify `frontend/supabase/migrations/013_storage_buckets.sql` created by plan 03-02.
6. **Do not author, edit, adapt, extend, or reduce the contents of the seeded `input_json`.** It is a byte-for-byte copy of `engine/examples/cases/02-married-3lc.json`. Changing a name, an amount, a relationship, or a date inside it is authoring legal content, which is prohibited. If the copy cannot be embedded verbatim for a syntactic reason, stop and report BLOCKED rather than adjusting the content.
7. Do not modify anything under `frontend/src` or under `engine/`.
8. `scripts/check-seed-fixture.mjs` imports only `node:` builtins, and has no `--fix`, `--update`, `--accept`, or `--regenerate` flag.
9. Do not stop, restart or reset any Docker container whose name does not end in `_inheritance`.
10. Every seeded email uses the reserved `.test` top-level domain, never a real domain. Every seeded password is the literal string `test-password-123`, stated here so it is never invented.
11. No point of Philippine law arises in this plan. The one place it could have — the seeded family tree — is removed by constraint 6. If a legal question appears anyway, stop and report BLOCKED rather than deciding it.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the id registry</name>
  <files>frontend/supabase/fixtures.json</files>
  <read_first>frontend/supabase/migrations/001_initial_schema.sql, .planning/phases/03-reproducible-environment-gate-reporting/03-RESEARCH.md</read_first>
  <action>
Create `frontend/supabase/fixtures.json`. This file is the contract later gates read, so it is written before the SQL rather than derived from it.

Top-level shape, exactly these three keys:

- `$comment` — a prose string stating that these ids are seeded by `frontend/supabase/seed.sql`, that they are stable and may be referenced literally by tests and gates, that `node scripts/check-seed-fixture.mjs` fails when this file and the SQL disagree, and that org Beta exists so a tenant-isolation test has a second tenant to be excluded from.
- `password` — the literal string `test-password-123`, so a test never has to hardcode it separately.
- `orgs` — an object with two keys, `alpha` and `beta`.

Each org object has exactly these keys: `org_id`, `org_name`, `org_slug`, `user_id`, `user_email`, `client_id`, `case_id`, `share_token`.

Use exactly these values. Every UUID is a valid version-4-shaped literal chosen so the tenant is readable from the id itself: alpha ids begin `a0000000`, beta ids begin `b0000000`.

| Key | alpha | beta |
|---|---|---|
| `org_id` | `a0000000-0000-4000-8000-000000000001` | `b0000000-0000-4000-8000-000000000001` |
| `org_name` | `Test Firm Alpha` | `Test Firm Beta` |
| `org_slug` | `test-firm-alpha` | `test-firm-beta` |
| `user_id` | `a0000000-0000-4000-8000-000000000002` | `b0000000-0000-4000-8000-000000000002` |
| `user_email` | `alpha@example.test` | `beta@example.test` |
| `client_id` | `a0000000-0000-4000-8000-000000000003` | `b0000000-0000-4000-8000-000000000003` |
| `case_id` | `a0000000-0000-4000-8000-000000000004` | `b0000000-0000-4000-8000-000000000004` |
| `share_token` | `a0000000-0000-4000-8000-000000000005` | `b0000000-0000-4000-8000-000000000005` |

Both slugs satisfy the `organizations.slug` constraint `^[a-z0-9\-]{3,60}$` from `001_initial_schema.sql:39`. Both emails use the reserved `.test` top-level domain.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && node -e "
const f=JSON.parse(require('fs').readFileSync('frontend/supabase/fixtures.json','utf8'));
console.log('keys', Object.keys(f).sort().join(','));
console.log('password', f.password);
for (const [k,v] of Object.entries(f.orgs)) console.log(k, Object.keys(v).sort().join(','), v.org_id, v.user_email);
"
  </verify>
  <acceptance_criteria>
    - `frontend/supabase/fixtures.json` is valid JSON with exactly the top-level keys `$comment`, `password`, `orgs`
    - `orgs` has exactly the keys `alpha` and `beta`
    - Each org object has exactly the eight keys `org_id`, `org_name`, `org_slug`, `user_id`, `user_email`, `client_id`, `case_id`, `share_token`
    - `password` equals `test-password-123`
    - Every alpha uuid starts with `a0000000-` and every beta uuid starts with `b0000000-`
    - Both `user_email` values end in `@example.test`
    - `$comment` contains the literal substring `tenant-isolation`
  </acceptance_criteria>
  <done>Later gates have a machine-readable contract of every seeded id rather than a set of literals scattered through test files.</done>
</task>

<task type="auto">
  <name>Task 2: Write the seed SQL</name>
  <files>frontend/supabase/seed.sql</files>
  <read_first>frontend/supabase/fixtures.json, frontend/supabase/migrations/001_initial_schema.sql, engine/examples/cases/02-married-3lc.json, frontend/supabase/config.toml</read_first>
  <action>
Create `frontend/supabase/seed.sql`. `[db.seed]` in `frontend/supabase/config.toml` already names it, so `supabase db reset` applies it after every migration with no further wiring.

Before writing any `auth.` insert, print the real column list of both auth tables and match it, rather than trusting this plan's column names:

```
docker exec supabase_db_inheritance psql -U postgres -d postgres -c "\d auth.users"
docker exec supabase_db_inheritance psql -U postgres -d postgres -c "\d auth.identities"
```

If a column named below is absent from the live schema, stop and report BLOCKED with the pasted `\d` output. Do not substitute a different column.

Open the file with a comment block stating: that this seed is applied by `supabase db reset`; that every id is published in `frontend/supabase/fixtures.json` and must be changed in both places together; that org Beta exists so a tenant-isolation test has a second tenant; and that the case `input_json` is a verbatim copy of `engine/examples/cases/02-married-3lc.json` and must not be edited.

Structure the file in six sections, in this order. Every insert uses `ON CONFLICT (id) DO NOTHING` so a repeated apply converges.

1. **Cleanup.** Delete the seeded rows by their fixed ids before inserting, in reverse dependency order: `cases`, `clients`, `organization_members`, `organizations`, `user_profiles`, `auth.identities`, `auth.users`. Restricting the deletes to the eight known uuids means the seed never touches a developer's own local rows.

2. **`auth.users`.** One row per user, with these column values:
   - `id` — the `user_id` from the registry
   - `instance_id` — `'00000000-0000-0000-0000-000000000000'`
   - `aud` — `'authenticated'`
   - `role` — `'authenticated'`
   - `email` — the `user_email` from the registry
   - `encrypted_password` — `crypt('test-password-123', gen_salt('bf'))`
   - `email_confirmed_at` — `NOW()`
   - `raw_app_meta_data` — `'{"provider":"email","providers":["email"]}'::jsonb`
   - `raw_user_meta_data` — `'{}'::jsonb`
   - `created_at`, `updated_at` — `NOW()`

   `crypt` and `gen_salt` come from the `pgcrypto` extension, which the Supabase local stack installs into the `extensions` schema. If either function is unresolvable, stop and report BLOCKED with the error rather than storing a plaintext password.

3. **`auth.identities`.** One row per user. Seeding only `auth.users` produces a user who exists but cannot sign in — a failure that would surface much later, in Phase 11, as a login gate failing for an environmental reason. Column values:
   - `id` — `gen_random_uuid()`
   - `user_id` — the `user_id` from the registry
   - `provider` — `'email'`
   - `provider_id` — the `user_id` from the registry, cast to text
   - `identity_data` — a jsonb object with `sub` set to the user id as text and `email` set to the user email
   - `last_sign_in_at`, `created_at`, `updated_at` — `NOW()`

4. **`user_profiles`.** One row per user: `id` set to the `user_id`, `email` set to the `user_email`, `full_name` set to `'Alpha Attorney'` and `'Beta Attorney'` respectively. Leave every firm and branding column at its schema default; plan 03-02 already handles the logo bucket, and a seeded `logo_url` would point at an object that does not exist.

5. **`organizations` and `organization_members`.** One organization per tenant using `org_id`, `org_name` and `org_slug` from the registry. Alpha gets `plan` `'team'` and `seat_limit` `5`; Beta gets `plan` `'solo'` and `seat_limit` `1`. Both `organization_members` rows get `role` `'admin'`, so each seeded user administers exactly its own org and belongs to no other.

6. **`clients` and `cases`.** One client per tenant, `full_name` `'Alpha Client'` and `'Beta Client'`, `org_id` set to that tenant's org. One case per tenant with:
   - `id`, `org_id`, `user_id`, `client_id` from the registry
   - `title` — `'Seeded Case Alpha'` and `'Seeded Case Beta'`
   - `status` — `'draft'`
   - `share_token` — the `share_token` from the registry
   - `share_enabled` — `true` for alpha, `false` for beta, so a later gate has both a shareable and a non-shareable case without seeding more rows
   - `input_json` — the verbatim contents of `engine/examples/cases/02-married-3lc.json`, cast with `::jsonb`

   Produce the `input_json` value by reading `engine/examples/cases/02-married-3lc.json` and embedding its exact bytes inside a dollar-quoted SQL literal (`$json$ ... $json$::jsonb`), which avoids escaping single quotes and avoids any transformation of the content. Both cases embed the same copy. Per constraint 6, no character inside it may be altered.

   Leave `output_json`, `tax_input_json`, `tax_output_json`, `decedent_name`, `date_of_death` and `gross_estate` unset. A seeded `output_json` would be a per-heir peso result nothing computed, which is precisely the kind of unverified number this project forbids.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/frontend && supabase db reset && cd /home/clsandoval/cs/monorepo/apps/inheritance && docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select (select count(*) from organizations), (select count(*) from organization_members), (select count(*) from clients), (select count(*) from cases), (select count(*) from auth.users), (select count(*) from auth.identities), (select count(*) from user_profiles)"
  </verify>
  <acceptance_criteria>
    - `cd frontend && supabase db reset` exits 0
    - The counts query returns `2|2|2|2|2|2|2`
    - `select org_slug from organizations order by slug` returns `test-firm-alpha` and `test-firm-beta`
    - `select share_enabled from cases order by title` returns `t` then `f`
    - `select input_json->'net_distributable_estate'->>'centavos' from cases limit 1` returns `600000000`, matching `engine/examples/cases/02-married-3lc.json`
    - `select jsonb_array_length(input_json->'family_tree') from cases limit 1` returns `4`, matching the same file
    - `frontend/supabase/seed.sql` contains the literal string `02-married-3lc.json` in its header comment
    - `grep -c "output_json" frontend/supabase/seed.sql` returns `0`
  </acceptance_criteria>
  <done>A database built from migrations plus seed contains two tenants whose ids are known in advance.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the seeded users can actually sign in</name>
  <files>frontend/supabase/seed.sql</files>
  <read_first>frontend/supabase/seed.sql, frontend/supabase/fixtures.json, frontend/.env.local.example</read_first>
  <action>
Row counts prove rows exist; they do not prove a user can log in. GoTrue rejects a user with no matching `auth.identities` row, and that failure would otherwise surface in Phase 11 as a login gate failing for an environmental reason.

Read the anon key from `frontend/.env.local` (written by `scripts/setup-env.sh` in plan 03-01) and call the local auth API on port 55321 for each seeded user:

```
curl -s -o /tmp/seed-auth-alpha.json -w '%{http_code}\n' \
  -X POST 'http://127.0.0.1:55321/auth/v1/token?grant_type=password' \
  -H "apikey: $ANON_KEY" -H 'Content-Type: application/json' \
  -d '{"email":"alpha@example.test","password":"test-password-123"}'
```

Repeat for `beta@example.test`. Each call must return HTTP `200`, and each response body must contain an `access_token` field. Print both status codes and confirm each body has the field with:

```
node -e "const j=require('/tmp/seed-auth-alpha.json');console.log('has token', typeof j.access_token === 'string' && j.access_token.length > 20)"
```

Then run a negative check: repeat the alpha call with the password `wrong-password`, and require a non-200 status. A seed that accepts any password would make every later authentication gate meaningless.

If any sign-in returns a non-200 with the correct password, stop and report BLOCKED with the pasted response body. Do not disable email confirmation, relax a policy, or edit an auth setting to make the call succeed — constraint 3 forbids weakening a check to pass it.

Record the two status codes and the negative-check status code; task 6 writes them into the summary.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && ANON_KEY=$(grep '^VITE_SUPABASE_ANON_KEY=' frontend/.env.local | cut -d= -f2-) && for e in alpha beta; do printf "%s " "$e"; curl -s -o /tmp/seed-auth-$e.json -w '%{http_code}\n' -X POST 'http://127.0.0.1:55321/auth/v1/token?grant_type=password' -H "apikey: $ANON_KEY" -H 'Content-Type: application/json' -d "{\"email\":\"$e@example.test\",\"password\":\"test-password-123\"}"; done; printf "wrong-password "; curl -s -o /dev/null -w '%{http_code}\n' -X POST 'http://127.0.0.1:55321/auth/v1/token?grant_type=password' -H "apikey: $ANON_KEY" -H 'Content-Type: application/json' -d '{"email":"alpha@example.test","password":"wrong-password"}'
  </verify>
  <acceptance_criteria>
    - The password sign-in for `alpha@example.test` returns HTTP `200`
    - The password sign-in for `beta@example.test` returns HTTP `200`
    - Both response bodies contain an `access_token` string longer than 20 characters
    - The sign-in for `alpha@example.test` with password `wrong-password` returns a status other than `200`
    - `select count(*) from auth.identities where provider = 'email'` returns `2`
    - No auth setting in `frontend/supabase/config.toml` was changed by this task
  </acceptance_criteria>
  <done>The seeded users are usable accounts, not merely rows, and a wrong password is still rejected.</done>
</task>

<task type="auto">
  <name>Task 4: Write the seed-versus-registry drift check</name>
  <files>scripts/check-seed-fixture.mjs</files>
  <read_first>scripts/check-storage-buckets.mjs, frontend/supabase/seed.sql, frontend/supabase/fixtures.json, engine/examples/cases/02-married-3lc.json</read_first>
  <action>
Create `scripts/check-seed-fixture.mjs`, a Node ESM script importing only `node:fs` and `node:path`. Read `scripts/check-storage-buckets.mjs` first and follow its shape: collect every violation, print all of them, then exit once. Resolve default paths relative to the script's own location. It reads only repo files and needs no database, so it can run wherever the other gates run.

Checks, in this order, collecting all violations:

1. `FIXTURE DRIFT` — a uuid appearing in `frontend/supabase/fixtures.json` that does not appear literally in `frontend/supabase/seed.sql`, or a uuid appearing in `seed.sql` that does not appear in `fixtures.json`. Extract uuids with the pattern for eight-four-four-four-twelve lowercase hexadecimal groups. Print the offending uuid and which file it is missing from.
2. `FIXTURE INCOMPLETE` — `fixtures.json` lacks the `password` key, lacks the `orgs` key, has other than exactly the two org keys `alpha` and `beta`, or any org object lacks one of the eight required keys.
3. `SEED INPUT NOT COPIED` — the `input_json` embedded in `seed.sql` is not a byte-for-byte copy of `engine/examples/cases/02-married-3lc.json`. Compare by extracting the text between the `$json$` delimiters in `seed.sql` and comparing it to the file's contents after trimming trailing whitespace from both. Any difference is a violation: the seeded family tree is copied legal content, and an edit to it is authored legal content.
4. `SEED WRITES OUTPUT` — `seed.sql` contains the token `output_json`. A seeded engine result is a per-heir peso figure nothing computed.
5. `SEED PLAINTEXT PASSWORD` — `seed.sql` contains the literal `test-password-123` outside a `crypt(` call, meaning a password was stored unhashed.

If any input file is missing or unparseable, print a message containing `SEED SCAN UNREADABLE` and exit 1. Never exit 0 on an internal error.

Three flags, and no others:
- `--seed <path>` — read the seed SQL from `<path>`.
- `--fixtures <path>` — read the registry from `<path>`.
- `--engine-case <path>` — read the reference engine case from `<path>`.

Exit codes: `0` only when there are zero violations, `1` otherwise. No other exit code.

Success output: two lines. The first contains the literal string `SEED OK` plus the counts, in the shape `SEED OK — 2 orgs, 16 ids matched`. The second contains the literal string `INPUT COPIED FROM engine/examples/cases/02-married-3lc.json`, so a reader of the log can see that the fixture's legal content is copied rather than authored without opening either file.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && node scripts/check-seed-fixture.mjs; echo "EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `node scripts/check-seed-fixture.mjs` exits 0 and prints both `SEED OK` and `INPUT COPIED FROM engine/examples/cases/02-married-3lc.json`
    - The script contains the literal strings `FIXTURE DRIFT`, `FIXTURE INCOMPLETE`, `SEED INPUT NOT COPIED`, `SEED WRITES OUTPUT`, `SEED PLAINTEXT PASSWORD`, `SEED SCAN UNREADABLE` and `SEED OK`
    - The script contains the literal strings `--seed`, `--fixtures` and `--engine-case`
    - `grep -cE "writeFileSync|appendFileSync|--fix|--update|--accept|--regenerate" scripts/check-seed-fixture.mjs` returns `0`
    - `grep -nE "^import" scripts/check-seed-fixture.mjs` shows only `node:` builtin imports
    - `grep -cE "child_process|fetch\(" scripts/check-seed-fixture.mjs` returns `0`
  </acceptance_criteria>
  <done>The registry and the SQL can no longer disagree without a command saying so.</done>
</task>

<task type="auto">
  <name>Task 5: Prove every verdict fires, using three committed fixtures</name>
  <files>scripts/fixtures/seed-drift.sql, scripts/fixtures/seed-ok.sql, scripts/fixtures/fixtures-drift.json</files>
  <read_first>scripts/check-seed-fixture.mjs, frontend/supabase/seed.sql, frontend/supabase/fixtures.json</read_first>
  <action>
Generate the three fixtures programmatically from the real files rather than hand-typing them, so they cannot drift from the originals.

1. `scripts/fixtures/seed-ok.sql` — a full copy of `frontend/supabase/seed.sql`, unmodified. Run with `--seed scripts/fixtures/seed-ok.sql` and expect exit 0 with `SEED OK`. This proves a pass verdict is reachable through the flag path, so a later fixture failure is not merely the flag being ignored.

2. `scripts/fixtures/seed-drift.sql` — a copy of the real seed with the alpha `case_id` uuid `a0000000-0000-4000-8000-000000000004` replaced throughout by `a0000000-0000-4000-8000-00000000dead`. Run with `--seed scripts/fixtures/seed-drift.sql` and expect exit 1 with `FIXTURE DRIFT` naming both the missing registry uuid and the unexpected seed uuid.

3. `scripts/fixtures/fixtures-drift.json` — a copy of the real registry with the `beta` org object's `client_id` key deleted. Run with `--fixtures scripts/fixtures/fixtures-drift.json` and expect exit 1 with `FIXTURE INCOMPLETE`.

Then drive the remaining three markers without new fixture files, using the flags:

- `SEED INPUT NOT COPIED` — run with `--engine-case engine/examples/cases/01-single-lc.json`, a different committed engine case. The embedded copy will not match it, so the marker fires. This confirms the comparison is a real byte comparison rather than a presence check.
- `SEED SCAN UNREADABLE` — run with `--seed /tmp/definitely-not-a-file.sql` and expect exit 1 with that marker.
- `SEED WRITES OUTPUT` and `SEED PLAINTEXT PASSWORD` — append two lines to `scripts/fixtures/seed-drift.sql`: a comment containing the token `output_json`, and a comment containing the literal `test-password-123` with no surrounding `crypt(`. Re-run against that fixture and require all three of `FIXTURE DRIFT`, `SEED WRITES OUTPUT` and `SEED PLAINTEXT PASSWORD` in the same output.

If any fixture run exits 0 where 1 was expected, the corresponding check is not wired up. Fix the script, never the fixture.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && echo "--- ok ---"; node scripts/check-seed-fixture.mjs --seed scripts/fixtures/seed-ok.sql; echo "EXIT=$?"; echo "--- drift ---"; node scripts/check-seed-fixture.mjs --seed scripts/fixtures/seed-drift.sql; echo "EXIT=$?"; echo "--- incomplete ---"; node scripts/check-seed-fixture.mjs --fixtures scripts/fixtures/fixtures-drift.json; echo "EXIT=$?"; echo "--- wrong engine case ---"; node scripts/check-seed-fixture.mjs --engine-case engine/examples/cases/01-single-lc.json; echo "EXIT=$?"; echo "--- unreadable ---"; node scripts/check-seed-fixture.mjs --seed /tmp/definitely-not-a-file.sql; echo "EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `--seed scripts/fixtures/seed-ok.sql` exits 0 and prints `SEED OK`
    - `--seed scripts/fixtures/seed-drift.sql` exits 1 and prints `FIXTURE DRIFT`, `SEED WRITES OUTPUT` and `SEED PLAINTEXT PASSWORD`
    - `--fixtures scripts/fixtures/fixtures-drift.json` exits 1 and prints `FIXTURE INCOMPLETE`
    - `--engine-case engine/examples/cases/01-single-lc.json` exits 1 and prints `SEED INPUT NOT COPIED`
    - `--seed /tmp/definitely-not-a-file.sql` exits 1 and prints `SEED SCAN UNREADABLE`
    - `node scripts/check-seed-fixture.mjs` with no flags still exits 0 after the fixtures exist
  </acceptance_criteria>
  <done>All six verdicts of the seed check have been observed firing, and its pass verdict has been observed through the flag path.</done>
</task>

<task type="auto">
  <name>Task 6: Prove the seed is idempotent, then commit</name>
  <files>frontend/supabase/seed.sql</files>
  <read_first>frontend/supabase/seed.sql, frontend/supabase/fixtures.json, scripts/check-seed-fixture.mjs</read_first>
  <action>
Run `cd frontend && supabase db reset` a second time, then re-run the counts query from task 2 and require the identical `2|2|2|2|2|2|2` result. A seed that doubles its rows on a second apply is a fixture no later gate can rely on.

Then confirm plan 03-02's bucket survived the reseed, since both plans write to the same database:

`docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select id, public from storage.buckets"` must still return exactly `firm-logos|t`.

Then run the full gate suite: `bash scripts/ci-gates.sh` — expect exit 0 and `ALL GATES PASSED (7/7)`. This plan adds no gate, so the count stays 7.

Then commit. Use the wrapper:

`bash scripts/safe-commit.sh -m "build(03): seed a two-tenant fixture with a published id registry" frontend/supabase/seed.sql frontend/supabase/fixtures.json scripts/check-seed-fixture.mjs scripts/fixtures/seed-ok.sql scripts/fixtures/seed-drift.sql scripts/fixtures/fixtures-drift.json`

Confirm with `git diff --cached --name-only` before the commit lands that exactly those six paths are staged and nothing else. NEVER run `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/frontend && supabase db reset && cd /home/clsandoval/cs/monorepo/apps/inheritance && docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select (select count(*) from organizations), (select count(*) from cases), (select count(*) from auth.users)" && docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select id, public from storage.buckets" && node scripts/check-seed-fixture.mjs && cd /home/clsandoval/cs/monorepo && git log -1 --name-only --format="%s"
  </verify>
  <acceptance_criteria>
    - A second `supabase db reset` exits 0 and the counts query still returns `2|2|2|2|2|2|2`
    - `select id, public from storage.buckets` still returns exactly `firm-logos|t`
    - `node scripts/check-seed-fixture.mjs` exits 0 and prints `SEED OK`
    - `bash scripts/ci-gates.sh` exits 0 and prints `ALL GATES PASSED (7/7)`
    - `git log -1 --format=%s` outputs `build(03): seed a two-tenant fixture with a published id registry`
    - `git log -1 --name-only --format=` lists exactly the six paths from the commit command and nothing else
  </acceptance_criteria>
  <done>The fixture is stable across resets, coexists with the bucket migration, and is committed.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|---|---|---|---|
| T-3-03a | The seed doubles or mutates rows on a second apply, so a gate that passed yesterday fails today for a reason unrelated to the product | High | Every insert uses `ON CONFLICT DO NOTHING` after an id-scoped cleanup; task 6 runs `supabase db reset` a second time and requires identical counts |
| T-3-03b | A seeded user exists as a row but cannot sign in, so Phase 11's login gate fails for an environmental reason that looks like a product defect | High | `auth.identities` is seeded alongside `auth.users`; task 3 verifies a real HTTP 200 password sign-in for both users rather than reading rows back |
| T-3-03c | The id registry and the SQL drift, so later gates reference rows that no longer exist | High | `FIXTURE DRIFT` compares uuid sets in both directions; observed firing in task 5 against a committed fixture |
| T-3-03d | The seeded family tree is edited, quietly authoring legal content into a fixture that later phases treat as ground truth | High | Constraint 6 prohibits it; `SEED INPUT NOT COPIED` performs a byte comparison against `engine/examples/cases/02-married-3lc.json`, observed firing in task 5 against a different engine case |
| T-3-03e | A seeded `output_json` supplies per-heir peso figures nothing computed, and a later gate asserts against them | High | The seed sets no `output_json`; `SEED WRITES OUTPUT` fails on the token's presence, observed firing in task 5 |
| T-3-03f | A password is stored unhashed, so the fixture teaches a pattern that reaches production | Medium | `crypt('test-password-123', gen_salt('bf'))` is required by task 2; `SEED PLAINTEXT PASSWORD` fails on the literal outside a `crypt(` call, observed firing in task 5 |
| T-3-03g | A single-tenant fixture cannot express Phase 11's cross-tenant isolation criterion, forcing a later phase to rewrite a fixture other gates already reference | Medium | Two tenants are seeded now, each user an admin of exactly its own org and a member of no other |
| T-3-03h | The seed deletes a developer's own local rows | Medium | The cleanup section deletes only the eight known uuids, never by table truncation |
| T-3-03i | Seeded emails reach a real domain if the local stack is ever pointed at a real SMTP server | Low | Both emails use the reserved `.test` top-level domain, required by constraint 10 and asserted in task 1 |

No new dependency is introduced. `scripts/check-seed-fixture.mjs` uses only `node:fs` and `node:path`, spawns no subprocess and makes no network call, which its acceptance criteria assert by grep. The `curl` calls in task 3 are verification commands run by the executor, not code added to the repo.
</threat_model>

<verification>
Before declaring plan complete, run each and confirm the stated result:
- [ ] `node -e "JSON.parse(require('fs').readFileSync('frontend/supabase/fixtures.json','utf8'))"` → exit 0
- [ ] `cd frontend && supabase db reset` → exit 0
- [ ] `docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select (select count(*) from organizations), (select count(*) from organization_members), (select count(*) from clients), (select count(*) from cases), (select count(*) from auth.users), (select count(*) from auth.identities), (select count(*) from user_profiles)"` → `2|2|2|2|2|2|2`
- [ ] `docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select input_json->'net_distributable_estate'->>'centavos' from cases limit 1"` → `600000000`
- [ ] `docker exec supabase_db_inheritance psql -U postgres -d postgres -tAc "select jsonb_array_length(input_json->'family_tree') from cases limit 1"` → `4`
- [ ] Password sign-in for `alpha@example.test` and `beta@example.test` → HTTP `200` each, both bodies containing an `access_token`
- [ ] Password sign-in for `alpha@example.test` with `wrong-password` → status other than `200`
- [ ] `node scripts/check-seed-fixture.mjs` → exit 0, prints `SEED OK` and `INPUT COPIED FROM engine/examples/cases/02-married-3lc.json`
- [ ] `node scripts/check-seed-fixture.mjs --seed scripts/fixtures/seed-drift.sql` → exit 1, prints `FIXTURE DRIFT`, `SEED WRITES OUTPUT`, `SEED PLAINTEXT PASSWORD`
- [ ] `node scripts/check-seed-fixture.mjs --fixtures scripts/fixtures/fixtures-drift.json` → exit 1, prints `FIXTURE INCOMPLETE`
- [ ] `node scripts/check-seed-fixture.mjs --engine-case engine/examples/cases/01-single-lc.json` → exit 1, prints `SEED INPUT NOT COPIED`
- [ ] `node scripts/check-seed-fixture.mjs --seed /tmp/definitely-not-a-file.sql` → exit 1, prints `SEED SCAN UNREADABLE`
- [ ] A second `cd frontend && supabase db reset` → exit 0, counts unchanged, `select id, public from storage.buckets` still `firm-logos|t`
- [ ] `bash scripts/ci-gates.sh` → exit 0, `ALL GATES PASSED (7/7)`
- [ ] `git diff --name-only HEAD~1 HEAD` → exactly the six paths from task 6
</verification>

<success_criteria>
- All six tasks completed
- `frontend/supabase/seed.sql` exists and is applied automatically by `supabase db reset` with no config change
- Two tenants exist with fixed ids published in `frontend/supabase/fixtures.json`, so Phase 11 has both a subject and an excluded tenant for its isolation criterion
- Both seeded users signed in over HTTP with the seeded password, and a wrong password was rejected
- The seeded case's `input_json` is a byte-for-byte copy of a committed engine fixture, verified by comparison rather than by claim, so no legal content was authored here
- No `output_json` is seeded, so no per-heir peso figure exists that nothing computed
- All six verdicts of `check-seed-fixture.mjs` have been observed firing against committed fixtures
- The seed is idempotent across two consecutive resets, and plan 03-02's bucket survives them
- `scripts/ci-gates.sh` still exits 0 with `ALL GATES PASSED (7/7)` — this plan changes no gate
- Exactly one commit was made, naming exactly six explicit file paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-reproducible-environment-gate-reporting/03-03-SUMMARY.md`.
Record: the counts query output after the first reset and after the second, the two sign-in status codes and the wrong-password status code, the `SEED OK` and `INPUT COPIED FROM` lines, the exit code and matched markers for each of the six fixture runs, the `storage.buckets` query proving plan 03-02's bucket survived, the `ALL GATES PASSED` line, and the commit SHA.
Also record the eight seeded uuids and the two emails verbatim, so Phases 10 and 11 can reference them without re-reading the registry.
</output>
