---
phase: 09-single-source-of-truth-dedup-classifiers-money-types
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/tests/integration.rs
  - engine/src/step2_lines.rs
  - engine/src/step8_collation.rs
autonomous: true
requirements: [EXT-04]
user_setup: []

must_haves:
  truths:
    - "The one dead helper that carries a legal invariant is revived by being called, never deleted"
    - "The dead helpers that carry no legal content are deleted outright"
    - "The engine's invariant coverage goes up in this plan and never down"
    - "No expected value anywhere in the vector suite is edited"
  artifacts:
    - path: "engine/tests/integration.rs"
      provides: "Invariant 6 asserted inside the adoption vector, and two stale dead-code attributes removed"
      contains: "check_adoption_equality(&output)"
  key_links:
    - from: "engine/tests/integration.rs"
      to: ".planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-RESEARCH.md"
      via: "the disposition of each of the six allow(dead_code) sites is the table in section 2.2"
      pattern: "09-RESEARCH"
---

<objective>
Clear the engine's dead-code inventory without losing a single assertion.

`grep -rn "allow(dead_code)" engine/src engine/tests` returns six sites. `09-RESEARCH.md` §2.2
measured the call count of each and fixed its disposition. The distinction that governs this plan:
`check_adoption_equality` is an **assertion** — invariant 6, that an adopted child's share equals a
legitimate child's share. Deleting an unused assertion removes verification, which is the one
direction this project never moves. It is revived by being called. `find_share_by_name` and the two
`#[cfg(test)]` module helpers carry no legal content and are deleted. `check_scenario_consistency` is
called 28 times and simply has a stale attribute.

Output: one more invariant actually asserted, five fewer suppressions, and no assertion lost.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PLAN-STANDARD.md
@.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-RESEARCH.md
@.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-VALIDATION.md
@engine/tests/integration.rs
@engine/src/step2_lines.rs
@engine/src/step8_collation.rs
</context>

<interfaces>
The six measured sites and their dispositions, transcribed from `09-RESEARCH.md` §2.2:

| Site | Call sites | Disposition |
|---|---|---|
| `engine/tests/integration.rs:598` `check_scenario_consistency` | 28 | remove the attribute only |
| `engine/tests/integration.rs:578` `check_adoption_equality` | 0 | remove the attribute and call it from `test_tv09_adopted_equals_legitimate` |
| `engine/tests/integration.rs:620` `find_share_by_name` | 0 | delete the function |
| `engine/src/step2_lines.rs:621` `fn ineligible` | 0 | delete the function |
| `engine/src/step8_collation.rs:386` `fn make_distribution` | 0 | delete the function |
| `engine/tests/defect_ledger.rs:53` field `requirement` | n/a | **keep** — a serde field the ledger's shape requires |

`test_tv09_adopted_equals_legitimate` begins at `engine/tests/integration.rs:1052` and its first
family-tree line is `let mut adopted = person("ac1", "Sam", Relationship::AdoptedChild);`.

The helper being revived:

```rust
fn check_adoption_equality(output: &EngineOutput);   // asserts all LegitimateChildGroup OwnRight rows are equal
```
</interfaces>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. Commit arguments are
   repo-root-relative; every other path in this plan is relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken or loosen a test, an assertion or a gate. **No expected value anywhere
   in `engine/tests/integration.rs` may be edited.** The only edits permitted in that file are the two
   attribute removals, the one added call line, and the deletion of `find_share_by_name`.
4. **Do not delete `check_adoption_equality` and do not delete `check_scenario_consistency`.** Both
   carry assertions. Deleting an assertion to clear a warning is the exact tradeoff this project
   never accepts.
5. **Do not delete the `requirement` field at `engine/tests/defect_ledger.rs:53`.** It is a serde
   field required for the ledger to deserialize; removing it breaks `engine/defect-baseline.json`
   parsing. Leave that file untouched.
6. **No point of Philippine law may be decided.** Reviving invariant 6 asserts a property the engine
   already implements and `test_tv09` already exercises; it interprets nothing. If a question arises
   that this sentence does not answer, stop, report BLOCKED, and append it to
   `.planning/LAWYER-AGENDA.md` without answering it.
7. Files this plan may edit: `engine/tests/integration.rs`, `engine/src/step2_lines.rs`,
   `engine/src/step8_collation.rs`. Do not edit anything else — in particular not
   `engine/tests/defect_ledger.rs`, `engine/defect-baseline.json`, `coverage-zero.lock`,
   `frontend/test-baseline.json`, `gate-skips.lock`, `assertion-baseline.json`,
   `gates.manifest.json` or `gates.manifest.lock`.
8. `cd engine && cargo test` must report **0 failed** with at least **543 passing** at the end of
   every task.
9. `bash scripts/ci-gates.sh` still halts at `G3` for Phase 5's unresolved OBS-05 and OBS-06 product
   decision, which this plan does not touch. `ALL GATES PASSED` is not achievable and must not be
   claimed in any summary.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Revive invariant 6 inside the adoption vector</name>
  <files>engine/tests/integration.rs</files>
  <read_first>engine/tests/integration.rs, .planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-RESEARCH.md</read_first>
  <action>
In `engine/tests/integration.rs`, delete the `#[allow(dead_code)]` attribute line immediately above
`fn check_adoption_equality` (currently line 578) and the one immediately above
`fn check_scenario_consistency` (currently line 598). Change nothing inside either function body.

Then, inside `test_tv09_adopted_equals_legitimate` (currently beginning at line 1052), add a call to
`check_adoption_equality(&output)` alongside the assertions the vector already makes. Place it
directly after the existing `check_sum_invariant` call in that test if one is present, otherwise
immediately after the per-heir amount assertions and before the end of the function. Do not remove
or alter any assertion already in that test.

Add a one-line comment above the new call, in the file's established style, stating that this is
invariant 6 from the spec's invariant list — that an adopted child's share equals a legitimate
child's share — and that it was defined but never called until this plan.

`check_scenario_consistency` needs no new call site: it already has 28. Its attribute was simply
stale after Phase 6 revived it.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && cargo test --test integration 2>&1 | grep -E "tv09|test result" | tail -6; grep -c "check_adoption_equality(&output)" tests/integration.rs; grep -c "allow(dead_code)" tests/integration.rs; cargo test 2>&1 | grep -E "^warning: |never used" | head -5; cargo test 2>&1 | awk '/^test result/{p+=$4; f+=$6} END{print "TOTAL_PASSED="p" TOTAL_FAILED="f}'
  </verify>
  <acceptance_criteria>
    - `grep -c "check_adoption_equality(&output)" engine/tests/integration.rs` returns 1
    - `grep -c "allow(dead_code)" engine/tests/integration.rs` drops from 3 to 1, the surviving one being `find_share_by_name` until task 2
    - `test_tv09_adopted_equals_legitimate` appears by name in the `cargo test --test integration` output and passes
    - `git diff engine/tests/integration.rs` shows no change to any `assert_total_pesos`, `assert_total_centavos` or `assert_eq!` line that existed before this task
    - `cd engine && cargo test` reports `0 failed` with at least 543 passing
  </acceptance_criteria>
  <done>Invariant 6 is asserted rather than merely defined.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the three helpers with no legal content and commit</name>
  <files>engine/tests/integration.rs, engine/src/step2_lines.rs, engine/src/step8_collation.rs</files>
  <read_first>engine/tests/integration.rs, engine/src/step2_lines.rs, engine/src/step8_collation.rs, scripts/safe-commit.sh</read_first>
  <action>
Delete three functions, each together with its `#[allow(dead_code)]` attribute and its doc comment if
it has one. Each has zero call sites, measured in `09-RESEARCH.md` §2.2, and none contains an
assertion or a rule:

1. `fn find_share_by_name` in `engine/tests/integration.rs` (currently at line 621) — a lookup by
   `heir_name`, superseded by `find_share`, which looks up by `heir_id` and is called 34 times.
2. `fn ineligible` in `engine/src/step2_lines.rs` (currently at line 621), inside the file's
   `#[cfg(test)] mod tests` block — a one-line mutator that sets `is_eligible = false`.
3. `fn make_distribution` in `engine/src/step8_collation.rs` (currently at line 386), inside that
   file's `#[cfg(test)] mod tests` block — a `HeirDistribution` constructor.

After the deletions, `grep -rn "allow(dead_code)" engine/src engine/tests` must return exactly one
line: `engine/tests/defect_ledger.rs:53`, the serde field that must stay.

Run `cargo test` and read the compiler output for `never used` warnings. A deletion that orphans
something else would surface there; if it does, that orphan is a fourth dead symbol and the correct
response is to report BLOCKED with the pasted warning rather than to add a new `#[allow(dead_code)]`.

Then stage and commit the three edited files:

    bash scripts/safe-commit.sh -m "refactor(09-02): revive invariant 6 and clear the engine dead-code inventory (EXT-04)" apps/inheritance/engine/tests/integration.rs apps/inheritance/engine/src/step2_lines.rs apps/inheritance/engine/src/step8_collation.rs

Do not stage anything under `engine/target/`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance/engine && grep -rn "allow(dead_code)" src/ tests/; echo "DEAD_CODE_SITES=$(grep -rc 'allow(dead_code)' src/ tests/ | awk -F: '{s+=$2} END {print s}')"; grep -c "find_share_by_name" tests/integration.rs; cargo test 2>&1 | grep -E "never used|^warning" | head -5; cargo test 2>&1 | awk '/^test result/{p+=$4; f+=$6} END{print "TOTAL_PASSED="p" TOTAL_FAILED="f}'; cd .. && node scripts/check-commit-discipline.mjs; echo "G7_EXIT=$?"; git status --porcelain engine/
  </verify>
  <acceptance_criteria>
    - `grep -rn "allow(dead_code)" engine/src engine/tests` returns exactly one line, and it is in `engine/tests/defect_ledger.rs`
    - `grep -c "find_share_by_name" engine/tests/integration.rs` returns 0
    - `cargo test` emits no `never used` warning
    - `cd engine && cargo test` reports `0 failed` with at least 543 passing, and the passing count did not drop relative to task 1
    - `git log -1 --stat` shows exactly three changed files and `node scripts/check-commit-discipline.mjs` exits 0
  </acceptance_criteria>
  <done>Nothing dead is left to be imported by accident, and nothing that asserted anything was removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dead-code cleanup → verification coverage | A cleanup pass that deletes assertion helpers silently reduces coverage while appearing tidy; that is the failure this plan is shaped to avoid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Repudiation | `engine/tests/integration.rs` | mitigate | The plan names each of the six sites with its measured call count and its disposition, so the executor deletes only the three with no assertion and revives the one that has one |
| T-09-SC | Tampering | cargo installs | mitigate | This plan installs no package and does not edit `engine/Cargo.toml` or `engine/Cargo.lock` |
</threat_model>

<verification>

Run from `/home/clsandoval/cs/monorepo/apps/inheritance`:

    cd engine && cargo test 2>&1 | tail -25
    cd engine && cargo test --test integration 2>&1 | grep -E "tv09|test result"
    grep -rn "allow(dead_code)" engine/src engine/tests
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs

Expected: `cargo test` reports `0 failed` with at least 543 passing and emits no `never used`
warning; exactly one `allow(dead_code)` survives, in `engine/tests/defect_ledger.rs`; both static
checks exit 0.

`bash scripts/ci-gates.sh` is expected to reach `G3` and halt there on the same five frontend failures
Phase 5 left behind. If the runner halts anywhere **before** `G3`, stop and report BLOCKED with the
pasted output.

</verification>

<success_criteria>

- [ ] `check_adoption_equality` is called from `test_tv09_adopted_equals_legitimate`
- [ ] Neither `check_adoption_equality` nor `check_scenario_consistency` was deleted
- [ ] `find_share_by_name`, `ineligible` and `make_distribution` are gone
- [ ] Exactly one `allow(dead_code)` survives in the engine, the serde field in `defect_ledger.rs`
- [ ] `cargo test` emits no `never used` warning
- [ ] No pre-existing expected value in `engine/tests/integration.rs` was edited
- [ ] `cd engine && cargo test` reports 0 failed with at least 543 passing
- [ ] `ALL GATES PASSED` was not claimed

</success_criteria>

<output>
Create `.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-02-SUMMARY.md` when done
</output>
