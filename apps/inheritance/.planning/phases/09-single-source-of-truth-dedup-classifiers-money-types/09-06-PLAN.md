---
phase: 09-single-source-of-truth-dedup-classifiers-money-types
plan: 06
type: execute
wave: 3
depends_on: ["09-04", "09-05"]
files_modified:
  - single-source.json
  - scripts/check-single-source.mjs
  - scripts/fixtures/single-source-violation/reintroduced-classifier.ts
  - scripts/fixtures/single-source-gutted.json
  - gates.manifest.json
  - gates.manifest.lock
  - GATES.md
  - SINGLE-SOURCE.md
autonomous: true
requirements: [EXT-02]
user_setup: []

must_haves:
  truths:
    - "Re-introducing any of the four duplicated implementations this phase removed fails the build"
    - "The rule is written down in prose a human can follow, not only encoded in a regex"
    - "The rule registry cannot be gutted by deleting an entry, because the required ids are hardcoded in the check"
    - "The new gate runs ahead of the G3 halt, so it is observable rather than merely registered"
  artifacts:
    - path: "scripts/check-single-source.mjs"
      provides: "Gate G14 — the automated single-source-of-truth check"
      contains: "SINGLE SOURCE VIOLATION"
    - path: "single-source.json"
      provides: "The four named rules, each with a literal pattern, a scan scope and a ceiling"
      contains: "SSOT-01"
    - path: "SINGLE-SOURCE.md"
      provides: "The documented rule and the procedure for adding a fifth"
      contains: "SSOT-04"
  key_links:
    - from: "gates.manifest.json"
      to: "scripts/check-single-source.mjs"
      via: "G14 registers the check as a blocking gate at order 6"
      pattern: "check-single-source"
    - from: "SINGLE-SOURCE.md"
      to: "single-source.json"
      via: "the prose rule and the machine-readable registry describe the same four rules"
      pattern: "SSOT-0"
---

<objective>
Make the duplication this phase removed impossible to re-introduce silently.

EXT-02 asks for "a documented rule and a check". Plans 09-01 through 09-05 deleted four
duplicated implementations: two scenario classifiers, one synthetic compute path, and one peso→centavo
converter. Nothing today stops a future change from adding a fifth.

`09-RESEARCH.md` §3 measured four patterns that detect exactly those duplications with zero false
positives in this tree, and recorded each one's count today and its ceiling after this phase's
deletions land. This plan turns those four measurements into a registry, a check, a gate at order 6,
and a page of prose that says what the rule is and how to add the next one.

Output: gate `G14`, a four-rule registry that cannot be gutted, two committed failure fixtures, and
`SINGLE-SOURCE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PLAN-STANDARD.md
@.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-RESEARCH.md
@.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-VALIDATION.md
@.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-04-SUMMARY.md
@.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-05-SUMMARY.md
@GATES.md
@gates.manifest.json
@gates.manifest.lock
@scripts/check-lawyer-agenda.mjs
@scripts/check-observability.mjs
@scripts/ci-gates.sh
</context>

<interfaces>
The four rules, measured in `09-RESEARCH.md` §3. `today` was measured before this phase; `ceiling` is
what the gate enforces:

| Id | Pattern (JavaScript regex source) | Scope | Ceiling | Today | After 09-04/09-05 |
|---|---|---|---|---|---|
| `SSOT-01` | `as\s+ScenarioCode` | `frontend/src`, `.ts` and `.tsx` | 0 files | 1 file, 12 hits | 0 |
| `SSOT-02` | `successionType\s*:\s*["'](Testate\|Intestate)` | `frontend/src`, `__tests__` excluded | 0 files | 1 file, 27 hits | 0 |
| `SSOT-03` | `function\s+predictScenario` | `frontend/src`, `.ts` and `.tsx` | 0 files | 2 files | 0 |
| `SSOT-04` | `Math\.round\(.*\*\s*100` | `frontend/src`, `__tests__` excluded | only the 3 allowed files | 4 files | 3 |

`SSOT-04`'s three allowed files and the reason each is allowed:

- `src/types/money-units.ts` — the one permitted implementation of peso→centavo conversion
- `src/lib/documents.ts` — `Math.round((obtained / total) * 100)`, a completion percentage
- `src/lib/timeline.ts` — `Math.round((completedStages / stages.length) * 100)`, a progress percentage

`SSOT-02` excludes `__tests__` with evidence: the pattern matches
`src/components/results/__tests__/ResultsHeader.test.tsx` and
`src/components/results/__tests__/DistributionSection.test.tsx`, where it appears inside an
`EngineOutput` a test constructs as *expected data*.

The gate-order rearrangement, from `09-RESEARCH.md` §3.2. `order` is unlocked;
`gates.manifest.lock` freezes only `{id, command, blocking}`:

| Gate | order before | order after |
|---|---|---|
| G5, G6, G7, G12, G13 | 1, 2, 3, 4, 5 | unchanged |
| **G14** | — | **6** |
| G1, G2, G3, G4 | 6, 7, 8, 9 | 7, 8, 9, 10 |
| G10, G11, G8, G9 | 10, 11, 12, 13 | 11, 12, 13, 14 |

The skip-accounting line every gate must print on every exit path, pattern taken from
`scripts/check-observability.mjs:85`:

```
GATE-SKIPS total=<n> skipped=<n>
```

The hardcoded-required-ids pattern to copy, from `scripts/check-lawyer-agenda.mjs:53-59`:

```js
/** The expected set, hardcoded so it is auditable here rather than inferred
 *  from whichever file is being checked. */
const REQUIRED_IDS = ['LAWYER-01', ...];
```
</interfaces>

<constraints>
Hard rules for this plan. Violating any of these fails the task.

1. All work happens under `/home/clsandoval/cs/monorepo/apps/inheritance`. Commit arguments are
   repo-root-relative; every other path in this plan is relative to that directory.
2. NEVER run `git add -A`, `git add .`, or `git commit -a`. Stage explicit paths only, using
   `bash scripts/safe-commit.sh -m "<message>" <path> ...`.
3. NEVER delete, skip, weaken or loosen a test, an assertion or a gate. In `gates.manifest.json` and
   `gates.manifest.lock` the **only** permitted changes are: appending the one new `G14` object to
   each, and rewriting the `order` number of the eight gates in the table above. No `command`, `id`,
   `blocking`, `cwd`, `precondition` or `requirements` value of any existing gate may change, and no
   entry may be removed from either file.
4. **`scripts/check-single-source.mjs` must have no flag that writes `single-source.json`.** Its only
   flags are `--root` and `--registry`, both read-only path overrides that exist so the fixtures can
   drive the failure paths. A check that can rewrite its own baseline is not a check; this is the same
   rule `scripts/check-gate-manifest.mjs`, `scripts/check-coverage.mjs` and
   `scripts/check-assertion-discipline.mjs` already state in their own headers.
5. **Do not add a rule that is not in the table above.** All four were measured against this tree and
   their post-phase counts are known. A fifth invented rule could be red on arrival, and the correct
   response to a red new gate is never to weaken it.
6. **If any of the four rules is not at its ceiling when the check first runs, report BLOCKED with the
   pasted output.** Do not raise a ceiling, do not add an allowed file that is not in the table, and
   do not delete a rule. A rule above its ceiling means plan 09-04 or 09-05 left a duplicate behind,
   which is a finding, not a gate defect.
7. **No point of Philippine law may be decided.** This plan writes a lint and a document. If a
   question arises, stop, report BLOCKED, and append it to `.planning/LAWYER-AGENDA.md` without
   answering it.
8. Files this plan may create or edit are exactly the eight in `files_modified`. Do not edit anything
   else — in particular not `frontend/test-baseline.json`, `gate-skips.lock`, `assertion-baseline.json`,
   `coverage-zero.lock`, `engine/defect-baseline.json`, or anything under `frontend/src/` or
   `engine/src/`.
9. `bash scripts/ci-gates.sh` still halts at `G3`, which is order 9 after this plan's renumbering.
   `G14` at order 6 runs before the halt and must be observed passing in a real runner invocation.
   **`ALL GATES PASSED (14/14)` is not achievable in this phase and must not be claimed in any
   summary.**
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the registry, the check and the two failure fixtures</name>
  <files>single-source.json, scripts/check-single-source.mjs, scripts/fixtures/single-source-violation/reintroduced-classifier.ts, scripts/fixtures/single-source-gutted.json</files>
  <read_first>scripts/check-lawyer-agenda.mjs, scripts/check-observability.mjs, scripts/check-coverage.mjs, .planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-RESEARCH.md</read_first>
  <action>
Create `single-source.json` at the app root, beside `gates.manifest.json`. It opens with a
`$comment` string in the style of `coverage-zero.lock` and `assertion-baseline.json`, stating: that
the file is OWNER-OWNED; that it names the legal and monetary rules that may exist in exactly one
implementation; that it **may only grow**, so adding a rule is welcome and removing one is prohibited;
that `scripts/check-single-source.mjs` hardcodes the four required ids so deleting an entry fails
rather than silently reducing coverage; and that the correct response to a red rule is to delete the
duplicate, never to raise the ceiling.

It then carries `frozen_at: "2026-07-31"` and a `rules` array of four objects, each with these seven
keys:

- `id` — `SSOT-01` through `SSOT-04`
- `rule` — one sentence of prose naming what may exist only once
- `owner` — the file or module that holds the single implementation, as a path string
- `pattern` — the JavaScript regex **source** string from the table in `<interfaces>`
- `scan` — an object `{ "root": "frontend/src", "extensions": [".ts", ".tsx"], "exclude_tests": true|false }`
  where `exclude_tests` skips any path containing `/__tests__/` or ending `.test.ts`, `.test.tsx`,
  `.spec.ts` or `.spec.tsx`
- `allowed_files` — an array of repo-relative paths, empty for `SSOT-01` through `SSOT-03` and the
  three files listed in `<interfaces>` for `SSOT-04`
- `reason` — for `SSOT-02` and `SSOT-04`, why the exclusion or each allowed file is legitimate; the
  three `SSOT-04` reasons are given verbatim in `<interfaces>`

Create `scripts/check-single-source.mjs`, dependency-free, `node:` builtins only, exit 0 on zero
violations and 1 otherwise. Its header comment follows the house style of
`scripts/check-gate-manifest.mjs`: the invocation lines, the enumerated violation markers each with
its own literal string, and the statement that the script never writes the registry and has no flag
that does.

Three violation markers, each printed to stderr with the offending file and the matched line:

1. `SINGLE SOURCE VIOLATION` — a file outside `allowed_files` matches a rule's `pattern`
2. `SINGLE SOURCE RULE MISSING` — an id in the hardcoded `REQUIRED_IDS` has no entry in the registry
3. `SINGLE SOURCE RULE MALFORMED` — a registry entry is missing one of the seven keys, or its
   `pattern` is not a compilable regex

Hardcode `const REQUIRED_IDS = ['SSOT-01', 'SSOT-02', 'SSOT-03', 'SSOT-04'];` with the same
"hardcoded so it is auditable here" comment `check-lawyer-agenda.mjs` uses.

Accept exactly two flags, both read-only: `--root <path>` overriding the scan root, and
`--registry <path>` overriding the registry path.

On every exit path — success and failure alike — print
`GATE-SKIPS total=<rules checked> skipped=<rules not checked>`, following
`scripts/check-observability.mjs:85`. On success also print a one-line summary naming the rule count
and the file count scanned, in the style of that script's `OBSERVABILITY OK` line.

Create the two failure fixtures.

`scripts/fixtures/single-source-violation/reintroduced-classifier.ts` is a small TypeScript file
containing one line that matches `SSOT-01` — a return of a template-string scenario code followed by
`as ScenarioCode` — plus a header comment saying it exists to drive the `SINGLE SOURCE VIOLATION`
path and must never be imported by application code. Because it lives under `scripts/fixtures/` and
not `frontend/src/`, the real gate never scans it; only `--root` reaches it.

`scripts/fixtures/single-source-gutted.json` is a copy of `single-source.json` with the `SSOT-01`
entry removed, to drive the `SINGLE SOURCE RULE MISSING` path.

Then run the check three ways and confirm each: against the repository it exits 0; with
`--root scripts/fixtures/single-source-violation` it exits 1 printing `SINGLE SOURCE VIOLATION`; with
`--registry scripts/fixtures/single-source-gutted.json` it exits 1 printing
`SINGLE SOURCE RULE MISSING`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && node scripts/check-single-source.mjs; echo "REPO_EXIT=$?"; node scripts/check-single-source.mjs --root scripts/fixtures/single-source-violation; echo "VIOLATION_EXIT=$?"; node scripts/check-single-source.mjs --registry scripts/fixtures/single-source-gutted.json; echo "GUTTED_EXIT=$?"; node scripts/check-single-source.mjs | grep "GATE-SKIPS"; grep -c "REQUIRED_IDS" scripts/check-single-source.mjs; grep -cE "\-\-fix|\-\-update|\-\-accept|\-\-regenerate|writeFileSync" scripts/check-single-source.mjs
  </verify>
  <acceptance_criteria>
    - `node scripts/check-single-source.mjs` exits 0 against the repository and prints a `GATE-SKIPS total=4 skipped=0` line
    - `node scripts/check-single-source.mjs --root scripts/fixtures/single-source-violation` exits 1 and prints `SINGLE SOURCE VIOLATION`
    - `node scripts/check-single-source.mjs --registry scripts/fixtures/single-source-gutted.json` exits 1 and prints `SINGLE SOURCE RULE MISSING`
    - `single-source.json` carries all four ids with all seven keys each, and `SSOT-04` names exactly the three allowed files
    - `grep -cE "\-\-fix|\-\-update|\-\-accept|\-\-regenerate|writeFileSync" scripts/check-single-source.mjs` returns 0, so the check cannot write its own registry
    - The check uses only `node:` builtins and no dependency was added to any manifest
  </acceptance_criteria>
  <done>A re-introduced duplicate fails, and the check cannot be talked out of it.</done>
</task>

<task type="auto">
  <name>Task 2: Register G14 and renumber the eight gates below it</name>
  <files>gates.manifest.json, gates.manifest.lock</files>
  <read_first>gates.manifest.json, gates.manifest.lock, scripts/check-gate-manifest.mjs, scripts/ci-gates.sh, GATES.md</read_first>
  <action>
Append one gate object to the `gates` array in `gates.manifest.json`, with exactly these values:

- `id`: `G14`
- `name`: `single source of truth`
- `order`: `6`
- `command`: `node scripts/check-single-source.mjs`
- `cwd`: `apps/inheritance`
- `precondition`: `test -f single-source.json`
- `blocking`: `true`
- `proves`: a sentence naming what the gate establishes — that no legal or monetary rule named in
  `single-source.json` has a second implementation, and that the four named rules cannot be removed
  from the registry. End it with `See GATES.md section 12.`, matching the style of `G12` and `G13`
- `requirements`: `["EXT-02"]`

Then rewrite the `order` field of the eight gates listed in the table in `<interfaces>`: `G1` to 7,
`G2` to 8, `G3` to 9, `G4` to 10, `G10` to 11, `G11` to 12, `G8` to 13, `G9` to 14. `G5`, `G6`, `G7`,
`G12` and `G13` keep orders 1 through 5. Change no other field of any existing gate.

`G9` must remain the highest order. `scripts/check-gate-results.mjs` fails with `RESULTS INCOMPLETE`
when any gate other than itself is `not-run`, and the runner republishes after every gate, so a gate
ordered after `G9` would fail `G9` on every run.

Append the matching freeze record to `locked_gates` in `gates.manifest.lock`:
`{ "id": "G14", "command": "node scripts/check-single-source.mjs", "blocking": true }`. Append only —
remove nothing, and change no existing entry's `command` or `blocking`.

Then run `node scripts/check-gate-manifest.mjs` and confirm it exits 0 with no `UNLOCKED GATE`,
`GATE REMOVED`, `GATE COMMAND CHANGED` or `GATE WEAKENED` violation, and run
`bash scripts/ci-gates.sh` and confirm `G14` executes at position 6 and passes before the runner
reaches `G3` at position 9 and halts there.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && node scripts/check-gate-manifest.mjs; echo "G5_EXIT=$?"; node -e "const m=require('./gates.manifest.json'); console.log(m.gates.map(g=>g.order+':'+g.id).sort((a,b)=>a.split(':')[0]-b.split(':')[0]).join(' ')); console.log('count', m.gates.length)"; node -e "const l=require('./gates.manifest.lock'); console.log('locked', l.locked_gates.length, l.locked_gates.map(g=>g.id).join(','))"; bash scripts/ci-gates.sh 2>&1 | grep -E "G14|G3|HALT|GATE|PASSED|FAILED" | head -25; echo "RUNNER_EXIT=$?"
  </verify>
  <acceptance_criteria>
    - `gates.manifest.json` has 14 gates and the order sequence 1 through 14 with no gap and no duplicate
    - `G14` sits at order 6 and `G9` sits at order 14, the highest
    - `gates.manifest.lock` has 14 locked entries and no pre-existing entry's `command` or `blocking` changed
    - `node scripts/check-gate-manifest.mjs` exits 0
    - `bash scripts/ci-gates.sh` shows `G14` running and passing at position 6 of 14, and still halts at `G3` at position 9
    - No gate's `command`, `cwd`, `precondition` or `requirements` was edited other than the appended `G14`
  </acceptance_criteria>
  <done>The check is a gate, it runs ahead of the halt, and the gate set grew rather than shifted.</done>
</task>

<task type="auto">
  <name>Task 3: Document the rule, add GATES.md section 12, and commit</name>
  <files>SINGLE-SOURCE.md, GATES.md</files>
  <read_first>GATES.md, single-source.json, README.md, .planning/PLAN-STANDARD.md, scripts/safe-commit.sh</read_first>
  <action>
Create `SINGLE-SOURCE.md` at the app root, beside `GATES.md`. It is the prose half of EXT-02 — the
part a human reads before writing code, where `single-source.json` is the part the build reads. Write
these five sections:

1. **Why this rule exists.** State the measured fact from `09-RESEARCH.md` §1.2 concretely: three
   implementations of scenario classification existed, and the live one in the wizard returned `I1`
   where the engine returned `I2` and `T1` where the engine returned `T2` — off by one code on the
   most common Philippine family shape, on the screen a lawyer sees immediately before pressing
   Compute. Name the general principle it illustrates: a second implementation of a rule does not stay
   in sync, it diverges silently, and in this codebase a silent divergence is a wrong number in a
   pleading.
2. **The rule.** Every legal rule and every unit conversion has exactly one implementation. The Rust
   engine owns succession rules. `frontend/src/lib/estate-tax-engine/` owns NIRC estate-tax rules.
   `frontend/src/types/money-units.ts` owns peso↔centavo conversion. A consumer *compares* against a
   rule's output; it never re-derives it. Give the concrete distinction the check relies on:
   `scenarioCode === 'I15'` in `src/components/results/utils.ts` is consumption and is fine; producing
   a code with `as ScenarioCode` is derivation and is prohibited.
3. **The four rules today.** Reproduce the id, rule sentence, owner and ceiling of each of the four
   entries in `single-source.json`, and state that `SSOT-04` permits three files, two of which
   (`src/lib/documents.ts`, `src/lib/timeline.ts`) compute percentages rather than money.
4. **Adding a fifth rule.** The procedure, in four numbered steps: measure the pattern's current count
   across the tree and confirm it is already at the ceiling you intend to set; add the entry to
   `single-source.json` with all seven keys; add its id to `REQUIRED_IDS` in
   `scripts/check-single-source.mjs` so it is mandatory rather than tolerated; run
   `node scripts/check-single-source.mjs` and confirm it exits 0. State plainly that a rule which is
   red on arrival must not be added with a raised ceiling — the duplicate is deleted first.
5. **What is deliberately not covered.** State the honest scope limit measured in `09-RESEARCH.md`
   §2.3: centavo→peso conversion for *display* still happens at eleven production sites
   (`lib/tax-bridge.ts`, `estate-tax-engine/explainer.ts` and the tax result panels), and no rule
   covers it yet, because those are formatting sites rather than rule implementations and unifying
   them is outside this phase's scope. A later phase may add it as `SSOT-05` by the procedure in
   section 4.

Then add a `## 12. Single source of truth` section to `GATES.md`, after the existing section 11 and
in that document's established voice. It covers: what `G14` proves; the three violation markers and
what each one catches; why the registry may only grow and how `REQUIRED_IDS` enforces that; why
`SSOT-02` and `SSOT-04` exclude test files, with the two named test files as the evidence; why `G14`
takes order 6 and why `G9` stays last; and that the check has no flag that writes its own registry.
Add a pointer to `SINGLE-SOURCE.md` for the prose rule, and update the `### Current gates` table in
GATES.md section 1 to include `G14` and the eight new order numbers.

Then stage and commit all eight files this plan touched:

    bash scripts/safe-commit.sh -m "feat(09-06): single-source-of-truth rule, registry and gate G14 (EXT-02)" apps/inheritance/single-source.json apps/inheritance/scripts/check-single-source.mjs apps/inheritance/scripts/fixtures/single-source-violation/reintroduced-classifier.ts apps/inheritance/scripts/fixtures/single-source-gutted.json apps/inheritance/gates.manifest.json apps/inheritance/gates.manifest.lock apps/inheritance/GATES.md apps/inheritance/SINGLE-SOURCE.md

Do not stage `gate-results.json` or anything under `.gate-runs/`.
  </action>
  <verify>
cd /home/clsandoval/cs/monorepo/apps/inheritance && grep -c "SSOT-0" SINGLE-SOURCE.md; grep -n "^## 12" GATES.md; grep -c "G14" GATES.md; node scripts/check-single-source.mjs; echo "G14_EXIT=$?"; node scripts/check-gate-manifest.mjs; echo "G5_EXIT=$?"; node scripts/check-plan-closed-world.mjs; echo "G6_EXIT=$?"; node scripts/check-commit-discipline.mjs; echo "G7_EXIT=$?"; bash scripts/ci-gates.sh 2>&1 | tail -20; git status --porcelain
  </verify>
  <acceptance_criteria>
    - `SINGLE-SOURCE.md` exists with all five sections and names all four `SSOT-0N` ids
    - `SINGLE-SOURCE.md` section 5 records the eleven uncovered display-side conversion sites as an honest scope limit
    - `GATES.md` has a `## 12. Single source of truth` section and its section 1 gate table lists `G14` with the corrected orders
    - `node scripts/check-single-source.mjs`, `node scripts/check-gate-manifest.mjs`, `node scripts/check-plan-closed-world.mjs` and `node scripts/check-commit-discipline.mjs` all exit 0
    - `bash scripts/ci-gates.sh` runs `G14` at position 6 and halts at `G3` at position 9, and no summary claims `ALL GATES PASSED (14/14)`
    - `git log -1 --stat` shows exactly eight changed files and `git status --porcelain` shows no unstaged file this plan created
  </acceptance_criteria>
  <done>The rule exists in prose for a human and in a gate for the build, and neither can be quietly deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → gate | The gate's strength is entirely the registry's content; a registry an agent can edit to go green is not a gate |
| gate → the loop | A gate ordered after the known `G3` halt never runs, so registration alone would prove nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | `single-source.json` | mitigate | `REQUIRED_IDS` is hardcoded in the check, so deleting a rule fails with `SINGLE SOURCE RULE MISSING`; the check has no flag that writes the registry and `writeFileSync` appears nowhere in it |
| T-09-11 | Repudiation | `scripts/check-single-source.mjs` | mitigate | Two committed fixtures drive the two failure paths and are asserted to exit 1, so the check is proven able to fail rather than assumed able to |
| T-09-14 | Denial of Service | `gates.manifest.json` ordering | mitigate | `G14` takes order 6, ahead of the `G3` halt at order 9, so a real runner invocation observes it; `G9` stays last so `check-gate-results.mjs` never sees a gate as `not-run` |
| T-09-SC | Tampering | npm/cargo installs | mitigate | The check is dependency-free `node:` builtins; no package manifest is edited |
</threat_model>

<verification>

Run from `/home/clsandoval/cs/monorepo/apps/inheritance`:

    node scripts/check-single-source.mjs
    node scripts/check-single-source.mjs --root scripts/fixtures/single-source-violation
    node scripts/check-single-source.mjs --registry scripts/fixtures/single-source-gutted.json
    node scripts/check-gate-manifest.mjs
    node scripts/check-plan-closed-world.mjs
    node scripts/check-commit-discipline.mjs
    bash scripts/ci-gates.sh

Expected: the first exits 0; the second exits 1 printing `SINGLE SOURCE VIOLATION`; the third exits 1
printing `SINGLE SOURCE RULE MISSING`; the three static checks exit 0; `ci-gates.sh` runs `G14` at
position 6, passes it, and halts at `G3` at position 9 on the same five frontend failures Phase 5 left
behind.

If the runner halts anywhere **before** `G3`, or if `G14` itself fails, stop and report BLOCKED with
the pasted output. A red `G14` means a duplicate survived plan 09-04 or 09-05; the response is to
report it, never to raise a ceiling or drop a rule.

</verification>

<success_criteria>

- [ ] `single-source.json` holds four rules, each with all seven keys, and its `$comment` states it may only grow
- [ ] `scripts/check-single-source.mjs` exits 0 on the repository and prints `GATE-SKIPS total=4 skipped=0`
- [ ] Both committed fixtures drive the check to exit 1, one per failure marker
- [ ] The check has no `--fix`, `--update`, `--accept` or `--regenerate` flag and calls no write function
- [ ] `G14` is registered blocking at order 6, `gates.manifest.lock` has 14 entries, and `check-gate-manifest.mjs` exits 0
- [ ] `G9` is still the highest order
- [ ] `SINGLE-SOURCE.md` documents the rule, the four entries, the procedure for a fifth, and the honest scope limit
- [ ] `GATES.md` section 12 exists and section 1's gate table lists all 14 gates with corrected orders
- [ ] `bash scripts/ci-gates.sh` runs `G14` before halting at `G3`
- [ ] `ALL GATES PASSED (14/14)` was not claimed

</success_criteria>

<output>
Create `.planning/phases/09-single-source-of-truth-dedup-classifiers-money-types/09-06-SUMMARY.md` when done
</output>
