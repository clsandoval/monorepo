import { WizardShell } from "@/components/wizard/wizard-shell";

export const metadata = {
  title: "Compliance Wizard — SEC Compliance Navigator",
};

export default function WizardPage() {
  return <WizardShell />;
}
